
================================================================================
ファイル: .claude/settings.local.json
行: 149 (開始行: 147)
コミット: a35fee7
作成日時: 2026-01-07T07:51:15Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**Consider using relative paths or variables for the file loop pattern.**

The Bash loop pattern includes a hardcoded absolute path with a specific username (`/Users/masakazuiwahara/Development/tempo-ai/...`). While the `.local.json` suffix suggests this is a local-only configuration, if this file is tracked in git, it may cause issues for other developers.



Consider either:
1. Using a relative path pattern if possible
2. Adding this file to `.gitignore` if it's truly local-only
3. Using environment variables or placeholders like `<USER_HOME>` (as seen on line 156)

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @.claude/settings.local.json around lines 147 - 149, Replace the hardcoded
absolute path in the Bash file loop pattern with a relative path or a
home-directory variable to avoid user-specific paths; specifically update the
pattern string "Bash(for file in
/Users/masakazuiwahara/Development/tempo-ai/app/src/domain/services/*.ts)" to
use a relative path like "Bash(for file in ./app/src/domain/services/*.ts)" or a
home variable like "Bash(for file in
$HOME/Development/tempo-ai/app/src/domain/services/*.ts)", or remove this local
file from source control by adding ".claude/settings.local.json" to .gitignore
if it must remain user-specific.
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -139,7 +139,15 @@
       "Bash(wc:*)",
       "Bash(npx expo install:*)",
       "Bash(npx expo-doctor:*)",
-      "Bash(jq:*)"
+      "Bash(jq:*)",
+      "Bash(gh pr merge:*)",
+      "Bash(npx wrangler:*)",
+      "Bash(tree:*)",
+      "Bash(cut:*)",
+      "Bash(for file in /Users/masakazuiwahara/Development/tempo-ai/app/src/domain/services/*.ts)",
+      "Bash(do if [[ ! \"$file\" =~ index.ts$ ]])",
+      "Bash(done)",



================================================================================
ファイル: app/app/(main)/index.tsx
行: 617 (開始行: 162)
コミット: a35fee7
作成日時: 2026-01-07T07:51:15Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🔴 Critical_

**Critical: File exceeds 400-line limit (617 lines).**

This screen component is 54% over the 400-line maximum specified in the coding guidelines. The file should be decomposed into smaller, focused components.



<details>
<summary>♻️ Suggested refactoring approach</summary>

Extract these inline components to separate files:

1. **MetricGridCard component** (lines 198-248) → `app/src/components/today/MetricGridCard.tsx`
2. **HealthSummaryCard component** (lines 251-400) → `app/src/components/today/HealthSummaryCard.tsx`
3. **Header section** (lines 412-428) → `app/src/components/today/TodayHeader.tsx`
4. **AI Insight Card** (lines 454-506) → `app/src/components/today/AIInsightCard.tsx`
5. **One Thing Card** (lines 508-574) → `app/src/components/today/OneThingCard.tsx`

After extraction, the main screen file should be ~200 lines, focusing only on layout and composition.

</details>

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(main)/index.tsx around lines 162 - 617, The TodayScreen component
exceeds the 400-line limit; extract the large inline UI blocks into focused
components: move renderMetricCard into a new MetricGridCard component (accepts a
MetricCard prop, router.push handler, colors/FontFamily as needed), move
renderHealthCard into HealthSummaryCard (accepts a HealthCard prop and router),
extract the header JSX into TodayHeader (accepts greeting, today, FontFamily),
extract the AI Insight Pressable into AIInsightCard (accepts
MOCK_TODAY.aiMessage, router, t, colors), and extract the One Thing Pressable
into OneThingCard (accepts MOCK_TODAY.oneThing, router, icons, colors); then
replace the inline functions/JSX in TodayScreen with imports of these components
and pass the necessary props (scores/dailySnapshot, metricCards/healthCards
arrays, router, t, colors, FontFamily, MOCK_TODAY), remove duplicated logic
(points calculation may stay in HealthSummaryCard), and ensure TodayScreen keeps
lifecycle (initialize/useEffect), loading state, and layout so the main file
shrinks to ~200 lines.
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -1,98 +1,617 @@
-import React, { useEffect } from 'react';
-import { StyleSheet, SafeAreaView, ScrollView } from 'react-native';
-import { useRouter } from 'expo-router';
-import { Colors, Spacing } from '../../src/theme';
-import { MOCK_SCORES, MOCK_QUICK_ACTIONS } from '../../src/constants/mockData';
+/**
+ * TodayScreen - メインホーム画面
+ * sozai/tempoai/screens/TodayScreen.tsx を React Native で完全再現
+ */
+
+import React, { useEffect } from "react";
 import {
-  useUserStore,
-  selectIsCalibrating,
-  useHealthStore,
-  useInsightStore,
-  selectCurrentGenerationMessage,
-} from '../../src/stores';
+  View,
+  Text,
+  ScrollView,
+  Pressable,
+  ActivityIndicator,
+} from "react-native";
 import {
-  HomeHeader,
-  CalibrationProgress,
-  InsightCard,
-  ScoresSection,
-  RhythmStatusCard,
-  WeatherCard,
-  QuickActionsSection,
-} from './components';
-import type { JSX } from 'react';
-
-export default function HomeScreen(): JSX.Element {
-  const router = useRouter();
+  SafeAreaView,
+  useSafeAreaInsets,
+} from "react-native-safe-area-context";
+import { useRouter } from "expo-router";
+import {
+  ChevronRight,
+  Sparkles,
+  ArrowRight,
+  Heart,
+  Activity,
+  Footprints,
+  Target,
+  Wind,
+  Droplets,
+  Thermometer,
+  CheckCircle2,
+} from "lucide-react-native";
+import Animated, { FadeInDown } from "react-native-reanimated";
+import Svg, { Polyline, Circle } from "react-native-svg";
+
+import { TAB_BAR_HEIGHT } from "./_layout";
+import { colors, FontFamily } from "../../src/theme";
+import { t } from "../../src/i18n";
+import {
+  MOCK_TODAY,
+  MOCK_SETTINGS,
+  getGreeting,
+  formatDate,
+} from "../../src/constants/mockData";
+import { useHealthStore } from "../../src/stores/healthStore";
 
-  // User store
-  const profile = useUserStore((state) => state.profile);
-  const isCalibrating = useUserStore(selectIsCalibrating);
+// 4つの指標カードのデータ（i18n対応）
+type MetricCard = {
+  id: string;
+  title: string;
+  value: string;
+  colorText: string;
+  colorAccent: string;
+  chartData: number[];
+  route: "/recovery-detail" | "/sleep-detail" | "/rhythm-detail" | "/energy-detail";
+};
 
-  // Health store
-  const weather = useHealthStore((state) => state.weather);
-  const rhythmAnalysis = useHealthStore((state) => state.rhythmAnalysis);
-  const setMockData = useHealthStore((state) => state.setMockData);
+const getMetricCards = (scores?: { recovery: number; sleep: number; rhythm: number; energy: number }): MetricCard[] => [
+  {
+    id: "recovery",
+    title: t("score.recovery.label"),
+    value: scores ? `${Math.round(scores.recovery)}%` : "--",
+    colorText: colors.emerald[600],
+    colorAccent: colors.emerald[500],
+    chartData: [40, 60, 55, 80, 70, 65, scores?.recovery ?? 0],
+    route: "/recovery-detail",
+  },
+  {
+    id: "sleep",
+    title: t("score.sleep.label"),
+    value: scores ? `${Math.round(scores.sleep)}%` : "--",
+    colorText: colors.indigo[600],
+    colorAccent: colors.indigo[500],
+    chartData: [70, 80, 60, 90, 85, 80, scores?.sleep ?? 0],
+    route: "/sleep-detail",
+  },
+  {
+    id: "rhythm",
+    title: t("score.rhythm.label"),
+    value: scores ? `${Math.round(scores.rhythm)}%` : "--",
+    colorText: colors.purple[600],
+    colorAccent: colors.purple[500],
+    chartData: [88, 90, 92, 91, 89, 94, scores?.rhythm ?? 0],
+    route: "/rhythm-detail",
+  },
+  {
+    id: "energy",
+    title: t("score.energy.label"),
+    value: scores ? `${Math.round(scores.energy)}%` : "--",
+    colorText: colors.amber[600],
+    colorAccent: colors.amber[500],
+    chartData: [50, 60, 55, 70, 80, 75, scores?.energy ?? 0],
+    route: "/energy-detail",
+  },
+];
 
-  // Insight store
-  const isLoading = useInsightStore((state) => state.isGeneratingInsight);
-  const shortGreeting = useInsightStore((state) => state.shortGreeting);
-  const loadingMessage = useInsightStore(selectCurrentGenerationMessage);
-  const generateDailyInsight = useInsightStore((state) => state.generateDailyInsight);
+// Health Summary カードのデータを取得（i18n対応）
+const getHealthCards = () => [
+  {
+    id: "hrv",
+    label: t("metric.health.hrv.shortName"),
+    value: "82",
+    unit: "ms",
+    Icon: Activity,
+    colorIcon: colors.emerald[500],
+    lineColor: "#10B981",
+    chartData: [65, 70, 68, 75, 72, 80, 82],
+  },
+  {
+    id: "rhr",
+    label: t("metric.health.rhr.shortName"),
+    value: "59",
+    unit: "bpm",
+    Icon: Heart,
+    colorIcon: colors.rose[500],
+    lineColor: "#F43F5E",
+    chartData: [60, 59, 61, 58, 59, 60, 59],
+  },
+  {
+    id: "resp",
+    label: t("metric.health.resp.shortName"),
+    value: "11.0",
+    unit: "brpm",
+    Icon: Wind,
+    colorIcon: colors.blue[500],
+    lineColor: "#3B82F6",
+    chartData: [12, 11.5, 11.2, 11.8, 11.0, 11.2, 11.0],
+  },
+  {
+    id: "spo2",
+    label: t("metric.health.spo2.shortName"),
+    value: "98",
+    unit: "%",
+    Icon: Droplets,
+    colorIcon: colors.teal[500],
+    lineColor: "#14B8A6",
+    chartData: [97, 98, 98, 99, 97, 98, 98],
+  },
+  {
+    id: "temp",
+    label: t("metric.health.temp.shortName"),
+    value: "36.4",
+    unit: "°C",
+    Icon: Thermometer,
+    colorIcon: colors.amber[500],
+    lineColor: "#F59E0B",
+    chartData: [36.5, 36.6, 36.4, 36.7, 36.5, 36.5, 36.4],
+  },
+];
 
-  // Use mock scores for now
-  const scores = MOCK_SCORES;
-  const nickname = profile?.nickname || 'ユーザー';
+// Health Cardの型定義
+type HealthCard = {
+  id: string;
+  label: string;
+  value: string;
+  unit: string;
+  Icon: typeof Activity;
+  colorIcon: string;
+  lineColor: string;
+  chartData: number[];
+};
 
+export default function TodayScreen(): React.ReactElement {
+  const router = useRouter();
+  const insets = useSafeAreaInsets();
+  const { dailySnapshot, isLoading, initialize } = useHealthStore();
+  
+  // 初回レンダリング時に初期化（データ取得 + 計算）
   useEffect(() => {
-    // Initialize mock data on mount
-    setMockData();
+    initialize();
+  }, [initialize]);
+  
+  // healthStoreから計算済みのスコアを取得
+  const scores = dailySnapshot?.scores;
+  
+  const healthCards = getHealthCards();
+  const metricCards = getMetricCards(scores);
 
-    // Generate insight with labor illusion
-    generateDailyInsight();
-    // eslint-disable-next-line react-hooks/exhaustive-deps
-  }, []);
+  const greeting = getGreeting(MOCK_SETTINGS.profile.name);
+  const today = formatDate();
+  
+  // ローディング表示
+  if (isLoading) {
+    return (
+      <View style={{ flex: 1, backgroundColor: colors.stone[50] }}>
+        <SafeAreaView style={{ flex: 1 }}>
+          <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
+            <ActivityIndicator size="large" color={colors.stone[900]} />
+            <Text style={{ color: colors.stone[600], marginTop: 16, fontSize: 14 }}>
+              {t("common.loading")}
+            </Text>
+          </View>
+        </SafeAreaView>
+      </View>
+    );
+  }
 
-  return (
-    <SafeAreaView style={styles.container}>
-      <ScrollView
-        style={styles.scrollView}
-        contentContainerStyle={styles.scrollContent}
-        showsVerticalScrollIndicator={false}
+  // MetricGridCard をインラインで実装（正方形、浮遊感）
+  const renderMetricCard = (metric: MetricCard) => (
+    <Pressable
+      key={metric.id}
+      onPress={() => router.push(metric.route)}
+      className="bg-white p-5 rounded-3xl border border-stone-100"
+      style={({ pressed }) => [
+        {
+          aspectRatio: 1,
+          justifyContent: "space-between",
+          shadowColor: "#000",
+          shadowOffset: { width: 0, height: 8 },
+          shadowOpacity: 0.08,
+          shadowRadius: 16,
+          elevation: 8,
+        },
+        pressed && { transform: [{ scale: 0.98 }] },
+      ]}
+    >
+      {/* Header */}
+      <View className="flex-row justify-between items-start">
+        <Text className="text-sm font-bold text-stone-400">{metric.title}</Text>
+        <ChevronRight size={16} color={colors.stone[300]} />
+      </View>
+
+      {/* Value */}
+      <Text
+        className="text-3xl font-bold tracking-tight"
+        style={{ color: metric.colorText, fontFamily: FontFamily.serif }}
+      >
+        {metric.value}
+      </Text>
+
+      {/* Mini Bar Chart */}
+      <View
+        className="flex-row items-end justify-between"
+        style={{ gap: 4, height: 48 }}
+      >
+        {metric.chartData.map((val, i) => (
+          <View
+            key={i}
+            className="flex-1 rounded-sm"
+            style={{
+              height: `${val}%`,
+              backgroundColor: metric.colorAccent,
+              opacity: i === metric.chartData.length - 1 ? 1 : 0.3,
+            }}
+          />
+        ))}
+      </View>
+    </Pressable>
+  );
+
+  // HealthSummaryCard
+  const renderHealthCard = (card: HealthCard) => {
+    const { Icon, chartData, lineColor } = card;
+    const min = Math.min(...chartData);
+    const max = Math.max(...chartData);
+    const range = max - min || 1;
+    const points = chartData
+      .map((val, i) => {
+        const x = (i / (chartData.length - 1)) * 100;
+        const y = 100 - ((val - min) / range) * 80 - 10;
+        return `${x},${y}`;
+      })
+      .join(" ");
+    const lastY =
+      100 - ((chartData[chartData.length - 1] - min) / range) * 80 - 10;
+
+    return (
+      <View
+        key={card.id}
+        style={{
+          width: 140,
+          aspectRatio: 1,
+          backgroundColor: colors.white,
+          borderRadius: 16,
+          borderWidth: 1,
+          borderColor: colors.stone[100],
+          shadowColor: "#000",
+          shadowOffset: { width: 0, height: 4 },
+          shadowOpacity: 0.06,
+          shadowRadius: 8,
+          elevation: 3,
+          overflow: "hidden",
+          padding: 16,
+        }}
       >
-        <HomeHeader nickname={nickname} />
+        <Pressable
+          onPress={() => router.push("/health-detail")}
+          style={({ pressed }) => [pressed && { opacity: 0.7 }]}
+        >
+          {/* Header: Icon + Label */}
+          <View style={{ flexDirection: "row", alignItems: "center", gap: 6 }}>
+            <View
+              style={{
+                width: 24,
+                height: 24,
+                borderRadius: 12,
+                backgroundColor: colors.stone[100],
+                alignItems: "center",
+                justifyContent: "center",
+              }}
+            >
+              <Icon size={12} color={card.colorIcon} />
+            </View>
+            <Text
+              style={{
+                fontSize: 12,
+                fontWeight: "600",
+                color: colors.stone[500],
+                textTransform: "uppercase",
+                letterSpacing: 0.3,
+              }}
+            >
+              {card.label}
+            </Text>
+          </View>
 
-        {isCalibrating && profile && <CalibrationProgress profile={profile} />}
+          {/* Value + Unit */}
+          <View
+            style={{
+              flexDirection: "row",
+              alignItems: "baseline",
+              gap: 3,
+              marginTop: 10,
+            }}
+          >
+            <Text
+              style={{
+                fontSize: 32,
+                fontWeight: "700",
+                color: colors.stone[900],
+                letterSpacing: -0.5,
+              }}
+            >
+              {card.value}
+            </Text>
+            <Text
+              style={{
+                fontSize: 14,
+                fontWeight: "500",
+                color: colors.stone[400],
+              }}
+            >
+              {card.unit}
+            </Text>
+          </View>
 
-        <InsightCard
-          isLoading={isLoading}
-          loadingMessage={loadingMessage}
-          shortGreeting={shortGreeting}
-          onPress={() => router.push('/insight-detail')}
-        />
+          {/* Bottom Section: Graph + Badge */}
+          <View style={{ marginTop: 12 }}>
+            {/* Mini Line Chart */}
+            <View style={{ height: 28 }}>
+              <Svg
+                width="100%"
+                height="100%"
+                viewBox="0 0 100 100"
+                preserveAspectRatio="none"
+              >
+                <Polyline
+                  points={points}
+                  fill="none"
+                  stroke={lineColor}
+                  strokeWidth="3"
+                  strokeLinecap="round"
+                  strokeLinejoin="round"
+                  opacity={0.5}
+                />
+                <Circle cx="100" cy={lastY} r="4" fill={lineColor} />
+              </Svg>
+            </View>
 
-        <ScoresSection scores={scores} isCalibrating={isCalibrating} />
+            {/* Status Badge - 右寄せ */}
+            <View
+              style={{
+                flexDirection: "row",
+                justifyContent: "flex-end",
+                marginTop: 8,
+              }}
+            >
+              <View
+                style={{
+                  flexDirection: "row",
+                  alignItems: "center",
+                  gap: 3,
+                }}
+              >
+                <CheckCircle2 size={12} color={colors.emerald[500]} />
+                <Text
+                  style={{
+                    fontSize: 11,
+                    fontWeight: "600",
+                    color: colors.emerald[500],
+                  }}
+                >
+                  {t("screen.today.normal")}
+                </Text>
+              </View>
+            </View>
+          </View>
+        </Pressable>
+      </View>
+    );
+  };
 
-        {rhythmAnalysis && <RhythmStatusCard rhythmAnalysis={rhythmAnalysis} />}
+  return (
+    <View className="flex-1 bg-stone-50">
+      <SafeAreaView className="flex-1" edges={["top"]}>
+        <ScrollView
+          contentContainerStyle={{
+            paddingBottom: TAB_BAR_HEIGHT + insets.bottom + 24,
+          }}
+          showsVerticalScrollIndicator={false}
+        >
+          {/* Header */}
+          <Animated.View
+            entering={FadeInDown.duration(400)}
+            className="pt-14 px-6 mb-6"
+          >
+            <Text
+              className="text-2xl font-bold text-stone-900 tracking-tight"
+              style={{ fontFamily: FontFamily.serif }}
+            >
+              {greeting}
+            </Text>
+            <Text
+              className="text-sm font-medium text-stone-500 uppercase mt-1"
+              style={{ letterSpacing: 1 }}
+            >
+              {today}
+            </Text>
+          </Animated.View>
 
-        {weather && <WeatherCard weather={weather} />}
+          <View className="px-6" style={{ gap: 32 }}>
+            {/* 2x2 Metric Grid */}
+            <Animated.View
+              entering={FadeInDown.delay(100).duration(400)}
+              style={{ gap: 16 }}
+            >
+              <View className="flex-row" style={{ gap: 16 }}>
+                <View style={{ flex: 1 }}>
+                  {renderMetricCard(metricCards[0])}
+                </View>
+                <View style={{ flex: 1 }}>
+                  {renderMetricCard(metricCards[1])}
+                </View>
+              </View>
+              <View className="flex-row" style={{ gap: 16 }}>
+                <View style={{ flex: 1 }}>
+                  {renderMetricCard(metricCards[2])}
+                </View>
+                <View style={{ flex: 1 }}>
+                  {renderMetricCard(metricCards[3])}
+                </View>
+              </View>
+            </Animated.View>
 
-        <QuickActionsSection actions={MOCK_QUICK_ACTIONS} />
-      </ScrollView>
-    </SafeAreaView>
+            {/* AI Insight Section */}
+            <Animated.View
+              entering={FadeInDown.delay(200).duration(400)}
+              style={{ gap: 12 }}
+            >
+              <View className="flex-row items-center px-1" style={{ gap: 8 }}>
+                <Sparkles size={18} color={colors.indigo[500]} />
+                <Text
+                  className="text-sm font-bold text-stone-400 uppercase"
+                  style={{ letterSpacing: 1.5 }}
+                >
+                  {t("screen.today.aiInsight")}
+                </Text>
+              </View>
+              <Pressable
+                onPress={() => router.push("/insight-detail")}
+                className="bg-white p-5 rounded-3xl border border-stone-100 overflow-hidden"
+                style={({ pressed }) => [
+                  {
+                    shadowColor: "#000",
+                    shadowOffset: { width: 0, height: 4 },
+                    shadowOpacity: 0.06,
+                    shadowRadius: 8,
+                    elevation: 3,
+                  },
+                  pressed && { transform: [{ scale: 0.98 }], opacity: 0.9 },
+                ]}
+              >
+                {/* Left accent bar - 紫の縦線 (absolute positioning) */}
+                <View
+                  style={{
+                    position: "absolute",
+                    top: 0,
+                    left: 0,
+                    bottom: 0,
+                    width: 4,
+                    backgroundColor: colors.indigo[500],
+                  }}
+                />
+                <Text className="text-lg font-bold text-stone-900 mb-2">
+                  {MOCK_TODAY.aiMessage.title}
+                </Text>
+                <Text className="text-sm text-stone-600 leading-relaxed mb-4">
+                  {MOCK_TODAY.aiMessage.body}
+                </Text>
+                <View className="flex-row items-center" style={{ gap: 4 }}>
+                  <Text className="text-xs font-bold text-indigo-600">
+                    {t("screen.today.viewAnalysis")}
+                  </Text>
+                  <ArrowRight size={14} color={colors.indigo[600]} />
+                </View>
+              </Pressable>
+            </Animated.View>
+
+            {/* Today's One Thing Section */}
+            <Animated.View
+              entering={FadeInDown.delay(300).duration(400)}
+              style={{ gap: 12 }}
+            >
+              <View className="flex-row items-center px-1" style={{ gap: 8 }}>
+                <Target size={18} color={colors.amber[500]} />
+                <Text
+                  className="text-sm font-bold text-stone-400 uppercase"
+                  style={{ letterSpacing: 1.5 }}
+                >
+                  {t("screen.today.todayOneThing.title")}
+                </Text>
+              </View>
+              <Pressable
+                onPress={() => router.push("/action-detail")}
+                className="bg-white p-6 rounded-3xl border border-stone-100 overflow-hidden flex-row items-center justify-between"
+                style={({ pressed }) => [
+                  {
+                    minHeight: 100,
+                    shadowColor: "#000",
+                    shadowOffset: { width: 0, height: 4 },
+                    shadowOpacity: 0.06,
+                    shadowRadius: 8,
+                    elevation: 3,
+                  },
+                  pressed && { transform: [{ scale: 0.98 }], opacity: 0.9 },
+                ]}
+              >
+                {/* Left accent bar - オレンジの縦線 (absolute positioning) */}
+                <View
+                  style={{
+                    position: "absolute",
+                    top: 0,
+                    left: 0,
+                    bottom: 0,
+                    width: 4,
+                    backgroundColor: colors.amber[500],
+                  }}
+                />
+                <View
+                  className="flex-row items-center"
+                  style={{ gap: 20, flex: 1 }}
+                >
+                  <View
+                    className="p-4 rounded-2xl"
+                    style={{ backgroundColor: colors.amber[50] }}
+                  >
+                    <Footprints size={28} color={colors.amber[600]} />
+                  </View>
+                  <View style={{ flex: 1 }}>
+                    <Text className="text-lg font-bold text-stone-900 mb-1">
+                      {MOCK_TODAY.oneThing.action}
+                    </Text>
+                    <Text className="text-sm text-stone-500 font-medium">
+                      {MOCK_TODAY.oneThing.summary}
+                    </Text>
+                  </View>
+                </View>
+                <View
+                  className="p-2.5 rounded-full"
+                  style={{ backgroundColor: colors.stone[50] }}
+                >
+                  <ChevronRight size={22} color={colors.stone[300]} />
+                </View>
+              </Pressable>
+            </Animated.View>
+
+            {/* Health Summary Section */}
+            <Animated.View
+              entering={FadeInDown.delay(400).duration(400)}
+              style={{ gap: 12 }}
+            >
+              <View className="flex-row items-center justify-between px-1">
+                <View className="flex-row items-center" style={{ gap: 8 }}>
+                  <Activity size={18} color={colors.rose[500]} />
+                  <Text
+                    className="text-sm font-bold text-stone-400 uppercase"
+                    style={{ letterSpacing: 1.5 }}
+                  >
+                    {t("screen.today.healthSummary")}
+                  </Text>
+                </View>
+                <Pressable onPress={() => router.push("/health-detail")}>
+                  <ChevronRight size={20} color={colors.stone[300]} />
+                </Pressable>
+              </View>
+            </Animated.View>
+          </View>
+
+          {/* Health Summary Horizontal Scroll */}
+          <Animated.View entering={FadeInDown.delay(450).duration(400)}>
+            <ScrollView
+              horizontal
+              showsHorizontalScrollIndicator={false}
+              contentContainerStyle={{
+                paddingHorizontal: 24,
+                paddingTop: 8,
+                paddingBottom: 32,
+                gap: 16,
+              }}
+            >
+              {healthCards.map(renderHealthCard)}
+            </ScrollView>
+          </Animated.View>
+        </ScrollView>
+      </SafeAreaView>
+    </View>
   );
 }



================================================================================
ファイル: app/app/(main)/index.tsx
行: 162 (開始行: N/A)
コミット: a35fee7
作成日時: 2026-01-07T07:51:15Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟠 Major_

**Replace default export with named export.**

The coding guidelines require named exports for better refactoring and tree-shaking.



<details>
<summary>🔧 Proposed fix</summary>

```diff
-export default function TodayScreen(): React.ReactElement {
+export const TodayScreen = (): React.ReactElement => {
```

Then update any imports of this component to use:
```typescript
import { TodayScreen } from './path/to/file';
```

</details>

As per coding guidelines: "Use named exports instead of default exports for better refactoring and tree-shaking"

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
export const TodayScreen = (): React.ReactElement => {
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(main)/index.tsx at line 162, The file currently uses a default
export for the React component TodayScreen; change it to a named export by
replacing the default export declaration with a named export (export function
TodayScreen(...)) and update any imports to use named imports (e.g., import {
TodayScreen } from '...'); ensure the component's symbol TodayScreen is exported
by name and adjust all references/imports across the codebase accordingly.
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -1,98 +1,617 @@
-import React, { useEffect } from 'react';
-import { StyleSheet, SafeAreaView, ScrollView } from 'react-native';
-import { useRouter } from 'expo-router';
-import { Colors, Spacing } from '../../src/theme';
-import { MOCK_SCORES, MOCK_QUICK_ACTIONS } from '../../src/constants/mockData';
+/**
+ * TodayScreen - メインホーム画面
+ * sozai/tempoai/screens/TodayScreen.tsx を React Native で完全再現
+ */
+
+import React, { useEffect } from "react";
 import {
-  useUserStore,
-  selectIsCalibrating,
-  useHealthStore,
-  useInsightStore,
-  selectCurrentGenerationMessage,
-} from '../../src/stores';
+  View,
+  Text,
+  ScrollView,
+  Pressable,
+  ActivityIndicator,
+} from "react-native";
 import {
-  HomeHeader,
-  CalibrationProgress,
-  InsightCard,
-  ScoresSection,
-  RhythmStatusCard,
-  WeatherCard,
-  QuickActionsSection,
-} from './components';
-import type { JSX } from 'react';
-
-export default function HomeScreen(): JSX.Element {
-  const router = useRouter();
+  SafeAreaView,
+  useSafeAreaInsets,
+} from "react-native-safe-area-context";
+import { useRouter } from "expo-router";
+import {
+  ChevronRight,
+  Sparkles,
+  ArrowRight,
+  Heart,
+  Activity,
+  Footprints,
+  Target,
+  Wind,
+  Droplets,
+  Thermometer,
+  CheckCircle2,
+} from "lucide-react-native";
+import Animated, { FadeInDown } from "react-native-reanimated";
+import Svg, { Polyline, Circle } from "react-native-svg";
+
+import { TAB_BAR_HEIGHT } from "./_layout";
+import { colors, FontFamily } from "../../src/theme";
+import { t } from "../../src/i18n";
+import {
+  MOCK_TODAY,
+  MOCK_SETTINGS,
+  getGreeting,
+  formatDate,
+} from "../../src/constants/mockData";
+import { useHealthStore } from "../../src/stores/healthStore";
 
-  // User store
-  const profile = useUserStore((state) => state.profile);
-  const isCalibrating = useUserStore(selectIsCalibrating);
+// 4つの指標カードのデータ（i18n対応）
+type MetricCard = {
+  id: string;
+  title: string;
+  value: string;
+  colorText: string;
+  colorAccent: string;
+  chartData: number[];
+  route: "/recovery-detail" | "/sleep-detail" | "/rhythm-detail" | "/energy-detail";
+};
 
-  // Health store
-  const weather = useHealthStore((state) => state.weather);
-  const rhythmAnalysis = useHealthStore((state) => state.rhythmAnalysis);
-  const setMockData = useHealthStore((state) => state.setMockData);
+const getMetricCards = (scores?: { recovery: number; sleep: number; rhythm: number; energy: number }): MetricCard[] => [
+  {
+    id: "recovery",
+    title: t("score.recovery.label"),
+    value: scores ? `${Math.round(scores.recovery)}%` : "--",
+    colorText: colors.emerald[600],
+    colorAccent: colors.emerald[500],
+    chartData: [40, 60, 55, 80, 70, 65, scores?.recovery ?? 0],
+    route: "/recovery-detail",
+  },
+  {
+    id: "sleep",
+    title: t("score.sleep.label"),
+    value: scores ? `${Math.round(scores.sleep)}%` : "--",
+    colorText: colors.indigo[600],
+    colorAccent: colors.indigo[500],
+    chartData: [70, 80, 60, 90, 85, 80, scores?.sleep ?? 0],
+    route: "/sleep-detail",
+  },
+  {
+    id: "rhythm",
+    title: t("score.rhythm.label"),
+    value: scores ? `${Math.round(scores.rhythm)}%` : "--",
+    colorText: colors.purple[600],
+    colorAccent: colors.purple[500],
+    chartData: [88, 90, 92, 91, 89, 94, scores?.rhythm ?? 0],
+    route: "/rhythm-detail",
+  },
+  {
+    id: "energy",
+    title: t("score.energy.label"),
+    value: scores ? `${Math.round(scores.energy)}%` : "--",
+    colorText: colors.amber[600],
+    colorAccent: colors.amber[500],
+    chartData: [50, 60, 55, 70, 80, 75, scores?.energy ?? 0],
+    route: "/energy-detail",
+  },
+];
 
-  // Insight store
-  const isLoading = useInsightStore((state) => state.isGeneratingInsight);
-  const shortGreeting = useInsightStore((state) => state.shortGreeting);
-  const loadingMessage = useInsightStore(selectCurrentGenerationMessage);
-  const generateDailyInsight = useInsightStore((state) => state.generateDailyInsight);
+// Health Summary カードのデータを取得（i18n対応）
+const getHealthCards = () => [
+  {
+    id: "hrv",
+    label: t("metric.health.hrv.shortName"),
+    value: "82",
+    unit: "ms",
+    Icon: Activity,
+    colorIcon: colors.emerald[500],
+    lineColor: "#10B981",
+    chartData: [65, 70, 68, 75, 72, 80, 82],
+  },
+  {
+    id: "rhr",
+    label: t("metric.health.rhr.shortName"),
+    value: "59",
+    unit: "bpm",
+    Icon: Heart,
+    colorIcon: colors.rose[500],
+    lineColor: "#F43F5E",
+    chartData: [60, 59, 61, 58, 59, 60, 59],
+  },
+  {
+    id: "resp",
+    label: t("metric.health.resp.shortName"),
+    value: "11.0",
+    unit: "brpm",
+    Icon: Wind,
+    colorIcon: colors.blue[500],
+    lineColor: "#3B82F6",
+    chartData: [12, 11.5, 11.2, 11.8, 11.0, 11.2, 11.0],
+  },
+  {
+    id: "spo2",
+    label: t("metric.health.spo2.shortName"),
+    value: "98",
+    unit: "%",
+    Icon: Droplets,
+    colorIcon: colors.teal[500],
+    lineColor: "#14B8A6",
+    chartData: [97, 98, 98, 99, 97, 98, 98],
+  },
+  {
+    id: "temp",
+    label: t("metric.health.temp.shortName"),
+    value: "36.4",
+    unit: "°C",
+    Icon: Thermometer,
+    colorIcon: colors.amber[500],
+    lineColor: "#F59E0B",
+    chartData: [36.5, 36.6, 36.4, 36.7, 36.5, 36.5, 36.4],
+  },
+];
 
-  // Use mock scores for now
-  const scores = MOCK_SCORES;
-  const nickname = profile?.nickname || 'ユーザー';
+// Health Cardの型定義
+type HealthCard = {
+  id: string;
+  label: string;
+  value: string;
+  unit: string;
+  Icon: typeof Activity;
+  colorIcon: string;
+  lineColor: string;
+  chartData: number[];
+};
 
+export default function TodayScreen(): React.ReactElement {



================================================================================
ファイル: app/app/(main)/index.tsx
行: 214 (開始行: 203)
コミット: a35fee7
作成日時: 2026-01-07T07:51:15Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟠 Major_

**Refactor inline styles to use StyleSheet.create().**

Multiple inline style objects violate React Native best practices. Inline styles have performance implications as they create new objects on each render.



<details>
<summary>♻️ Proposed refactoring pattern</summary>

Move all inline styles to a StyleSheet definition at the bottom of the file (or in extracted components):

```typescript
const styles = StyleSheet.create({
  metricCard: {
    aspectRatio: 1,
    justifyContent: "space-between",
    shadowColor: "#000",
    shadowOffset: { width: 0, height: 8 },
    shadowOpacity: 0.08,
    shadowRadius: 16,
    elevation: 8,
  },
  healthCardContainer: {
    width: 140,
    aspectRatio: 1,
    backgroundColor: colors.white,
    borderRadius: 16,
    // ... etc
  },
  // ... more styles
});
```

Then reference them via `style={styles.metricCard}`.

</details>

As per coding guidelines: "**StyleSheet.create()** でスタイル定義（インラインスタイル禁止）"


Also applies to: 269-283, 291-314, 317-344, 347-367, 370-396

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -1,98 +1,617 @@
-import React, { useEffect } from 'react';
-import { StyleSheet, SafeAreaView, ScrollView } from 'react-native';
-import { useRouter } from 'expo-router';
-import { Colors, Spacing } from '../../src/theme';
-import { MOCK_SCORES, MOCK_QUICK_ACTIONS } from '../../src/constants/mockData';
+/**
+ * TodayScreen - メインホーム画面
+ * sozai/tempoai/screens/TodayScreen.tsx を React Native で完全再現
+ */
+
+import React, { useEffect } from "react";
 import {
-  useUserStore,
-  selectIsCalibrating,
-  useHealthStore,
-  useInsightStore,
-  selectCurrentGenerationMessage,
-} from '../../src/stores';
+  View,
+  Text,
+  ScrollView,
+  Pressable,
+  ActivityIndicator,
+} from "react-native";
 import {
-  HomeHeader,
-  CalibrationProgress,
-  InsightCard,
-  ScoresSection,
-  RhythmStatusCard,
-  WeatherCard,
-  QuickActionsSection,
-} from './components';
-import type { JSX } from 'react';
-
-export default function HomeScreen(): JSX.Element {
-  const router = useRouter();
+  SafeAreaView,
+  useSafeAreaInsets,
+} from "react-native-safe-area-context";
+import { useRouter } from "expo-router";
+import {
+  ChevronRight,
+  Sparkles,
+  ArrowRight,
+  Heart,
+  Activity,
+  Footprints,
+  Target,
+  Wind,
+  Droplets,
+  Thermometer,
+  CheckCircle2,
+} from "lucide-react-native";
+import Animated, { FadeInDown } from "react-native-reanimated";
+import Svg, { Polyline, Circle } from "react-native-svg";
+
+import { TAB_BAR_HEIGHT } from "./_layout";
+import { colors, FontFamily } from "../../src/theme";
+import { t } from "../../src/i18n";
+import {
+  MOCK_TODAY,
+  MOCK_SETTINGS,
+  getGreeting,
+  formatDate,
+} from "../../src/constants/mockData";
+import { useHealthStore } from "../../src/stores/healthStore";
 
-  // User store
-  const profile = useUserStore((state) => state.profile);
-  const isCalibrating = useUserStore(selectIsCalibrating);
+// 4つの指標カードのデータ（i18n対応）
+type MetricCard = {
+  id: string;
+  title: string;
+  value: string;
+  colorText: string;
+  colorAccent: string;
+  chartData: number[];
+  route: "/recovery-detail" | "/sleep-detail" | "/rhythm-detail" | "/energy-detail";
+};
 
-  // Health store
-  const weather = useHealthStore((state) => state.weather);
-  const rhythmAnalysis = useHealthStore((state) => state.rhythmAnalysis);
-  const setMockData = useHealthStore((state) => state.setMockData);
+const getMetricCards = (scores?: { recovery: number; sleep: number; rhythm: number; energy: number }): MetricCard[] => [
+  {
+    id: "recovery",
+    title: t("score.recovery.label"),
+    value: scores ? `${Math.round(scores.recovery)}%` : "--",
+    colorText: colors.emerald[600],
+    colorAccent: colors.emerald[500],
+    chartData: [40, 60, 55, 80, 70, 65, scores?.recovery ?? 0],
+    route: "/recovery-detail",
+  },
+  {
+    id: "sleep",
+    title: t("score.sleep.label"),
+    value: scores ? `${Math.round(scores.sleep)}%` : "--",
+    colorText: colors.indigo[600],
+    colorAccent: colors.indigo[500],
+    chartData: [70, 80, 60, 90, 85, 80, scores?.sleep ?? 0],
+    route: "/sleep-detail",
+  },
+  {
+    id: "rhythm",
+    title: t("score.rhythm.label"),
+    value: scores ? `${Math.round(scores.rhythm)}%` : "--",
+    colorText: colors.purple[600],
+    colorAccent: colors.purple[500],
+    chartData: [88, 90, 92, 91, 89, 94, scores?.rhythm ?? 0],
+    route: "/rhythm-detail",
+  },
+  {
+    id: "energy",
+    title: t("score.energy.label"),
+    value: scores ? `${Math.round(scores.energy)}%` : "--",
+    colorText: colors.amber[600],
+    colorAccent: colors.amber[500],
+    chartData: [50, 60, 55, 70, 80, 75, scores?.energy ?? 0],
+    route: "/energy-detail",
+  },
+];
 
-  // Insight store
-  const isLoading = useInsightStore((state) => state.isGeneratingInsight);
-  const shortGreeting = useInsightStore((state) => state.shortGreeting);
-  const loadingMessage = useInsightStore(selectCurrentGenerationMessage);
-  const generateDailyInsight = useInsightStore((state) => state.generateDailyInsight);
+// Health Summary カードのデータを取得（i18n対応）
+const getHealthCards = () => [
+  {
+    id: "hrv",
+    label: t("metric.health.hrv.shortName"),
+    value: "82",
+    unit: "ms",
+    Icon: Activity,
+    colorIcon: colors.emerald[500],
+    lineColor: "#10B981",
+    chartData: [65, 70, 68, 75, 72, 80, 82],
+  },
+  {
+    id: "rhr",
+    label: t("metric.health.rhr.shortName"),
+    value: "59",
+    unit: "bpm",
+    Icon: Heart,
+    colorIcon: colors.rose[500],
+    lineColor: "#F43F5E",
+    chartData: [60, 59, 61, 58, 59, 60, 59],
+  },
+  {
+    id: "resp",
+    label: t("metric.health.resp.shortName"),
+    value: "11.0",
+    unit: "brpm",
+    Icon: Wind,
+    colorIcon: colors.blue[500],
+    lineColor: "#3B82F6",
+    chartData: [12, 11.5, 11.2, 11.8, 11.0, 11.2, 11.0],
+  },
+  {
+    id: "spo2",
+    label: t("metric.health.spo2.shortName"),
+    value: "98",
+    unit: "%",
+    Icon: Droplets,
+    colorIcon: colors.teal[500],
+    lineColor: "#14B8A6",
+    chartData: [97, 98, 98, 99, 97, 98, 98],
+  },
+  {
+    id: "temp",
+    label: t("metric.health.temp.shortName"),
+    value: "36.4",
+    unit: "°C",
+    Icon: Thermometer,
+    colorIcon: colors.amber[500],
+    lineColor: "#F59E0B",
+    chartData: [36.5, 36.6, 36.4, 36.7, 36.5, 36.5, 36.4],
+  },
+];
 
-  // Use mock scores for now
-  const scores = MOCK_SCORES;
-  const nickname = profile?.nickname || 'ユーザー';
+// Health Cardの型定義
+type HealthCard = {
+  id: string;
+  label: string;
+  value: string;
+  unit: string;
+  Icon: typeof Activity;
+  colorIcon: string;
+  lineColor: string;
+  chartData: number[];
+};
 
+export default function TodayScreen(): React.ReactElement {
+  const router = useRouter();
+  const insets = useSafeAreaInsets();
+  const { dailySnapshot, isLoading, initialize } = useHealthStore();
+  
+  // 初回レンダリング時に初期化（データ取得 + 計算）
   useEffect(() => {
-    // Initialize mock data on mount
-    setMockData();
+    initialize();
+  }, [initialize]);
+  
+  // healthStoreから計算済みのスコアを取得
+  const scores = dailySnapshot?.scores;
+  
+  const healthCards = getHealthCards();
+  const metricCards = getMetricCards(scores);
 
-    // Generate insight with labor illusion
-    generateDailyInsight();
-    // eslint-disable-next-line react-hooks/exhaustive-deps
-  }, []);
+  const greeting = getGreeting(MOCK_SETTINGS.profile.name);
+  const today = formatDate();
+  
+  // ローディング表示
+  if (isLoading) {
+    return (
+      <View style={{ flex: 1, backgroundColor: colors.stone[50] }}>
+        <SafeAreaView style={{ flex: 1 }}>
+          <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
+            <ActivityIndicator size="large" color={colors.stone[900]} />
+            <Text style={{ color: colors.stone[600], marginTop: 16, fontSize: 14 }}>
+              {t("common.loading")}
+            </Text>
+          </View>
+        </SafeAreaView>
+      </View>
+    );
+  }
 
-  return (
-    <SafeAreaView style={styles.container}>
-      <ScrollView
-        style={styles.scrollView}
-        contentContainerStyle={styles.scrollContent}
-        showsVerticalScrollIndicator={false}
+  // MetricGridCard をインラインで実装（正方形、浮遊感）
+  const renderMetricCard = (metric: MetricCard) => (
+    <Pressable
+      key={metric.id}
+      onPress={() => router.push(metric.route)}
+      className="bg-white p-5 rounded-3xl border border-stone-100"
+      style={({ pressed }) => [
+        {
+          aspectRatio: 1,
+          justifyContent: "space-between",
+          shadowColor: "#000",
+          shadowOffset: { width: 0, height: 8 },
+          shadowOpacity: 0.08,
+          shadowRadius: 16,
+          elevation: 8,
+        },
+        pressed && { transform: [{ scale: 0.98 }] },
+      ]}



================================================================================
ファイル: app/app/(onboarding)/complete.tsx
行: 19 (開始行: 17)
コミット: a35fee7
作成日時: 2026-01-07T07:51:15Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**Consider using `useWindowDimensions` hook for responsive layouts.**

Using `Dimensions.get('window')` at module level captures dimensions once at module load time. If the window size changes (orientation change, split-screen on tablets), these values become stale.



<details>
<summary>♻️ Recommended approach using useWindowDimensions</summary>

```diff
-import { View, Text, StyleSheet, Dimensions } from 'react-native';
+import { View, Text, StyleSheet, useWindowDimensions } from 'react-native';
```

Then inside the component:
```typescript
const { width, height } = useWindowDimensions();
```

Note: This would require moving the blob styles that depend on `width`/`height` to be computed dynamically or using percentage-based positioning instead.

</details>


> Committable suggestion skipped: line range outside the PR's diff.

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(onboarding)/complete.tsx around lines 17 - 19, The module currently
reads static window size via Dimensions.get('window') into width/height at
module scope which becomes stale; move that measurement into the component by
importing and calling useWindowDimensions() (e.g., const { width, height } =
useWindowDimensions()) inside the React component that renders the onboarding
completion screen, then update any blob/positioning style calculations that
reference the module-level width/height to compute dynamically (or convert to
percentage-based styles) so CURRENT_STEP and TOTAL_STEPS logic remains unchanged
while the layout responds to orientation/size changes.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -1,65 +1,94 @@
+/**
+ * CompleteScreen - オンボーディング完了画面
+ * sozai/new のスタイルを React Native で再現
+ * Step 9 of 9 (Final)
+ */
+
 import React from 'react';
-import { View, Text, StyleSheet, SafeAreaView } from 'react-native';
+import { View, Text, StyleSheet, Dimensions } from 'react-native';
+import { SafeAreaView } from 'react-native-safe-area-context';
 import { useRouter } from 'expo-router';
-import { CheckCircle } from 'lucide-react-native';
-import { Colors, Spacing, Typography } from '../../src/theme';
+import { Colors, Spacing, BorderRadius, FontFamily } from '../../src/theme';
 import { PrimaryButton, ProgressBar } from '../../src/components';
 import { CALIBRATION_PERIOD_DAYS } from '../../src/domain/models';
 import { useUserStore } from '../../src/stores';
 import type { JSX } from 'react';
 
+const { width, height } = Dimensions.get('window');
+const CURRENT_STEP = 9;
+const TOTAL_STEPS = 9;



================================================================================
ファイル: app/package.json
行: 46 (開始行: 38)
コミット: a35fee7
作成日時: 2026-01-07T07:51:15Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟠 Major_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
# First, let's read the actual app/package.json file
cat app/package.json
```

Repository: Bluefinee/tempo-ai

Length of output: 180

---

🏁 Script executed:

```shell
# Check if React Native standards document exists
find . -name "*react-native-standards*" -o -name "*CLAUDE*" -o -name ".claude" -type d
```

Repository: Bluefinee/tempo-ai

Length of output: 71

---

🏁 Script executed:

```shell
# Search for any existing compatibility notes or issues in the codebase
rg "React 19|react 19|Zod 4|zod 4" --type json --type ts --type tsx --type js
```

Repository: Bluefinee/tempo-ai

Length of output: 89

---

🏁 Script executed:

```shell
# List contents of .claude directory
ls -la .claude/
```

Repository: Bluefinee/tempo-ai

Length of output: 241

---

🏁 Script executed:

```shell
# Read app/package.json
cat app/package.json
```

Repository: Bluefinee/tempo-ai

Length of output: 1883

---

🏁 Script executed:

```shell
# Search for React and Zod mentions in JSON files
rg "react|zod" -t json --ignore-case
```

Repository: Bluefinee/tempo-ai

Length of output: 1065

---

🏁 Script executed:

```shell
# Check if there's a standards file in .claude
find .claude -type f -name "*.md"
```

Repository: Bluefinee/tempo-ai

Length of output: 44

---

🏁 Script executed:

```shell
# Search for react-native-standards or any standards documentation
find . -type f -name "*standards*" -o -name "*react-native*"
```

Repository: Bluefinee/tempo-ai

Length of output: 44

---

🏁 Script executed:

```shell
# Check if there's any documentation about compatibility
find . -type f \( -name "README.md" -o -name "CLAUDE.md" -o -name ".clauderc*" \) -exec cat {} \;
```

Repository: Bluefinee/tempo-ai

Length of output: 44

---

🌐 Web query:

`React 19 Expo SDK 54 compatibility`

💡 Result:

Short answer: Expo SDK 54 is intended to work with React 19 — SDK 54 ships with React 19.1.0 (and React Native 0.81). [1][2]

Notes/caveats: there have been community-reported issues (web bundling / Metro errors and some iOS hang reports) when using React 19 + SDK 54; if you hit problems check the Expo changelog/docs and open GitHub issues for workarounds or fixes. [3]

Sources:
- Expo docs (SDK 54 → React 19.1.0, RN 0.81). [1]
- Expo SDK 54 beta announcement (lists React 19.1.0). [2]
- Recent GitHub issues reporting React‑19-related web/Metro and runtime problems on SDK 54. [3]

---

🌐 Web query:

`Zod 4.3.5 React Native compatibility`

💡 Result:

Short answer: Yes — Zod 4.3.5 is compatible with React Native (it has no native/node-only dependencies and the Zod docs state it works in Node.js and modern browsers). Use the normal package (or the v4 subpath if you need it) and ensure your Metro/Babel setup handles ESM/modern JS if you run into bundling issues. [1][2]

Sources:
- Zod docs (works in Node.js and modern browsers / v4 info). [1]
- zod package listing / versions (shows current v4.x releases). [2]

---



</details>

**Address known React 19 + Expo SDK 54 compatibility issues.**

React 19.1.0 and Expo SDK 54 are officially supported together, but known issues exist: web bundling/Metro errors and iOS hang reports have been reported by the community. Verify the app builds and runs correctly for both platforms before shipping. Zod 4.3.5 is compatible with React Native and has no native dependencies, so no action needed there. Note: the backend uses Zod 3.23.0 while the app uses 4.3.5—ensure schema compatibility between frontend and backend if they share validation logic.

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/package.json around lines 38 - 46, The PR uses "react": "19.1.0"
alongside Expo SDK 54 which has known React 19-related Metro/web and iOS issues;
reproduce and verify full app builds on iOS, Android and web (run the Metro
bundler, expo start, and an iOS simulator/device build) and if you hit bundling
or runtime hangs either pin React to a known-working version (e.g., 18.x) or
upgrade Expo/patch per the Expo release notes; also exercise components using
"react-native-reanimated" and "react-native-gesture-handler" to ensure native
module compatibility and confirm no additional config (hermes, babel plugin
order) is required. Finally, ensure schema compatibility between frontend "zod":
"^4.3.5" and backend zod 3.23.0—either align to the same major version or add
explicit migration/adapters for any shared validation logic.
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -12,34 +12,48 @@
     "test": "jest"
   },
   "dependencies": {
+    "@expo-google-fonts/plus-jakarta-sans": "^0.4.2",
+    "@expo/vector-icons": "^15.0.3",
     "@react-native-async-storage/async-storage": "2.2.0",
     "@react-native-community/netinfo": "^11.4.1",
     "@react-navigation/bottom-tabs": "^7.4.0",
     "@react-navigation/native": "^7.1.8",
     "date-fns": "^4.1.0",
     "expo": "~54.0.30",
+    "expo-blur": "^15.0.8",
     "expo-constants": "~18.0.12",
+    "expo-font": "^14.0.10",
+    "expo-haptics": "^15.0.8",
+    "expo-linear-gradient": "^15.0.8",
     "expo-linking": "~8.0.11",
+    "expo-localization": "^17.0.8",
     "expo-location": "~19.0.8",
     "expo-router": "~6.0.21",
     "expo-secure-store": "~15.0.8",
+    "expo-splash-screen": "^31.0.13",
     "expo-status-bar": "~3.0.9",
+    "i18n-js": "^4.5.1",
     "lucide-react-native": "^0.562.0",
+    "nativewind": "^4.2.1",
     "react": "19.1.0",
     "react-native": "0.81.5",
+    "react-native-css-interop": "^0.2.1",
     "react-native-gesture-handler": "~2.28.0",
     "react-native-reanimated": "~4.1.1",
     "react-native-safe-area-context": "~5.6.0",
     "react-native-screens": "~4.16.0",
     "react-native-svg": "15.12.1",
-    "react-native-worklets": "^0.5.1",
     "zod": "^4.3.5",



================================================================================
ファイル: app/src/components/BarChart.tsx
行: 9 (開始行: N/A)
コミット: a35fee7
作成日時: 2026-01-07T07:51:15Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
# Check what the theme file exports
rg -n "export.*colors" app/src/theme/ -A 2
```

Repository: Bluefinee/tempo-ai

Length of output: 292

---

🏁 Script executed:

```shell
# Check the actual import in BarChart.tsx
head -20 app/src/components/BarChart.tsx
```

Repository: Bluefinee/tempo-ai

Length of output: 467

---

🏁 Script executed:

```shell
# Check imports in other components for comparison
rg -n "import.*[Cc]olors.*from.*theme" app/src/components/ --no-heading
```

Repository: Bluefinee/tempo-ai

Length of output: 1571

---



</details>

**Use `Colors` import for consistency with design tokens.**

The codebase has mixed usage: 10 components import `Colors` (uppercase) while 8 use `colors` (lowercase). The canonical export is `Colors`, which aligns with the design token naming convention (Colors, Spacing, Typography). Update this import to match.

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/components/BarChart.tsx at line 9, The BarChart component imports
the design tokens using the lowercase symbol `colors`; change this import to use
the canonical `Colors` export to match the rest of the codebase and design token
naming. Update the import statement that currently references `colors` so it
imports `Colors` instead, and adjust any local usages in the file that reference
`colors` to use `Colors` (e.g., references within the BarChart component).
Ensure the import name matches exactly `Colors` to maintain consistency with
other components that use the design tokens.
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,85 @@
+/**
+ * BarChart - 履歴バーチャート
+ * スコア履歴を表示するバーチャート
+ */
+
+import React from 'react';
+import { View, Text, StyleSheet } from 'react-native';
+import Animated, { FadeInUp } from 'react-native-reanimated';
+import { colors } from '../theme';



================================================================================
ファイル: app/src/components/BarChart.tsx
行: 24 (開始行: 20)
コミット: a35fee7
作成日時: 2026-01-07T07:51:15Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟠 Major_

**Add explicit return type annotation.**

Per coding guidelines, all functions must declare explicit return types. The component should specify its return type.



<details>
<summary>🔧 Proposed fix</summary>

```diff
-export const BarChart: React.FC<BarChartProps> = ({
+export const BarChart: React.FC<BarChartProps> = ({
   data,
   color,
   maxValue = 100,
-}) => {
+}): React.ReactElement => {
```
</details>

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/components/BarChart.tsx around lines 20 - 24, The BarChart component
currently types the variable as React.FC<BarChartProps> but lacks an explicit
function return type; update the BarChart declaration to include an explicit
return annotation (e.g., : JSX.Element or : React.ReactElement) on the arrow
function so the signature for BarChart (the React component using BarChartProps)
clearly declares its return type while preserving props and default values.
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,85 @@
+/**
+ * BarChart - 履歴バーチャート
+ * スコア履歴を表示するバーチャート
+ */
+
+import React from 'react';
+import { View, Text, StyleSheet } from 'react-native';
+import Animated, { FadeInUp } from 'react-native-reanimated';
+import { colors } from '../theme';
+
+interface BarChartProps {
+  data: {
+    label: string;
+    value: number;
+  }[];
+  color: string;
+  maxValue?: number;
+}
+
+export const BarChart: React.FC<BarChartProps> = ({
+  data,
+  color,
+  maxValue = 100,
+}) => {



================================================================================
ファイル: app/src/components/BarChart.tsx
行: 30 (開始行: N/A)
コミット: a35fee7
作成日時: 2026-01-07T07:51:15Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**Avoid using index as key.**

Using array index as key is an anti-pattern that can cause rendering issues when data is reordered or filtered. Use a unique identifier from the data instead.



<details>
<summary>♻️ Recommended solution</summary>

If the data has a unique identifier, use it:
```diff
-<View key={index} style={styles.barContainer}>
+<View key={`${item.label}-${item.value}`} style={styles.barContainer}>
```

Or update the `BarChartProps` interface to require unique IDs:
```typescript
interface BarChartProps {
  data: {
    id: string;
    label: string;
    value: number;
  }[];
  color: string;
  maxValue?: number;
}
```
</details>


> Committable suggestion skipped: line range outside the PR's diff.

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/components/BarChart.tsx at line 30, The BarChart uses array index as
a React key which can break rendering; update the BarChartProps to require a
unique id on each data item (e.g., interface BarChartProps { data: { id: string;
label: string; value: number }[]; ... }) and change the key on the mapped
element in the BarChart component (replace key={index} on the View with key
derived from the item's id, referencing styles.barContainer for the element) —
if you cannot require ids immediately, use a stable derived key (e.g.,
`${item.label}-${item.value}`) until data includes unique ids.
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,85 @@
+/**
+ * BarChart - 履歴バーチャート
+ * スコア履歴を表示するバーチャート
+ */
+
+import React from 'react';
+import { View, Text, StyleSheet } from 'react-native';
+import Animated, { FadeInUp } from 'react-native-reanimated';
+import { colors } from '../theme';
+
+interface BarChartProps {
+  data: {
+    label: string;
+    value: number;
+  }[];
+  color: string;
+  maxValue?: number;
+}
+
+export const BarChart: React.FC<BarChartProps> = ({
+  data,
+  color,
+  maxValue = 100,
+}) => {
+  return (
+    <View style={styles.container}>
+      {data.map((item, index) => {
+        const heightPercentage = (item.value / maxValue) * 100;
+        return (
+          <View key={index} style={styles.barContainer}>



================================================================================
ファイル: app/src/components/BarChart.tsx
行: 58 (開始行: 50)
コミット: a35fee7
作成日時: 2026-01-07T07:51:15Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟠 Major_

**Add explicit return types to helper functions.**

Per coding guidelines, all functions must have explicit return type annotations.



<details>
<summary>🔧 Proposed fix</summary>

```diff
-const addHours = (date: Date, hours: number) => {
+const addHours = (date: Date, hours: number): Date => {
   return new Date(date.getTime() + hours * 60 * 60 * 1000);
 };
 
-const formatTime = (date: Date) => {
+const formatTime = (date: Date): string => {
   const hours = date.getHours().toString().padStart(2, '0');
   const minutes = date.getMinutes().toString().padStart(2, '0');
   return `${hours}:${minutes}`;
 };
```
</details>


> Committable suggestion skipped: line range outside the PR's diff.

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/components/BarChart.tsx around lines 50 - 58, Several helper
functions in this BarChart component lack explicit TypeScript return type
annotations; update each helper (e.g., functions that compute bar heights,
format labels, or determine colors — common names might be calculateBarHeight,
formatLabel, getColorForValue, renderBar) to include explicit return types
(number, string, JSX.Element, boolean, or appropriate object/array types). Add
these annotations to the function signatures (not inferred) and update any
exported types if needed so the component compiles with strict typing.
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,85 @@
+/**
+ * BarChart - 履歴バーチャート
+ * スコア履歴を表示するバーチャート
+ */
+
+import React from 'react';
+import { View, Text, StyleSheet } from 'react-native';
+import Animated, { FadeInUp } from 'react-native-reanimated';
+import { colors } from '../theme';
+
+interface BarChartProps {
+  data: {
+    label: string;
+    value: number;
+  }[];
+  color: string;
+  maxValue?: number;
+}
+
+export const BarChart: React.FC<BarChartProps> = ({
+  data,
+  color,
+  maxValue = 100,
+}) => {
+  return (
+    <View style={styles.container}>
+      {data.map((item, index) => {
+        const heightPercentage = (item.value / maxValue) * 100;
+        return (
+          <View key={index} style={styles.barContainer}>
+            <Animated.View
+              entering={FadeInUp.delay(index * 50).duration(400)}
+              style={styles.barWrapper}
+            >
+              <View
+                style={[
+                  styles.bar,
+                  {
+                    height: `${heightPercentage}%`,
+                    backgroundColor: color,
+                  },
+                ]}
+              />
+            </Animated.View>
+            <Text style={styles.label}>{item.label}</Text>
+          </View>
+        );
+      })}
+    </View>
+  );
+};
+
+const styles = StyleSheet.create({
+  container: {
+    flexDirection: 'row',
+    justifyContent: 'space-between',
+    alignItems: 'flex-end',
+    height: 200,



================================================================================
ファイル: app/src/components/CircularProgress.tsx
行: 82 (開始行: 27)
コミット: a35fee7
作成日時: 2026-01-07T07:51:15Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

**Add explicit return type annotation for component.**

The `CircularProgress` component is missing an explicit return type. Per TypeScript coding guidelines, all functions must declare explicit return types.



<details>
<summary>📝 Proposed fix</summary>

```diff
-export const CircularProgress: React.FC<CircularProgressProps> = ({
+export const CircularProgress = ({
   size,
   strokeWidth,
   progress,
   color,
   backgroundColor = '#E7E5E4',
   duration = 1000,
-}) => {
+}: CircularProgressProps): React.ReactElement => {
```
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
export const CircularProgress = ({
  size,
  strokeWidth,
  progress,
  color,
  backgroundColor = '#E7E5E4',
  duration = 1000,
}: CircularProgressProps): React.ReactElement => {
  const radius = (size - strokeWidth) / 2;
  const circumference = 2 * Math.PI * radius;
  const progressValue = useSharedValue(0);

  React.useEffect(() => {
    progressValue.value = withTiming(progress, {
      duration,
      easing: Easing.bezier(0.25, 0.1, 0.25, 1),
    });
  }, [progress, duration, progressValue]);

  const animatedProps = useAnimatedProps(() => {
    const strokeDashoffset = circumference - (progressValue.value / 100) * circumference;
    return {
      strokeDashoffset,
    };
  });

  return (
    <View style={[styles.container, { width: size, height: size }]}>
      <Svg width={size} height={size}>
        {/* Background Circle */}
        <Circle
          cx={size / 2}
          cy={size / 2}
          r={radius}
          stroke={backgroundColor}
          strokeWidth={strokeWidth}
          fill="none"
        />
        {/* Progress Circle */}
        <AnimatedCircle
          cx={size / 2}
          cy={size / 2}
          r={radius}
          stroke={color}
          strokeWidth={strokeWidth}
          fill="none"
          strokeDasharray={circumference}
          strokeLinecap="round"
          animatedProps={animatedProps}
          rotation="-90"
          origin={`${size / 2}, ${size / 2}`}
        />
      </Svg>
    </View>
  );
};
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/components/CircularProgress.tsx around lines 27 - 82, The
CircularProgress component currently lacks an explicit function return type;
update the component signature for CircularProgress (which accepts
CircularProgressProps) to declare a return type (e.g. : JSX.Element or :
React.ReactElement) — adjust the export line for CircularProgress and add any
necessary type imports from React so the file compiles with the new explicit
return type.
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,90 @@
+/**
+ * CircularProgress - サーキュラープログレスリング
+ * スコア表示用のサークルプログレス
+ */
+
+import React from 'react';
+import { View, StyleSheet } from 'react-native';
+import Svg, { Circle } from 'react-native-svg';
+import Animated, {
+  useAnimatedProps,
+  useSharedValue,
+  withTiming,
+  Easing,
+} from 'react-native-reanimated';
+
+interface CircularProgressProps {
+  size: number;
+  strokeWidth: number;
+  progress: number; // 0-100
+  color: string;
+  backgroundColor?: string;
+  duration?: number;
+}
+
+const AnimatedCircle = Animated.createAnimatedComponent(Circle);
+
+export const CircularProgress: React.FC<CircularProgressProps> = ({
+  size,
+  strokeWidth,
+  progress,
+  color,
+  backgroundColor = '#E7E5E4',
+  duration = 1000,
+}) => {
+  const radius = (size - strokeWidth) / 2;
+  const circumference = 2 * Math.PI * radius;
+  const progressValue = useSharedValue(0);
+
+  React.useEffect(() => {
+    progressValue.value = withTiming(progress, {
+      duration,
+      easing: Easing.bezier(0.25, 0.1, 0.25, 1),
+    });
+  }, [progress, duration, progressValue]);
+
+  const animatedProps = useAnimatedProps(() => {
+    const strokeDashoffset = circumference - (progressValue.value / 100) * circumference;
+    return {
+      strokeDashoffset,
+    };
+  });
+
+  return (
+    <View style={[styles.container, { width: size, height: size }]}>
+      <Svg width={size} height={size}>
+        {/* Background Circle */}
+        <Circle
+          cx={size / 2}
+          cy={size / 2}
+          r={radius}
+          stroke={backgroundColor}
+          strokeWidth={strokeWidth}
+          fill="none"
+        />
+        {/* Progress Circle */}
+        <AnimatedCircle
+          cx={size / 2}
+          cy={size / 2}
+          r={radius}
+          stroke={color}
+          strokeWidth={strokeWidth}
+          fill="none"
+          strokeDasharray={circumference}
+          strokeLinecap="round"
+          animatedProps={animatedProps}
+          rotation="-90"
+          origin={`${size / 2}, ${size / 2}`}
+        />
+      </Svg>
+    </View>
+  );
+};



================================================================================
ファイル: app/src/components/DualRingProgress.tsx
行: 38 (開始行: 29)
コミット: a35fee7
作成日時: 2026-01-07T07:51:16Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟠 Major_

**Add explicit return type for the component.**

According to TypeScript best practices in the coding guidelines, all functions must declare explicit return types. Add `: JSX.Element` to the component function.



<details>
<summary>🔧 Proposed fix</summary>

```diff
-export const DualRingProgress: React.FC<DualRingProgressProps> = ({
+export const DualRingProgress = ({
   size,
   strokeWidth,
   innerProgress,
   outerProgress,
   innerColor,
   outerColor,
   backgroundColor = '#E7E5E4',
   duration = 1000,
-}) => {
+}): JSX.Element => {
```

Note: Also removed `React.FC` as it's no longer recommended in modern React patterns and adds unnecessary complexity.
</details>


> Committable suggestion skipped: line range outside the PR's diff.

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/components/DualRingProgress.tsx around lines 29 - 38, The
DualRingProgress component currently uses React.FC and lacks an explicit return
type; update the declaration for DualRingProgress to remove the React.FC generic
and add an explicit return type of JSX.Element (e.g., change the signature that
references DualRingProgressProps to return JSX.Element), ensuring the parameter
list (size, strokeWidth, innerProgress, outerProgress, innerColor, outerColor,
backgroundColor, duration) remains unchanged and types continue to come from
DualRingProgressProps.
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,135 @@
+/**
+ * DualRingProgress - 二重サークル
+ * 2つの指標を同時に表示するサーキュラープログレス
+ */
+
+import React from 'react';
+import { View, StyleSheet } from 'react-native';
+import Svg, { Circle } from 'react-native-svg';
+import Animated, {
+  useAnimatedProps,
+  useSharedValue,
+  withTiming,
+  Easing,
+} from 'react-native-reanimated';
+
+interface DualRingProgressProps {
+  size: number;
+  strokeWidth: number;
+  innerProgress: number; // 0-100
+  outerProgress: number; // 0-100
+  innerColor: string;
+  outerColor: string;
+  backgroundColor?: string;
+  duration?: number;
+}
+
+const AnimatedCircle = Animated.createAnimatedComponent(Circle);
+
+export const DualRingProgress: React.FC<DualRingProgressProps> = ({
+  size,
+  strokeWidth,
+  innerProgress,
+  outerProgress,
+  innerColor,
+  outerColor,
+  backgroundColor = '#E7E5E4',
+  duration = 1000,
+}) => {



================================================================================
ファイル: app/src/components/DualRingProgress.tsx
行: 36 (開始行: N/A)
コミット: a35fee7
作成日時: 2026-01-07T07:51:16Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**Use design tokens instead of hardcoded color values.**

The hardcoded color `'#E7E5E4'` should be replaced with a design token from the Colors constant to maintain consistency across the app.



<details>
<summary>♻️ Suggested approach</summary>

```diff
+import { Colors } from '@/constants/Colors';

-  backgroundColor = '#E7E5E4',
+  backgroundColor = Colors.stone[200], // or appropriate design token
```

As per coding guidelines: デザイントークン（Colors, Spacing, Typography）使用
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
import { Colors } from '@/constants/Colors';

  backgroundColor = Colors.stone[200],
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/components/DualRingProgress.tsx at line 36, Replace the hardcoded
background color '#E7E5E4' in DualRingProgress with the appropriate design token
from the Colors constant; locate where backgroundColor is defined (e.g., the
default prop or style in DualRingProgress) and swap the literal string for
Colors.Neutral200 (or the matching token in your app’s Colors export) so the
component uses the centralized design token instead of a hardcoded hex value.
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,135 @@
+/**
+ * DualRingProgress - 二重サークル
+ * 2つの指標を同時に表示するサーキュラープログレス
+ */
+
+import React from 'react';
+import { View, StyleSheet } from 'react-native';
+import Svg, { Circle } from 'react-native-svg';
+import Animated, {
+  useAnimatedProps,
+  useSharedValue,
+  withTiming,
+  Easing,
+} from 'react-native-reanimated';
+
+interface DualRingProgressProps {
+  size: number;
+  strokeWidth: number;
+  innerProgress: number; // 0-100
+  outerProgress: number; // 0-100
+  innerColor: string;
+  outerColor: string;
+  backgroundColor?: string;
+  duration?: number;
+}
+
+const AnimatedCircle = Animated.createAnimatedComponent(Circle);
+
+export const DualRingProgress: React.FC<DualRingProgressProps> = ({
+  size,
+  strokeWidth,
+  innerProgress,
+  outerProgress,
+  innerColor,
+  outerColor,
+  backgroundColor = '#E7E5E4',



================================================================================
ファイル: app/src/components/DualRingProgress.tsx
行: 42 (開始行: 39)
コミット: a35fee7
作成日時: 2026-01-07T07:51:16Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**Consider memoizing calculations for minor performance optimization.**

The radius and circumference calculations are recomputed on every render. While the performance impact is negligible for simple arithmetic, memoization follows React optimization best practices.



<details>
<summary>⚡ Optional optimization</summary>

```diff
+  const { innerRadius, outerRadius, innerCircumference, outerCircumference } = React.useMemo(() => {
-  const innerRadius = (size - strokeWidth * 3) / 2;
-  const outerRadius = (size - strokeWidth) / 2;
-  const innerCircumference = 2 * Math.PI * innerRadius;
-  const outerCircumference = 2 * Math.PI * outerRadius;
+    const inner = (size - strokeWidth * 3) / 2;
+    const outer = (size - strokeWidth) / 2;
+    return {
+      innerRadius: inner,
+      outerRadius: outer,
+      innerCircumference: 2 * Math.PI * inner,
+      outerCircumference: 2 * Math.PI * outer,
+    };
+  }, [size, strokeWidth]);
```
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
  const { innerRadius, outerRadius, innerCircumference, outerCircumference } = React.useMemo(() => {
    const inner = (size - strokeWidth * 3) / 2;
    const outer = (size - strokeWidth) / 2;
    return {
      innerRadius: inner,
      outerRadius: outer,
      innerCircumference: 2 * Math.PI * inner,
      outerCircumference: 2 * Math.PI * outer,
    };
  }, [size, strokeWidth]);
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/components/DualRingProgress.tsx around lines 39 - 42, The radius and
circumference math in DualRingProgress (innerRadius, outerRadius,
innerCircumference, outerCircumference) are recomputed on every render; wrap
these calculations in React.useMemo so they are only recalculated when their
inputs (size, strokeWidth) change, e.g., replace the plain const declarations
with memoized values dependent on [size, strokeWidth] to avoid unnecessary work
on rerenders.
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,135 @@
+/**
+ * DualRingProgress - 二重サークル
+ * 2つの指標を同時に表示するサーキュラープログレス
+ */
+
+import React from 'react';
+import { View, StyleSheet } from 'react-native';
+import Svg, { Circle } from 'react-native-svg';
+import Animated, {
+  useAnimatedProps,
+  useSharedValue,
+  withTiming,
+  Easing,
+} from 'react-native-reanimated';
+
+interface DualRingProgressProps {
+  size: number;
+  strokeWidth: number;
+  innerProgress: number; // 0-100
+  outerProgress: number; // 0-100
+  innerColor: string;
+  outerColor: string;
+  backgroundColor?: string;
+  duration?: number;
+}
+
+const AnimatedCircle = Animated.createAnimatedComponent(Circle);
+
+export const DualRingProgress: React.FC<DualRingProgressProps> = ({
+  size,
+  strokeWidth,
+  innerProgress,
+  outerProgress,
+  innerColor,
+  outerColor,
+  backgroundColor = '#E7E5E4',
+  duration = 1000,
+}) => {
+  const innerRadius = (size - strokeWidth * 3) / 2;
+  const outerRadius = (size - strokeWidth) / 2;
+  const innerCircumference = 2 * Math.PI * innerRadius;
+  const outerCircumference = 2 * Math.PI * outerRadius;



================================================================================
ファイル: app/src/components/DualRingProgress.tsx
行: 56 (開始行: 47)
コミット: a35fee7
作成日時: 2026-01-07T07:51:16Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

**Remove shared values from useEffect dependency array.**

`innerProgressValue` and `outerProgressValue` are refs created by `useSharedValue` and never change identity, so they shouldn't be in the dependency array. This may trigger ESLint warnings and is unnecessary.



<details>
<summary>🔧 Proposed fix</summary>

```diff
   React.useEffect(() => {
     innerProgressValue.value = withTiming(innerProgress, {
       duration,
       easing: Easing.bezier(0.25, 0.1, 0.25, 1),
     });
     outerProgressValue.value = withTiming(outerProgress, {
       duration,
       easing: Easing.bezier(0.25, 0.1, 0.25, 1),
     });
-  }, [innerProgress, outerProgress, duration, innerProgressValue, outerProgressValue]);
+  }, [innerProgress, outerProgress, duration]);
```
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
  React.useEffect(() => {
    innerProgressValue.value = withTiming(innerProgress, {
      duration,
      easing: Easing.bezier(0.25, 0.1, 0.25, 1),
    });
    outerProgressValue.value = withTiming(outerProgress, {
      duration,
      easing: Easing.bezier(0.25, 0.1, 0.25, 1),
    });
  }, [innerProgress, outerProgress, duration]);
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/components/DualRingProgress.tsx around lines 47 - 56, The useEffect
in DualRingProgress includes shared value refs innerProgressValue and
outerProgressValue in its dependency array even though their identities do not
change; remove innerProgressValue and outerProgressValue from the dependency
array and keep only the actual changing inputs (innerProgress, outerProgress,
duration) so the effect runs correctly without ESLint warnings.
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,135 @@
+/**
+ * DualRingProgress - 二重サークル
+ * 2つの指標を同時に表示するサーキュラープログレス
+ */
+
+import React from 'react';
+import { View, StyleSheet } from 'react-native';
+import Svg, { Circle } from 'react-native-svg';
+import Animated, {
+  useAnimatedProps,
+  useSharedValue,
+  withTiming,
+  Easing,
+} from 'react-native-reanimated';
+
+interface DualRingProgressProps {
+  size: number;
+  strokeWidth: number;
+  innerProgress: number; // 0-100
+  outerProgress: number; // 0-100
+  innerColor: string;
+  outerColor: string;
+  backgroundColor?: string;
+  duration?: number;
+}
+
+const AnimatedCircle = Animated.createAnimatedComponent(Circle);
+
+export const DualRingProgress: React.FC<DualRingProgressProps> = ({
+  size,
+  strokeWidth,
+  innerProgress,
+  outerProgress,
+  innerColor,
+  outerColor,
+  backgroundColor = '#E7E5E4',
+  duration = 1000,
+}) => {
+  const innerRadius = (size - strokeWidth * 3) / 2;
+  const outerRadius = (size - strokeWidth) / 2;
+  const innerCircumference = 2 * Math.PI * innerRadius;
+  const outerCircumference = 2 * Math.PI * outerRadius;
+
+  const innerProgressValue = useSharedValue(0);
+  const outerProgressValue = useSharedValue(0);
+
+  React.useEffect(() => {
+    innerProgressValue.value = withTiming(innerProgress, {
+      duration,
+      easing: Easing.bezier(0.25, 0.1, 0.25, 1),
+    });
+    outerProgressValue.value = withTiming(outerProgress, {
+      duration,
+      easing: Easing.bezier(0.25, 0.1, 0.25, 1),
+    });
+  }, [innerProgress, outerProgress, duration, innerProgressValue, outerProgressValue]);



================================================================================
ファイル: app/src/components/MetricGridCard.tsx
行: 11 (開始行: N/A)
コミット: a35fee7
作成日時: 2026-01-07T07:51:16Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

**Inconsistent use of design tokens.**

The `colors` theme is imported (line 11) and used once for the chevron color (line 47), but the StyleSheet contains several hardcoded color values:
- `#FFFFFF` (line 79)
- `#000` (line 86)
- `#A8A29E` (line 105)

For consistency and maintainability, replace these hardcoded values with design tokens from the theme.



<details>
<summary>♻️ Proposed fix to use design tokens consistently</summary>

```diff
  card: {
-    backgroundColor: '#FFFFFF',
+    backgroundColor: colors.white,
    borderRadius: 20,
    padding: 16,
    height: 160,
    justifyContent: 'space-between',
    overflow: 'hidden',
    // シャドウ（iOS）
-    shadowColor: '#000',
+    shadowColor: colors.black,
    shadowOffset: { width: 0, height: 8 },
    shadowOpacity: 0.12,
    shadowRadius: 16,
    // シャドウ（Android）
    elevation: 8,
  },
```

```diff
  title: {
    fontSize: 14,
    fontWeight: '500',
-    color: '#A8A29E',
+    color: colors.stone[400], // or appropriate token
  },
```
</details>

As per coding guidelines, maintain consistency with design tokens across the codebase.


Also applies to: 77-126

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,126 @@
+/**
+ * MetricGridCard - 2x2グリッド用のメトリクスカード
+ * Recovery, Sleep, Rhythm, Energyスコアを表示
+ * StyleSheet.createのみ使用（NativeWind排除）
+ */
+
+import React from 'react';
+import { View, Text, Pressable, StyleSheet } from 'react-native';
+import { ChevronRight } from 'lucide-react-native';
+import Animated, { FadeInDown } from 'react-native-reanimated';
+import { colors } from '../theme';



================================================================================
ファイル: app/src/components/MetricGridCard.tsx
行: 75 (開始行: 25)
コミット: a35fee7
作成日時: 2026-01-07T07:51:16Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**Consider wrapping with React.memo for grid performance.**

Since this component is designed for use in a 2x2 grid and receives primitive props (strings, arrays, function), wrapping it with `React.memo` would prevent unnecessary re-renders when sibling cards update.



<details>
<summary>⚡ Proposed optimization</summary>

```diff
-export const MetricGridCard: React.FC<MetricGridCardProps> = ({
+export const MetricGridCard: React.FC<MetricGridCardProps> = React.memo(({
  title,
  value,
  color,
  accentColor,
  chartData,
  onPress,
  delay = 0,
-}) => {
+}) => {
  return (
    <AnimatedPressable
      entering={FadeInDown.delay(delay).duration(400)}
      onPress={onPress}
      style={({ pressed }) => [
        styles.card,
        pressed && styles.cardPressed,
      ]}
    >
      {/* Header */}
      <View style={styles.header}>
        <Text style={styles.title}>{title}</Text>
        <View style={styles.chevronWrapper}>
          <ChevronRight size={18} color={colors.stone[300]} />
        </View>
      </View>

      {/* Value */}
      <Text style={[styles.value, { color }]}>{value}</Text>

      {/* Mini Bar Chart */}
      <View style={styles.barChart}>
        {chartData.map((val, i) => {
          const isLast = i === chartData.length - 1;
          return (
            <View
              key={i}
              style={[
                styles.bar,
                {
                  height: `${Math.max(val, 5)}%`,
                  backgroundColor: accentColor,
                  opacity: isLast ? 1 : 0.25,
                },
              ]}
            />
          );
        })}
      </View>
    </AnimatedPressable>
  );
-};
+});
```
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
export const MetricGridCard: React.FC<MetricGridCardProps> = React.memo(({
  title,
  value,
  color,
  accentColor,
  chartData,
  onPress,
  delay = 0,
}) => {
  return (
    <AnimatedPressable
      entering={FadeInDown.delay(delay).duration(400)}
      onPress={onPress}
      style={({ pressed }) => [
        styles.card,
        pressed && styles.cardPressed,
      ]}
    >
      {/* Header */}
      <View style={styles.header}>
        <Text style={styles.title}>{title}</Text>
        <View style={styles.chevronWrapper}>
          <ChevronRight size={18} color={colors.stone[300]} />
        </View>
      </View>

      {/* Value */}
      <Text style={[styles.value, { color }]}>{value}</Text>

      {/* Mini Bar Chart */}
      <View style={styles.barChart}>
        {chartData.map((val, i) => {
          const isLast = i === chartData.length - 1;
          return (
            <View
              key={i}
              style={[
                styles.bar,
                {
                  height: `${Math.max(val, 5)}%`,
                  backgroundColor: accentColor,
                  opacity: isLast ? 1 : 0.25,
                },
              ]}
            />
          );
        })}
      </View>
    </AnimatedPressable>
  );
});
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/components/MetricGridCard.tsx around lines 25 - 75, Wrap the
MetricGridCard component with React.memo to avoid unnecessary re-renders: change
the current export of MetricGridCard so the component is memoized (e.g., export
the result of React.memo(MetricGridCard) or assign React.memo(...) to
MetricGridCard) and ensure props are stable (keep prop names title, value,
color, accentColor, chartData, onPress, delay) so shallow comparison works; if
any props are non-primitive callbacks, consider wrapping them with useCallback
at the caller to get full benefit.
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,126 @@
+/**
+ * MetricGridCard - 2x2グリッド用のメトリクスカード
+ * Recovery, Sleep, Rhythm, Energyスコアを表示
+ * StyleSheet.createのみ使用（NativeWind排除）
+ */
+
+import React from 'react';
+import { View, Text, Pressable, StyleSheet } from 'react-native';
+import { ChevronRight } from 'lucide-react-native';
+import Animated, { FadeInDown } from 'react-native-reanimated';
+import { colors } from '../theme';
+
+interface MetricGridCardProps {
+  title: string;
+  value: string;
+  color: string;
+  accentColor: string;
+  chartData: number[];
+  onPress: () => void;
+  delay?: number;
+}
+
+const AnimatedPressable = Animated.createAnimatedComponent(Pressable);
+
+export const MetricGridCard: React.FC<MetricGridCardProps> = ({
+  title,
+  value,
+  color,
+  accentColor,
+  chartData,
+  onPress,
+  delay = 0,
+}) => {
+  return (
+    <AnimatedPressable
+      entering={FadeInDown.delay(delay).duration(400)}
+      onPress={onPress}
+      style={({ pressed }) => [
+        styles.card,
+        pressed && styles.cardPressed,
+      ]}
+    >
+      {/* Header */}
+      <View style={styles.header}>
+        <Text style={styles.title}>{title}</Text>
+        <View style={styles.chevronWrapper}>
+          <ChevronRight size={18} color={colors.stone[300]} />
+        </View>
+      </View>
+
+      {/* Value */}
+      <Text style={[styles.value, { color }]}>{value}</Text>
+
+      {/* Mini Bar Chart */}
+      <View style={styles.barChart}>
+        {chartData.map((val, i) => {
+          const isLast = i === chartData.length - 1;
+          return (
+            <View
+              key={i}
+              style={[
+                styles.bar,
+                {
+                  height: `${Math.max(val, 5)}%`,
+                  backgroundColor: accentColor,
+                  opacity: isLast ? 1 : 0.25,
+                },
+              ]}
+            />
+          );
+        })}
+      </View>
+    </AnimatedPressable>
+  );
+};



================================================================================
ファイル: app/src/components/MiniBarChart.tsx
行: 11 (開始行: N/A)
コミット: a35fee7
作成日時: 2026-01-07T07:51:16Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

**Remove unused import `TextStyle`.**

The `TextStyle` type is imported but never used in this file.



<details>
<summary>🧹 Proposed fix</summary>

```diff
-import { View, Text, StyleSheet, ViewStyle, TextStyle } from 'react-native';
+import { View, Text, StyleSheet, ViewStyle } from 'react-native';
```
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
import { View, Text, StyleSheet, ViewStyle } from 'react-native';
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/components/MiniBarChart.tsx at line 11, The import list in
MiniBarChart.tsx includes an unused type TextStyle; remove TextStyle from the
import statement (keep View, Text, StyleSheet, ViewStyle) so the top-line import
no longer references TextStyle and rerun lint/typecheck to ensure no remaining
references to TextStyle in this file.
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,148 @@
+/**
+ * MiniBarChart - 統一バーチャートコンポーネント
+ *
+ * ホーム画面と詳細画面で共通のバーチャートスタイルを提供
+ * - シンプルなバー表示
+ * - 最後のバーをハイライト
+ * - オプションでラベル表示
+ */
+
+import React from 'react';
+import { View, Text, StyleSheet, ViewStyle, TextStyle } from 'react-native';



================================================================================
ファイル: app/src/components/MiniBarChart.tsx
行: 75 (開始行: 64)
コミット: a35fee7
作成日時: 2026-01-07T07:51:16Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

**Add explicit return type for the component.**

Per coding guidelines, all functions must declare explicit return types.



<details>
<summary>📝 Proposed fix</summary>

```diff
-export const MiniBarChart: React.FC<MiniBarChartProps> = ({
+export const MiniBarChart = ({
   data,
   color,
   height = 48,
   showLabels = false,
   maxValue = 100,
   inactiveOpacity = 0.3,
   animated = false,
   gap = 4,
   borderRadius = 2,
   style,
-}) => {
+}: MiniBarChartProps): React.ReactElement => {
```
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
export const MiniBarChart = ({
  data,
  color,
  height = 48,
  showLabels = false,
  maxValue = 100,
  inactiveOpacity = 0.3,
  animated = false,
  gap = 4,
  borderRadius = 2,
  style,
}: MiniBarChartProps): React.ReactElement => {
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/components/MiniBarChart.tsx around lines 64 - 75, The MiniBarChart
component is missing an explicit function return type per guidelines; change its
signature so the function explicitly returns a JSX.Element (or
React.ReactElement) while keeping the props typed as MiniBarChartProps — i.e.,
keep MiniBarChartProps for the parameter type but remove/replace the React.FC
annotation and annotate the arrow function with an explicit return type for
MiniBarChart (symbol: MiniBarChart, type: MiniBarChartProps) so the component
signature declares the return type.
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,148 @@
+/**
+ * MiniBarChart - 統一バーチャートコンポーネント
+ *
+ * ホーム画面と詳細画面で共通のバーチャートスタイルを提供
+ * - シンプルなバー表示
+ * - 最後のバーをハイライト
+ * - オプションでラベル表示
+ */
+
+import React from 'react';
+import { View, Text, StyleSheet, ViewStyle, TextStyle } from 'react-native';
+import Animated, { FadeInUp } from 'react-native-reanimated';
+import { colors } from '../theme';
+
+export interface MiniBarChartData {
+  label?: string;
+  value: number;
+}
+
+export interface MiniBarChartProps {
+  /** チャートデータ */
+  data: MiniBarChartData[];
+  /** バーの色 */
+  color: string;
+  /** チャートの高さ（デフォルト: 48） */
+  height?: number;
+  /** ラベルを表示するか（デフォルト: false） */
+  showLabels?: boolean;
+  /** 最大値（デフォルト: 100） */
+  maxValue?: number;
+  /** 非アクティブバーの透明度（デフォルト: 0.3） */
+  inactiveOpacity?: number;
+  /** アニメーションを有効にするか（デフォルト: false） */
+  animated?: boolean;
+  /** バー間のギャップ（デフォルト: 4） */
+  gap?: number;
+  /** バーの角丸（デフォルト: 2） */
+  borderRadius?: number;
+  /** コンテナスタイル */
+  style?: ViewStyle;
+}
+
+/**
+ * MiniBarChart コンポーネント
+ *
+ * @example
+ * // ホーム画面での使用（ラベルなし、コンパクト）
+ * <MiniBarChart
+ *   data={[{ value: 40 }, { value: 60 }, { value: 70 }]}
+ *   color={colors.emerald[500]}
+ *   height={48}
+ * />
+ *
+ * @example
+ * // 詳細画面での使用（ラベルあり、大きめ）
+ * <MiniBarChart
+ *   data={history['7D']}
+ *   color={colors.emerald[500]}
+ *   height={120}
+ *   showLabels
+ *   animated
+ * />
+ */
+export const MiniBarChart: React.FC<MiniBarChartProps> = ({
+  data,
+  color,
+  height = 48,
+  showLabels = false,
+  maxValue = 100,
+  inactiveOpacity = 0.3,
+  animated = false,
+  gap = 4,
+  borderRadius = 2,
+  style,
+}) => {



================================================================================
ファイル: app/src/components/MiniBarChart.tsx
行: 115 (開始行: 79)
コミット: a35fee7
作成日時: 2026-01-07T07:51:16Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

**Add explicit return type for `renderBar` function.**

The `renderBar` function should declare its return type explicitly.



<details>
<summary>📝 Proposed fix</summary>

```diff
-  const renderBar = (item: MiniBarChartData, index: number) => {
+  const renderBar = (item: MiniBarChartData, index: number): React.ReactElement => {
     const heightPercentage = Math.min((item.value / maxValue) * 100, 100);
```
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
  const renderBar = (item: MiniBarChartData, index: number): React.ReactElement => {
    const heightPercentage = Math.min((item.value / maxValue) * 100, 100);
    const isLast = index === lastIndex;
    const opacity = isLast ? 1 : inactiveOpacity;

    const barStyle: ViewStyle = {
      height: `${heightPercentage}%`,
      backgroundColor: color,
      opacity,
      borderRadius,
      minHeight: 2,
    };

    if (animated) {
      return (
        <Animated.View
          key={index}
          entering={FadeInUp.delay(index * 30).duration(300)}
          style={[styles.barWrapper, { height: chartHeight }]}
        >
          <View style={barStyle} />
          {showLabels && item.label && (
            <Text style={styles.label}>{item.label}</Text>
          )}
        </Animated.View>
      );
    }

    return (
      <View key={index} style={[styles.barWrapper, { height: chartHeight }]}>
        <View style={barStyle} />
        {showLabels && item.label && (
          <Text style={styles.label}>{item.label}</Text>
        )}
      </View>
    );
  };
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/components/MiniBarChart.tsx around lines 79 - 115, The renderBar
function lacks an explicit return type—update the renderBar signature (the
function named renderBar that takes (item: MiniBarChartData, index: number)) to
declare its return type explicitly (e.g., JSX.Element | null or
React.ReactElement | null) so the component's returned JSX is typed; adjust the
signature accordingly and ensure any early returns match the chosen type.
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,148 @@
+/**
+ * MiniBarChart - 統一バーチャートコンポーネント
+ *
+ * ホーム画面と詳細画面で共通のバーチャートスタイルを提供
+ * - シンプルなバー表示
+ * - 最後のバーをハイライト
+ * - オプションでラベル表示
+ */
+
+import React from 'react';
+import { View, Text, StyleSheet, ViewStyle, TextStyle } from 'react-native';
+import Animated, { FadeInUp } from 'react-native-reanimated';
+import { colors } from '../theme';
+
+export interface MiniBarChartData {
+  label?: string;
+  value: number;
+}
+
+export interface MiniBarChartProps {
+  /** チャートデータ */
+  data: MiniBarChartData[];
+  /** バーの色 */
+  color: string;
+  /** チャートの高さ（デフォルト: 48） */
+  height?: number;
+  /** ラベルを表示するか（デフォルト: false） */
+  showLabels?: boolean;
+  /** 最大値（デフォルト: 100） */
+  maxValue?: number;
+  /** 非アクティブバーの透明度（デフォルト: 0.3） */
+  inactiveOpacity?: number;
+  /** アニメーションを有効にするか（デフォルト: false） */
+  animated?: boolean;
+  /** バー間のギャップ（デフォルト: 4） */
+  gap?: number;
+  /** バーの角丸（デフォルト: 2） */
+  borderRadius?: number;
+  /** コンテナスタイル */
+  style?: ViewStyle;
+}
+
+/**
+ * MiniBarChart コンポーネント
+ *
+ * @example
+ * // ホーム画面での使用（ラベルなし、コンパクト）
+ * <MiniBarChart
+ *   data={[{ value: 40 }, { value: 60 }, { value: 70 }]}
+ *   color={colors.emerald[500]}
+ *   height={48}
+ * />
+ *
+ * @example
+ * // 詳細画面での使用（ラベルあり、大きめ）
+ * <MiniBarChart
+ *   data={history['7D']}
+ *   color={colors.emerald[500]}
+ *   height={120}
+ *   showLabels
+ *   animated
+ * />
+ */
+export const MiniBarChart: React.FC<MiniBarChartProps> = ({
+  data,
+  color,
+  height = 48,
+  showLabels = false,
+  maxValue = 100,
+  inactiveOpacity = 0.3,
+  animated = false,
+  gap = 4,
+  borderRadius = 2,
+  style,
+}) => {
+  const chartHeight = showLabels ? height - 20 : height;
+  const lastIndex = data.length - 1;
+
+  const renderBar = (item: MiniBarChartData, index: number) => {
+    const heightPercentage = Math.min((item.value / maxValue) * 100, 100);
+    const isLast = index === lastIndex;
+    const opacity = isLast ? 1 : inactiveOpacity;
+
+    const barStyle: ViewStyle = {
+      height: `${heightPercentage}%`,
+      backgroundColor: color,
+      opacity,
+      borderRadius,
+      minHeight: 2,
+    };
+
+    if (animated) {
+      return (
+        <Animated.View
+          key={index}
+          entering={FadeInUp.delay(index * 30).duration(300)}
+          style={[styles.barWrapper, { height: chartHeight }]}
+        >
+          <View style={barStyle} />
+          {showLabels && item.label && (
+            <Text style={styles.label}>{item.label}</Text>
+          )}
+        </Animated.View>
+      );
+    }
+
+    return (
+      <View key={index} style={[styles.barWrapper, { height: chartHeight }]}>
+        <View style={barStyle} />
+        {showLabels && item.label && (
+          <Text style={styles.label}>{item.label}</Text>
+        )}
+      </View>
+    );
+  };



================================================================================
ファイル: app/src/components/PrimaryButton.tsx
行: 17 (開始行: N/A)
コミット: a35fee7
作成日時: 2026-01-07T07:51:16Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

**Remove unused imports: BorderRadius and Spacing.**

Per coding guidelines, unused imports must be removed. `BorderRadius` and `Spacing` are imported but never referenced in this file.



<details>
<summary>🧹 Proposed fix</summary>

```diff
-import { Colors, BorderRadius, Spacing, FontFamily } from '../theme';
+import { Colors, FontFamily } from '../theme';
```
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
import { Colors, FontFamily } from '../theme';
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/components/PrimaryButton.tsx at line 17, Remove the unused imports
BorderRadius and Spacing from the import that currently reads with Colors and
FontFamily; update the import statement so it only imports Colors and FontFamily
(e.g., adjust the import in PrimaryButton.tsx), ensuring no other references to
BorderRadius or Spacing remain in the file.
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -6,8 +11,10 @@ import {
   ViewStyle,
   StyleProp,
   ActivityIndicator,
+  View,
 } from 'react-native';
-import { Colors, BorderRadius, Spacing, Typography } from '../theme';
+import { ChevronRight, Check } from 'lucide-react-native';
+import { Colors, BorderRadius, Spacing, FontFamily } from '../theme';



================================================================================
ファイル: app/src/components/PrimaryButton.tsx
行: 82 (開始行: 71)
コミット: a35fee7
作成日時: 2026-01-07T07:51:16Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**Consider using Spacing design tokens for consistency.**

Multiple hardcoded spacing values (999, 16, 32, 24, 8) could be replaced with the `Spacing` design token for better maintainability and consistency with the theme system. While the current implementation works, using design tokens makes it easier to maintain consistent spacing across the app and adapt to design changes.




Also applies to: 93-93, 98-98

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/components/PrimaryButton.tsx around lines 71 - 82, The PrimaryButton
component uses hardcoded spacing numbers in its style (borderRadius,
paddingVertical, paddingLeft, paddingRight, shadowOffset.height, elevation);
import and use the app's Spacing design tokens instead (e.g., map 16→Spacing[4],
32→Spacing[8], 24→Spacing[6], 8→Spacing[2], and replace 999 with the theme's
rounded/full token) and update the same hardcoded values in the other
PrimaryButton style occurrences; ensure you reference the Spacing tokens from
your theme export and replace numeric literals in the PrimaryButton style object
so spacing is consistent and maintainable.
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -39,31 +50,51 @@ export const PrimaryButton: React.FC<PrimaryButtonProps> = ({
       {loading ? (
         <ActivityIndicator color={Colors.white} />
       ) : (
-        <Text style={styles.text}>{children}</Text>
+        <View style={styles.content}>
+          <Text style={styles.text}>{children}</Text>
+          {showIcon && (
+            isLast ? (
+              <Check size={20} color={Colors.white} style={styles.icon} />
+            ) : (
+              <ChevronRight size={20} color={Colors.white} style={styles.icon} />
+            )
+          )}
+        </View>
       )}
     </TouchableOpacity>
   );
 };
 
 const styles = StyleSheet.create({
   button: {
-    backgroundColor: Colors.primary[500],
-    borderRadius: BorderRadius.full,
-    paddingVertical: Spacing.lg,
-    paddingHorizontal: Spacing.xxl,
+    backgroundColor: Colors.indigo[900], // bg-indigo-900 (matching sozai)
+    borderRadius: 999, // rounded-full
+    paddingVertical: 16, // py-4
+    paddingLeft: 32, // pl-8
+    paddingRight: 24, // pr-6
     alignItems: 'center',
     justifyContent: 'center',
-    shadowColor: Colors.primary[500],
-    shadowOffset: { width: 0, height: 4 },
-    shadowOpacity: 0.3,
-    shadowRadius: 8,
-    elevation: 4,
+    // shadow-xl shadow-indigo-900/20
+    shadowColor: Colors.indigo[900],
+    shadowOffset: { width: 0, height: 8 },
+    shadowOpacity: 0.2,
+    shadowRadius: 16,
+    elevation: 8,



================================================================================
ファイル: app/src/components/SleepStagesBar.tsx
行: 71 (開始行: 34)
コミット: a35fee7
作成日時: 2026-01-07T07:51:16Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

**Add explicit return type annotation for component.**

The `SleepStagesBar` component is missing an explicit return type. Per TypeScript coding guidelines, all functions (including React components) must declare explicit return types.



<details>
<summary>📝 Proposed fix</summary>

```diff
-export const SleepStagesBar: React.FC<SleepStagesBarProps> = ({ stages }) => {
+export const SleepStagesBar = ({ stages }: SleepStagesBarProps): React.ReactElement => {
```

Note: `React.FC` is being removed in favor of explicit typing. This approach also provides better type inference.
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
export const SleepStagesBar = ({ stages }: SleepStagesBarProps): React.ReactElement => {
  return (
    <View style={styles.container}>
      {/* Bar */}
      <View style={styles.bar}>
        {stages.map((stage, index) => (
          <Animated.View
            key={index}
            entering={FadeIn.delay(index * 100).duration(400)}
            style={[
              styles.segment,
              {
                flex: stage.percentage,
                backgroundColor: stageColors[stage.stage],
              },
            ]}
          />
        ))}
      </View>

      {/* Legend */}
      <View style={styles.legend}>
        {stages.map((stage, index) => (
          <View key={index} style={styles.legendItem}>
            <View
              style={[
                styles.legendDot,
                { backgroundColor: stageColors[stage.stage] },
              ]}
            />
            <Text style={styles.legendLabel}>{stageLabels[stage.stage]}</Text>
            <Text style={styles.legendValue}>{stage.percentage}%</Text>
          </View>
        ))}
      </View>
    </View>
  );
};
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/components/SleepStagesBar.tsx around lines 34 - 71, The component
SleepStagesBar currently lacks an explicit return type; update its signature to
include an explicit JSX return type by changing the declaration from using
React.FC to an explicit props and return annotation—for example, keep the props
type SleepStagesBarProps on the parameter ({ stages }: SleepStagesBarProps) and
add a return type of JSX.Element (or React.ReactElement) to the SleepStagesBar
function signature so the function is explicitly typed.
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,113 @@
+/**
+ * SleepStagesBar - 睡眠ステージバー
+ * 睡眠詳細画面で睡眠ステージを表示
+ */
+
+import React from 'react';
+import { View, Text, StyleSheet } from 'react-native';
+import Animated, { FadeIn } from 'react-native-reanimated';
+import { colors } from '../theme';
+
+interface SleepStage {
+  stage: 'deep' | 'rem' | 'light' | 'awake';
+  percentage: number;
+}
+
+interface SleepStagesBarProps {
+  stages: SleepStage[];
+}
+
+const stageColors = {
+  deep: colors.indigo[600],
+  rem: colors.purple[500],
+  light: colors.blue[400],
+  awake: colors.amber[400],
+};
+
+const stageLabels = {
+  deep: 'Deep',
+  rem: 'REM',
+  light: 'Light',
+  awake: 'Awake',
+};
+
+export const SleepStagesBar: React.FC<SleepStagesBarProps> = ({ stages }) => {
+  return (
+    <View style={styles.container}>
+      {/* Bar */}
+      <View style={styles.bar}>
+        {stages.map((stage, index) => (
+          <Animated.View
+            key={index}
+            entering={FadeIn.delay(index * 100).duration(400)}
+            style={[
+              styles.segment,
+              {
+                flex: stage.percentage,
+                backgroundColor: stageColors[stage.stage],
+              },
+            ]}
+          />
+        ))}
+      </View>
+
+      {/* Legend */}
+      <View style={styles.legend}>
+        {stages.map((stage, index) => (
+          <View key={index} style={styles.legendItem}>
+            <View
+              style={[
+                styles.legendDot,
+                { backgroundColor: stageColors[stage.stage] },
+              ]}
+            />
+            <Text style={styles.legendLabel}>{stageLabels[stage.stage]}</Text>
+            <Text style={styles.legendValue}>{stage.percentage}%</Text>
+          </View>
+        ))}
+      </View>
+    </View>
+  );
+};



================================================================================
ファイル: app/src/components/SunInfoCard.tsx
行: 42 (開始行: 25)
コミット: a35fee7
作成日時: 2026-01-07T07:51:17Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

**Add explicit return type for `getTypeConfig`.**

The helper function lacks an explicit return type annotation. Per coding guidelines, all functions should declare explicit return types.



<details>
<summary>📝 Proposed fix</summary>

```diff
+type TypeConfig = {
+  Icon: React.ComponentType<{ size: number; color: string; strokeWidth: number }>;
+  iconColor: string;
+  iconBgColor: string;
+  labelColor: string;
+  defaultLabel: string;
+};
+
-const getTypeConfig = (type: SunType) => {
+const getTypeConfig = (type: SunType): TypeConfig => {
   if (type === 'sunrise') {
```
</details>

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/components/SunInfoCard.tsx around lines 25 - 42, The getTypeConfig
helper lacks an explicit return type; define a specific return type (e.g., an
interface or type alias like SunTypeConfig describing Icon:
React.ComponentType<any>, iconColor: string, iconBgColor: string, labelColor:
string, defaultLabel: string) and annotate getTypeConfig as (type: SunType):
SunTypeConfig so the function signature is explicit—update or add the
SunTypeConfig type and apply it to getTypeConfig while keeping the same returned
object shapes using existing symbols (getTypeConfig, SunType, Sunrise, Sunset,
Colors).
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,133 @@
+/**
+ * SunInfoCard - 日の出/日の入りカード
+ * StyleSheet.createのみ使用（NativeWind排除）
+ */
+
+import React from 'react';
+import { View, Text, Pressable, StyleSheet } from 'react-native';
+import { Sunrise, Sunset } from 'lucide-react-native';
+import Animated, { FadeInDown } from 'react-native-reanimated';
+
+import { Colors } from '../theme';
+
+type SunType = 'sunrise' | 'sunset';
+
+interface SunInfoCardProps {
+  type: SunType;
+  time: string;
+  label?: string;
+  delay?: number;
+  onPress?: () => void;
+}
+
+const AnimatedPressable = Animated.createAnimatedComponent(Pressable);
+
+const getTypeConfig = (type: SunType) => {
+  if (type === 'sunrise') {
+    return {
+      Icon: Sunrise,
+      iconColor: Colors.amber[500],
+      iconBgColor: 'rgba(251, 191, 36, 0.12)',
+      labelColor: Colors.stone[400],
+      defaultLabel: 'Sunrise',
+    };
+  }
+  return {
+    Icon: Sunset,
+    iconColor: Colors.indigo[500],
+    iconBgColor: 'rgba(99, 102, 241, 0.12)',
+    labelColor: Colors.stone[400],
+    defaultLabel: 'Sunset',
+  };
+};



================================================================================
ファイル: app/src/components/SunInfoCard.tsx
行: 50 (開始行: 44)
コミット: a35fee7
作成日時: 2026-01-07T07:51:17Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

**Add explicit return type for the component.**

While `React.FC` provides an implicit return type, coding guidelines require explicit return type declarations for all functions.



<details>
<summary>📝 Proposed fix</summary>

```diff
-export const SunInfoCard: React.FC<SunInfoCardProps> = ({
+export const SunInfoCard = ({
   type,
   time,
   label,
   delay = 0,
   onPress,
-}) => {
+}: SunInfoCardProps): React.ReactElement => {
```
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
export const SunInfoCard = ({
  type,
  time,
  label,
  delay = 0,
  onPress,
}: SunInfoCardProps): React.ReactElement => {
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/components/SunInfoCard.tsx around lines 44 - 50, The SunInfoCard
declaration uses React.FC but lacks an explicit function return type; change the
component signature to specify the returned element (e.g., replace "export const
SunInfoCard: React.FC<SunInfoCardProps> = ({ ... }) => { ... }" with "export
const SunInfoCard = ({ ... }: SunInfoCardProps): JSX.Element => { ... }" or use
": React.ReactElement" as the return type), keeping the same props type
SunInfoCardProps and the SunInfoCard name so the component explicitly returns a
JSX element.
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,133 @@
+/**
+ * SunInfoCard - 日の出/日の入りカード
+ * StyleSheet.createのみ使用（NativeWind排除）
+ */
+
+import React from 'react';
+import { View, Text, Pressable, StyleSheet } from 'react-native';
+import { Sunrise, Sunset } from 'lucide-react-native';
+import Animated, { FadeInDown } from 'react-native-reanimated';
+
+import { Colors } from '../theme';
+
+type SunType = 'sunrise' | 'sunset';
+
+interface SunInfoCardProps {
+  type: SunType;
+  time: string;
+  label?: string;
+  delay?: number;
+  onPress?: () => void;
+}
+
+const AnimatedPressable = Animated.createAnimatedComponent(Pressable);
+
+const getTypeConfig = (type: SunType) => {
+  if (type === 'sunrise') {
+    return {
+      Icon: Sunrise,
+      iconColor: Colors.amber[500],
+      iconBgColor: 'rgba(251, 191, 36, 0.12)',
+      labelColor: Colors.stone[400],
+      defaultLabel: 'Sunrise',
+    };
+  }
+  return {
+    Icon: Sunset,
+    iconColor: Colors.indigo[500],
+    iconBgColor: 'rgba(99, 102, 241, 0.12)',
+    labelColor: Colors.stone[400],
+    defaultLabel: 'Sunset',
+  };
+};
+
+export const SunInfoCard: React.FC<SunInfoCardProps> = ({
+  type,
+  time,
+  label,
+  delay = 0,
+  onPress,
+}) => {



================================================================================
ファイル: app/src/config/dataSource.ts
行: 22 (開始行: 17)
コミット: a35fee7
作成日時: 2026-01-07T07:51:17Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

**Add explicit return type to getDataSourceConfig.**

The function is missing an explicit return type annotation, which is required per coding guidelines.



<details>
<summary>🔧 Add explicit return type</summary>

```diff
 /**
  * 設定の取得(将来的に環境変数から読み込む可能性も考慮)
  */
-export const getDataSourceConfig = () => {
+export const getDataSourceConfig = (): typeof DATA_SOURCE_CONFIG => {
   // 将来的にはここで環境変数から読み込む
   // if (process.env.EXPO_PUBLIC_USE_MOCK_DATA === 'false') { ... }
 
   return DATA_SOURCE_CONFIG;
 };
```
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
export const getDataSourceConfig = (): typeof DATA_SOURCE_CONFIG => {
  // 将来的にはここで環境変数から読み込む
  // if (process.env.EXPO_PUBLIC_USE_MOCK_DATA === 'false') { ... }

  return DATA_SOURCE_CONFIG;
};
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/config/dataSource.ts around lines 17 - 22, The function
getDataSourceConfig lacks an explicit return type; update its signature to
include one (for example use a concrete interface/type for the config or `():
typeof DATA_SOURCE_CONFIG`) so the function becomes explicitly typed; ensure the
chosen type matches the shape of DATA_SOURCE_CONFIG and adjust any
imports/exports if you introduce a named type.
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,23 @@
+/**
+ * データソース切り替え設定
+ * このフラグを false にするだけで実データに切り替わります
+ */
+export const DATA_SOURCE_CONFIG = {
+  USE_MOCK_DATA: true, // 全体の切り替えマスタースイッチ
+
+  // 個別の切り替え（デバッグ用）
+  USE_MOCK_AI: true, // AIアドバイスのMock
+  USE_MOCK_WEATHER: true, // 天気データのMock
+  USE_MOCK_HEALTHKIT: true, // HealthKitのMock
+} as const;
+
+/**
+ * 設定の取得（将来的に環境変数から読み込む可能性も考慮）
+ */
+export const getDataSourceConfig = () => {
+  // 将来的にはここで環境変数から読み込む
+  // if (process.env.EXPO_PUBLIC_USE_MOCK_DATA === 'false') { ... }
+
+  return DATA_SOURCE_CONFIG;
+};



================================================================================
ファイル: app/src/domain/models/index.ts
行: 30 (開始行: 19)
コミット: a35fee7
作成日時: 2026-01-07T07:51:17Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
# List files in domain/models to confirm they exist
ls -la app/src/domain/models/
```

Repository: Bluefinee/tempo-ai

Length of output: 786

---

🏁 Script executed:

```shell
# Check the export structure of each referenced module
for file in rhythm insight aiResponse healthHistory; do
  echo "=== ${file}.ts ==="
  head -30 "app/src/domain/models/${file}.ts" | grep -E "^(export|import)" || echo "No immediate exports found"
done
```

Repository: Bluefinee/tempo-ai

Length of output: 700

---



</details>

**Remove section comments per coding guidelines.**

The referenced modules (rhythm.ts, insight.ts, aiResponse.ts, healthHistory.ts) all exist with properly structured named exports, and the barrel export pattern is appropriate. However, the section comments ("// Rhythm", "// Insight", etc.) describe *what* the sections are rather than *why*, which violates the guideline to only comment when explaining business logic or workarounds. Remove these comments to maintain minimal, purposeful documentation.

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/domain/models/index.ts around lines 19 - 30, Remove the
non-actionable section comments above the barrel exports in this file: delete
the lines containing "// Rhythm", "// Insight", "// AI Response", and "// Health
History (HealthKit 対応)" so the file only contains the export statements export *
from './rhythm'; export * from './insight'; export * from './aiResponse'; and
export * from './healthHistory'; leaving no explanatory comments that don't add
business logic or workarounds.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -16,3 +16,15 @@ export * from './advice';
 
 // 天気
 export * from './weather';
+
+// Rhythm
+export * from './rhythm';
+
+// Insight
+export * from './insight';
+
+// AI Response
+export * from './aiResponse';
+
+// Health History (HealthKit 対応)
+export * from './healthHistory';



================================================================================
ファイル: app/src/domain/models/weather.ts
行: 6 (開始行: N/A)
コミット: a35fee7
作成日時: 2026-01-07T07:51:17Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🔴 Critical_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
#!/bin/bash
# Search for PressureTrend type definition and all its usages
rg -n "PressureTrend" --type=ts --type=tsx -A2 -B2
```

Repository: Bluefinee/tempo-ai

Length of output: 89

---

🏁 Script executed:

```shell
#!/bin/bash
# Search for old pressure trend values ('up', 'down') in the codebase
rg -n "'up'|'down'" --type=ts --type=tsx -C2 | grep -i "pressure\|trend" -i
```

Repository: Bluefinee/tempo-ai

Length of output: 89

---

🏁 Script executed:

```shell
#!/bin/bash
# Search for literal pressure trend assignments or comparisons
rg -n "pressure.*=.*'(up|down|rising|falling|stable)'" --type=ts --type=tsx
```

Repository: Bluefinee/tempo-ai

Length of output: 89

---

🏁 Script executed:

```shell
#!/bin/bash
# Search for PressureTrend usages across the codebase
rg -n "PressureTrend" -A2 -B2
```

Repository: Bluefinee/tempo-ai

Length of output: 4252

---

🏁 Script executed:

```shell
#!/bin/bash
# Find all TypeScript files and search for pressure-related trends
fd -e ts -e tsx | xargs rg -l "pressure|trend" | head -20
```

Repository: Bluefinee/tempo-ai

Length of output: 840

---

🏁 Script executed:

```shell
#!/bin/bash
# Search for old pressure values in files
rg -n "\bup\b|\bdown\b" -C2 | grep -i "pressure" -C2
```

Repository: Bluefinee/tempo-ai

Length of output: 815

---

🏁 Script executed:

```shell
#!/bin/bash
# List the file we're reviewing to understand the context
cat -n app/src/domain/models/weather.ts
```

Repository: Bluefinee/tempo-ai

Length of output: 3700

---



</details>

**Update remaining references to PressureTrend values across the codebase.**

This breaking change to `PressureTrend` is incomplete. The following files still reference the old `'up'` and `'down'` values and must be updated to use `'rising'` and `'falling'`:

- `app/src/constants/mockData.ts` (line 139): Type definition still exports old values `"up" | "stable" | "down"`
- `app/app/(main)/rhythm.tsx` (line 53): Function signature uses old values; switch cases reference `'up'` and `'down'` (line 55)
- `app/src/i18n/locales/ja.json` (line 75): Localization keys reference old pressure trend keys (`"up"`, `"down"`)

Ensure all files are updated to use the new `'rising'` | `'stable'` | `'falling'` enum values consistently.

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -3,7 +3,7 @@
  */
 
 // 気圧トレンド
-export type PressureTrend = 'up' | 'stable' | 'down';
+export type PressureTrend = 'rising' | 'stable' | 'falling';



================================================================================
ファイル: app/src/domain/services/rhythmCalculator.ts
行: 106 (開始行: 76)
コミット: a35fee7
作成日時: 2026-01-07T07:51:17Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
cat -n app/src/domain/services/rhythmCalculator.ts
```

Repository: Bluefinee/tempo-ai

Length of output: 7098

---

🏁 Script executed:

```shell
# Search for tests related to phase calculations
find . -name "*.test.ts" -o -name "*.spec.ts" | head -20
```

Repository: Bluefinee/tempo-ai

Length of output: 652

---

🏁 Script executed:

```shell
# Search for any documentation or comments about phase gaps
rg -i "gap|overlap|transition|phase.*definition" app/src/domain/services/rhythmCalculator.ts -B2 -A2
```

Repository: Bluefinee/tempo-ai

Length of output: 696

---

🏁 Script executed:

```shell
# Check if there are any tests specifically for rhythmCalculator
fd -e test.ts -e spec.ts | xargs rg -l "rhythmCalculator|calculatePhases" 2>/dev/null
```

Repository: Bluefinee/tempo-ai

Length of output: 46

---

🏁 Script executed:

```shell
# Check for documentation about phase design
rg -i "circadian|phase.*design|energy.*curve" docs/ 2>/dev/null || echo "Checking for specs file"
```

Repository: Bluefinee/tempo-ai

Length of output: 85

---

🏁 Script executed:

```shell
# Look for the referenced spec file
find . -name "*metrics*spec*" -o -name "*rhythm*spec*" 2>/dev/null
```

Repository: Bluefinee/tempo-ai

Length of output: 44

---

🏁 Script executed:

```shell
# Check how currentPhase null is handled in consumers of calculateCircadianRhythm
rg -l "calculateCircadianRhythm|currentPhase" app/src --type=ts | head -10
```

Repository: Bluefinee/tempo-ai

Length of output: 167

---

🏁 Script executed:

```shell
# Check how currentPhase null is handled in healthStore
rg -A5 -B5 "currentPhase|calculateCircadianRhythm" app/src/stores/healthStore.ts
```

Repository: Bluefinee/tempo-ai

Length of output: 969

---

🏁 Script executed:

```shell
# Check how currentPhase is used in the rhythm model
rg -A5 -B5 "currentPhase" app/src/domain/models/rhythm.ts
```

Repository: Bluefinee/tempo-ai

Length of output: 363

---

🏁 Script executed:

```shell
# Search for any UI components that render currentPhase to see how null is handled
rg -l "currentPhase" app/src --type=ts --type=tsx | grep -E "\.(tsx?|jsx?)$"
```

Repository: Bluefinee/tempo-ai

Length of output: 89

---

🏁 Script executed:

```shell
# Search for UI components that consume currentPhase
rg "selectCurrentPhase|currentPhase" app/src --type=ts | head -20
```

Repository: Bluefinee/tempo-ai

Length of output: 840

---

🏁 Script executed:

```shell
# Check specifically for tsx files that might render currentPhase
fd "\.tsx$" app/src | xargs rg "currentPhase" 2>/dev/null | head -20
```

Repository: Bluefinee/tempo-ai

Length of output: 44

---

🏁 Script executed:

```shell
# Check the energy curve more carefully - does it fill all the gaps?
rg -A30 "getEnergyLevel" app/src/domain/services/rhythmCalculator.ts | grep -E "if|return|//.*\("
```

Repository: Bluefinee/tempo-ai

Length of output: 588

---

🏁 Script executed:

```shell
# Check if there are any comments in the codebase mentioning intentional phase gaps
rg -i "intentional|by design|gap|overlap|neutral|transition.*period" app/src/domain --type=ts
```

Repository: Bluefinee/tempo-ai

Length of output: 44

---

🏁 Script executed:

```shell
# Verify the exact overlap timing between Wind Down and Melatonin Window
python3 << 'EOF'
# Wind Down: sleep - 120min to sleep
# Melatonin Window: sleep - 30min to wake

# Overlap starts at: sleep - 30min
# Overlap ends at: sleep (since Wind Down ends at sleep)
# Duration: 30 minutes of overlap

print("Wind Down: [sleep - 120min, sleep)")
print("Melatonin Window: [sleep - 30min, wake)")
print("Overlap period: [sleep - 30min, sleep) = 30 minutes")
print("\nNote: Using exclusive end [start, end) semantics from isTimeInRange")
EOF
```

Repository: Bluefinee/tempo-ai

Length of output: 148

---

🏁 Script executed:

```shell
# Check how the gaps are handled - is there a "no phase" scenario rendered?
fd "\.tsx$" app/src | xargs grep -l "currentPhase\|selectCurrentPhase" 2>/dev/null
```

Repository: Bluefinee/tempo-ai

Length of output: 46

---

🏁 Script executed:

```shell
# Look at the actual implementation to understand phase usage
fd "\.tsx$" app/src | head -5 | xargs grep -A10 "currentPhase" 2>/dev/null | head -30
```

Repository: Bluefinee/tempo-ai

Length of output: 44

---

🏁 Script executed:

```shell
# Look for any UI that handles null currentPhase
find app/src -name "*.tsx" -type f | head -5
```

Repository: Bluefinee/tempo-ai

Length of output: 240

---

🏁 Script executed:

```shell
# Check if there are tests for the calculatePhases or calculateCircadianRhythm functions
rg "describe\(|test\(|it\(" app/src --type=ts -A2 | grep -i "phase\|rhythm"
```

Repository: Bluefinee/tempo-ai

Length of output: 506

---

🏁 Script executed:

```shell
# Check the energy curve more carefully - are the gaps in phases intentional and covered by energy curve?
sed -n '157,206p' app/src/domain/services/rhythmCalculator.ts | grep -E "^  // |if \(|return"
```

Repository: Bluefinee/tempo-ai

Length of output: 915

---

🏁 Script executed:

```shell
# Verify the overlap issue: check if Wind Down and Melatonin Window overlapping is by design
rg "Wind Down|Melatonin" app/src --type=ts -B2 -A2
```

Repository: Bluefinee/tempo-ai

Length of output: 2172

---

🏁 Script executed:

```shell
# Check the spec file mentioned in the comments
cat -n app/src/domain/services/rhythmCalculator.ts | head -5
```

Repository: Bluefinee/tempo-ai

Length of output: 178

---

🏁 Script executed:

```shell
# Check the referenced spec file location
find . -path "*/docs/specs*" -type f 2>/dev/null
```

Repository: Bluefinee/tempo-ai

Length of output: 44

---

🏁 Script executed:

```shell
# Verify the isTimeInRange behavior with exclusive end
python3 << 'EOF'
# The review claims gaps and overlap exist. Let's verify isTimeInRange logic:
# From line 38-48:
# if (startMs <= endMs) {
#   return timeMs >= startMs && timeMs < endMs;  // [start, end) - exclusive end
# }

# This means:
# Peak Focus: [wake+2h, wake+5h) 
# Afternoon Dip: [wake+7h, wake+9h)
# Gap: [wake+5h, wake+7h) - 2 hours NOT in any named phase

# Wind Down: [sleep-120min, sleep)
# Melatonin: [sleep-30min, wake) - extends PAST sleep to next day's wake
# Overlap: [sleep-30min, sleep) - 30 minutes in BOTH phases

print("Peak Focus ends: wake+5h (exclusive)")
print("Afternoon Dip starts: wake+7h (inclusive)")
print("Gap duration: 2 hours ✓")
print()
print("Wind Down: [sleep-120min, sleep)")
print("Melatonin: [sleep-30min, wake)")  
print("Overlap: [sleep-30min, sleep) = 30 minutes ✓")
print()
print("KEY FINDING: Energy curve has 'Post-Peak (5-7h)' and 'Recovery (9-10h)'")
print("that fill the phase gaps - but these aren't in calculatePhases output!")
EOF
```

Repository: Bluefinee/tempo-ai

Length of output: 418

---



</details>

**Phase gaps are by design but create UI inconsistency: energy curve and phase definitions diverge.**

The phase definitions do have intentional gaps (e.g., `Peak Focus` ends at wake+5h, `Afternoon Dip` starts at wake+7h) and an overlap between `Wind Down` [sleep−120min, sleep) and `Melatonin Window` [sleep−30min, wake).

However, the real issue is a mismatch: the `getEnergyLevel` function explicitly covers these gaps with `Post-Peak (5-7h)` and `Recovery (9-10h)` periods, but `calculatePhases` does not return phases for these times. This means `currentPhase` will be `null` during these periods even though the energy curve has valid data for them. Either:
1. Add missing phases to `calculatePhases` to match energy curve coverage, or
2. Document why gaps are intentional and how the UI should handle `null` currentPhase

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/domain/services/rhythmCalculator.ts around lines 76 - 106,
calculatePhases currently leaves intentional time gaps (e.g., no phase between
wake+5h and wake+7h and between wake+9h and wake+10h) which causes currentPhase
to be null while getEnergyLevel returns values for those intervals; update
calculatePhases to include matching phases (e.g., "Post-Peak" spanning
wake+5h–wake+7h and "Recovery" spanning wake+9h–wake+10h) with appropriate
definition types so phase coverage mirrors the energy curve, or alternatively
add clear documentation in calculatePhases (and callers using currentPhase)
explaining the intentional gaps and how the UI should handle null currentPhase;
locate and modify the calculatePhases function and its phase array, and ensure
consistency with getEnergyLevel's intervals.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,231 @@
+/**
+ * Rhythm（サーカディアンリズム）計算サービス
+ * @see docs/specs/metrics_spec.md Section 3
+ */
+
+import {
+  RhythmPhase,
+  RhythmPhaseName,
+  RhythmPhaseType,
+  CircadianRhythm,
+  EnergyCurve,
+  EnergyCurvePoint,
+} from '../models/rhythm';
+
+// ========================================
+// Utility Functions
+// ========================================
+
+const parseTime = (timeString: string): Date => {
+  const [hours, minutes] = timeString.split(':').map(Number);
+  const date = new Date();
+  date.setHours(hours, minutes, 0, 0);
+  return date;
+};
+
+const parseHour = (timeString: string): number => {
+  return parseInt(timeString.split(':')[0], 10);
+};
+
+const addHours = (date: Date, hours: number): Date => {
+  return new Date(date.getTime() + hours * 60 * 60 * 1000);
+};
+
+const addMinutes = (date: Date, minutes: number): Date => {
+  return new Date(date.getTime() + minutes * 60 * 1000);
+};
+
+const isTimeInRange = (time: Date, start: Date, end: Date): boolean => {
+  const timeMs = time.getTime();
+  const startMs = start.getTime();
+  const endMs = end.getTime();
+
+  if (startMs <= endMs) {
+    return timeMs >= startMs && timeMs < endMs;
+  }
+  // 日付をまたぐ場合
+  return timeMs >= startMs || timeMs < endMs;
+};
+
+const getMinutesBetween = (from: Date, to: Date): number => {
+  return Math.round((to.getTime() - from.getTime()) / (1000 * 60));
+};
+
+// ========================================
+// Phase Calculation
+// ========================================
+
+interface PhaseDefinition {
+  name: RhythmPhaseName;
+  type: RhythmPhaseType;
+}
+
+export const calculatePhases = (
+  wakeUpTime: string,
+  windDownTime: string
+): readonly RhythmPhase[] => {
+  const wake = parseTime(wakeUpTime);
+  const sleep = parseTime(windDownTime);
+  const now = new Date();
+
+  const phaseDefinitions: {
+    definition: PhaseDefinition;
+    start: Date;
+    end: Date;
+  }[] = [
+    {
+      definition: { name: 'Wake Window', type: 'transition' },
+      start: wake,
+      end: addHours(wake, 2),
+    },
+    {
+      definition: { name: 'Peak Focus', type: 'high' },
+      start: addHours(wake, 2),
+      end: addHours(wake, 5),
+    },
+    {
+      definition: { name: 'Afternoon Dip', type: 'low' },
+      start: addHours(wake, 7),
+      end: addHours(wake, 9),
+    },
+    {
+      definition: { name: 'Second Wind', type: 'high' },
+      start: addHours(wake, 10),
+      end: addHours(wake, 13),
+    },
+    {
+      definition: { name: 'Wind Down', type: 'transition' },
+      start: addMinutes(sleep, -120),
+      end: sleep,
+    },
+    {
+      definition: { name: 'Melatonin Window', type: 'sleep' },
+      start: addMinutes(sleep, -30),
+      end: wake,
+    },
+  ];



================================================================================
ファイル: app/src/domain/services/rhythmCalculator.ts
行: 206 (開始行: 157)
コミット: a35fee7
作成日時: 2026-01-07T07:51:17Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**Consider extracting energy level constants for maintainability.**

The energy level values (10, 30, 50, 60, 70, 80, 90) are used throughout the function. Extracting these as named constants would improve readability and make future adjustments easier.



<details>
<summary>♻️ Optional: Extract constants</summary>

```typescript
const ENERGY_LEVELS = {
  SLEEP: 10,
  MIN_AWAKE: 30,
  LOW: 50,
  WAKE: 60,
  MODERATE: 70,
  HIGH: 80,
  PEAK: 90,
} as const;
```

</details>

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/domain/services/rhythmCalculator.ts around lines 157 - 206, The
getEnergyLevel function embeds magic numeric energy values
(10,30,50,60,70,80,90); extract them into a descriptive constant object (e.g.,
ENERGY_LEVELS with keys like SLEEP, MIN_AWAKE, LOW, WAKE, MODERATE, HIGH, PEAK
declared as const) and replace all literal numbers in getEnergyLevel with the
corresponding named constants; ensure the constant is exported or colocated near
getEnergyLevel, use the constants in all return expressions and calculations so
readability and future adjustments are centralized.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,231 @@
+/**
+ * Rhythm（サーカディアンリズム）計算サービス
+ * @see docs/specs/metrics_spec.md Section 3
+ */
+
+import {
+  RhythmPhase,
+  RhythmPhaseName,
+  RhythmPhaseType,
+  CircadianRhythm,
+  EnergyCurve,
+  EnergyCurvePoint,
+} from '../models/rhythm';
+
+// ========================================
+// Utility Functions
+// ========================================
+
+const parseTime = (timeString: string): Date => {
+  const [hours, minutes] = timeString.split(':').map(Number);
+  const date = new Date();
+  date.setHours(hours, minutes, 0, 0);
+  return date;
+};
+
+const parseHour = (timeString: string): number => {
+  return parseInt(timeString.split(':')[0], 10);
+};
+
+const addHours = (date: Date, hours: number): Date => {
+  return new Date(date.getTime() + hours * 60 * 60 * 1000);
+};
+
+const addMinutes = (date: Date, minutes: number): Date => {
+  return new Date(date.getTime() + minutes * 60 * 1000);
+};
+
+const isTimeInRange = (time: Date, start: Date, end: Date): boolean => {
+  const timeMs = time.getTime();
+  const startMs = start.getTime();
+  const endMs = end.getTime();
+
+  if (startMs <= endMs) {
+    return timeMs >= startMs && timeMs < endMs;
+  }
+  // 日付をまたぐ場合
+  return timeMs >= startMs || timeMs < endMs;
+};
+
+const getMinutesBetween = (from: Date, to: Date): number => {
+  return Math.round((to.getTime() - from.getTime()) / (1000 * 60));
+};
+
+// ========================================
+// Phase Calculation
+// ========================================
+
+interface PhaseDefinition {
+  name: RhythmPhaseName;
+  type: RhythmPhaseType;
+}
+
+export const calculatePhases = (
+  wakeUpTime: string,
+  windDownTime: string
+): readonly RhythmPhase[] => {
+  const wake = parseTime(wakeUpTime);
+  const sleep = parseTime(windDownTime);
+  const now = new Date();
+
+  const phaseDefinitions: {
+    definition: PhaseDefinition;
+    start: Date;
+    end: Date;
+  }[] = [
+    {
+      definition: { name: 'Wake Window', type: 'transition' },
+      start: wake,
+      end: addHours(wake, 2),
+    },
+    {
+      definition: { name: 'Peak Focus', type: 'high' },
+      start: addHours(wake, 2),
+      end: addHours(wake, 5),
+    },
+    {
+      definition: { name: 'Afternoon Dip', type: 'low' },
+      start: addHours(wake, 7),
+      end: addHours(wake, 9),
+    },
+    {
+      definition: { name: 'Second Wind', type: 'high' },
+      start: addHours(wake, 10),
+      end: addHours(wake, 13),
+    },
+    {
+      definition: { name: 'Wind Down', type: 'transition' },
+      start: addMinutes(sleep, -120),
+      end: sleep,
+    },
+    {
+      definition: { name: 'Melatonin Window', type: 'sleep' },
+      start: addMinutes(sleep, -30),
+      end: wake,
+    },
+  ];
+
+  return phaseDefinitions.map(({ definition, start, end }) => ({
+    name: definition.name,
+    type: definition.type,
+    start,
+    end,
+    isCurrent: isTimeInRange(now, start, end),
+  }));
+};
+
+// ========================================
+// Circadian Rhythm
+// ========================================
+
+export const calculateCircadianRhythm = (
+  wakeUpTime: string,
+  windDownTime: string,
+  sunrise: string,
+  sunset: string
+): CircadianRhythm => {
+  const phases = calculatePhases(wakeUpTime, windDownTime);
+  const now = new Date();
+
+  const currentPhase = phases.find((p) => p.isCurrent) ?? null;
+
+  // 次のフェーズを見つける
+  let nextPhase: RhythmPhase | null = null;
+  let minutesToNextPhase: number | null = null;
+
+  if (currentPhase) {
+    const currentIndex = phases.findIndex((p) => p.name === currentPhase.name);
+    const nextIndex = (currentIndex + 1) % phases.length;
+    nextPhase = phases[nextIndex];
+    minutesToNextPhase = getMinutesBetween(now, currentPhase.end);
+  }
+
+  return {
+    phases,
+    currentPhase,
+    nextPhase,
+    minutesToNextPhase,
+    sunrise,
+    sunset,
+  };
+};
+
+// ========================================
+// Energy Curve (for Graph)
+// ========================================
+
+const getEnergyLevel = (
+  hoursSinceWake: number,
+  wakeHour: number,
+  sleepHour: number
+): number => {
+  const awakeHours = (sleepHour - wakeHour + 24) % 24;
+
+  // 睡眠中
+  if (hoursSinceWake < 0 || hoursSinceWake > awakeHours) {
+    return 10;
+  }
+
+  // Wake Window (0-2h): 30→60に上昇
+  if (hoursSinceWake < 2) {
+    return 30 + (hoursSinceWake / 2) * 30;
+  }
+
+  // Peak Focus (2-5h): 60→90に上昇
+  if (hoursSinceWake < 5) {
+    return 60 + ((hoursSinceWake - 2) / 3) * 30;
+  }
+
+  // Post-Peak (5-7h): 90→70に下降
+  if (hoursSinceWake < 7) {
+    return 90 - ((hoursSinceWake - 5) / 2) * 20;
+  }
+
+  // Afternoon Dip (7-9h): 70→50に下降
+  if (hoursSinceWake < 9) {
+    return 70 - ((hoursSinceWake - 7) / 2) * 20;
+  }
+
+  // Recovery (9-10h): 50→70に上昇
+  if (hoursSinceWake < 10) {
+    return 50 + (hoursSinceWake - 9) * 20;
+  }
+
+  // Second Wind (10-13h): 70→80
+  if (hoursSinceWake < 13) {
+    return 70 + ((hoursSinceWake - 10) / 3) * 10;
+  }
+
+  // Wind Down (13h+): 80→30に下降
+  const hoursUntilSleep = awakeHours - hoursSinceWake;
+  if (hoursUntilSleep > 0) {
+    return 30 + (hoursUntilSleep / 3) * 20;
+  }
+
+  return 30;
+};



================================================================================
ファイル: app/src/hooks/useAdvice.ts
行: 44 (開始行: 28)
コミット: a35fee7
作成日時: 2026-01-07T07:51:17Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**Optimize Zustand selector to prevent unnecessary re-renders.**

The current selector creates new object references on every render, which will cause the hook to re-run even when underlying values haven't changed. This can lead to performance issues.



<details>
<summary>♻️ Proposed optimization</summary>

Consider creating a stable selector or using individual selectors:

```diff
-  const healthStore = useHealthStore((state) => ({
-    metrics: {
-      sleep: state.metrics.sleep ? {
-        durationMinutes: state.metrics.sleep.durationMinutes,
-        deepSleepMinutes: Math.round(state.metrics.sleep.durationMinutes * state.metrics.sleep.deepSleepRatio),
-        remSleepMinutes: Math.round(state.metrics.sleep.durationMinutes * state.metrics.sleep.remSleepRatio),
-      } : null,
-      hrv: state.metrics.hrv ? {
-        value: state.metrics.hrv.current,
-        baseline30d: state.metrics.hrv.baseline30d,
-      } : null,
-      activity: state.metrics.activity ? {
-        steps: state.metrics.activity.steps,
-      } : null,
-    },
-    tempoScore: state.tempoScore,
-  }));
+  const metrics = useHealthStore((state) => state.metrics);
+  const tempoScore = useHealthStore((state) => state.tempoScore);
+  
+  // Transform data only when needed in fetchAdvice
```

Or use `shallow` comparison from Zustand:

```typescript
import { shallow } from 'zustand/shallow';

const healthStore = useHealthStore((state) => ({
  // ... your selector
}), shallow);
```
</details>


> Committable suggestion skipped: line range outside the PR's diff.

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/hooks/useAdvice.ts around lines 28 - 44, The selector passed to
useHealthStore (assigned to healthStore) builds new nested objects on every call
causing unnecessary re-renders; fix it by either (A) splitting into smaller
selectors and call useHealthStore separately for each primitive (e.g., sleep
duration, deepSleepMinutes, remSleepMinutes, hrv.current, hrv.baseline30d,
activity.steps, tempoScore) or (B) memoize the selected shape by using Zustand's
shallow comparator (import shallow from 'zustand/shallow' and pass it as the
second argument to useHealthStore) so object identity is preserved when
underlying values haven't changed; update the selector references (healthStore
and useHealthStore) accordingly.
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,102 @@
+/**
+ * AIアドバイス取得フック
+ */
+
+import { useCallback, useState } from 'react';
+import { apiClient } from '../api/client';
+import { buildAdviceRequest } from '../api/helpers/adviceRequestBuilder';
+import type { WeatherData, UserGoal } from '../api/types';
+import { useHealthStore } from '../stores/healthStore';
+import { useInsightStore } from '../stores/insightStore';
+import { useUserStore } from '../stores/userStore';
+
+interface UseAdviceReturn {
+  isLoading: boolean;
+  error: string | null;
+  fetchAdvice: (weather: WeatherData) => Promise<void>;
+  refresh: () => Promise<void>;
+}
+
+/**
+ * AIアドバイス取得フック
+ */
+export const useAdvice = (): UseAdviceReturn => {
+  const [isLoading, setIsLoading] = useState(false);
+  const [error, setError] = useState<string | null>(null);
+  const [lastWeather, setLastWeather] = useState<WeatherData | null>(null);
+
+  const healthStore = useHealthStore((state) => ({
+    metrics: {
+      sleep: state.metrics.sleep ? {
+        durationMinutes: state.metrics.sleep.durationMinutes,
+        deepSleepMinutes: Math.round(state.metrics.sleep.durationMinutes * state.metrics.sleep.deepSleepRatio),
+        remSleepMinutes: Math.round(state.metrics.sleep.durationMinutes * state.metrics.sleep.remSleepRatio),
+      } : null,
+      hrv: state.metrics.hrv ? {
+        value: state.metrics.hrv.current,
+        baseline30d: state.metrics.hrv.baseline30d,
+      } : null,
+      activity: state.metrics.activity ? {
+        steps: state.metrics.activity.steps,
+      } : null,
+    },
+    tempoScore: state.tempoScore,
+  }));



================================================================================
ファイル: app/src/hooks/useAdvice.ts
行: 33 (開始行: 32)
コミット: a35fee7
作成日時: 2026-01-07T07:51:17Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**Consider extracting calculation logic to helper function.**

The sleep minutes calculation is embedded in the selector, making it harder to test and reuse. Consider extracting to a domain helper.



<details>
<summary>♻️ Suggested refactor</summary>

Create a helper in `app/src/domain/helpers/sleepCalculations.ts`:

```typescript
export const calculateSleepMinutes = (
  durationMinutes: number,
  deepSleepRatio: number,
  remSleepRatio: number
): { deepSleepMinutes: number; remSleepMinutes: number } => ({
  deepSleepMinutes: Math.round(durationMinutes * deepSleepRatio),
  remSleepMinutes: Math.round(durationMinutes * remSleepRatio),
});
```

Then use it:

```typescript
const sleepMinutes = state.metrics.sleep 
  ? calculateSleepMinutes(
      state.metrics.sleep.durationMinutes,
      state.metrics.sleep.deepSleepRatio,
      state.metrics.sleep.remSleepRatio
    )
  : null;
```
</details>

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/hooks/useAdvice.ts around lines 32 - 33, Extract the embedded
sleep-minute math into a reusable helper and replace the inline calculations in
the selector: create a function named calculateSleepMinutes(durationMinutes,
deepSleepRatio, remSleepRatio) that returns { deepSleepMinutes, remSleepMinutes
} by rounding the products, then in useAdvice.ts call this helper when
state.metrics.sleep exists (pass state.metrics.sleep.durationMinutes,
.deepSleepRatio, .remSleepRatio) and assign deepSleepMinutes/remSleepMinutes
from the helper result instead of using Math.round(...) inline.
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,102 @@
+/**
+ * AIアドバイス取得フック
+ */
+
+import { useCallback, useState } from 'react';
+import { apiClient } from '../api/client';
+import { buildAdviceRequest } from '../api/helpers/adviceRequestBuilder';
+import type { WeatherData, UserGoal } from '../api/types';
+import { useHealthStore } from '../stores/healthStore';
+import { useInsightStore } from '../stores/insightStore';
+import { useUserStore } from '../stores/userStore';
+
+interface UseAdviceReturn {
+  isLoading: boolean;
+  error: string | null;
+  fetchAdvice: (weather: WeatherData) => Promise<void>;
+  refresh: () => Promise<void>;
+}
+
+/**
+ * AIアドバイス取得フック
+ */
+export const useAdvice = (): UseAdviceReturn => {
+  const [isLoading, setIsLoading] = useState(false);
+  const [error, setError] = useState<string | null>(null);
+  const [lastWeather, setLastWeather] = useState<WeatherData | null>(null);
+
+  const healthStore = useHealthStore((state) => ({
+    metrics: {
+      sleep: state.metrics.sleep ? {
+        durationMinutes: state.metrics.sleep.durationMinutes,
+        deepSleepMinutes: Math.round(state.metrics.sleep.durationMinutes * state.metrics.sleep.deepSleepRatio),
+        remSleepMinutes: Math.round(state.metrics.sleep.durationMinutes * state.metrics.sleep.remSleepRatio),



================================================================================
ファイル: app/src/hooks/useAdvice.ts
行: 52 (開始行: 46)
コミット: a35fee7
作成日時: 2026-01-07T07:51:17Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**Optimize user store selector.**

Similar to the health store selector, this creates new object references on every render.



<details>
<summary>♻️ Proposed optimization</summary>

```diff
-  const userStore = useUserStore((state) => ({
-    profile: state.profile ? {
-      goals: (state.profile as { goals?: UserGoal[] }).goals ?? ['better_sleep'],
-      wakeUpTime: (state.profile as { wakeUpTime?: string }).wakeUpTime ?? '07:00',
-      windDownTime: (state.profile as { windDownTime?: string }).windDownTime ?? '23:00',
-    } : null,
-  }));
+  const profile = useUserStore((state) => state.profile);
+  
+  // Transform in fetchAdvice or extract to helper function
```
</details>


> Committable suggestion skipped: line range outside the PR's diff.

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/hooks/useAdvice.ts around lines 46 - 52, The selector passed to
useUserStore creates a new object on every call (userStore => { profile: { ... }
}) causing unnecessary re-renders; change the selector to return the raw
state.profile (or null) instead of constructing a new object and move the
defaulting for goals/wakeUpTime/windDownTime into a stable place (e.g., memoize
with useMemo in useAdvice or compute defaults where profile is consumed),
referencing useUserStore and the profile/goals/wakeUpTime/windDownTime fields so
the selector no longer allocates a fresh object each render.
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,102 @@
+/**
+ * AIアドバイス取得フック
+ */
+
+import { useCallback, useState } from 'react';
+import { apiClient } from '../api/client';
+import { buildAdviceRequest } from '../api/helpers/adviceRequestBuilder';
+import type { WeatherData, UserGoal } from '../api/types';
+import { useHealthStore } from '../stores/healthStore';
+import { useInsightStore } from '../stores/insightStore';
+import { useUserStore } from '../stores/userStore';
+
+interface UseAdviceReturn {
+  isLoading: boolean;
+  error: string | null;
+  fetchAdvice: (weather: WeatherData) => Promise<void>;
+  refresh: () => Promise<void>;
+}
+
+/**
+ * AIアドバイス取得フック
+ */
+export const useAdvice = (): UseAdviceReturn => {
+  const [isLoading, setIsLoading] = useState(false);
+  const [error, setError] = useState<string | null>(null);
+  const [lastWeather, setLastWeather] = useState<WeatherData | null>(null);
+
+  const healthStore = useHealthStore((state) => ({
+    metrics: {
+      sleep: state.metrics.sleep ? {
+        durationMinutes: state.metrics.sleep.durationMinutes,
+        deepSleepMinutes: Math.round(state.metrics.sleep.durationMinutes * state.metrics.sleep.deepSleepRatio),
+        remSleepMinutes: Math.round(state.metrics.sleep.durationMinutes * state.metrics.sleep.remSleepRatio),
+      } : null,
+      hrv: state.metrics.hrv ? {
+        value: state.metrics.hrv.current,
+        baseline30d: state.metrics.hrv.baseline30d,
+      } : null,
+      activity: state.metrics.activity ? {
+        steps: state.metrics.activity.steps,
+      } : null,
+    },
+    tempoScore: state.tempoScore,
+  }));
+
+  const userStore = useUserStore((state) => ({
+    profile: state.profile ? {
+      goals: (state.profile as { goals?: UserGoal[] }).goals ?? ['better_sleep'],
+      wakeUpTime: (state.profile as { wakeUpTime?: string }).wakeUpTime ?? '07:00',
+      windDownTime: (state.profile as { windDownTime?: string }).windDownTime ?? '23:00',
+    } : null,
+  }));



================================================================================
ファイル: app/src/hooks/useFadeIn.ts
行: 44 (開始行: 14)
コミット: a35fee7
作成日時: 2026-01-07T07:51:17Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

**Add explicit return type annotation.**

The function `useFadeIn` is missing an explicit return type. Per TypeScript coding guidelines, all functions must declare explicit return types.



<details>
<summary>📝 Proposed fix</summary>

```diff
-export const useFadeIn = (options: UseFadeInOptions = {}) => {
+export const useFadeIn = (options: UseFadeInOptions = {}): {
+  opacity: Animated.Value;
+  transform: { translateY: Animated.Value }[];
+} => {
```
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
export const useFadeIn = (options: UseFadeInOptions = {}): {
  opacity: Animated.Value;
  transform: { translateY: Animated.Value }[];
} => {
  const { delay = 0, duration = 600 } = options;
  const opacity = useRef(new Animated.Value(0)).current;
  const translateY = useRef(new Animated.Value(10)).current;

  useEffect(() => {
    const timeout = setTimeout(() => {
      Animated.parallel([
        Animated.timing(opacity, {
          toValue: 1,
          duration,
          easing: Easing.out(Easing.ease),
          useNativeDriver: true,
        }),
        Animated.timing(translateY, {
          toValue: 0,
          duration,
          easing: Easing.out(Easing.ease),
          useNativeDriver: true,
        }),
      ]).start();
    }, delay);

    return () => clearTimeout(timeout);
  }, [delay, duration, opacity, translateY]);

  return {
    opacity,
    transform: [{ translateY }],
  };
};
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/hooks/useFadeIn.ts around lines 14 - 44, Add an explicit return type
to the useFadeIn function: declare it returns an object whose opacity is an
Animated.Value and whose transform is an array containing an object with
translateY as an Animated.Value (i.e., the same shape as the returned { opacity,
transform: [{ translateY }] }); update the function signature (useFadeIn) to
include that return type so TypeScript enforces the shape.
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,52 @@
+/**
+ * useFadeIn - フェードインアニメーション用カスタムフック
+ * sozai/new/new2/tempoaiの.fade-inクラスを再現
+ */
+
+import { useEffect, useRef } from 'react';
+import { Animated, Easing } from 'react-native';
+
+interface UseFadeInOptions {
+  delay?: number;
+  duration?: number;
+}
+
+export const useFadeIn = (options: UseFadeInOptions = {}) => {
+  const { delay = 0, duration = 600 } = options;
+  const opacity = useRef(new Animated.Value(0)).current;
+  const translateY = useRef(new Animated.Value(10)).current;
+
+  useEffect(() => {
+    const timeout = setTimeout(() => {
+      Animated.parallel([
+        Animated.timing(opacity, {
+          toValue: 1,
+          duration,
+          easing: Easing.out(Easing.ease),
+          useNativeDriver: true,
+        }),
+        Animated.timing(translateY, {
+          toValue: 0,
+          duration,
+          easing: Easing.out(Easing.ease),
+          useNativeDriver: true,
+        }),
+      ]).start();
+    }, delay);
+
+    return () => clearTimeout(timeout);
+  }, [delay, duration, opacity, translateY]);
+
+  return {
+    opacity,
+    transform: [{ translateY }],
+  };
+};



================================================================================
ファイル: app/src/hooks/useFadeIn.ts
行: 52 (開始行: 49)
コミット: a35fee7
作成日時: 2026-01-07T07:51:17Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

**Add explicit return type annotation.**

The function `createFadeInStyle` is missing an explicit return type.



<details>
<summary>📝 Proposed fix</summary>

```diff
-export const createFadeInStyle = (opacity: Animated.Value, translateY: Animated.Value) => ({
+export const createFadeInStyle = (
+  opacity: Animated.Value,
+  translateY: Animated.Value
+): { opacity: Animated.Value; transform: { translateY: Animated.Value }[] } => ({
```
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
export const createFadeInStyle = (
  opacity: Animated.Value,
  translateY: Animated.Value
): { opacity: Animated.Value; transform: { translateY: Animated.Value }[] } => ({
  opacity,
  transform: [{ translateY }],
});
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/hooks/useFadeIn.ts around lines 49 - 52, The function
createFadeInStyle lacks an explicit return type; update its signature to include
a clear type for the style object (for example: createFadeInStyle(opacity:
Animated.Value, translateY: Animated.Value): { opacity: Animated.Value;
transform: { translateY: Animated.Value }[] } ) so the returned object shape is
typed explicitly—modify the createFadeInStyle declaration to include this return
type annotation.
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,52 @@
+/**
+ * useFadeIn - フェードインアニメーション用カスタムフック
+ * sozai/new/new2/tempoaiの.fade-inクラスを再現
+ */
+
+import { useEffect, useRef } from 'react';
+import { Animated, Easing } from 'react-native';
+
+interface UseFadeInOptions {
+  delay?: number;
+  duration?: number;
+}
+
+export const useFadeIn = (options: UseFadeInOptions = {}) => {
+  const { delay = 0, duration = 600 } = options;
+  const opacity = useRef(new Animated.Value(0)).current;
+  const translateY = useRef(new Animated.Value(10)).current;
+
+  useEffect(() => {
+    const timeout = setTimeout(() => {
+      Animated.parallel([
+        Animated.timing(opacity, {
+          toValue: 1,
+          duration,
+          easing: Easing.out(Easing.ease),
+          useNativeDriver: true,
+        }),
+        Animated.timing(translateY, {
+          toValue: 0,
+          duration,
+          easing: Easing.out(Easing.ease),
+          useNativeDriver: true,
+        }),
+      ]).start();
+    }, delay);
+
+    return () => clearTimeout(timeout);
+  }, [delay, duration, opacity, translateY]);
+
+  return {
+    opacity,
+    transform: [{ translateY }],
+  };
+};
+
+/**
+ * FadeInView - フェードインアニメーション付きのViewラッパー
+ */
+export const createFadeInStyle = (opacity: Animated.Value, translateY: Animated.Value) => ({
+  opacity,
+  transform: [{ translateY }],
+});



================================================================================
ファイル: app/src/hooks/useWeather.ts
行: 68 (開始行: 65)
コミット: a35fee7
作成日時: 2026-01-07T07:51:17Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**Remove trailing blank lines.**

Clean up the excessive blank lines at the end of the file.



<details>
<summary>🧹 Cleanup trailing lines</summary>

```diff
   return {
     isLoading,
     error,
     weather,
     fetchWeather,
   };
 };
-
-
-
-
```
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
   return {
     isLoading,
     error,
     weather,
     fetchWeather,
   };
 };
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/hooks/useWeather.ts around lines 65 - 68, The file ends with
unnecessary trailing blank lines; open the file containing the useWeather hook
(e.g., useWeather or export default useWeather) and remove the extra blank lines
at the end so the file ends immediately after the last statement or export,
ensuring there is a single newline at EOF if your linter requires it.
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,68 @@
+/**
+ * 天気情報取得フック
+ */
+
+import { useCallback, useState } from 'react';
+import * as Location from 'expo-location';
+import { apiClient } from '../api/client';
+import type { WeatherResponse } from '../api/types';
+
+interface UseWeatherReturn {
+  isLoading: boolean;
+  error: string | null;
+  weather: WeatherResponse | null;
+  fetchWeather: () => Promise<WeatherResponse | null>;
+}
+
+/**
+ * 天気情報取得フック
+ */
+export const useWeather = (): UseWeatherReturn => {
+  const [isLoading, setIsLoading] = useState(false);
+  const [error, setError] = useState<string | null>(null);
+  const [weather, setWeather] = useState<WeatherResponse | null>(null);
+
+  const fetchWeather = useCallback(async (): Promise<WeatherResponse | null> => {
+    setIsLoading(true);
+    setError(null);
+
+    try {
+      // 位置情報の取得
+      const { status } = await Location.requestForegroundPermissionsAsync();
+      if (status !== 'granted') {
+        throw new Error('Location permission not granted');
+      }
+
+      const location = await Location.getCurrentPositionAsync({});
+      const { latitude, longitude } = location.coords;
+
+      const response = await apiClient.getWeather(latitude, longitude);
+
+      if (response.success) {
+        setWeather(response.data);
+        return response.data;
+      } else {
+        setError(response.error.message ?? 'Failed to fetch weather');
+        return null;
+      }
+    } catch (err) {
+      const message = err instanceof Error ? err.message : 'Unknown error';
+      setError(message);
+      return null;
+    } finally {
+      setIsLoading(false);
+    }
+  }, []);
+
+  return {
+    isLoading,
+    error,
+    weather,
+    fetchWeather,
+  };
+};
+
+
+
+
+



================================================================================
ファイル: app/src/i18n/index.ts
行: 11 (開始行: N/A)
コミット: a35fee7
作成日時: 2026-01-07T07:51:17Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**Simplify redundant ternary expression.**

The ternary expression always evaluates to `'ja'` regardless of the condition, making it redundant.



<details>
<summary>♻️ Proposed simplification</summary>

```diff
-i18n.locale = deviceLocale === 'ja' ? 'ja' : 'ja';
+i18n.locale = 'ja';
```

Or if the intent is to prepare for future locale support:

```diff
// 日本語のみサポート（将来英語追加）
-i18n.locale = deviceLocale === 'ja' ? 'ja' : 'ja';
+i18n.locale = 'ja'; // TODO: Add locale support when translations are ready
```
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
i18n.locale = 'ja';
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/i18n/index.ts at line 11, The ternary assigning i18n.locale is
redundant (deviceLocale === 'ja' ? 'ja' : 'ja'); replace it with a single value
or the intended dynamic value: either set i18n.locale = 'ja' if Japanese only is
intended, or set i18n.locale = deviceLocale to support other locales; update the
assignment to use the chosen value and remove the unnecessary conditional,
referencing the i18n.locale and deviceLocale identifiers.
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,32 @@
+import { getLocales } from 'expo-localization';
+import { I18n } from 'i18n-js';
+import ja from './locales/ja.json';
+
+const i18n = new I18n({ ja });
+
+// デバイスロケール取得
+const deviceLocale = getLocales()[0]?.languageCode ?? 'ja';
+
+// 日本語のみサポート（将来英語追加）
+i18n.locale = deviceLocale === 'ja' ? 'ja' : 'ja';



================================================================================
ファイル: app/src/i18n/index.ts
行: 32 (開始行: 29)
コミット: a35fee7
作成日時: 2026-01-07T07:51:17Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

**Remove trailing empty lines.**

Per cleanup guidelines, unnecessary empty lines at the end of files should be removed.



<details>
<summary>🧹 Proposed cleanup</summary>

```diff
 export default i18n;
-
-
-
-
```
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
export default i18n;
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/i18n/index.ts around lines 29 - 32, Remove the trailing empty lines
at the end of index.ts in the i18n module: open the file and delete any blank
lines after the final code so the file ends immediately after the last
token/statement (no extra newlines).
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,32 @@
+import { getLocales } from 'expo-localization';
+import { I18n } from 'i18n-js';
+import ja from './locales/ja.json';
+
+const i18n = new I18n({ ja });
+
+// デバイスロケール取得
+const deviceLocale = getLocales()[0]?.languageCode ?? 'ja';
+
+// 日本語のみサポート（将来英語追加）
+i18n.locale = deviceLocale === 'ja' ? 'ja' : 'ja';
+i18n.defaultLocale = 'ja';
+i18n.enableFallback = true;
+
+export const t = (key: string, options?: Record<string, unknown>): string => {
+  return i18n.t(key, options);
+};
+
+export const setLocale = (locale: string): void => {
+  i18n.locale = locale;
+};
+
+export const getLocale = (): string => {
+  return i18n.locale;
+};
+
+export default i18n;
+
+
+
+
+



================================================================================
ファイル: app/src/stores/insightStore.ts
行: 145 (開始行: 140)
コミット: a35fee7
作成日時: 2026-01-07T07:51:18Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟠 Major_

**Avoid type assertions - fix the underlying type definitions instead.**

Multiple `as` casts suggest the `UserProfile` type in `userStore` doesn't match the expected shape. This bypasses TypeScript's safety guarantees.



<details>
<summary>🔧 Recommended fix</summary>

Either update the `UserProfile` type in `userStore` to include these fields:

```typescript
interface UserProfile {
  goals?: UserGoal[];
  wakeUpTime?: string;
  windDownTime?: string;
  // ... other fields
}
```

Or use proper type narrowing:

```diff
-            profile: userStoreState.profile ? {
-              goals: (userStoreState.profile as { goals?: unknown }).goals ?? ['better_sleep'],
-              wakeUpTime: (userStoreState.profile as { wakeUpTime?: unknown }).wakeUpTime ?? '07:00',
-              windDownTime: (userStoreState.profile as { windDownTime?: unknown }).windDownTime ?? '23:00',
-            } : null,
+            profile: userStoreState.profile && 'goals' in userStoreState.profile ? {
+              goals: userStoreState.profile.goals ?? ['better_sleep'],
+              wakeUpTime: userStoreState.profile.wakeUpTime ?? '07:00',
+              windDownTime: userStoreState.profile.windDownTime ?? '23:00',
+            } : null,
```

</details>

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/stores/insightStore.ts around lines 140 - 145, The code is using
type assertions on userStoreState.profile to access goals, wakeUpTime, and
windDownTime in insightStore; instead update the UserProfile type (or the type
of userStoreState.profile) to include optional properties goals?: UserGoal[];
wakeUpTime?: string; windDownTime?: string so you can safely access those fields
without casts, or alternatively perform proper type narrowing (check profile
exists and use guarded property access) before reading profile.goals,
profile.wakeUpTime, and profile.windDownTime in the userStore -> insightStore
mapping.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -80,11 +111,43 @@ export const useInsightStore = create<InsightState>()((set) => ({
     try {
       // Labor Illusion: API呼び出しと並行してフェーズ表示
       const advicePromise = (async () => {
-        const request = buildAdviceRequest();
-        if (!request) {
-          throw new Error('プロファイルが設定されていません');
-        }
-        return apiClient.advice.generate(request);
+        const healthStoreState = useHealthStore.getState();
+        const userStoreState = useUserStore.getState();
+        
+        // 天気データは一時的に空のオブジェクトを使用（実際にはuseWeatherフックから取得する必要がある）
+        const weather: WeatherData = {
+          temperature: 20,
+          pressure: 1013,
+          pressureTrend: 'stable',
+          sunrise: '06:00',
+          sunset: '18:00',
+        };
+
+        const request = buildAdviceRequest({
+          healthStore: {
+            metrics: {
+              sleep: healthStoreState.metrics.sleep ? {
+                durationMinutes: healthStoreState.metrics.sleep.durationMinutes,
+                deepSleepMinutes: Math.round(healthStoreState.metrics.sleep.durationMinutes * healthStoreState.metrics.sleep.deepSleepRatio),
+                remSleepMinutes: Math.round(healthStoreState.metrics.sleep.durationMinutes * healthStoreState.metrics.sleep.remSleepRatio),
+              } : null,
+              hrv: healthStoreState.metrics.hrv ? {
+                current: healthStoreState.metrics.hrv.current,
+                baseline30d: healthStoreState.metrics.hrv.baseline30d,
+              } : null,
+            },
+          },
+          userStore: {
+            profile: userStoreState.profile ? {
+              goals: (userStoreState.profile as { goals?: unknown }).goals ?? ['better_sleep'],
+              wakeUpTime: (userStoreState.profile as { wakeUpTime?: unknown }).wakeUpTime ?? '07:00',
+              windDownTime: (userStoreState.profile as { windDownTime?: unknown }).windDownTime ?? '23:00',
+            } : null,



================================================================================
ファイル: app/src/stores/insightStore.ts
行: 197 (開始行: 194)
コミット: a35fee7
作成日時: 2026-01-07T07:51:18Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**TODO: Send feedback to backend.**

This feedback functionality is incomplete. The user's insight feedback is stored locally but not persisted to the backend.



Would you like me to help implement the backend feedback submission, or open an issue to track this task?

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/stores/insightStore.ts around lines 194 - 197, The
setInsightFeedback setter currently only updates local state (insightFeedback)
and doesn't persist it; update setInsightFeedback to perform the local
optimistic update then POST the feedback to the backend (e.g., call an existing
API helper or fetch to /api/insights/feedback) with the feedback payload and any
relevant identifiers (e.g., current insightId or userId) so the server can store
it; ensure you handle and log errors from the request and, on permanent failure,
either roll back the optimistic update or surface an error state so the UI can
notify the user; key symbols to change: setInsightFeedback and insightFeedback.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -96,109 +159,145 @@ export const useInsightStore = create<InsightState>()((set) => ({
       // API レスポンス待機
       const response = await advicePromise;
 
-      if (!response.success || !response.data) {
-        throw new Error(response.error || 'アドバイスの生成に失敗しました');
+      if (!response.success) {
+        throw new Error(response.error?.message || 'アドバイスの生成に失敗しました');
       }
 
-      const { summary, insight, recommendedAction } = response.data;
-
-      // DailyAdvice形式に変換
-      const dailyAdvice: DailyAdvice = {
-        id: `advice_${Date.now()}`,
-        date: new Date(),
-        greeting: insight.greeting,
-        condition: insight.condition,
-        sleep: insight.sleep,
-        rhythm: insight.rhythm,
-        environment: insight.environment,
-        advice: insight.advice,
-        closing: insight.closing,
-      };
-
-      // RecommendedAction形式に変換
-      const action = createRecommendedAction(
-        recommendedAction.type,
-        recommendedAction.message
-      );
+      if (!response.data) {
+        throw new Error('アドバイスの生成に失敗しました');
+      }
+
+      const data = response.data;
 
       set({
-        dailyAdvice,
-        shortGreeting: summary,
-        quickActions: MOCK_QUICK_ACTIONS, // TODO: APIから取得
-        recommendedAction: action,
+        todayInsight: data.todayInsight,
+        todayOneThing: data.todayOneThing,
+        relatedInsight: data.relatedInsight,
         isGeneratingInsight: false,
         lastInsightUpdate: new Date(),
+        lastFetchedDate: new Date().toISOString().split('T')[0],
+        error: null,
       });
     } catch (error) {
       set({
         isGeneratingInsight: false,
         insightError:
           error instanceof Error ? error.message : 'アドバイスの生成に失敗しました',
       });
-      console.error('Insight generation error:', error);
     }
   },
 
-  setMood: (mood) => set({ todayMood: mood }),
+      setMood: (mood) => set({ todayMood: mood }),
 
-  setTodayMode: (mode) => set({ todayMode: mode }),
+      setTodayMode: (mode) => set({ todayMode: mode }),
 
-  setInsightFeedback: (feedback) => {
-    set({ insightFeedback: feedback });
-    // TODO: Send feedback to backend
-  },
+      setInsightFeedback: (feedback) => {
+        set({ insightFeedback: feedback });
+        // TODO: Send feedback to backend
+      },



================================================================================
ファイル: app/app/(main)/action-detail.tsx
行: 46 (開始行: 32)
コミット: a35fee7
作成日時: 2026-01-07T07:52:32Z
================================================================================

【コメント】
_🛠️ Refactor suggestion_ | _🟠 Major_

**Duplicate `useFadeIn` hook - consider reusing or consolidating.**

A `useFadeIn` hook already exists at `app/src/hooks/useFadeIn.ts` (see relevant code snippets). This local definition creates code duplication. Note that the implementations differ (this uses react-native-reanimated, the other uses React Native's Animated API), so you may want to consolidate to a single reanimated-based implementation.


<details>
<summary>♻️ Proposed approach</summary>

Either:
1. Update the shared hook in `app/src/hooks/useFadeIn.ts` to use react-native-reanimated, then import it here
2. Or rename this local hook to avoid confusion (e.g., `useReanimatedFadeIn`)

```diff
-// Fade-in animation hook
-const useFadeIn = (delay: number = 0) => {
-  const opacity = useSharedValue(0);
-  const translateY = useSharedValue(10);
-
-  useEffect(() => {
-    opacity.value = withDelay(delay, withTiming(1, { duration: 600 }));
-    translateY.value = withDelay(delay, withTiming(0, { duration: 600 }));
-  }, [opacity, translateY, delay]);
-
-  return useAnimatedStyle(() => ({
-    opacity: opacity.value,
-    transform: [{ translateY: translateY.value }],
-  }));
-};
+import { useFadeIn } from '../../src/hooks/useFadeIn';
```
</details>


> Committable suggestion skipped: line range outside the PR's diff.

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(main)/action-detail.tsx around lines 32 - 46, The local useFadeIn
hook duplicates an existing hook with a different implementation; either
consolidate by updating the shared hook to the reanimated implementation and
import that shared useFadeIn here, or rename the local hook (e.g.,
useReanimatedFadeIn) to avoid collision. Locate the local useFadeIn definition
in this file and either remove it and replace its usage with the imported shared
hook, or rename the function and update all references in this file to the new
name (ensure the signature/params match the reanimated behavior if
consolidating).
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,268 @@
+/**
+ * ActionDetailScreen - アクション詳細画面
+ * MOCK_AI_RESPONSE からデータを取得し、Today画面と共有
+ */
+
+import React, { useEffect } from 'react';
+import { View, Text, ScrollView, Pressable } from 'react-native';
+import { useSafeAreaInsets } from 'react-native-safe-area-context';
+import { useRouter } from 'expo-router';
+import { ChevronLeft, Footprints, Sun, Battery, Moon, Bell } from 'lucide-react-native';
+import { LinearGradient } from 'expo-linear-gradient';
+import * as Haptics from 'expo-haptics';
+import Animated, {
+  useSharedValue,
+  useAnimatedStyle,
+  withDelay,
+  withTiming,
+  withSpring,
+} from 'react-native-reanimated';
+
+import { t } from '../../src/i18n';
+import { colors, FontFamily } from '../../src/theme';
+import { MOCK_AI_RESPONSE } from '../../src/constants/mockData';
+
+// アイコン設定（UI表示用）
+const BENEFIT_ICONS = [
+  { icon: Sun, iconColor: colors.amber[500], iconBg: colors.amber[50] },
+  { icon: Moon, iconColor: colors.indigo[500], iconBg: colors.indigo[50] },
+  { icon: Battery, iconColor: colors.emerald[500], iconBg: colors.emerald[50] },
+];
+
+// Fade-in animation hook
+const useFadeIn = (delay: number = 0) => {
+  const opacity = useSharedValue(0);
+  const translateY = useSharedValue(10);
+
+  useEffect(() => {
+    opacity.value = withDelay(delay, withTiming(1, { duration: 600 }));
+    translateY.value = withDelay(delay, withTiming(0, { duration: 600 }));
+  }, [opacity, translateY, delay]);
+
+  return useAnimatedStyle(() => ({
+    opacity: opacity.value,
+    transform: [{ translateY: translateY.value }],
+  }));
+};



================================================================================
ファイル: app/app/(main)/action-detail.tsx
行: 74 (開始行: 71)
コミット: a35fee7
作成日時: 2026-01-07T07:52:32Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**TODO comment noted - reminder functionality pending.**

The `handleReminder` function triggers haptic feedback but has incomplete implementation. Offering assistance to implement this if needed.

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,268 @@
+/**
+ * ActionDetailScreen - アクション詳細画面
+ * MOCK_AI_RESPONSE からデータを取得し、Today画面と共有
+ */
+
+import React, { useEffect } from 'react';
+import { View, Text, ScrollView, Pressable } from 'react-native';
+import { useSafeAreaInsets } from 'react-native-safe-area-context';
+import { useRouter } from 'expo-router';
+import { ChevronLeft, Footprints, Sun, Battery, Moon, Bell } from 'lucide-react-native';
+import { LinearGradient } from 'expo-linear-gradient';
+import * as Haptics from 'expo-haptics';
+import Animated, {
+  useSharedValue,
+  useAnimatedStyle,
+  withDelay,
+  withTiming,
+  withSpring,
+} from 'react-native-reanimated';
+
+import { t } from '../../src/i18n';
+import { colors, FontFamily } from '../../src/theme';
+import { MOCK_AI_RESPONSE } from '../../src/constants/mockData';
+
+// アイコン設定（UI表示用）
+const BENEFIT_ICONS = [
+  { icon: Sun, iconColor: colors.amber[500], iconBg: colors.amber[50] },
+  { icon: Moon, iconColor: colors.indigo[500], iconBg: colors.indigo[50] },
+  { icon: Battery, iconColor: colors.emerald[500], iconBg: colors.emerald[50] },
+];
+
+// Fade-in animation hook
+const useFadeIn = (delay: number = 0) => {
+  const opacity = useSharedValue(0);
+  const translateY = useSharedValue(10);
+
+  useEffect(() => {
+    opacity.value = withDelay(delay, withTiming(1, { duration: 600 }));
+    translateY.value = withDelay(delay, withTiming(0, { duration: 600 }));
+  }, [opacity, translateY, delay]);
+
+  return useAnimatedStyle(() => ({
+    opacity: opacity.value,
+    transform: [{ translateY: translateY.value }],
+  }));
+};
+
+const AnimatedPressable = Animated.createAnimatedComponent(Pressable);
+
+export default function ActionDetailScreen(): React.ReactElement {
+  const router = useRouter();
+  const insets = useSafeAreaInsets();
+
+  // MOCK_AI_RESPONSE からデータを取得（Today画面と共有）
+  const { todayOneThing } = MOCK_AI_RESPONSE;
+
+  // Fade-in animations
+  const titleFadeIn = useFadeIn(0);
+  const whyFadeIn = useFadeIn(100);
+  const howFadeIn = useFadeIn(200);
+  const benefitFadeIn = useFadeIn(300);
+  const buttonFadeIn = useFadeIn(400);
+
+  // Button press animation
+  const buttonScale = useSharedValue(1);
+
+  const buttonPressStyle = useAnimatedStyle(() => ({
+    transform: [{ scale: buttonScale.value }],
+  }));
+
+  const handleReminder = () => {
+    Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium);
+    // TODO: Implement reminder functionality
+  };



================================================================================
ファイル: app/app/(main)/action-detail.tsx
行: 263 (開始行: N/A)
コミット: a35fee7
作成日時: 2026-01-07T07:52:32Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

**Hardcoded Japanese text should use i18n.**

The button text `{todayOneThing.time}にリマインド` mixes data interpolation with hardcoded Japanese. Use the `t()` function with interpolation for consistency.


<details>
<summary>🌐 Proposed fix</summary>

```diff
-          <Text className="text-white font-bold">{todayOneThing.time}にリマインド</Text>
+          <Text className="text-white font-bold">{t('screen.actionDetail.remindAt', { time: todayOneThing.time })}</Text>
```

Add to locale file:
```json
"screen.actionDetail.remindAt": "{{time}}にリマインド"
```
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
          <Text className="text-white font-bold">{t('screen.actionDetail.remindAt', { time: todayOneThing.time })}</Text>
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(main)/action-detail.tsx at line 263, The hardcoded Japanese string
in the Text component concatenating {todayOneThing.time} with "にリマインド" should be
replaced with an i18n lookup; update the locale file with a key like
"screen.actionDetail.remindAt": "{{time}}にリマインド" and change the UI to call
t('screen.actionDetail.remindAt', { time: todayOneThing.time }) inside the Text
component (replace the current Text that uses todayOneThing.time directly) so
the interpolated time is localized via the t() function.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,268 @@
+/**
+ * ActionDetailScreen - アクション詳細画面
+ * MOCK_AI_RESPONSE からデータを取得し、Today画面と共有
+ */
+
+import React, { useEffect } from 'react';
+import { View, Text, ScrollView, Pressable } from 'react-native';
+import { useSafeAreaInsets } from 'react-native-safe-area-context';
+import { useRouter } from 'expo-router';
+import { ChevronLeft, Footprints, Sun, Battery, Moon, Bell } from 'lucide-react-native';
+import { LinearGradient } from 'expo-linear-gradient';
+import * as Haptics from 'expo-haptics';
+import Animated, {
+  useSharedValue,
+  useAnimatedStyle,
+  withDelay,
+  withTiming,
+  withSpring,
+} from 'react-native-reanimated';
+
+import { t } from '../../src/i18n';
+import { colors, FontFamily } from '../../src/theme';
+import { MOCK_AI_RESPONSE } from '../../src/constants/mockData';
+
+// アイコン設定（UI表示用）
+const BENEFIT_ICONS = [
+  { icon: Sun, iconColor: colors.amber[500], iconBg: colors.amber[50] },
+  { icon: Moon, iconColor: colors.indigo[500], iconBg: colors.indigo[50] },
+  { icon: Battery, iconColor: colors.emerald[500], iconBg: colors.emerald[50] },
+];
+
+// Fade-in animation hook
+const useFadeIn = (delay: number = 0) => {
+  const opacity = useSharedValue(0);
+  const translateY = useSharedValue(10);
+
+  useEffect(() => {
+    opacity.value = withDelay(delay, withTiming(1, { duration: 600 }));
+    translateY.value = withDelay(delay, withTiming(0, { duration: 600 }));
+  }, [opacity, translateY, delay]);
+
+  return useAnimatedStyle(() => ({
+    opacity: opacity.value,
+    transform: [{ translateY: translateY.value }],
+  }));
+};
+
+const AnimatedPressable = Animated.createAnimatedComponent(Pressable);
+
+export default function ActionDetailScreen(): React.ReactElement {
+  const router = useRouter();
+  const insets = useSafeAreaInsets();
+
+  // MOCK_AI_RESPONSE からデータを取得（Today画面と共有）
+  const { todayOneThing } = MOCK_AI_RESPONSE;
+
+  // Fade-in animations
+  const titleFadeIn = useFadeIn(0);
+  const whyFadeIn = useFadeIn(100);
+  const howFadeIn = useFadeIn(200);
+  const benefitFadeIn = useFadeIn(300);
+  const buttonFadeIn = useFadeIn(400);
+
+  // Button press animation
+  const buttonScale = useSharedValue(1);
+
+  const buttonPressStyle = useAnimatedStyle(() => ({
+    transform: [{ scale: buttonScale.value }],
+  }));
+
+  const handleReminder = () => {
+    Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium);
+    // TODO: Implement reminder functionality
+  };
+
+  return (
+    <View className="flex-1 bg-stone-100">
+      {/* Sticky Header */}
+      <View
+        className="flex-row items-center px-6 pb-4 border-b border-stone-100 z-20"
+        style={{
+          paddingTop: insets.top + 16,
+          backgroundColor: 'rgba(250, 250, 249, 0.9)',
+          gap: 16,
+        }}
+      >
+        <Pressable
+          onPress={() => router.back()}
+          className="w-10 h-10 items-center justify-center rounded-full"
+        >
+          <ChevronLeft size={24} color="#57534E" />
+        </Pressable>
+        <Text className="text-sm font-bold text-stone-400 uppercase tracking-widest">
+          {t('screen.actionDetail.header')}
+        </Text>
+      </View>
+
+      {/* Scrollable Content */}
+      <ScrollView
+        contentContainerStyle={{
+          paddingHorizontal: 24,
+          paddingTop: 24,
+          paddingBottom: 160,
+        }}
+        showsVerticalScrollIndicator={false}
+      >
+        {/* Title Section */}
+        <Animated.View style={titleFadeIn} className="items-center">
+          <View
+            className="w-20 h-20 rounded-full items-center justify-center mb-4"
+            style={{
+              backgroundColor: colors.amber[100],
+              shadowColor: colors.amber[500],
+              shadowOffset: { width: 0, height: 4 },
+              shadowOpacity: 0.15,
+              shadowRadius: 12,
+              elevation: 4,
+            }}
+          >
+            <Footprints size={40} color={colors.amber[600]} strokeWidth={1.5} />
+          </View>
+          <Text
+            className="text-2xl font-bold text-stone-900 tracking-tight mb-2 text-center leading-tight"
+            style={{ fontFamily: FontFamily.serif }}
+          >
+            {todayOneThing.action}
+          </Text>
+          <Text className="text-stone-500 font-medium text-center">{todayOneThing.summary}</Text>
+        </Animated.View>
+
+        {/* Section 1: Why This Action - 説明文のみ、タイトルなし */}
+        <Animated.View style={[whyFadeIn, { marginTop: 32 }]}>
+          <View
+            className="bg-amber-50 rounded-2xl p-5 border border-amber-100"
+          >
+            <Text className="text-stone-700 text-sm leading-relaxed">
+              {todayOneThing.whyThisAction}
+            </Text>
+          </View>
+        </Animated.View>
+
+        {/* Section 2: Benefits - ピルスタイル */}
+        <Animated.View
+          style={[whyFadeIn, { marginTop: 24 }]}
+          className="flex-row flex-wrap justify-center"
+        >
+          {todayOneThing.benefits.map((benefit, index) => {
+            const iconConfig = BENEFIT_ICONS[index % BENEFIT_ICONS.length];
+            const IconComponent = iconConfig.icon;
+            return (
+              <View
+                key={index}
+                className="flex-row items-center bg-white rounded-full px-4 py-2 m-1"
+                style={{
+                  borderWidth: 1,
+                  borderColor: colors.stone[100],
+                  shadowColor: '#000',
+                  shadowOffset: { width: 0, height: 2 },
+                  shadowOpacity: 0.04,
+                  shadowRadius: 4,
+                  elevation: 2,
+                }}
+              >
+                <IconComponent size={14} color={iconConfig.iconColor} />
+                <Text className="text-xs font-medium text-stone-700 ml-2">{benefit}</Text>
+              </View>
+            );
+          })}
+        </Animated.View>
+
+        {/* Section 3: How To Do It - シンプルなリスト */}
+        <Animated.View style={[howFadeIn, { marginTop: 32 }]}>
+          <Text className="text-xs font-bold text-stone-400 uppercase tracking-widest mb-4">
+            {t('screen.actionDetail.howToDoIt')}
+          </Text>
+          <View style={{ gap: 12 }}>
+            {todayOneThing.howToDoIt.map((step, index) => (
+              <View
+                key={index}
+                className="flex-row items-start"
+                style={{ gap: 12 }}
+              >
+                <View
+                  className="w-6 h-6 rounded-full items-center justify-center"
+                  style={{ backgroundColor: colors.amber[100] }}
+                >
+                  <Text className="text-xs font-bold" style={{ color: colors.amber[600] }}>{index + 1}</Text>
+                </View>
+                <Text className="text-sm text-stone-700 flex-1 leading-relaxed">{step}</Text>
+              </View>
+            ))}
+          </View>
+        </Animated.View>
+
+        {/* Section 4: Expected Benefit */}
+        <Animated.View style={[benefitFadeIn, { marginTop: 32 }]}>
+          <Text className="text-xs font-bold text-stone-400 uppercase tracking-widest mb-3">
+            {t('screen.actionDetail.expectedBenefit')}
+          </Text>
+          <View
+            className="bg-white p-5 rounded-2xl border border-stone-100"
+            style={{
+              shadowColor: '#000',
+              shadowOffset: { width: 0, height: 2 },
+              shadowOpacity: 0.04,
+              shadowRadius: 8,
+              elevation: 2,
+            }}
+          >
+            <Text className="text-sm text-stone-800 leading-relaxed mb-2">
+              {todayOneThing.expectedBenefit.text}
+            </Text>
+            <Text className="text-xs text-stone-400">
+              — {todayOneThing.expectedBenefit.source}
+            </Text>
+          </View>
+        </Animated.View>
+      </ScrollView>
+
+      {/* Sticky Bottom Button - グラデーション背景付きアンバーボタン */}
+      <Animated.View
+        style={[
+          buttonFadeIn,
+          {
+            position: 'absolute',
+            bottom: 0,
+            left: 0,
+            right: 0,
+            paddingHorizontal: 24,
+            paddingBottom: Math.max(insets.bottom, 24) + 16,
+            paddingTop: 16,
+          },
+        ]}
+      >
+        <LinearGradient
+          colors={['rgba(250, 250, 249, 0)', 'rgba(250, 250, 249, 0.95)', 'rgba(250, 250, 249, 1)']}
+          locations={[0, 0.4, 1]}
+          style={{ position: 'absolute', top: 0, left: 0, right: 0, bottom: 0 }}
+        />
+        <AnimatedPressable
+          onPress={handleReminder}
+          onPressIn={() => { buttonScale.value = withSpring(0.98); }}
+          onPressOut={() => { buttonScale.value = withSpring(1); }}
+          style={[
+            buttonPressStyle,
+            {
+              backgroundColor: colors.amber[500],
+              paddingVertical: 16,
+              borderRadius: 16,
+              flexDirection: 'row',
+              alignItems: 'center',
+              justifyContent: 'center',
+              gap: 8,
+              shadowColor: colors.amber[600],
+              shadowOffset: { width: 0, height: 6 },
+              shadowOpacity: 0.3,
+              shadowRadius: 12,
+              elevation: 8,
+            },
+          ]}
+        >
+          <Bell size={20} color="#FFFFFF" />
+          <Text className="text-white font-bold">{todayOneThing.time}にリマインド</Text>



================================================================================
ファイル: app/app/(main)/breathe.tsx
行: 5 (開始行: 1)
コミット: a35fee7
作成日時: 2026-01-07T07:52:32Z
================================================================================

【コメント】
_🛠️ Refactor suggestion_ | _🟠 Major_

**File exceeds 400-line limit (431 lines).**

Per coding guidelines, files should be under 400 lines. Consider extracting the breathing animation logic or the visual components into separate files.



Potential extractions:
- `BreathingCircle` component (lines 263-362)
- `useBreathingAnimation` hook (animation values and effects)
- Constants/types to a separate file

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(main)/breathe.tsx around lines 1 - 5, The file is over 400 lines;
extract the large visual/animation parts into separate modules: move the
BreathingCircle component (currently defined as BreathingCircle) into its own
file and export it, create a useBreathingAnimation hook that contains the
animated values, timers, and effects used by the screen (replace in-file
animation logic with a call to useBreathingAnimation), and relocate any
constants/types (animation durations, radii, and prop types) into a
constants/types file and import them back into breathe.tsx; when extracting,
preserve the existing component props and return values, update the breathe.tsx
imports/exports to use the new modules, and run/update any references to ensure
TypeScript types and default exports match the originals.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,431 @@
+/**
+ * BreatheScreen - 呼吸エクササイズ画面
+ * sozai/new/tempoai/screens/BreatheScreen.tsx を React Native で完全再現
+ * フルスクリーンモーダル風デザイン
+ */



================================================================================
ファイル: app/app/(main)/breathe.tsx
行: 28 (開始行: N/A)
コミット: a35fee7
作成日時: 2026-01-07T07:52:32Z
================================================================================

【コメント】
_🛠️ Refactor suggestion_ | _🟠 Major_

**Remove unused variables `SCREEN_HEIGHT` and `breatheRingOpacity`.**


<details>
<summary>♻️ Proposed fix</summary>

```diff
-const { width: SCREEN_WIDTH, height: SCREEN_HEIGHT } = Dimensions.get('window');
+const { width: SCREEN_WIDTH } = Dimensions.get('window');
```

```diff
   const glowScale = useSharedValue(1);
-  const breatheRingOpacity = useSharedValue(0.3);
```
</details>


Also applies to: 63-63

<details>
<summary>🧰 Tools</summary>

<details>
<summary>🪛 GitHub Check: 🧹 Lint</summary>

[warning] 28-28: 
'SCREEN_HEIGHT' is assigned a value but never used     

</details>

</details>

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(main)/breathe.tsx at line 28, Remove the unused SCREEN_HEIGHT and
breatheRingOpacity variables: change the Dimensions destructure to only extract
width (e.g., const { width: SCREEN_WIDTH } = Dimensions.get('window')) instead
of pulling height, and delete the breatheRingOpacity constant declaration
(referenced as breatheRingOpacity). Ensure no other code expects those symbols
before removing.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,431 @@
+/**
+ * BreatheScreen - 呼吸エクササイズ画面
+ * sozai/new/tempoai/screens/BreatheScreen.tsx を React Native で完全再現
+ * フルスクリーンモーダル風デザイン
+ */
+
+import React, { useCallback, useEffect, useRef, useState } from 'react';
+import { View, Text, Pressable, Dimensions } from 'react-native';
+import { useSafeAreaInsets } from 'react-native-safe-area-context';
+import { useRouter } from 'expo-router';
+import { X, Play, Pause, Wind } from 'lucide-react-native';
+import Svg, { Circle, Defs, RadialGradient, Stop } from 'react-native-svg';
+import { LinearGradient } from 'expo-linear-gradient';
+import * as Haptics from 'expo-haptics';
+import Animated, {
+  useSharedValue,
+  useAnimatedStyle,
+  withTiming,
+  withRepeat,
+  withSequence,
+  Easing,
+} from 'react-native-reanimated';
+
+import { TAB_BAR_HEIGHT } from './_layout';
+import { t } from '../../src/i18n';
+
+const DEEP_NAVY = '#0F172A';
+const { width: SCREEN_WIDTH, height: SCREEN_HEIGHT } = Dimensions.get('window');



================================================================================
ファイル: app/app/(main)/health-detail.tsx
行: 5 (開始行: 1)
コミット: a35fee7
作成日時: 2026-01-07T07:52:32Z
================================================================================

【コメント】
_🛠️ Refactor suggestion_ | _🟠 Major_

**File significantly exceeds 400-line limit (738 lines).**

This file is nearly double the recommended size. Consider extracting:
- Mock data (`getMetrics`) to `mockData.ts`
- `MetricCard` component to separate file
- `MetricDetail` component to separate file  
- Types to a shared types file

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,738 @@
+/**
+ * HealthDetailScreen - Health詳細画面
+ * sozai/tempoai-health-summary/ を React Native で完全再現
+ */
+



================================================================================
ファイル: app/app/(main)/health-detail.tsx
行: 33 (開始行: 30)
コミット: a35fee7
作成日時: 2026-01-07T07:52:33Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**Duplicate `Timeframe` type definition.**

`Timeframe` is also exported from `app/src/components/TimeframeSelector.tsx`. Consider importing from a single source to avoid type drift.



```typescript
// Import from shared location instead
import { type Timeframe } from '../../src/components/TimeframeSelector';
```

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(main)/health-detail.tsx around lines 30 - 33, The file declares a
duplicate exported type Timeframe which conflicts with the same type in
TimeframeSelector; remove the local Timeframe declaration from health-detail.tsx
and import the shared type instead (use import { type Timeframe } from the
component that currently exports it, e.g. TimeframeSelector) and update any
references/exports in health-detail.tsx to use the imported Timeframe so the
project shares a single source of truth.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,738 @@
+/**
+ * HealthDetailScreen - Health詳細画面
+ * sozai/tempoai-health-summary/ を React Native で完全再現
+ */
+
+import React, { useState, useRef, useCallback, useMemo } from 'react';
+import { View, Text, ScrollView, Pressable, Dimensions } from 'react-native';
+import { SafeAreaView, useSafeAreaInsets } from 'react-native-safe-area-context';
+import { useRouter } from 'expo-router';
+import {
+  ChevronLeft,
+  Check,
+  AlertCircle,
+  ArrowUp,
+  ArrowDown,
+  CheckCircle2,
+  Activity,
+  Heart,
+  Wind,
+  Droplets,
+  Thermometer,
+} from 'lucide-react-native';
+import { BlurView } from 'expo-blur';
+import Animated, { FadeInDown } from 'react-native-reanimated';
+
+import { HealthAreaChart, type ChartDataPoint } from '../../src/components';
+import { colors } from '../../src/theme';
+import { t } from '../../src/i18n';
+
+export type IconType = 'activity' | 'heart' | 'wind' | 'droplet' | 'thermometer';
+export type MetricStatus = 'in-range' | 'out-of-range';
+export type Timeframe = '7D' | '30D' | '60D';
+export type BaselineTrend = 'up' | 'down' | 'neutral';



================================================================================
ファイル: app/app/(main)/health-detail.tsx
行: 639 (開始行: 561)
コミット: a35fee7
作成日時: 2026-01-07T07:52:33Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**`renderMetricCard` recreated on every render.**

This function is defined inside an IIFE within the render, causing it to be recreated on each render. Extract it as a memoized component or move outside the render function.



Consider extracting to a `MetricCard` component defined outside the main component, or use `useCallback` if it needs access to closure variables.

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(main)/health-detail.tsx around lines 561 - 639, renderMetricCard is
recreated on every render because it's defined inside the render IIFE; extract
it to avoid needless re-creation and re-renders. Move the logic into a
standalone functional component (e.g., MetricCard) defined outside the main
component that accepts props matching MetricData plus onPress and uses
ICON_MAP/MetricData for icon and values; alternatively, if it must live inside
the parent, wrap the function with useCallback and list dependencies (cardWidth,
cardHeight, colors, ICON_MAP) so it's memoized. Ensure the new MetricCard uses
the same props (metric, onPress) and preserves the AlertCircle/Check render and
styling.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,738 @@
+/**
+ * HealthDetailScreen - Health詳細画面
+ * sozai/tempoai-health-summary/ を React Native で完全再現
+ */
+
+import React, { useState, useRef, useCallback, useMemo } from 'react';
+import { View, Text, ScrollView, Pressable, Dimensions } from 'react-native';
+import { SafeAreaView, useSafeAreaInsets } from 'react-native-safe-area-context';
+import { useRouter } from 'expo-router';
+import {
+  ChevronLeft,
+  Check,
+  AlertCircle,
+  ArrowUp,
+  ArrowDown,
+  CheckCircle2,
+  Activity,
+  Heart,
+  Wind,
+  Droplets,
+  Thermometer,
+} from 'lucide-react-native';
+import { BlurView } from 'expo-blur';
+import Animated, { FadeInDown } from 'react-native-reanimated';
+
+import { HealthAreaChart, type ChartDataPoint } from '../../src/components';
+import { colors } from '../../src/theme';
+import { t } from '../../src/i18n';
+
+export type IconType = 'activity' | 'heart' | 'wind' | 'droplet' | 'thermometer';
+export type MetricStatus = 'in-range' | 'out-of-range';
+export type Timeframe = '7D' | '30D' | '60D';
+export type BaselineTrend = 'up' | 'down' | 'neutral';
+
+// メトリクスデータ型
+interface MetricData {
+  id: string;
+  name: string;
+  shortName: string;
+  cardLabel: string; // カード用の改行付きラベル
+  value: number | string;
+  unit: string;
+  status: MetricStatus;
+  statusLabel: string;
+  colorHex: string;
+  typicalRange: { min: number; max: number };
+  baseline: string;
+  baselineTrend: BaselineTrend;
+  iconType: IconType;
+  chartData: ChartDataPoint[];
+}
+
+// アイコンマッピング
+const ICON_MAP = {
+  activity: Activity,
+  heart: Heart,
+  wind: Wind,
+  droplet: Droplets,
+  thermometer: Thermometer,
+};
+
+// ステータスラベルを動的に生成するヘルパー
+const getStatusLabel = (
+  status: MetricStatus,
+  typicalRange: { min: number; max: number },
+  value: number | string
+): string => {
+  if (status === 'in-range') {
+    return `${t('metric.health.status.within')} ${typicalRange.min}-${typicalRange.max}`;
+  }
+  const numValue = typeof value === 'number' ? value : parseFloat(value);
+  if (numValue < typicalRange.min) {
+    return `Low < ${typicalRange.min}`;
+  }
+  return `High > ${typicalRange.max}`;
+};
+
+// モックデータ - sozai/tempoai-health-summary と同じ（i18n対応）
+const getMetrics = (): MetricData[] => [
+  {
+    id: 'hrv',
+    name: t('metric.health.hrv.name'),
+    shortName: t('metric.health.hrv.shortName'),
+    cardLabel: t('metric.health.hrv.cardLabel'),
+    value: 82,
+    unit: 'ms',
+    status: 'in-range',
+    statusLabel: `${t('metric.health.status.within')} 58-97`,
+    colorHex: '#22C55E',
+    typicalRange: { min: 58, max: 97 },
+    baseline: '77 ms',
+    baselineTrend: 'up',
+    iconType: 'activity',
+    chartData: [
+      { day: 'T', value: 65 },
+      { day: 'F', value: 78 },
+      { day: 'S', value: 72 },
+      { day: 'S', value: 85 },
+      { day: 'M', value: 71 },
+      { day: 'T', value: 88 },
+      { day: 'W', value: 82 },
+    ],
+  },
+  {
+    id: 'rhr',
+    name: t('metric.health.rhr.name'),
+    shortName: t('metric.health.rhr.shortName'),
+    cardLabel: t('metric.health.rhr.cardLabel'),
+    value: 59,
+    unit: 'bpm',
+    status: 'out-of-range',
+    statusLabel: 'Low < 53',
+    colorHex: '#F87171',
+    typicalRange: { min: 54, max: 63 },
+    baseline: '59 bpm',
+    baselineTrend: 'down',
+    iconType: 'heart',
+    chartData: [
+      { day: 'T', value: 56 },
+      { day: 'F', value: 52 },
+      { day: 'S', value: 52 },
+      { day: 'S', value: 59 },
+      { day: 'M', value: 62 },
+      { day: 'T', value: 59 },
+      { day: 'W', value: 51 },
+    ],
+  },
+  {
+    id: 'resp',
+    name: t('metric.health.resp.name'),
+    shortName: t('metric.health.resp.shortName'),
+    cardLabel: t('metric.health.resp.cardLabel'),
+    value: 11.0,
+    unit: 'BrPM',
+    status: 'in-range',
+    statusLabel: `${t('metric.health.status.within')} 10.5-16`,
+    colorHex: '#3B82F6',
+    typicalRange: { min: 10.5, max: 16 },
+    baseline: '11.2 BrPM',
+    baselineTrend: 'neutral',
+    iconType: 'wind',
+    chartData: [
+      { day: 'T', value: 11.2 },
+      { day: 'F', value: 10.8 },
+      { day: 'S', value: 11.5 },
+      { day: 'S', value: 11.0 },
+      { day: 'M', value: 11.1 },
+      { day: 'T', value: 10.9 },
+      { day: 'W', value: 11.0 },
+    ],
+  },
+  {
+    id: 'spo2',
+    name: t('metric.health.spo2.name'),
+    shortName: t('metric.health.spo2.shortName'),
+    cardLabel: t('metric.health.spo2.cardLabel'),
+    value: 98,
+    unit: '%',
+    status: 'in-range',
+    statusLabel: `${t('metric.health.status.within')} 96-99`,
+    colorHex: '#14B8A6',
+    typicalRange: { min: 96, max: 99 },
+    baseline: '98%',
+    baselineTrend: 'neutral',
+    iconType: 'droplet',
+    chartData: [
+      { day: 'T', value: 97 },
+      { day: 'F', value: 98 },
+      { day: 'S', value: 99 },
+      { day: 'S', value: 98 },
+      { day: 'M', value: 97 },
+      { day: 'T', value: 98 },
+      { day: 'W', value: 98 },
+    ],
+  },
+  {
+    id: 'temp',
+    name: t('metric.health.temp.name'),
+    shortName: t('metric.health.temp.shortName'),
+    cardLabel: t('metric.health.temp.cardLabel'),
+    value: 36.4,
+    unit: '°C',
+    status: 'in-range',
+    statusLabel: `${t('metric.health.status.within')} 36.1-36.9`,
+    colorHex: '#F59E0B',
+    typicalRange: { min: 36.1, max: 36.9 },
+    baseline: '36.4°C',
+    baselineTrend: 'neutral',
+    iconType: 'thermometer',
+    chartData: [
+      { day: 'T', value: 36.3 },
+      { day: 'F', value: 36.5 },
+      { day: 'S', value: 36.4 },
+      { day: 'S', value: 36.2 },
+      { day: 'M', value: 36.6 },
+      { day: 'T', value: 36.3 },
+      { day: 'W', value: 36.4 },
+    ],
+  },
+];
+
+export default function HealthDetailScreen(): React.ReactElement {
+  const router = useRouter();
+  const insets = useSafeAreaInsets();
+  const scrollViewRef = useRef<ScrollView>(null);
+
+  // i18n対応のメトリクスデータを取得
+  const metrics = useMemo(() => getMetrics(), []);
+  const [activeSection, setActiveSection] = useState<string>('hrv');
+
+  // セクションの位置を保持
+  const sectionPositions = useRef<Record<string, number>>({});
+
+  const handleBack = () => {
+    router.back();
+  };
+
+  // セクションへスクロール
+  const scrollToSection = useCallback((id: string) => {
+    setActiveSection(id);
+    const sectionY = sectionPositions.current[id];
+    const containerY = sectionPositions.current['_container'] || 0;
+    if (sectionY !== undefined && scrollViewRef.current) {
+      // コンテナの開始位置 + セクションの相対位置 - ヘッダー分のオフセット
+      const scrollY = containerY + sectionY - 70;
+      scrollViewRef.current.scrollTo({ y: scrollY, animated: true });
+    }
+  }, []);
+
+  // セクションの位置を記録
+  const handleSectionLayout = useCallback((id: string, y: number) => {
+    sectionPositions.current[id] = y;
+  }, []);
+
+  // タイムフレームに基づいてデータを生成
+  const generateTimeframeData = useCallback(
+    (
+      baseData: ChartDataPoint[],
+      tf: Timeframe,
+      typicalRange: { min: number; max: number }
+    ): ChartDataPoint[] => {
+      const baseValue =
+        typeof baseData[0]?.value === 'number' ? baseData[0].value : typicalRange.min;
+      const range = typicalRange.max - typicalRange.min;
+
+      const seededRandom = (seed: number): number => {
+        const x = Math.sin(seed * 12.9898 + seed * 78.233) * 43758.5453;
+        return x - Math.floor(x);
+      };
+
+      const generateValue = (index: number, seed: number): number => {
+        const variance = range * 0.4;
+        const random = seededRandom(index + seed);
+        const val =
+          baseValue + (random - 0.5) * variance * 2;
+        return (
+          Math.round(
+            Math.max(typicalRange.min * 0.85, Math.min(typicalRange.max * 1.15, val)) * 10
+          ) / 10
+        );
+      };
+
+      switch (tf) {
+        case '7D':
+          return baseData;
+        case '30D': {
+          const labels = ['W1', 'W2', 'W3', 'W4', 'Now'];
+          return labels.map((label, i) => ({
+            day: label,
+            value:
+              i === labels.length - 1
+                ? typeof baseData[baseData.length - 1]?.value === 'number'
+                  ? baseData[baseData.length - 1].value
+                  : baseValue
+                : generateValue(i, 30),
+          }));
+        }
+        case '60D': {
+          const labels = ['6w', '4w', '2w', '1w', 'Now'];
+          return labels.map((label, i) => ({
+            day: label,
+            value:
+              i === labels.length - 1
+                ? typeof baseData[baseData.length - 1]?.value === 'number'
+                  ? baseData[baseData.length - 1].value
+                  : baseValue
+                : generateValue(i, 60),
+          }));
+        }
+        default:
+          return baseData;
+      }
+    },
+    []
+  );
+
+  // タイムフレーム状態を各メトリックごとに管理
+  const [timeframes, setTimeframes] = useState<Record<string, Timeframe>>({});
+  const getTimeframe = (id: string): Timeframe => timeframes[id] || '7D';
+  const setTimeframe = (id: string, tf: Timeframe) =>
+    setTimeframes((prev) => ({ ...prev, [id]: tf }));
+
+  // 画面幅を取得
+  const screenWidth = Dimensions.get('window').width;
+
+  // Temperatureカード用（横長レイアウト）- アイコン付き
+  const renderTemperatureCard = (metric: MetricData, onPress: () => void) => {
+    const isOutOfRange = metric.status === 'out-of-range';
+    const IconComponent = ICON_MAP[metric.iconType];
+    return (
+      <View
+        style={{
+          backgroundColor: colors.white,
+          borderRadius: 24,
+          borderWidth: 1,
+          borderColor: colors.stone[100],
+          overflow: 'hidden',
+        }}
+      >
+        <Pressable
+          onPress={onPress}
+          style={{
+            flexDirection: 'row',
+            alignItems: 'center',
+            justifyContent: 'space-between',
+            padding: 16,
+          }}
+        >
+          {/* 左側: アイコン + ラベル（縦中央） */}
+          <View style={{ flexDirection: 'row', alignItems: 'center', gap: 6 }}>
+            <IconComponent size={18} color={colors.stone[400]} />
+            <Text
+              style={{
+                fontSize: 14,
+                fontWeight: '700',
+                color: colors.stone[400],
+              }}
+            >
+              {metric.cardLabel}
+            </Text>
+          </View>
+          {/* 右側: 数値とステータス */}
+          <View style={{ alignItems: 'flex-end' }}>
+            <View style={{ flexDirection: 'row', alignItems: 'baseline', gap: 4 }}>
+              <Text
+                style={{
+                  fontSize: 36,
+                  fontWeight: '700',
+                  color: colors.stone[900],
+                  letterSpacing: -1,
+                }}
+              >
+                {metric.value}
+              </Text>
+              <Text
+                style={{
+                  fontSize: 16,
+                  fontWeight: '500',
+                  color: colors.stone[400],
+                }}
+              >
+                {metric.unit}
+              </Text>
+            </View>
+            <View style={{ flexDirection: 'row', alignItems: 'center', gap: 6, marginTop: 4 }}>
+              {isOutOfRange ? (
+                <AlertCircle size={14} strokeWidth={2.5} color={colors.amber[500]} />
+              ) : (
+                <Check size={14} strokeWidth={3} color={colors.emerald[500]} />
+              )}
+              <Text
+                style={{
+                  fontSize: 14,
+                  fontWeight: '500',
+                  color: isOutOfRange ? colors.amber[500] : colors.emerald[500],
+                }}
+              >
+                {metric.statusLabel}
+              </Text>
+            </View>
+          </View>
+        </Pressable>
+      </View>
+    );
+  };
+
+  // インラインMetricDetail
+  const renderMetricDetail = (metric: MetricData, index: number) => {
+    const tf = getTimeframe(metric.id);
+    const displayChartData = generateTimeframeData(metric.chartData, tf, metric.typicalRange);
+    const tfOptions: Timeframe[] = ['7D', '30D', '60D'];
+
+    return (
+      <Animated.View
+        key={metric.id}
+        entering={FadeInDown.delay(100 + index * 50).duration(400)}
+        style={{ marginBottom: 32 }}
+        onLayout={(event) => {
+          const { y } = event.nativeEvent.layout;
+          handleSectionLayout(metric.id, y);
+        }}
+      >
+        {/* Section Header with Left Border */}
+        <View
+          className="flex-row items-center mb-4 pl-4"
+          style={{ borderLeftWidth: 4, borderLeftColor: metric.colorHex }}
+        >
+          <Text className="text-lg font-semibold text-stone-900">{metric.name}</Text>
+        </View>
+
+        {/* Main Card */}
+        <View
+          className="bg-white p-5 rounded-2xl border border-stone-100"
+          style={{
+            shadowColor: '#000',
+            shadowOffset: { width: 0, height: 4 },
+            shadowOpacity: 0.06,
+            shadowRadius: 10,
+            elevation: 4,
+          }}
+        >
+          {/* Top Stats Row */}
+          <View className="flex-row justify-between items-end mb-6">
+            <View>
+              <Text
+                className="text-xs font-semibold text-stone-400 uppercase mb-1"
+                style={{ letterSpacing: 0.5 }}
+              >
+                {t('detail.health.mostRecent')}
+              </Text>
+              <View className="flex-row items-baseline" style={{ gap: 4 }}>
+                <Text className="text-4xl font-bold" style={{ color: metric.colorHex }}>
+                  {metric.value}
+                </Text>
+                <Text className="text-sm font-medium text-stone-500">{metric.unit}</Text>
+              </View>
+            </View>
+            <View style={{ alignItems: 'flex-end' }}>
+              <Text
+                className="text-xs font-semibold text-stone-400 uppercase mb-1"
+                style={{ letterSpacing: 0.5 }}
+              >
+                {t('detail.health.baseline')}
+              </Text>
+              <View className="flex-row items-center" style={{ gap: 4 }}>
+                <Text className="text-sm font-medium text-stone-600">{metric.baseline}</Text>
+                {metric.baselineTrend === 'up' && (
+                  <ArrowUp size={16} color={metric.colorHex} />
+                )}
+                {metric.baselineTrend === 'down' && (
+                  <ArrowDown size={16} color={metric.colorHex} />
+                )}
+              </View>
+            </View>
+          </View>
+
+          {/* Timeframe Selector */}
+          <View className="items-center mb-6">
+            <View
+              className="flex-row bg-stone-100 p-1 rounded-full"
+              style={{ gap: 4 }}
+            >
+              {tfOptions.map((option) => (
+                <Pressable
+                  key={option}
+                  onPress={() => setTimeframe(metric.id, option)}
+                  className="px-4 py-1.5 rounded-full"
+                  style={[
+                    tf === option && {
+                      backgroundColor: colors.stone[800],
+                      shadowColor: colors.stone[900],
+                      shadowOffset: { width: 0, height: 2 },
+                      shadowOpacity: 0.1,
+                      shadowRadius: 4,
+                      elevation: 2,
+                    },
+                  ]}
+                >
+                  <Text
+                    className="text-xs font-medium"
+                    style={{ color: tf === option ? colors.white : colors.stone[500] }}
+                  >
+                    {option}
+                  </Text>
+                </Pressable>
+              ))}
+            </View>
+          </View>
+
+          {/* Chart */}
+          <View className="mb-4">
+            <HealthAreaChart
+              data={displayChartData}
+              colorHex={metric.colorHex}
+              typicalRange={metric.typicalRange}
+              unit={metric.unit}
+              height={180}
+            />
+          </View>
+
+          {/* Legend */}
+          <View
+            className="flex-row items-center pt-2 border-t border-stone-100"
+            style={{ gap: 8 }}
+          >
+            <CheckCircle2 size={16} color={metric.colorHex} />
+            <Text className="text-xs font-medium text-stone-500">
+              {t('detail.health.typicalRange')}: {metric.typicalRange.min}-{metric.typicalRange.max}{' '}
+              {metric.unit.replace(' ', '')}
+            </Text>
+          </View>
+        </View>
+      </Animated.View>
+    );
+  };
+
+  return (
+    <View className="flex-1 bg-stone-50">
+      <SafeAreaView className="flex-1" edges={['top']}>
+        {/* Header */}
+        <View
+          className="flex-row items-center justify-between px-5 border-b border-stone-100 bg-stone-50"
+          style={{
+            height: 56,
+            shadowColor: colors.stone[900],
+            shadowOffset: { width: 0, height: 1 },
+            shadowOpacity: 0.05,
+            shadowRadius: 2,
+          }}
+        >
+          <Pressable
+            onPress={handleBack}
+            className="p-2 -ml-2 rounded-full"
+            style={({ pressed }) => [
+              { backgroundColor: pressed ? colors.stone[100] : 'transparent' },
+            ]}
+          >
+            <ChevronLeft size={24} strokeWidth={2.5} color={colors.stone[800]} />
+          </Pressable>
+          <Text className="text-lg font-bold text-stone-900">Health</Text>
+          <View style={{ width: 40 }} />
+        </View>
+
+        <ScrollView
+          ref={scrollViewRef}
+          contentContainerStyle={{
+            paddingBottom: insets.bottom + 100,
+          }}
+          showsVerticalScrollIndicator={false}
+        >
+          <View className="px-4 pt-6">
+            {/* Summary Grid - 2x2 + full width */}
+            <Animated.View entering={FadeInDown.duration(400)} className="mb-8">
+              {(() => {
+                const CARD_GAP = 12;
+                const HORIZONTAL_PADDING = 16;
+                const containerWidth = screenWidth - HORIZONTAL_PADDING * 2;
+                const cardWidth = (containerWidth - CARD_GAP) / 2;
+                const cardHeight = cardWidth * 0.85; // 縦幅を少し狭める
+
+                const renderMetricCard = (metric: MetricData, onPress: () => void) => {
+                  const isOutOfRange = metric.status === 'out-of-range';
+                  const IconComponent = ICON_MAP[metric.iconType];
+                  return (
+                    <View
+                      style={{
+                        width: cardWidth,
+                        height: cardHeight,
+                        backgroundColor: colors.white,
+                        borderRadius: 24,
+                        borderWidth: 1,
+                        borderColor: colors.stone[100],
+                        overflow: 'hidden',
+                      }}
+                    >
+                      <Pressable
+                        onPress={onPress}
+                        style={{
+                          padding: 16,
+                        }}
+                      >
+                        {/* Icon + Label */}
+                        <View style={{ flexDirection: 'row', alignItems: 'center', gap: 6 }}>
+                          <IconComponent size={18} color={colors.stone[400]} />
+                          <Text
+                            style={{
+                              fontSize: 14,
+                              fontWeight: '700',
+                              color: colors.stone[400],
+                            }}
+                          >
+                            {metric.cardLabel}
+                          </Text>
+                        </View>
+
+                        {/* Value */}
+                        <View style={{ flexDirection: 'row', alignItems: 'baseline', gap: 4, marginTop: 8 }}>
+                          <Text
+                            style={{
+                              fontSize: 36,
+                              fontWeight: '700',
+                              color: colors.stone[900],
+                              letterSpacing: -1,
+                            }}
+                          >
+                            {metric.value}
+                          </Text>
+                          <Text
+                            style={{
+                              fontSize: 16,
+                              fontWeight: '500',
+                              color: colors.stone[400],
+                            }}
+                          >
+                            {metric.unit}
+                          </Text>
+                        </View>
+
+                        {/* Status */}
+                        <View style={{ flexDirection: 'row', alignItems: 'center', gap: 6, marginTop: 8 }}>
+                          {isOutOfRange ? (
+                            <AlertCircle size={14} strokeWidth={2.5} color={colors.amber[500]} />
+                          ) : (
+                            <Check size={14} strokeWidth={3} color={colors.emerald[500]} />
+                          )}
+                          <Text
+                            style={{
+                              fontSize: 14,
+                              fontWeight: '500',
+                              color: isOutOfRange ? colors.amber[500] : colors.emerald[500],
+                            }}
+                          >
+                            {metric.statusLabel}
+                          </Text>
+                        </View>
+                      </Pressable>
+                    </View>
+                  );
+                };



================================================================================
ファイル: app/app/(main)/insight-detail.tsx
行: 43 (開始行: 29)
コミット: a35fee7
作成日時: 2026-01-07T07:52:33Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
# Check if the shared useFadeIn hook exists and find all useFadeIn usages
rg -n "useFadeIn" --type=ts --type=tsx -g '!node_modules' | head -50
```

Repository: Bluefinee/tempo-ai

Length of output: 89

---

🏁 Script executed:

```shell
# Find the actual locations of hook files
fd -e ts -e tsx -path "*/hooks/*" | grep -i fade
```

Repository: Bluefinee/tempo-ai

Length of output: 230

---

🏁 Script executed:

```shell
# Read the insight-detail.tsx file to check imports and the useFadeIn implementation
cat -n app/app/\(main\)/insight-detail.tsx | head -50
```

Repository: Bluefinee/tempo-ai

Length of output: 2058

---

🏁 Script executed:

```shell
# Also search for the shared hooks directory structure
fd -type f -path "*/hooks/*" | head -20
```

Repository: Bluefinee/tempo-ai

Length of output: 232

---

🏁 Script executed:

```shell
# Search for all useFadeIn usages across the codebase
rg "useFadeIn" -n | head -50
```

Repository: Bluefinee/tempo-ai

Length of output: 1317

---

🏁 Script executed:

```shell
# Check if the shared useFadeIn hook exists
find . -name "useFadeIn.ts" -o -name "useFadeIn.tsx" 2>/dev/null
```

Repository: Bluefinee/tempo-ai

Length of output: 90

---

🏁 Script executed:

```shell
# Search for hooks directory
find . -type d -name "hooks" 2>/dev/null
```

Repository: Bluefinee/tempo-ai

Length of output: 90

---

🏁 Script executed:

```shell
cat -n app/src/hooks/useFadeIn.ts
```

Repository: Bluefinee/tempo-ai

Length of output: 1700

---



</details>

**Extract duplicate reanimated-based fade-in hooks to a shared location.**

This file defines a local `useFadeIn` hook using `react-native-reanimated`, while the codebase has a shared hook at `app/src/hooks/useFadeIn.ts` that uses React Native's standard `Animated` API. More critically, the same reanimated-based hook is duplicated across three files: `insight-detail.tsx`, `settings.tsx`, and `action-detail.tsx`.

Consider:
1. Creating a shared reanimated-based `useFadeInReanimated` hook if the codebase is shifting toward reanimated for better performance
2. Or updating the shared hook to use reanimated and removing all three local implementations for consistency

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,173 @@
+/**
+ * InsightDetailScreen - インサイト詳細画面
+ * MOCK_AI_RESPONSE からデータを取得し、Today画面と共有
+ */
+
+import React, { useEffect } from 'react';
+import { View, Text, ScrollView, Pressable } from 'react-native';
+import { useSafeAreaInsets } from 'react-native-safe-area-context';
+import { useRouter } from 'expo-router';
+import { ChevronLeft, Sparkles, Activity, Moon, Clock } from 'lucide-react-native';
+import Animated, {
+  useSharedValue,
+  useAnimatedStyle,
+  withDelay,
+  withTiming,
+} from 'react-native-reanimated';
+
+import { t } from '../../src/i18n';
+import { FontFamily } from '../../src/theme';
+import { MOCK_AI_RESPONSE, formatDate } from '../../src/constants/mockData';
+
+// アイコン設定（UI表示用）
+const DATA_POINT_ICONS = [
+  { key: 'hrv', icon: Activity, iconColor: '#FB7185', iconBg: '#FFF1F2' },
+  { key: 'sleep', icon: Moon, iconColor: '#6366F1', iconBg: '#EEF2FF' },
+  { key: 'rhythm', icon: Clock, iconColor: '#F59E0B', iconBg: '#FFFBEB' },
+] as const;
+
+// Fade-in animation hook
+const useFadeIn = (delay: number = 0) => {
+  const opacity = useSharedValue(0);
+  const translateY = useSharedValue(10);
+
+  useEffect(() => {
+    opacity.value = withDelay(delay, withTiming(1, { duration: 600 }));
+    translateY.value = withDelay(delay, withTiming(0, { duration: 600 }));
+  }, [opacity, translateY, delay]);
+
+  return useAnimatedStyle(() => ({
+    opacity: opacity.value,
+    transform: [{ translateY: translateY.value }],
+  }));
+};



================================================================================
ファイル: app/app/(main)/recovery-detail.tsx
行: 44 (開始行: 42)
コミット: a35fee7
作成日時: 2026-01-07T07:52:33Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**Add explicit return type.**


<details>
<summary>Minor improvement</summary>

```diff
-  const handleBack = () => {
+  const handleBack = (): void => {
     router.back();
   };
```
</details>

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(main)/recovery-detail.tsx around lines 42 - 44, The function
handleBack currently has an implicit return type; add an explicit return type
(void) to its declaration (function handleBack(): void) so the intent is clear
and TypeScript enforces no value is returned; update the handleBack symbol where
it calls router.back() to use the explicit void return type.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,269 @@
+/**
+ * RecoveryDetailScreen - Recovery詳細画面
+ */
+
+import React, { useState } from 'react';
+import { View, Text, ScrollView, Pressable } from 'react-native';
+import { SafeAreaView, useSafeAreaInsets } from 'react-native-safe-area-context';
+import { useRouter } from 'expo-router';
+import { ChevronLeft, Activity } from 'lucide-react-native';
+import Animated, { FadeInDown } from 'react-native-reanimated';
+
+import {
+  CircularProgress,
+  TimeframeSelector,
+  MiniBarChart,
+  type Timeframe,
+} from '../../src/components';
+import { colors, FontFamily } from '../../src/theme';
+import { t } from '../../src/i18n';
+import { MOCK_DETAIL } from '../../src/constants/mockData';
+import { useHealthStore } from '../../src/stores/healthStore';
+
+// スコアに応じたステータスを取得
+const getRecoveryStatus = (score: number): string => {
+  if (score >= 80) return t('score.recovery.status.excellent');
+  if (score >= 60) return t('score.recovery.status.good');
+  if (score >= 40) return t('score.recovery.status.fair');
+  return t('score.recovery.status.needsRest');
+};
+
+export default function RecoveryDetailScreen(): React.ReactElement {
+  const router = useRouter();
+  const insets = useSafeAreaInsets();
+  const [timeframe, setTimeframe] = useState<Timeframe>('7D');
+  const { dailySnapshot } = useHealthStore();
+  
+  // healthStoreから計算済みのスコアを取得
+  const recoveryScore = dailySnapshot?.scores?.recovery ?? 0;
+  
+  const data = MOCK_DETAIL.recovery;
+
+  const handleBack = () => {
+    router.back();
+  };



================================================================================
ファイル: app/app/(main)/recovery-detail.tsx
行: 62 (開始行: 46)
コミット: a35fee7
作成日時: 2026-01-07T07:52:33Z
================================================================================

【コメント】
_🛠️ Refactor suggestion_ | _🟠 Major_

**Add explicit return types to helper functions.**

These helper functions are missing explicit return type annotations. As per coding guidelines, all functions should declare explicit return types.


<details>
<summary>Suggested fix</summary>

```diff
-  const getChangeColor = (change: number) => {
+  const getChangeColor = (change: number): string => {
     if (change > 0) return colors.emerald[600];
     if (change < 0) return colors.rose[500];
     return colors.stone[500];
   };

-  const getChangeBgColor = (change: number) => {
+  const getChangeBgColor = (change: number): string => {
     if (change > 0) return colors.emerald[50];
     if (change < 0) return colors.rose[50];
     return colors.stone[100];
   };

-  const getChangeSymbol = (change: number) => {
+  const getChangeSymbol = (change: number): string => {
     if (change > 0) return '↑';
     if (change < 0) return '↓';
     return '=';
   };
```
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
  const getChangeColor = (change: number): string => {
    if (change > 0) return colors.emerald[600];
    if (change < 0) return colors.rose[500];
    return colors.stone[500];
  };

  const getChangeBgColor = (change: number): string => {
    if (change > 0) return colors.emerald[50];
    if (change < 0) return colors.rose[50];
    return colors.stone[100];
  };

  const getChangeSymbol = (change: number): string => {
    if (change > 0) return '↑';
    if (change < 0) return '↓';
    return '=';
  };
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(main)/recovery-detail.tsx around lines 46 - 62, The three helper
functions getChangeColor, getChangeBgColor, and getChangeSymbol lack explicit
return type annotations; update their signatures to declare return types (e.g.,
add ": string" to each function) so getChangeColor(change: number): string,
getChangeBgColor(change: number): string, and getChangeSymbol(change: number):
string; ensure the implementations still return string literals and adjust any
callers if necessary.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,269 @@
+/**
+ * RecoveryDetailScreen - Recovery詳細画面
+ */
+
+import React, { useState } from 'react';
+import { View, Text, ScrollView, Pressable } from 'react-native';
+import { SafeAreaView, useSafeAreaInsets } from 'react-native-safe-area-context';
+import { useRouter } from 'expo-router';
+import { ChevronLeft, Activity } from 'lucide-react-native';
+import Animated, { FadeInDown } from 'react-native-reanimated';
+
+import {
+  CircularProgress,
+  TimeframeSelector,
+  MiniBarChart,
+  type Timeframe,
+} from '../../src/components';
+import { colors, FontFamily } from '../../src/theme';
+import { t } from '../../src/i18n';
+import { MOCK_DETAIL } from '../../src/constants/mockData';
+import { useHealthStore } from '../../src/stores/healthStore';
+
+// スコアに応じたステータスを取得
+const getRecoveryStatus = (score: number): string => {
+  if (score >= 80) return t('score.recovery.status.excellent');
+  if (score >= 60) return t('score.recovery.status.good');
+  if (score >= 40) return t('score.recovery.status.fair');
+  return t('score.recovery.status.needsRest');
+};
+
+export default function RecoveryDetailScreen(): React.ReactElement {
+  const router = useRouter();
+  const insets = useSafeAreaInsets();
+  const [timeframe, setTimeframe] = useState<Timeframe>('7D');
+  const { dailySnapshot } = useHealthStore();
+  
+  // healthStoreから計算済みのスコアを取得
+  const recoveryScore = dailySnapshot?.scores?.recovery ?? 0;
+  
+  const data = MOCK_DETAIL.recovery;
+
+  const handleBack = () => {
+    router.back();
+  };
+
+  const getChangeColor = (change: number) => {
+    if (change > 0) return colors.emerald[600];
+    if (change < 0) return colors.rose[500];
+    return colors.stone[500];
+  };
+
+  const getChangeBgColor = (change: number) => {
+    if (change > 0) return colors.emerald[50];
+    if (change < 0) return colors.rose[50];
+    return colors.stone[100];
+  };
+
+  const getChangeSymbol = (change: number) => {
+    if (change > 0) return '↑';
+    if (change < 0) return '↓';
+    return '=';
+  };



================================================================================
ファイル: app/app/(main)/rhythm-detail.tsx
行: 84 (開始行: N/A)
コミット: a35fee7
作成日時: 2026-01-07T07:52:33Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**Consider using arrow function with named export.**

Per coding guidelines, prefer arrow functions and named exports over function declarations with default exports for better refactoring and tree-shaking.

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(main)/rhythm-detail.tsx at line 84, The component is declared as a
function declaration with a default export (RhythmDetailScreen); convert it to a
named arrow export to follow guidelines: replace the function declaration with a
named const arrow (e.g., export const RhythmDetailScreen = ():
React.ReactElement => { ... }) and remove the default export, then update any
imports that referenced the default to use the named import (import {
RhythmDetailScreen } from '...'). Ensure the component type annotation stays
(React.ReactElement) and preserve the existing body and props.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,385 @@
+/**
+ * RhythmDetailScreen - Rhythm詳細画面
+ * Bedtime/Wake Consistency、Weekly Patternを含む
+ */
+
+import React from 'react';
+import { View, Text, ScrollView, Pressable } from 'react-native';
+import { SafeAreaView, useSafeAreaInsets } from 'react-native-safe-area-context';
+import { useRouter } from 'expo-router';
+import { ChevronLeft, Clock, Moon, Sun, Calendar, Plane } from 'lucide-react-native';
+import Animated, { FadeInDown } from 'react-native-reanimated';
+import Svg, { Rect, Line } from 'react-native-svg';
+
+import { CircularProgress } from '../../src/components';
+import { colors, FontFamily } from '../../src/theme';
+import { t } from '../../src/i18n';
+import { MOCK_DETAIL } from '../../src/constants/mockData';
+import { useHealthStore } from '../../src/stores/healthStore';
+
+// スコアに応じたステータスを取得
+const getRhythmStatus = (score: number): string => {
+  if (score >= 90) return t('score.rhythm.status.excellent');
+  if (score >= 75) return t('score.rhythm.status.good');
+  if (score >= 50) return t('score.rhythm.status.fair');
+  return t('score.rhythm.status.poor');
+};
+
+// Contributing Factor カードコンポーネント（詳細版）
+interface FactorCardProps {
+  icon: React.ElementType;
+  iconColor: string;
+  iconBg: string;
+  label: string;
+  value: number;
+  trend: string;
+  trendDirection: 'up' | 'down' | 'stable';
+  detail: string;
+}
+
+const FactorCard = ({
+  icon: Icon,
+  iconColor,
+  iconBg,
+  label,
+  value,
+  trend,
+  trendDirection,
+  detail,
+}: FactorCardProps): React.ReactElement => {
+  const getTrendColor = () => {
+    if (trendDirection === 'up') return colors.emerald[500];
+    if (trendDirection === 'down') return colors.rose[500];
+    return colors.stone[400];
+  };
+
+  return (
+    <View
+      className="bg-white p-4 rounded-2xl border border-stone-100"
+      style={{
+        shadowColor: '#000',
+        shadowOffset: { width: 0, height: 2 },
+        shadowOpacity: 0.04,
+        shadowRadius: 6,
+        elevation: 2,
+      }}
+    >
+      <View className="flex-row items-center justify-between mb-2">
+        <View className="flex-row items-center" style={{ gap: 10 }}>
+          <View className="p-2 rounded-xl" style={{ backgroundColor: iconBg }}>
+            <Icon size={16} color={iconColor} />
+          </View>
+          <Text className="text-xs font-bold text-stone-500 uppercase">{label}</Text>
+        </View>
+        <Text className="text-xs font-bold" style={{ color: getTrendColor() }}>
+          {trend}
+        </Text>
+      </View>
+      <Text className="text-2xl font-bold text-stone-900 mb-1">{value}%</Text>
+      <Text className="text-[10px] text-stone-400">{detail}</Text>
+    </View>
+  );
+};
+
+export default function RhythmDetailScreen(): React.ReactElement {



================================================================================
ファイル: app/app/(main)/rhythm-detail.tsx
行: 92 (開始行: 88)
コミット: a35fee7
作成日時: 2026-01-07T07:52:33Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟠 Major_

**Inconsistent data sources: `rhythmScore` vs `data.score`.**

The component fetches `rhythmScore` from `healthStore.dailySnapshot` (Line 90) but also uses `data` from `MOCK_DETAIL.rhythm` (Line 92). At Line 239, `getRhythmStatus(data.score)` uses mock data while Line 230 displays `rhythmScore` from the store. This creates a mismatch between the displayed score and its status label.


<details>
<summary>🐛 Proposed fix</summary>

```diff
               <Text className="text-xs font-bold text-purple-600 uppercase tracking-widest">
-                  {getRhythmStatus(data.score)}
+                  {getRhythmStatus(rhythmScore)}
               </Text>
```
</details>


> Committable suggestion skipped: line range outside the PR's diff.

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(main)/rhythm-detail.tsx around lines 88 - 92, The component mixes
sources: rhythmScore (from healthStore.dailySnapshot) and data
(MOCK_DETAIL.rhythm), causing getRhythmStatus(data.score) to use mock data while
the UI shows rhythmScore; fix by using a single source of truth—either pass
rhythmScore into getRhythmStatus (replace getRhythmStatus(data.score) with
getRhythmStatus(rhythmScore)) or merge the mock object with the store value
(e.g., keep MOCK_DETAIL.rhythm but override its score with rhythmScore) so both
displayed score and status derive from the same variable (references:
rhythmScore, data, MOCK_DETAIL.rhythm, getRhythmStatus).
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,385 @@
+/**
+ * RhythmDetailScreen - Rhythm詳細画面
+ * Bedtime/Wake Consistency、Weekly Patternを含む
+ */
+
+import React from 'react';
+import { View, Text, ScrollView, Pressable } from 'react-native';
+import { SafeAreaView, useSafeAreaInsets } from 'react-native-safe-area-context';
+import { useRouter } from 'expo-router';
+import { ChevronLeft, Clock, Moon, Sun, Calendar, Plane } from 'lucide-react-native';
+import Animated, { FadeInDown } from 'react-native-reanimated';
+import Svg, { Rect, Line } from 'react-native-svg';
+
+import { CircularProgress } from '../../src/components';
+import { colors, FontFamily } from '../../src/theme';
+import { t } from '../../src/i18n';
+import { MOCK_DETAIL } from '../../src/constants/mockData';
+import { useHealthStore } from '../../src/stores/healthStore';
+
+// スコアに応じたステータスを取得
+const getRhythmStatus = (score: number): string => {
+  if (score >= 90) return t('score.rhythm.status.excellent');
+  if (score >= 75) return t('score.rhythm.status.good');
+  if (score >= 50) return t('score.rhythm.status.fair');
+  return t('score.rhythm.status.poor');
+};
+
+// Contributing Factor カードコンポーネント（詳細版）
+interface FactorCardProps {
+  icon: React.ElementType;
+  iconColor: string;
+  iconBg: string;
+  label: string;
+  value: number;
+  trend: string;
+  trendDirection: 'up' | 'down' | 'stable';
+  detail: string;
+}
+
+const FactorCard = ({
+  icon: Icon,
+  iconColor,
+  iconBg,
+  label,
+  value,
+  trend,
+  trendDirection,
+  detail,
+}: FactorCardProps): React.ReactElement => {
+  const getTrendColor = () => {
+    if (trendDirection === 'up') return colors.emerald[500];
+    if (trendDirection === 'down') return colors.rose[500];
+    return colors.stone[400];
+  };
+
+  return (
+    <View
+      className="bg-white p-4 rounded-2xl border border-stone-100"
+      style={{
+        shadowColor: '#000',
+        shadowOffset: { width: 0, height: 2 },
+        shadowOpacity: 0.04,
+        shadowRadius: 6,
+        elevation: 2,
+      }}
+    >
+      <View className="flex-row items-center justify-between mb-2">
+        <View className="flex-row items-center" style={{ gap: 10 }}>
+          <View className="p-2 rounded-xl" style={{ backgroundColor: iconBg }}>
+            <Icon size={16} color={iconColor} />
+          </View>
+          <Text className="text-xs font-bold text-stone-500 uppercase">{label}</Text>
+        </View>
+        <Text className="text-xs font-bold" style={{ color: getTrendColor() }}>
+          {trend}
+        </Text>
+      </View>
+      <Text className="text-2xl font-bold text-stone-900 mb-1">{value}%</Text>
+      <Text className="text-[10px] text-stone-400">{detail}</Text>
+    </View>
+  );
+};
+
+export default function RhythmDetailScreen(): React.ReactElement {
+  const router = useRouter();
+  const insets = useSafeAreaInsets();
+  const { dailySnapshot } = useHealthStore();
+  
+  // healthStoreから計算済みのスコアを取得
+  const rhythmScore = dailySnapshot?.scores?.rhythm ?? 0;
+  
+  const data = MOCK_DETAIL.rhythm;



================================================================================
ファイル: app/app/(main)/rhythm-detail.tsx
行: 186 (開始行: 183)
コミット: a35fee7
作成日時: 2026-01-07T07:52:33Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

**Hardcoded Japanese text should use i18n.**

Lines 184-185 contain hardcoded Japanese strings ("目標からのズレ", "上: 早い / 下: 遅い") while the rest of the file uses `t()` for localization. This inconsistency will cause issues if the app needs to support multiple languages.


<details>
<summary>🌐 Proposed fix</summary>

```diff
         <View className="mt-3 pt-3 border-t border-stone-50 flex-row justify-between items-center">
-          <Text className="text-xs text-stone-400">目標からのズレ</Text>
-          <Text className="text-xs text-stone-500">上: 早い / 下: 遅い</Text>
+          <Text className="text-xs text-stone-400">{t('detail.rhythm.deviationFromTarget')}</Text>
+          <Text className="text-xs text-stone-500">{t('detail.rhythm.deviationLegend')}</Text>
         </View>
```
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
        <View className="mt-3 pt-3 border-t border-stone-50 flex-row justify-between items-center">
          <Text className="text-xs text-stone-400">{t('detail.rhythm.deviationFromTarget')}</Text>
          <Text className="text-xs text-stone-500">{t('detail.rhythm.deviationLegend')}</Text>
        </View>
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(main)/rhythm-detail.tsx around lines 183 - 186, Replace the two
hardcoded Japanese Text nodes in the JSX View with localized strings using the
existing t() i18n helper (same pattern used elsewhere in this file); for
example, swap "目標からのズレ" and "上: 早い / 下: 遅い" for t('rhythm.deviationLabel') and
t('rhythm.deviationHint') (or other keys consistent with your i18n namespace),
ensuring you keep the Text components and styling unchanged and add the
corresponding entries to your locale resource files.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,385 @@
+/**
+ * RhythmDetailScreen - Rhythm詳細画面
+ * Bedtime/Wake Consistency、Weekly Patternを含む
+ */
+
+import React from 'react';
+import { View, Text, ScrollView, Pressable } from 'react-native';
+import { SafeAreaView, useSafeAreaInsets } from 'react-native-safe-area-context';
+import { useRouter } from 'expo-router';
+import { ChevronLeft, Clock, Moon, Sun, Calendar, Plane } from 'lucide-react-native';
+import Animated, { FadeInDown } from 'react-native-reanimated';
+import Svg, { Rect, Line } from 'react-native-svg';
+
+import { CircularProgress } from '../../src/components';
+import { colors, FontFamily } from '../../src/theme';
+import { t } from '../../src/i18n';
+import { MOCK_DETAIL } from '../../src/constants/mockData';
+import { useHealthStore } from '../../src/stores/healthStore';
+
+// スコアに応じたステータスを取得
+const getRhythmStatus = (score: number): string => {
+  if (score >= 90) return t('score.rhythm.status.excellent');
+  if (score >= 75) return t('score.rhythm.status.good');
+  if (score >= 50) return t('score.rhythm.status.fair');
+  return t('score.rhythm.status.poor');
+};
+
+// Contributing Factor カードコンポーネント（詳細版）
+interface FactorCardProps {
+  icon: React.ElementType;
+  iconColor: string;
+  iconBg: string;
+  label: string;
+  value: number;
+  trend: string;
+  trendDirection: 'up' | 'down' | 'stable';
+  detail: string;
+}
+
+const FactorCard = ({
+  icon: Icon,
+  iconColor,
+  iconBg,
+  label,
+  value,
+  trend,
+  trendDirection,
+  detail,
+}: FactorCardProps): React.ReactElement => {
+  const getTrendColor = () => {
+    if (trendDirection === 'up') return colors.emerald[500];
+    if (trendDirection === 'down') return colors.rose[500];
+    return colors.stone[400];
+  };
+
+  return (
+    <View
+      className="bg-white p-4 rounded-2xl border border-stone-100"
+      style={{
+        shadowColor: '#000',
+        shadowOffset: { width: 0, height: 2 },
+        shadowOpacity: 0.04,
+        shadowRadius: 6,
+        elevation: 2,
+      }}
+    >
+      <View className="flex-row items-center justify-between mb-2">
+        <View className="flex-row items-center" style={{ gap: 10 }}>
+          <View className="p-2 rounded-xl" style={{ backgroundColor: iconBg }}>
+            <Icon size={16} color={iconColor} />
+          </View>
+          <Text className="text-xs font-bold text-stone-500 uppercase">{label}</Text>
+        </View>
+        <Text className="text-xs font-bold" style={{ color: getTrendColor() }}>
+          {trend}
+        </Text>
+      </View>
+      <Text className="text-2xl font-bold text-stone-900 mb-1">{value}%</Text>
+      <Text className="text-[10px] text-stone-400">{detail}</Text>
+    </View>
+  );
+};
+
+export default function RhythmDetailScreen(): React.ReactElement {
+  const router = useRouter();
+  const insets = useSafeAreaInsets();
+  const { dailySnapshot } = useHealthStore();
+  
+  // healthStoreから計算済みのスコアを取得
+  const rhythmScore = dailySnapshot?.scores?.rhythm ?? 0;
+  
+  const data = MOCK_DETAIL.rhythm;
+
+  const handleBack = () => {
+    router.back();
+  };
+
+  // Weekly Pattern バーチャートをレンダリング
+  const renderWeeklyPattern = () => {
+    const pattern = data.weeklyPattern;
+    const maxOffset = Math.max(...pattern.map((p) => Math.abs(p.offset)));
+    const chartHeight = 120;
+    const centerY = chartHeight / 2;
+    const barWidth = 12;
+    const gap = 8;
+    const totalWidth = pattern.length * (barWidth + gap) - gap;
+    const startX = (300 - totalWidth) / 2;
+
+    return (
+      <View
+        className="bg-white p-5 rounded-3xl border border-stone-100"
+        style={{
+          shadowColor: '#000',
+          shadowOffset: { width: 0, height: 4 },
+          shadowOpacity: 0.06,
+          shadowRadius: 10,
+          elevation: 4,
+        }}
+      >
+        <Text className="text-xs font-bold text-stone-400 uppercase mb-4">
+          {t('detail.rhythm.weeklyPattern')}
+        </Text>
+        <View style={{ height: chartHeight, position: 'relative' }}>
+          <Svg width="100%" height={chartHeight} viewBox={`0 0 300 ${chartHeight}`}>
+            {/* Center line (target bedtime) */}
+            <Line
+              x1="0"
+              y1={centerY}
+              x2="300"
+              y2={centerY}
+              stroke={colors.stone[200]}
+              strokeWidth={1}
+              strokeDasharray="4,4"
+            />
+            {/* Bars */}
+            {pattern.map((item, index) => {
+              const x = startX + index * (barWidth + gap);
+              const barHeight = maxOffset > 0 ? (Math.abs(item.offset) / maxOffset) * 40 : 0;
+              const y = item.offset >= 0 ? centerY - barHeight : centerY;
+
+              return (
+                <Rect
+                  key={item.day}
+                  x={x}
+                  y={y}
+                  width={barWidth}
+                  height={Math.max(barHeight, 4)}
+                  rx={4}
+                  fill={colors.purple[500]}
+                  opacity={index === pattern.length - 1 ? 1 : 0.5}
+                />
+              );
+            })}
+          </Svg>
+          {/* Day labels */}
+          <View
+            style={{
+              position: 'absolute',
+              bottom: 0,
+              left: 0,
+              right: 0,
+              flexDirection: 'row',
+              justifyContent: 'space-around',
+              paddingHorizontal: 20,
+            }}
+          >
+            {pattern.map((item, index) => (
+              <Text
+                key={item.day}
+                style={{
+                  fontSize: 10,
+                  fontWeight: '500',
+                  color: index === pattern.length - 1 ? colors.purple[600] : colors.stone[400],
+                  width: barWidth + gap,
+                  textAlign: 'center',
+                }}
+              >
+                {item.day}
+              </Text>
+            ))}
+          </View>
+        </View>
+        <View className="mt-3 pt-3 border-t border-stone-50 flex-row justify-between items-center">
+          <Text className="text-xs text-stone-400">目標からのズレ</Text>
+          <Text className="text-xs text-stone-500">上: 早い / 下: 遅い</Text>
+        </View>



================================================================================
ファイル: app/app/(main)/settings.tsx
行: 58 (開始行: 44)
コミット: a35fee7
作成日時: 2026-01-07T07:52:33Z
================================================================================

【コメント】
_🛠️ Refactor suggestion_ | _🟠 Major_

**Duplicate `useFadeIn` hook - third instance in codebase.**

This is the same `useFadeIn` hook duplicated in `action-detail.tsx` and defined in `app/src/hooks/useFadeIn.ts`. This violates DRY principles. Consider consolidating to a single reanimated-based implementation in the hooks folder.


<details>
<summary>♻️ Proposed fix</summary>

Import from the shared hooks location instead:

```diff
-// Fade-in animation hook
-const useFadeIn = (delay: number = 0) => {
-  const opacity = useSharedValue(0);
-  const translateY = useSharedValue(10);
-
-  useEffect(() => {
-    opacity.value = withDelay(delay, withTiming(1, { duration: 600 }));
-    translateY.value = withDelay(delay, withTiming(0, { duration: 600 }));
-  }, [opacity, translateY, delay]);
-
-  return useAnimatedStyle(() => ({
-    opacity: opacity.value,
-    transform: [{ translateY: translateY.value }],
-  }));
-};
+import { useFadeIn } from '../../src/hooks';
```

Note: The shared hook may need to be updated to use react-native-reanimated to match this implementation.
</details>


> Committable suggestion skipped: line range outside the PR's diff.

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(main)/settings.tsx around lines 44 - 58, The local useFadeIn hook
is a duplicate of an existing shared hook; remove this local definition and
import the canonical useFadeIn from the shared hooks module instead, and update
the shared hook to use the same reanimated APIs (useSharedValue,
useAnimatedStyle, withDelay, withTiming) if it doesn’t already so behavior
remains identical to the current implementation.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -1,300 +1,364 @@
-import React from 'react';
-import {
-  View,
-  Text,
-  StyleSheet,
-  SafeAreaView,
-  ScrollView,
-  TouchableOpacity,
-} from 'react-native';
+/**
+ * SettingsScreen - 設定画面
+ * sozai/new/tempoai/screens/SettingsScreen.tsx を React Native で完全再現
+ */
+
+import React, { useState, useEffect } from 'react';
+import { View, Text, ScrollView, Pressable } from 'react-native';
+import { SafeAreaView, useSafeAreaInsets } from 'react-native-safe-area-context';
+import { LinearGradient } from 'expo-linear-gradient';
 import {
+  Moon,
+  Sun,
+  Bell,
+  Zap,
   Heart,
-  MapPin,
-  Shield,
-  FileText,
-  MessageCircle,
   ChevronRight,
-  Moon,
+  HelpCircle,
+  Shield,
+  LogOut,
+  Smartphone,
+  LucideIcon,
 } from 'lucide-react-native';
-import { Colors, Spacing, Typography } from '../../src/theme';
-import { Card } from '../../src/components';
-import { getChronotypeLabel, getGenderLabel } from '../../src/domain/models';
-import { useUserStore } from '../../src/stores';
-import type { JSX } from 'react';
+import Animated, {
+  useSharedValue,
+  useAnimatedStyle,
+  withSpring,
+  withDelay,
+  withTiming,
+} from 'react-native-reanimated';
 
-export default function SettingsScreen(): JSX.Element {
-  const profile = useUserStore((state) => state.profile);
+import { TAB_BAR_HEIGHT } from './_layout';
+import { t } from '../../src/i18n';
+import { FontFamily } from '../../src/theme';
 
-  // Default values if profile is not loaded
-  const nickname = profile?.nickname || 'ユーザー';
-  const age = profile?.age || 30;
-  const gender = profile?.gender || 'other';
-  const chronotype = profile?.chronotype || 'intermediate';
-  const targetBedtime = profile?.targetBedtime || '23:00';
+// 日本語化されたMOCKデータ
+const MOCK_DATA = {
+  nickname: '田中 太郎',
+  plan: 'フリープラン',
+  memberSince: '2024',
+  targetBedtime: '22:30',
+  targetWakeUp: '6:30',
+};
 
-  return (
-    <SafeAreaView style={styles.container}>
-      <ScrollView
-        style={styles.scrollView}
-        contentContainerStyle={styles.scrollContent}
-        showsVerticalScrollIndicator={false}
-      >
-        {/* Header */}
-        <View style={styles.header}>
-          <Text style={styles.title}>設定</Text>
-        </View>
+// Fade-in animation hook
+const useFadeIn = (delay: number = 0) => {
+  const opacity = useSharedValue(0);
+  const translateY = useSharedValue(10);
 
-        {/* Profile Section */}
-        <View style={styles.section}>
-          <Text style={styles.sectionTitle}>プロフィール</Text>
-          <Card style={styles.profileCard}>
-            <View style={styles.profileHeader}>
-              <View style={styles.avatar}>
-                <Text style={styles.avatarText}>{nickname.charAt(0)}</Text>
-              </View>
-              <View style={styles.profileInfo}>
-                <Text style={styles.profileName}>{nickname}</Text>
-                <Text style={styles.profileMeta}>
-                  {age}歳 · {getGenderLabel(gender)}
-                </Text>
-              </View>
-              <ChevronRight size={20} color={Colors.slate[400]} />
-            </View>
+  useEffect(() => {
+    opacity.value = withDelay(delay, withTiming(1, { duration: 600 }));
+    translateY.value = withDelay(delay, withTiming(0, { duration: 600 }));
+  }, [opacity, translateY, delay]);
 
-            <View style={styles.profileDetails}>
-              <ProfileRow
-                icon={<Moon size={18} color={Colors.indigo[500]} />}
-                label="クロノタイプ"
-                value={getChronotypeLabel(chronotype)}
-              />
-              <ProfileRow
-                icon={<Moon size={18} color={Colors.slate[500]} />}
-                label="目標就寝時刻"
-                value={targetBedtime}
-              />
-            </View>
-          </Card>
-        </View>
+  return useAnimatedStyle(() => ({
+    opacity: opacity.value,
+    transform: [{ translateY: translateY.value }],
+  }));
+};



================================================================================
ファイル: app/app/(main)/settings.tsx
行: 256 (開始行: 252)
コミット: a35fee7
作成日時: 2026-01-07T07:52:33Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

**Profile card Pressable has no onPress handler.**

The `Pressable` wrapping the chevron icon (Line 253-255) has no `onPress` handler, making it a non-interactive element styled as a button. Either add navigation to a profile edit screen or replace with a `View`.


<details>
<summary>🐛 Proposed fix</summary>

If profile editing is not implemented yet:
```diff
-              <Pressable className="p-2 rounded-full">
+              <View className="p-2 rounded-full">
                 <ChevronRight size={20} color="#A8A29E" />
-              </Pressable>
+              </View>
```

Or add TODO and onPress:
```diff
               <Pressable 
                 className="p-2 rounded-full"
+                onPress={() => { /* TODO: Navigate to profile edit */ }}
               >
```
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
              </View>
              <View className="p-2 rounded-full">
                <ChevronRight size={20} color="#A8A29E" />
              </View>
            </View>
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(main)/settings.tsx around lines 252 - 256, The Pressable wrapping
the ChevronRight icon (the Pressable element containing <ChevronRight />) has no
onPress handler and appears as a button but is not interactive; either add an
onPress that navigates to the profile edit screen (use the existing navigation
method used in this file, e.g., call navigate('ProfileEdit') or the appropriate
route) or replace that Pressable with a non-interactive View if no navigation
exists yet; if you add a placeholder handler, include a TODO comment inside the
handler to implement the real edit flow later and keep accessibility by adding
an accessible/aria label or hitSlop as appropriate.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -1,300 +1,364 @@
-import React from 'react';
-import {
-  View,
-  Text,
-  StyleSheet,
-  SafeAreaView,
-  ScrollView,
-  TouchableOpacity,
-} from 'react-native';
+/**
+ * SettingsScreen - 設定画面
+ * sozai/new/tempoai/screens/SettingsScreen.tsx を React Native で完全再現
+ */
+
+import React, { useState, useEffect } from 'react';
+import { View, Text, ScrollView, Pressable } from 'react-native';
+import { SafeAreaView, useSafeAreaInsets } from 'react-native-safe-area-context';
+import { LinearGradient } from 'expo-linear-gradient';
 import {
+  Moon,
+  Sun,
+  Bell,
+  Zap,
   Heart,
-  MapPin,
-  Shield,
-  FileText,
-  MessageCircle,
   ChevronRight,
-  Moon,
+  HelpCircle,
+  Shield,
+  LogOut,
+  Smartphone,
+  LucideIcon,
 } from 'lucide-react-native';
-import { Colors, Spacing, Typography } from '../../src/theme';
-import { Card } from '../../src/components';
-import { getChronotypeLabel, getGenderLabel } from '../../src/domain/models';
-import { useUserStore } from '../../src/stores';
-import type { JSX } from 'react';
+import Animated, {
+  useSharedValue,
+  useAnimatedStyle,
+  withSpring,
+  withDelay,
+  withTiming,
+} from 'react-native-reanimated';
 
-export default function SettingsScreen(): JSX.Element {
-  const profile = useUserStore((state) => state.profile);
+import { TAB_BAR_HEIGHT } from './_layout';
+import { t } from '../../src/i18n';
+import { FontFamily } from '../../src/theme';
 
-  // Default values if profile is not loaded
-  const nickname = profile?.nickname || 'ユーザー';
-  const age = profile?.age || 30;
-  const gender = profile?.gender || 'other';
-  const chronotype = profile?.chronotype || 'intermediate';
-  const targetBedtime = profile?.targetBedtime || '23:00';
+// 日本語化されたMOCKデータ
+const MOCK_DATA = {
+  nickname: '田中 太郎',
+  plan: 'フリープラン',
+  memberSince: '2024',
+  targetBedtime: '22:30',
+  targetWakeUp: '6:30',
+};
 
-  return (
-    <SafeAreaView style={styles.container}>
-      <ScrollView
-        style={styles.scrollView}
-        contentContainerStyle={styles.scrollContent}
-        showsVerticalScrollIndicator={false}
-      >
-        {/* Header */}
-        <View style={styles.header}>
-          <Text style={styles.title}>設定</Text>
-        </View>
+// Fade-in animation hook
+const useFadeIn = (delay: number = 0) => {
+  const opacity = useSharedValue(0);
+  const translateY = useSharedValue(10);
 
-        {/* Profile Section */}
-        <View style={styles.section}>
-          <Text style={styles.sectionTitle}>プロフィール</Text>
-          <Card style={styles.profileCard}>
-            <View style={styles.profileHeader}>
-              <View style={styles.avatar}>
-                <Text style={styles.avatarText}>{nickname.charAt(0)}</Text>
-              </View>
-              <View style={styles.profileInfo}>
-                <Text style={styles.profileName}>{nickname}</Text>
-                <Text style={styles.profileMeta}>
-                  {age}歳 · {getGenderLabel(gender)}
-                </Text>
-              </View>
-              <ChevronRight size={20} color={Colors.slate[400]} />
-            </View>
+  useEffect(() => {
+    opacity.value = withDelay(delay, withTiming(1, { duration: 600 }));
+    translateY.value = withDelay(delay, withTiming(0, { duration: 600 }));
+  }, [opacity, translateY, delay]);
 
-            <View style={styles.profileDetails}>
-              <ProfileRow
-                icon={<Moon size={18} color={Colors.indigo[500]} />}
-                label="クロノタイプ"
-                value={getChronotypeLabel(chronotype)}
-              />
-              <ProfileRow
-                icon={<Moon size={18} color={Colors.slate[500]} />}
-                label="目標就寝時刻"
-                value={targetBedtime}
-              />
-            </View>
-          </Card>
-        </View>
+  return useAnimatedStyle(() => ({
+    opacity: opacity.value,
+    transform: [{ translateY: translateY.value }],
+  }));
+};
 
-        {/* Data Section */}
-        <View style={styles.section}>
-          <Text style={styles.sectionTitle}>データ</Text>
-          <Card style={styles.menuCard}>
-            <MenuItem
-              icon={<Heart size={20} color={Colors.rose[500]} />}
-              label="ヘルスケア連携"
-              value="未確認"
-              valueColor={Colors.primary[600]}
-              onPress={() => {
-                // TODO: Implement
-              }}
-            />
-            <MenuItem
-              icon={<MapPin size={20} color={Colors.blue[500]} />}
-              label="位置情報"
-              value="許可済み"
-              valueColor={Colors.primary[600]}
-              onPress={() => {
-                // TODO: Implement
-              }}
-            />
-          </Card>
-        </View>
+// Toggle Switch Component
+const ToggleSwitch: React.FC<{
+  value: boolean;
+  onValueChange: (value: boolean) => void;
+}> = ({ value, onValueChange }) => {
+  const translateX = useSharedValue(value ? 20 : 0);
 
-        {/* App Section */}
-        <View style={styles.section}>
-          <Text style={styles.sectionTitle}>アプリについて</Text>
-          <Card style={styles.menuCard}>
-            <MenuItem
-              icon={<Shield size={20} color={Colors.slate[500]} />}
-              label="プライバシーポリシー"
-              onPress={() => {
-                // TODO: Implement
-              }}
-            />
-            <MenuItem
-              icon={<FileText size={20} color={Colors.slate[500]} />}
-              label="利用規約"
-              onPress={() => {
-                // TODO: Implement
-              }}
-            />
-            <MenuItem
-              icon={<MessageCircle size={20} color={Colors.slate[500]} />}
-              label="お問い合わせ"
-              onPress={() => {
-                // TODO: Implement
-              }}
-            />
-          </Card>
-        </View>
+  useEffect(() => {
+    translateX.value = withSpring(value ? 20 : 0, { damping: 15, stiffness: 120 });
+  }, [value, translateX]);
+
+  const thumbStyle = useAnimatedStyle(() => ({
+    transform: [{ translateX: translateX.value }],
+  }));
 
-        {/* Version */}
-        <View style={styles.versionContainer}>
-          <Text style={styles.versionText}>TempoAI v1.0.0</Text>
-        </View>
-      </ScrollView>
-    </SafeAreaView>
+  return (
+    <Pressable
+      onPress={() => onValueChange(!value)}
+      className="w-12 h-7 rounded-full p-1 justify-center"
+      style={{ backgroundColor: value ? '#6366F1' : '#E7E5E4' }}
+    >
+      <Animated.View
+        style={[
+          thumbStyle,
+          {
+            width: 20,
+            height: 20,
+            borderRadius: 10,
+            backgroundColor: '#FFFFFF',
+            shadowColor: '#000',
+            shadowOffset: { width: 0, height: 2 },
+            shadowOpacity: 0.15,
+            shadowRadius: 4,
+            elevation: 3,
+          },
+        ]}
+      />
+    </Pressable>
   );
-}
+};
 
-const ProfileRow: React.FC<{
-  icon: React.ReactNode;
-  label: string;
-  value: string;
-}> = ({ icon, label, value }): JSX.Element => (
-  <View style={styles.profileRow}>
-    {icon}
-    <Text style={styles.profileRowLabel}>{label}</Text>
-    <Text style={styles.profileRowValue}>{value}</Text>
+// Section Component
+const Section: React.FC<{ title: string; children: React.ReactNode }> = ({ title, children }) => (
+  <View className="mb-8">
+    <Text className="text-xs font-bold text-stone-400 uppercase tracking-widest mb-3 ml-2">
+      {title}
+    </Text>
+    <View
+      className="bg-white rounded-2xl border border-stone-100 p-2"
+      style={{
+        shadowColor: '#000',
+        shadowOffset: { width: 0, height: 4 },
+        shadowOpacity: 0.06,
+        shadowRadius: 20,
+        elevation: 4,
+      }}
+    >
+      {children}
+    </View>
   </View>
 );
 
-const MenuItem: React.FC<{
-  icon: React.ReactNode;
+// Settings Row Component
+interface SettingsRowProps {
+  icon: LucideIcon;
+  iconColor: string;
+  iconBg: string;
   label: string;
   value?: string;
   valueColor?: string;
-  onPress: () => void;
-}> = ({ icon, label, value, valueColor, onPress }): JSX.Element => (
-  <TouchableOpacity style={styles.menuItem} onPress={onPress}>
-    <View style={styles.menuItemLeft}>
-      {icon}
-      <Text style={styles.menuItemLabel}>{label}</Text>
+  showChevron?: boolean;
+  onPress?: () => void;
+}
+
+const SettingsRow: React.FC<SettingsRowProps> = ({
+  icon: Icon,
+  iconColor,
+  iconBg,
+  label,
+  value,
+  valueColor = '#A8A29E',
+  showChevron = true,
+  onPress,
+}) => (
+  <Pressable
+    onPress={onPress}
+    className="flex-row items-center justify-between p-3 rounded-xl"
+    style={({ pressed }) => ({ opacity: pressed && onPress ? 0.7 : 1 })}
+  >
+    <View className="flex-row items-center" style={{ gap: 12 }}>
+      <View className="p-2 rounded-xl" style={{ backgroundColor: iconBg }}>
+        <Icon size={18} color={iconColor} />
+      </View>
+      <Text className="text-sm font-medium text-stone-700">{label}</Text>
     </View>
-    <View style={styles.menuItemRight}>
+    <View className="flex-row items-center" style={{ gap: 8 }}>
       {value && (
-        <Text style={[styles.menuItemValue, valueColor && { color: valueColor }]}>
+        <Text className="text-sm" style={{ color: valueColor }}>
           {value}
         </Text>
       )}
-      <ChevronRight size={20} color={Colors.slate[300]} />
+      {showChevron && <ChevronRight size={20} color="#D6D3D1" />}
+    </View>
+  </Pressable>
+);
+
+// Toggle Row Component
+interface ToggleRowProps {
+  icon: LucideIcon;
+  iconColor: string;
+  iconBg: string;
+  label: string;
+  value: boolean;
+  onValueChange: (value: boolean) => void;
+}
+
+const ToggleRow: React.FC<ToggleRowProps> = ({
+  icon: Icon,
+  iconColor,
+  iconBg,
+  label,
+  value,
+  onValueChange,
+}) => (
+  <View className="flex-row items-center justify-between p-3">
+    <View className="flex-row items-center" style={{ gap: 12 }}>
+      <View className="p-2 rounded-xl" style={{ backgroundColor: iconBg }}>
+        <Icon size={18} color={iconColor} />
+      </View>
+      <Text className="text-sm font-medium text-stone-700">{label}</Text>
     </View>
-  </TouchableOpacity>
+    <ToggleSwitch value={value} onValueChange={onValueChange} />
+  </View>
 );
 
-const styles = StyleSheet.create({
-  container: {
-    flex: 1,
-    backgroundColor: Colors.slate[50],
-  },
-  scrollView: {
-    flex: 1,
-  },
-  scrollContent: {
-    paddingHorizontal: Spacing.lg,
-    paddingTop: Spacing.lg,
-    paddingBottom: Spacing.huge,
-  },
-  header: {
-    marginBottom: Spacing.lg,
-  },
-  title: {
-    ...Typography.h3,
-    color: Colors.slate[800],
-  },
-  section: {
-    marginBottom: Spacing.xl,
-  },
-  sectionTitle: {
-    ...Typography.caption,
-    color: Colors.slate[500],
-    marginBottom: Spacing.sm,
-    marginLeft: Spacing.xs,
-    textTransform: 'uppercase',
-    letterSpacing: 1,
-  },
-  profileCard: {
-    padding: 0,
-    overflow: 'hidden',
-  },
-  profileHeader: {
-    flexDirection: 'row',
-    alignItems: 'center',
-    padding: Spacing.lg,
-    borderBottomWidth: 1,
-    borderBottomColor: Colors.slate[100],
-  },
-  avatar: {
-    width: 56,
-    height: 56,
-    borderRadius: 28,
-    backgroundColor: Colors.primary[100],
-    justifyContent: 'center',
-    alignItems: 'center',
-    marginRight: Spacing.md,
-  },
-  avatarText: {
-    ...Typography.h4,
-    color: Colors.primary[600],
-  },
-  profileInfo: {
-    flex: 1,
-  },
-  profileName: {
-    ...Typography.h5,
-    color: Colors.slate[800],
-    marginBottom: 2,
-  },
-  profileMeta: {
-    ...Typography.bodySmall,
-    color: Colors.slate[500],
-  },
-  profileDetails: {
-    padding: Spacing.lg,
-    gap: Spacing.md,
-  },
-  profileRow: {
-    flexDirection: 'row',
-    alignItems: 'center',
-  },
-  profileRowLabel: {
-    ...Typography.bodySmall,
-    color: Colors.slate[500],
-    marginLeft: Spacing.sm,
-    flex: 1,
-  },
-  profileRowValue: {
-    ...Typography.bodySmall,
-    color: Colors.slate[700],
-    fontWeight: '500',
-  },
-  menuCard: {
-    padding: 0,
-    overflow: 'hidden',
-  },
-  menuItem: {
-    flexDirection: 'row',
-    alignItems: 'center',
-    justifyContent: 'space-between',
-    padding: Spacing.lg,
-    borderBottomWidth: 1,
-    borderBottomColor: Colors.slate[50],
-  },
-  menuItemLeft: {
-    flexDirection: 'row',
-    alignItems: 'center',
-  },
-  menuItemLabel: {
-    ...Typography.body,
-    color: Colors.slate[700],
-    marginLeft: Spacing.md,
-  },
-  menuItemRight: {
-    flexDirection: 'row',
-    alignItems: 'center',
-    gap: Spacing.sm,
-  },
-  menuItemValue: {
-    ...Typography.bodySmall,
-    color: Colors.slate[500],
-  },
-  versionContainer: {
-    alignItems: 'center',
-    paddingVertical: Spacing.xl,
-  },
-  versionText: {
-    ...Typography.caption,
-    color: Colors.slate[400],
-  },
-});
+export default function SettingsScreen(): React.ReactElement {
+  const insets = useSafeAreaInsets();
+  const [notifications, setNotifications] = useState(true);
+  const [haptic, setHaptic] = useState(true);
+
+  // Fade-in animations
+  const headerFadeIn = useFadeIn(0);
+  const contentFadeIn = useFadeIn(100);
+
+  return (
+    <View className="flex-1 bg-stone-100">
+      <SafeAreaView className="flex-1" edges={['top']}>
+        <ScrollView
+          contentContainerStyle={{
+            paddingHorizontal: 24,
+            paddingBottom: TAB_BAR_HEIGHT + insets.bottom + 24,
+          }}
+          showsVerticalScrollIndicator={false}
+        >
+          {/* Header */}
+          <Animated.View style={headerFadeIn} className="pt-14 mb-6">
+            <Text
+              className="text-3xl text-stone-900 tracking-tight mb-1"
+              style={{ fontFamily: FontFamily.serif }}
+            >
+              {t('screen.settings.title')}
+            </Text>
+            <Text className="text-sm text-stone-500">{t('screen.settings.subtitle')}</Text>
+          </Animated.View>
+
+          <Animated.View style={contentFadeIn}>
+            {/* Profile Card */}
+            <View
+              className="bg-white p-4 rounded-3xl border border-stone-100 flex-row items-center mb-8"
+              style={{
+                gap: 16,
+                shadowColor: '#000',
+                shadowOffset: { width: 0, height: 4 },
+                shadowOpacity: 0.06,
+                shadowRadius: 20,
+                elevation: 4,
+              }}
+            >
+              <LinearGradient
+                colors={['#E0E7FF', '#FFE4E6']}
+                start={{ x: 0, y: 0 }}
+                end={{ x: 1, y: 1 }}
+                className="items-center justify-center"
+                style={{ width: 64, height: 64, borderRadius: 32 }}
+              >
+                <Text style={{ fontSize: 28 }}>🧘</Text>
+              </LinearGradient>
+              <View className="flex-1">
+                <Text className="text-lg font-bold text-stone-900">{MOCK_DATA.nickname}</Text>
+                <Text className="text-xs text-stone-500">
+                  {MOCK_DATA.plan} • {MOCK_DATA.memberSince}年から利用
+                </Text>
+              </View>
+              <Pressable className="p-2 rounded-full">
+                <ChevronRight size={20} color="#A8A29E" />
+              </Pressable>
+            </View>



================================================================================
ファイル: app/app/(main)/settings.tsx
行: 351 (開始行: 341)
コミット: a35fee7
作成日時: 2026-01-07T07:52:33Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟠 Major_

**Sign Out button has no onPress handler.**

The Sign Out `Pressable` (Lines 342-351) has no `onPress` handler. This is a critical user action that should either be implemented or the button should be disabled/hidden until ready.


<details>
<summary>🐛 Proposed fix</summary>

```diff
             <Pressable
+              onPress={() => {
+                // TODO: Implement sign out logic
+                // Clear stores, navigate to onboarding/login
+              }}
               className="flex-row items-center justify-center py-4 rounded-2xl border border-rose-100 mb-8"
```
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
            {/* Sign Out Button */}
            <Pressable
              onPress={() => {
                // TODO: Implement sign out logic - Clear stores, navigate to onboarding/login
              }}
              className="flex-row items-center justify-center py-4 rounded-2xl border border-rose-100 mb-8"
              style={{
                gap: 8,
                backgroundColor: 'rgba(255, 241, 242, 0.5)',
              }}
            >
              <LogOut size={16} color="#E11D48" />
              <Text className="text-sm font-medium text-rose-600">{t('screen.settings.resetSignOut')}</Text>
            </Pressable>
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(main)/settings.tsx around lines 341 - 351, The Sign Out Pressable
lacks an onPress handler so the action is non-functional; add an onPress prop
that calls a sign-out handler (e.g., create or wire up a handleSignOut function
or existing auth.signOut method) on the Pressable containing the LogOut icon and
t('screen.settings.resetSignOut') Text, and ensure the handler manages state
(disable the Pressable or show loading to prevent double-tap) and handles
errors/ navigation after sign-out; alternatively hide/disable the Pressable
until a proper handler is available.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -1,300 +1,364 @@
-import React from 'react';
-import {
-  View,
-  Text,
-  StyleSheet,
-  SafeAreaView,
-  ScrollView,
-  TouchableOpacity,
-} from 'react-native';
+/**
+ * SettingsScreen - 設定画面
+ * sozai/new/tempoai/screens/SettingsScreen.tsx を React Native で完全再現
+ */
+
+import React, { useState, useEffect } from 'react';
+import { View, Text, ScrollView, Pressable } from 'react-native';
+import { SafeAreaView, useSafeAreaInsets } from 'react-native-safe-area-context';
+import { LinearGradient } from 'expo-linear-gradient';
 import {
+  Moon,
+  Sun,
+  Bell,
+  Zap,
   Heart,
-  MapPin,
-  Shield,
-  FileText,
-  MessageCircle,
   ChevronRight,
-  Moon,
+  HelpCircle,
+  Shield,
+  LogOut,
+  Smartphone,
+  LucideIcon,
 } from 'lucide-react-native';
-import { Colors, Spacing, Typography } from '../../src/theme';
-import { Card } from '../../src/components';
-import { getChronotypeLabel, getGenderLabel } from '../../src/domain/models';
-import { useUserStore } from '../../src/stores';
-import type { JSX } from 'react';
+import Animated, {
+  useSharedValue,
+  useAnimatedStyle,
+  withSpring,
+  withDelay,
+  withTiming,
+} from 'react-native-reanimated';
 
-export default function SettingsScreen(): JSX.Element {
-  const profile = useUserStore((state) => state.profile);
+import { TAB_BAR_HEIGHT } from './_layout';
+import { t } from '../../src/i18n';
+import { FontFamily } from '../../src/theme';
 
-  // Default values if profile is not loaded
-  const nickname = profile?.nickname || 'ユーザー';
-  const age = profile?.age || 30;
-  const gender = profile?.gender || 'other';
-  const chronotype = profile?.chronotype || 'intermediate';
-  const targetBedtime = profile?.targetBedtime || '23:00';
+// 日本語化されたMOCKデータ
+const MOCK_DATA = {
+  nickname: '田中 太郎',
+  plan: 'フリープラン',
+  memberSince: '2024',
+  targetBedtime: '22:30',
+  targetWakeUp: '6:30',
+};
 
-  return (
-    <SafeAreaView style={styles.container}>
-      <ScrollView
-        style={styles.scrollView}
-        contentContainerStyle={styles.scrollContent}
-        showsVerticalScrollIndicator={false}
-      >
-        {/* Header */}
-        <View style={styles.header}>
-          <Text style={styles.title}>設定</Text>
-        </View>
+// Fade-in animation hook
+const useFadeIn = (delay: number = 0) => {
+  const opacity = useSharedValue(0);
+  const translateY = useSharedValue(10);
 
-        {/* Profile Section */}
-        <View style={styles.section}>
-          <Text style={styles.sectionTitle}>プロフィール</Text>
-          <Card style={styles.profileCard}>
-            <View style={styles.profileHeader}>
-              <View style={styles.avatar}>
-                <Text style={styles.avatarText}>{nickname.charAt(0)}</Text>
-              </View>
-              <View style={styles.profileInfo}>
-                <Text style={styles.profileName}>{nickname}</Text>
-                <Text style={styles.profileMeta}>
-                  {age}歳 · {getGenderLabel(gender)}
-                </Text>
-              </View>
-              <ChevronRight size={20} color={Colors.slate[400]} />
-            </View>
+  useEffect(() => {
+    opacity.value = withDelay(delay, withTiming(1, { duration: 600 }));
+    translateY.value = withDelay(delay, withTiming(0, { duration: 600 }));
+  }, [opacity, translateY, delay]);
 
-            <View style={styles.profileDetails}>
-              <ProfileRow
-                icon={<Moon size={18} color={Colors.indigo[500]} />}
-                label="クロノタイプ"
-                value={getChronotypeLabel(chronotype)}
-              />
-              <ProfileRow
-                icon={<Moon size={18} color={Colors.slate[500]} />}
-                label="目標就寝時刻"
-                value={targetBedtime}
-              />
-            </View>
-          </Card>
-        </View>
+  return useAnimatedStyle(() => ({
+    opacity: opacity.value,
+    transform: [{ translateY: translateY.value }],
+  }));
+};
 
-        {/* Data Section */}
-        <View style={styles.section}>
-          <Text style={styles.sectionTitle}>データ</Text>
-          <Card style={styles.menuCard}>
-            <MenuItem
-              icon={<Heart size={20} color={Colors.rose[500]} />}
-              label="ヘルスケア連携"
-              value="未確認"
-              valueColor={Colors.primary[600]}
-              onPress={() => {
-                // TODO: Implement
-              }}
-            />
-            <MenuItem
-              icon={<MapPin size={20} color={Colors.blue[500]} />}
-              label="位置情報"
-              value="許可済み"
-              valueColor={Colors.primary[600]}
-              onPress={() => {
-                // TODO: Implement
-              }}
-            />
-          </Card>
-        </View>
+// Toggle Switch Component
+const ToggleSwitch: React.FC<{
+  value: boolean;
+  onValueChange: (value: boolean) => void;
+}> = ({ value, onValueChange }) => {
+  const translateX = useSharedValue(value ? 20 : 0);
 
-        {/* App Section */}
-        <View style={styles.section}>
-          <Text style={styles.sectionTitle}>アプリについて</Text>
-          <Card style={styles.menuCard}>
-            <MenuItem
-              icon={<Shield size={20} color={Colors.slate[500]} />}
-              label="プライバシーポリシー"
-              onPress={() => {
-                // TODO: Implement
-              }}
-            />
-            <MenuItem
-              icon={<FileText size={20} color={Colors.slate[500]} />}
-              label="利用規約"
-              onPress={() => {
-                // TODO: Implement
-              }}
-            />
-            <MenuItem
-              icon={<MessageCircle size={20} color={Colors.slate[500]} />}
-              label="お問い合わせ"
-              onPress={() => {
-                // TODO: Implement
-              }}
-            />
-          </Card>
-        </View>
+  useEffect(() => {
+    translateX.value = withSpring(value ? 20 : 0, { damping: 15, stiffness: 120 });
+  }, [value, translateX]);
+
+  const thumbStyle = useAnimatedStyle(() => ({
+    transform: [{ translateX: translateX.value }],
+  }));
 
-        {/* Version */}
-        <View style={styles.versionContainer}>
-          <Text style={styles.versionText}>TempoAI v1.0.0</Text>
-        </View>
-      </ScrollView>
-    </SafeAreaView>
+  return (
+    <Pressable
+      onPress={() => onValueChange(!value)}
+      className="w-12 h-7 rounded-full p-1 justify-center"
+      style={{ backgroundColor: value ? '#6366F1' : '#E7E5E4' }}
+    >
+      <Animated.View
+        style={[
+          thumbStyle,
+          {
+            width: 20,
+            height: 20,
+            borderRadius: 10,
+            backgroundColor: '#FFFFFF',
+            shadowColor: '#000',
+            shadowOffset: { width: 0, height: 2 },
+            shadowOpacity: 0.15,
+            shadowRadius: 4,
+            elevation: 3,
+          },
+        ]}
+      />
+    </Pressable>
   );
-}
+};
 
-const ProfileRow: React.FC<{
-  icon: React.ReactNode;
-  label: string;
-  value: string;
-}> = ({ icon, label, value }): JSX.Element => (
-  <View style={styles.profileRow}>
-    {icon}
-    <Text style={styles.profileRowLabel}>{label}</Text>
-    <Text style={styles.profileRowValue}>{value}</Text>
+// Section Component
+const Section: React.FC<{ title: string; children: React.ReactNode }> = ({ title, children }) => (
+  <View className="mb-8">
+    <Text className="text-xs font-bold text-stone-400 uppercase tracking-widest mb-3 ml-2">
+      {title}
+    </Text>
+    <View
+      className="bg-white rounded-2xl border border-stone-100 p-2"
+      style={{
+        shadowColor: '#000',
+        shadowOffset: { width: 0, height: 4 },
+        shadowOpacity: 0.06,
+        shadowRadius: 20,
+        elevation: 4,
+      }}
+    >
+      {children}
+    </View>
   </View>
 );
 
-const MenuItem: React.FC<{
-  icon: React.ReactNode;
+// Settings Row Component
+interface SettingsRowProps {
+  icon: LucideIcon;
+  iconColor: string;
+  iconBg: string;
   label: string;
   value?: string;
   valueColor?: string;
-  onPress: () => void;
-}> = ({ icon, label, value, valueColor, onPress }): JSX.Element => (
-  <TouchableOpacity style={styles.menuItem} onPress={onPress}>
-    <View style={styles.menuItemLeft}>
-      {icon}
-      <Text style={styles.menuItemLabel}>{label}</Text>
+  showChevron?: boolean;
+  onPress?: () => void;
+}
+
+const SettingsRow: React.FC<SettingsRowProps> = ({
+  icon: Icon,
+  iconColor,
+  iconBg,
+  label,
+  value,
+  valueColor = '#A8A29E',
+  showChevron = true,
+  onPress,
+}) => (
+  <Pressable
+    onPress={onPress}
+    className="flex-row items-center justify-between p-3 rounded-xl"
+    style={({ pressed }) => ({ opacity: pressed && onPress ? 0.7 : 1 })}
+  >
+    <View className="flex-row items-center" style={{ gap: 12 }}>
+      <View className="p-2 rounded-xl" style={{ backgroundColor: iconBg }}>
+        <Icon size={18} color={iconColor} />
+      </View>
+      <Text className="text-sm font-medium text-stone-700">{label}</Text>
     </View>
-    <View style={styles.menuItemRight}>
+    <View className="flex-row items-center" style={{ gap: 8 }}>
       {value && (
-        <Text style={[styles.menuItemValue, valueColor && { color: valueColor }]}>
+        <Text className="text-sm" style={{ color: valueColor }}>
           {value}
         </Text>
       )}
-      <ChevronRight size={20} color={Colors.slate[300]} />
+      {showChevron && <ChevronRight size={20} color="#D6D3D1" />}
+    </View>
+  </Pressable>
+);
+
+// Toggle Row Component
+interface ToggleRowProps {
+  icon: LucideIcon;
+  iconColor: string;
+  iconBg: string;
+  label: string;
+  value: boolean;
+  onValueChange: (value: boolean) => void;
+}
+
+const ToggleRow: React.FC<ToggleRowProps> = ({
+  icon: Icon,
+  iconColor,
+  iconBg,
+  label,
+  value,
+  onValueChange,
+}) => (
+  <View className="flex-row items-center justify-between p-3">
+    <View className="flex-row items-center" style={{ gap: 12 }}>
+      <View className="p-2 rounded-xl" style={{ backgroundColor: iconBg }}>
+        <Icon size={18} color={iconColor} />
+      </View>
+      <Text className="text-sm font-medium text-stone-700">{label}</Text>
     </View>
-  </TouchableOpacity>
+    <ToggleSwitch value={value} onValueChange={onValueChange} />
+  </View>
 );
 
-const styles = StyleSheet.create({
-  container: {
-    flex: 1,
-    backgroundColor: Colors.slate[50],
-  },
-  scrollView: {
-    flex: 1,
-  },
-  scrollContent: {
-    paddingHorizontal: Spacing.lg,
-    paddingTop: Spacing.lg,
-    paddingBottom: Spacing.huge,
-  },
-  header: {
-    marginBottom: Spacing.lg,
-  },
-  title: {
-    ...Typography.h3,
-    color: Colors.slate[800],
-  },
-  section: {
-    marginBottom: Spacing.xl,
-  },
-  sectionTitle: {
-    ...Typography.caption,
-    color: Colors.slate[500],
-    marginBottom: Spacing.sm,
-    marginLeft: Spacing.xs,
-    textTransform: 'uppercase',
-    letterSpacing: 1,
-  },
-  profileCard: {
-    padding: 0,
-    overflow: 'hidden',
-  },
-  profileHeader: {
-    flexDirection: 'row',
-    alignItems: 'center',
-    padding: Spacing.lg,
-    borderBottomWidth: 1,
-    borderBottomColor: Colors.slate[100],
-  },
-  avatar: {
-    width: 56,
-    height: 56,
-    borderRadius: 28,
-    backgroundColor: Colors.primary[100],
-    justifyContent: 'center',
-    alignItems: 'center',
-    marginRight: Spacing.md,
-  },
-  avatarText: {
-    ...Typography.h4,
-    color: Colors.primary[600],
-  },
-  profileInfo: {
-    flex: 1,
-  },
-  profileName: {
-    ...Typography.h5,
-    color: Colors.slate[800],
-    marginBottom: 2,
-  },
-  profileMeta: {
-    ...Typography.bodySmall,
-    color: Colors.slate[500],
-  },
-  profileDetails: {
-    padding: Spacing.lg,
-    gap: Spacing.md,
-  },
-  profileRow: {
-    flexDirection: 'row',
-    alignItems: 'center',
-  },
-  profileRowLabel: {
-    ...Typography.bodySmall,
-    color: Colors.slate[500],
-    marginLeft: Spacing.sm,
-    flex: 1,
-  },
-  profileRowValue: {
-    ...Typography.bodySmall,
-    color: Colors.slate[700],
-    fontWeight: '500',
-  },
-  menuCard: {
-    padding: 0,
-    overflow: 'hidden',
-  },
-  menuItem: {
-    flexDirection: 'row',
-    alignItems: 'center',
-    justifyContent: 'space-between',
-    padding: Spacing.lg,
-    borderBottomWidth: 1,
-    borderBottomColor: Colors.slate[50],
-  },
-  menuItemLeft: {
-    flexDirection: 'row',
-    alignItems: 'center',
-  },
-  menuItemLabel: {
-    ...Typography.body,
-    color: Colors.slate[700],
-    marginLeft: Spacing.md,
-  },
-  menuItemRight: {
-    flexDirection: 'row',
-    alignItems: 'center',
-    gap: Spacing.sm,
-  },
-  menuItemValue: {
-    ...Typography.bodySmall,
-    color: Colors.slate[500],
-  },
-  versionContainer: {
-    alignItems: 'center',
-    paddingVertical: Spacing.xl,
-  },
-  versionText: {
-    ...Typography.caption,
-    color: Colors.slate[400],
-  },
-});
+export default function SettingsScreen(): React.ReactElement {
+  const insets = useSafeAreaInsets();
+  const [notifications, setNotifications] = useState(true);
+  const [haptic, setHaptic] = useState(true);
+
+  // Fade-in animations
+  const headerFadeIn = useFadeIn(0);
+  const contentFadeIn = useFadeIn(100);
+
+  return (
+    <View className="flex-1 bg-stone-100">
+      <SafeAreaView className="flex-1" edges={['top']}>
+        <ScrollView
+          contentContainerStyle={{
+            paddingHorizontal: 24,
+            paddingBottom: TAB_BAR_HEIGHT + insets.bottom + 24,
+          }}
+          showsVerticalScrollIndicator={false}
+        >
+          {/* Header */}
+          <Animated.View style={headerFadeIn} className="pt-14 mb-6">
+            <Text
+              className="text-3xl text-stone-900 tracking-tight mb-1"
+              style={{ fontFamily: FontFamily.serif }}
+            >
+              {t('screen.settings.title')}
+            </Text>
+            <Text className="text-sm text-stone-500">{t('screen.settings.subtitle')}</Text>
+          </Animated.View>
+
+          <Animated.View style={contentFadeIn}>
+            {/* Profile Card */}
+            <View
+              className="bg-white p-4 rounded-3xl border border-stone-100 flex-row items-center mb-8"
+              style={{
+                gap: 16,
+                shadowColor: '#000',
+                shadowOffset: { width: 0, height: 4 },
+                shadowOpacity: 0.06,
+                shadowRadius: 20,
+                elevation: 4,
+              }}
+            >
+              <LinearGradient
+                colors={['#E0E7FF', '#FFE4E6']}
+                start={{ x: 0, y: 0 }}
+                end={{ x: 1, y: 1 }}
+                className="items-center justify-center"
+                style={{ width: 64, height: 64, borderRadius: 32 }}
+              >
+                <Text style={{ fontSize: 28 }}>🧘</Text>
+              </LinearGradient>
+              <View className="flex-1">
+                <Text className="text-lg font-bold text-stone-900">{MOCK_DATA.nickname}</Text>
+                <Text className="text-xs text-stone-500">
+                  {MOCK_DATA.plan} • {MOCK_DATA.memberSince}年から利用
+                </Text>
+              </View>
+              <Pressable className="p-2 rounded-full">
+                <ChevronRight size={20} color="#A8A29E" />
+              </Pressable>
+            </View>
+
+            {/* My Rhythm Section */}
+            <Section title={t('screen.settings.myRhythm')}>
+              <SettingsRow
+                icon={Moon}
+                iconColor="#6366F1"
+                iconBg="#EEF2FF"
+                label={t('screen.settings.targetBedtime')}
+                value={MOCK_DATA.targetBedtime}
+              />
+              <SettingsRow
+                icon={Sun}
+                iconColor="#F59E0B"
+                iconBg="#FFFBEB"
+                label={t('screen.settings.targetWakeUp')}
+                value={MOCK_DATA.targetWakeUp}
+              />
+            </Section>
+
+            {/* Preferences Section */}
+            <Section title={t('screen.settings.preferences')}>
+              <ToggleRow
+                icon={Bell}
+                iconColor="#F43F5E"
+                iconBg="#FFF1F2"
+                label={t('screen.settings.gentleNudges')}
+                value={notifications}
+                onValueChange={setNotifications}
+              />
+              <ToggleRow
+                icon={Zap}
+                iconColor="#57534E"
+                iconBg="#F5F5F4"
+                label={t('screen.settings.hapticFeedback')}
+                value={haptic}
+                onValueChange={setHaptic}
+              />
+            </Section>
+
+            {/* Data Source Section */}
+            <Section title={t('screen.settings.dataSource')}>
+              {/* Apple Health - Connected */}
+              <View className="flex-row items-center justify-between p-3">
+                <View className="flex-row items-center" style={{ gap: 12 }}>
+                  <View className="p-2 rounded-xl bg-stone-900">
+                    <Heart size={18} color="#FFFFFF" fill="#FFFFFF" />
+                  </View>
+                  <Text className="text-sm font-medium text-stone-700">{t('screen.settings.appleHealth')}</Text>
+                </View>
+                <View className="bg-emerald-50 px-2 py-1 rounded-md">
+                  <Text className="text-xs font-bold text-emerald-600">{t('screen.settings.connected')}</Text>
+                </View>
+              </View>
+
+              {/* Divider */}
+              <View className="h-px bg-stone-100 my-2 mx-12" />
+
+              {/* Oura Ring - Connect */}
+              <SettingsRow
+                icon={Smartphone}
+                iconColor="#78716C"
+                iconBg="#F5F5F4"
+                label={t('screen.settings.ouraRing')}
+                value={t('screen.settings.connect')}
+                valueColor="#4F46E5"
+              />
+            </Section>
+
+            {/* Support Section */}
+            <Section title={t('screen.settings.support')}>
+              <SettingsRow
+                icon={HelpCircle}
+                iconColor="#6366F1"
+                iconBg="#EEF2FF"
+                label={t('screen.settings.helpCenter')}
+              />
+              <SettingsRow
+                icon={Shield}
+                iconColor="#6366F1"
+                iconBg="#EEF2FF"
+                label={t('screen.settings.privacyPolicy')}
+              />
+            </Section>
+
+            {/* Sign Out Button */}
+            <Pressable
+              className="flex-row items-center justify-center py-4 rounded-2xl border border-rose-100 mb-8"
+              style={{
+                gap: 8,
+                backgroundColor: 'rgba(255, 241, 242, 0.5)',
+              }}
+            >
+              <LogOut size={16} color="#E11D48" />
+              <Text className="text-sm font-medium text-rose-600">{t('screen.settings.resetSignOut')}</Text>
+            </Pressable>



================================================================================
ファイル: app/app/(onboarding)/index.tsx
行: 15 (開始行: N/A)
コミット: a35fee7
作成日時: 2026-01-07T07:52:33Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**Consider using `useWindowDimensions` hook for responsive updates.**

`Dimensions.get('window')` captures dimensions at module load time and won't update on orientation changes or window resizes. For more robust responsiveness, consider using `useWindowDimensions()` from React Native inside the component.



<details>
<summary>♻️ Suggested approach</summary>

```diff
-import { View, Text, StyleSheet, Dimensions } from "react-native";
+import { View, Text, StyleSheet, useWindowDimensions } from "react-native";

-const { width, height } = Dimensions.get("window");

export default function WelcomeScreen(): JSX.Element {
+  const { width, height } = useWindowDimensions();
   const router = useRouter();
```

Note: This would require moving the blob positioning styles to be computed dynamically or using percentage-based positioning.
</details>


> Committable suggestion skipped: line range outside the PR's diff.

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(onboarding)/index.tsx at line 15, Replace the module-level static
Dimensions.get("window") call with the useWindowDimensions() hook inside the
component so width/height update on orientation/window size changes; update any
code that references the const { width, height } = Dimensions.get("window")
(search for that symbol) to read dimensions from useWindowDimensions() and move
blob positioning styles (the computed styles that depend on width/height) into
the component render or a memoized function so they recompute on dimension
changes, or convert those positions to percentage-based styles to remain
responsive.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -1,132 +1,147 @@
-import React from 'react';
-import { View, Text, StyleSheet, SafeAreaView } from 'react-native';
-import { useRouter } from 'expo-router';
-import { Sun } from 'lucide-react-native';
-import { Colors, Spacing, Typography } from '../../src/theme';
-import { PrimaryButton } from '../../src/components';
-import type { JSX } from 'react';
+/**
+ * WelcomeScreen - オンボーディング開始画面
+ * sozai/new/screens/OnboardingScreen.tsx のスタイルを React Native で再現
+ * 9ステップのオンボーディングフローを維持
+ */
+
+import React from "react";
+import { View, Text, StyleSheet, Dimensions } from "react-native";
+import { SafeAreaView } from "react-native-safe-area-context";
+import { useRouter } from "expo-router";
+import { Colors, Spacing, BorderRadius, FontFamily } from "../../src/theme";
+import { PrimaryButton } from "../../src/components";
+import type { JSX } from "react";
+
+const { width, height } = Dimensions.get("window");



================================================================================
ファイル: app/app/(onboarding)/lifestyle.tsx
行: 210 (開始行: 119)
コミット: a35fee7
作成日時: 2026-01-07T07:52:33Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**Consider extracting the repeated section pattern into a reusable component.**

The three sections (Work Type, Exercise Frequency, Alcohol Consumption) share identical structure:
- Section header with icon
- Chip-style options with selection state

This could be refactored into a `LifestyleSection` component to reduce duplication (~90 lines).



<details>
<summary>♻️ Example extraction</summary>

```typescript
interface LifestyleSectionProps<T extends string> {
  icon: React.ComponentType<{ size: number; color: string }>;
  title: string;
  options: { value: T; label: string }[];
  selected: T | null;
  onSelect: (value: T) => void;
}

const LifestyleSection = <T extends string>({
  icon: Icon,
  title,
  options,
  selected,
  onSelect,
}: LifestyleSectionProps<T>): JSX.Element => (
  <View style={styles.section}>
    <View style={styles.sectionHeader}>
      <View style={styles.sectionIconContainer}>
        <Icon size={18} color={Colors.indigo[500]} />
      </View>
      <Text style={styles.sectionTitle}>{title}</Text>
    </View>
    <View style={styles.options}>
      {options.map((opt) => (
        <TouchableOpacity
          key={opt.value}
          style={[styles.chip, selected === opt.value && styles.chipSelected]}
          onPress={() => onSelect(opt.value)}
        >
          <Text style={[styles.chipText, selected === opt.value && styles.chipTextSelected]}>
            {opt.label}
          </Text>
        </TouchableOpacity>
      ))}
    </View>
  </View>
);
```
</details>

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(onboarding)/lifestyle.tsx around lines 119 - 210, The three
repeated blocks for Work Type, Exercise Frequency, and Alcohol Consumption
should be extracted into a reusable component to remove duplication; create a
generic LifestyleSection component (e.g., LifestyleSection<T extends string>)
that accepts props icon, title, options, selected, and onSelect and replace each
block by rendering this component with Icon={Briefcase|Dumbbell|Wine},
title="...", options={OCCUPATIONS|EXERCISE_OPTIONS|ALCOHOL_OPTIONS},
selected={occupation|exercise|alcohol}, and
onSelect={setOccupation|setExercise|setAlcohol}; ensure the component uses the
existing styles.section, styles.sectionHeader, styles.sectionIconContainer,
styles.sectionTitle, styles.options, styles.chip, styles.chipSelected,
styles.chipText, and styles.chipTextSelected and preserves key handling by using
opt.value for keys.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -73,182 +84,287 @@ export default function LifestyleScreen(): JSX.Element {
   };
 
   return (
-    <SafeAreaView style={styles.container}>
-      <ScrollView
-        style={styles.scrollView}
-        contentContainerStyle={styles.scrollContent}
-      >
-        <Text style={styles.title}>ライフスタイル</Text>
-        <Text style={styles.description}>
-          より精度の高いアドバイスのために{'\n'}
-          教えてください（任意）
-        </Text>
-
-        <View style={styles.section}>
-          <View style={styles.sectionHeader}>
-            <Briefcase size={20} color={Colors.slate[500]} />
-            <Text style={styles.sectionTitle}>職業タイプ</Text>
-          </View>
-          <View style={styles.options}>
-            {OCCUPATIONS.map((opt) => (
-              <TouchableOpacity
-                key={opt.value}
-                style={[
-                  styles.chip,
-                  occupation === opt.value && styles.chipSelected,
-                ]}
-                onPress={() => setOccupation(opt.value)}
-              >
-                <Text
-                  style={[
-                    styles.chipText,
-                    occupation === opt.value && styles.chipTextSelected,
-                  ]}
-                >
-                  {opt.label}
-                </Text>
-              </TouchableOpacity>
-            ))}
-          </View>
-        </View>
+    <View style={styles.container}>
+      {/* Decorative background blobs */}
+      <View style={styles.blobTopRight} />
+      <View style={styles.blobBottomLeft} />
 
-        <View style={styles.section}>
-          <View style={styles.sectionHeader}>
-            <Dumbbell size={20} color={Colors.slate[500]} />
-            <Text style={styles.sectionTitle}>運動頻度</Text>
-          </View>
-          <View style={styles.options}>
-            {EXERCISE_OPTIONS.map((opt) => (
-              <TouchableOpacity
-                key={opt.value}
-                style={[
-                  styles.chip,
-                  exercise === opt.value && styles.chipSelected,
-                ]}
-                onPress={() => setExercise(opt.value)}
-              >
-                <Text
-                  style={[
-                    styles.chipText,
-                    exercise === opt.value && styles.chipTextSelected,
-                  ]}
-                >
-                  {opt.label}
-                </Text>
-              </TouchableOpacity>
-            ))}
-          </View>
+      <SafeAreaView style={styles.safeArea}>
+        {/* Progress Bar */}
+        <View style={styles.progressContainer}>
+          {[...Array(TOTAL_STEPS)].map((_, idx) => (
+            <View
+              key={idx}
+              style={[
+                styles.progressSegment,
+                idx < CURRENT_STEP ? styles.progressActive : styles.progressInactive,
+              ]}
+            />
+          ))}
         </View>
 
-        <View style={styles.section}>
-          <View style={styles.sectionHeader}>
-            <Wine size={20} color={Colors.slate[500]} />
-            <Text style={styles.sectionTitle}>飲酒頻度</Text>
-          </View>
-          <View style={styles.options}>
-            {ALCOHOL_OPTIONS.map((opt) => (
-              <TouchableOpacity
-                key={opt.value}
-                style={[
-                  styles.chip,
-                  alcohol === opt.value && styles.chipSelected,
-                ]}
-                onPress={() => setAlcohol(opt.value)}
-              >
-                <Text
-                  style={[
-                    styles.chipText,
-                    alcohol === opt.value && styles.chipTextSelected,
-                  ]}
-                >
-                  {opt.label}
-                </Text>
-              </TouchableOpacity>
-            ))}
+        <ScrollView
+          style={styles.scrollView}
+          contentContainerStyle={styles.scrollContent}
+          showsVerticalScrollIndicator={false}
+        >
+          {/* Content */}
+          <View style={styles.content}>
+            <Text style={styles.emoji}>🏃</Text>
+            <Text style={styles.title}>Daily Rhythms</Text>
+            <Text style={styles.description}>
+              Help us understand your lifestyle for more personalized insights. (Optional)
+            </Text>
+
+            {/* Occupation Section */}
+            <View style={styles.section}>
+              <View style={styles.sectionHeader}>
+                <View style={styles.sectionIconContainer}>
+                  <Briefcase size={18} color={Colors.indigo[500]} />
+                </View>
+                <Text style={styles.sectionTitle}>Work Type</Text>
+              </View>
+              <View style={styles.options}>
+                {OCCUPATIONS.map((opt) => (
+                  <TouchableOpacity
+                    key={opt.value}
+                    style={[
+                      styles.chip,
+                      occupation === opt.value && styles.chipSelected,
+                    ]}
+                    onPress={() => setOccupation(opt.value)}
+                  >
+                    <Text
+                      style={[
+                        styles.chipText,
+                        occupation === opt.value && styles.chipTextSelected,
+                      ]}
+                    >
+                      {opt.label}
+                    </Text>
+                  </TouchableOpacity>
+                ))}
+              </View>
+            </View>
+
+            {/* Exercise Section */}
+            <View style={styles.section}>
+              <View style={styles.sectionHeader}>
+                <View style={styles.sectionIconContainer}>
+                  <Dumbbell size={18} color={Colors.indigo[500]} />
+                </View>
+                <Text style={styles.sectionTitle}>Exercise Frequency</Text>
+              </View>
+              <View style={styles.options}>
+                {EXERCISE_OPTIONS.map((opt) => (
+                  <TouchableOpacity
+                    key={opt.value}
+                    style={[
+                      styles.chip,
+                      exercise === opt.value && styles.chipSelected,
+                    ]}
+                    onPress={() => setExercise(opt.value)}
+                  >
+                    <Text
+                      style={[
+                        styles.chipText,
+                        exercise === opt.value && styles.chipTextSelected,
+                      ]}
+                    >
+                      {opt.label}
+                    </Text>
+                  </TouchableOpacity>
+                ))}
+              </View>
+            </View>
+
+            {/* Alcohol Section */}
+            <View style={styles.section}>
+              <View style={styles.sectionHeader}>
+                <View style={styles.sectionIconContainer}>
+                  <Wine size={18} color={Colors.indigo[500]} />
+                </View>
+                <Text style={styles.sectionTitle}>Alcohol Consumption</Text>
+              </View>
+              <View style={styles.options}>
+                {ALCOHOL_OPTIONS.map((opt) => (
+                  <TouchableOpacity
+                    key={opt.value}
+                    style={[
+                      styles.chip,
+                      alcohol === opt.value && styles.chipSelected,
+                    ]}
+                    onPress={() => setAlcohol(opt.value)}
+                  >
+                    <Text
+                      style={[
+                        styles.chipText,
+                        alcohol === opt.value && styles.chipTextSelected,
+                      ]}
+                    >
+                      {opt.label}
+                    </Text>
+                  </TouchableOpacity>
+                ))}
+              </View>
+            </View>



================================================================================
ファイル: app/app/(onboarding)/location.tsx
行: 17 (開始行: 15)
コミット: a35fee7
作成日時: 2026-01-07T07:52:33Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**Static Dimensions at module level won't update on rotation.**

`Dimensions.get('window')` called at module level (Line 15) captures dimensions once at import time. If the device rotates, the blob positions will be incorrect. Consider using `useWindowDimensions` hook inside the component for dynamic values.


<details>
<summary>♻️ Proposed fix</summary>

```diff
-import { View, Text, StyleSheet, Dimensions } from 'react-native';
+import { View, Text, StyleSheet, useWindowDimensions } from 'react-native';

-const { width, height } = Dimensions.get('window');
 const CURRENT_STEP = 8;
 const TOTAL_STEPS = 9;

 export default function LocationScreen(): JSX.Element {
   const router = useRouter();
+  const { width, height } = useWindowDimensions();
```

Then update the blob styles to be computed dynamically or pass width/height as inline style overrides.
</details>


> Committable suggestion skipped: line range outside the PR's diff.

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -1,141 +1,221 @@
+/**
+ * LocationScreen - 位置情報許可画面
+ * sozai/new のスタイルを React Native で再現
+ * Step 8 of 9
+ */
+
 import React from 'react';
-import { View, Text, StyleSheet, SafeAreaView } from 'react-native';
+import { View, Text, StyleSheet, Dimensions } from 'react-native';
+import { SafeAreaView } from 'react-native-safe-area-context';
 import { useRouter } from 'expo-router';
-import { MapPin } from 'lucide-react-native';
-import { Colors, Spacing, Typography } from '../../src/theme';
+import { Colors, Spacing, BorderRadius, FontFamily } from '../../src/theme';
 import { PrimaryButton, SecondaryButton } from '../../src/components';
 import type { JSX } from 'react';
 
+const { width, height } = Dimensions.get('window');
+const CURRENT_STEP = 8;
+const TOTAL_STEPS = 9;



================================================================================
ファイル: app/app/(onboarding)/nickname.tsx
行: 70 (開始行: 66)
コミット: a35fee7
作成日時: 2026-01-07T07:52:33Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

**Content mismatch: Title and description don't match the screen's purpose.**

The screen is for nickname input (`NicknameScreen`, Step 3), but the content displays:
- Title: "Warm AI Guidance"
- Description: "No robotic charts. Just gentle, poetic advice..."

This appears to be copy-paste content from another onboarding step. The content should relate to collecting the user's nickname.



<details>
<summary>💡 Suggested fix</summary>

```diff
-            <Text style={styles.emoji}>🤖</Text>
-            <Text style={styles.title}>Warm AI Guidance</Text>
-            <Text style={styles.description}>
-              No robotic charts. Just gentle, poetic advice to help you feel your best.
-            </Text>
+            <Text style={styles.emoji}>👋</Text>
+            <Text style={styles.title}>What Should We Call You?</Text>
+            <Text style={styles.description}>
+              Choose a nickname for a more personal experience.
+            </Text>
```
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
            <Text style={styles.emoji}>👋</Text>
            <Text style={styles.title}>What Should We Call You?</Text>
            <Text style={styles.description}>
              Choose a nickname for a more personal experience.
            </Text>
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(onboarding)/nickname.tsx around lines 66 - 70, Update the copy in
NicknameScreen: replace the current unrelated title and description Text nodes
(the Text elements using styles.title and styles.description in nickname.tsx /
NicknameScreen) with nickname-focused copy such as a title like "What's your
nickname?" and a description like "This is how others will see you—keep it short
and friendly." Ensure the emoji Text (styles.emoji) can remain or be changed to
a relevant icon; only update the title and description strings so the screen
clearly reflects Step 3: nickname input.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -16,103 +26,166 @@ export default function NicknameScreen(): JSX.Element {
 
   const handleNext = (): void => {
     if (!nickname.trim()) {
-      setError('ニックネームを入力してください');
+      setError('Please enter a nickname');
       return;
     }
     if (nickname.length > 20) {
-      setError('20文字以内で入力してください');
+      setError('Nickname must be 20 characters or less');
       return;
     }
     setDraftNickname(nickname.trim());
     router.push('/(onboarding)/basic-info');
   };
 
   return (
-    <SafeAreaView style={styles.container}>
-      <KeyboardAvoidingView
-        behavior={Platform.OS === 'ios' ? 'padding' : 'height'}
-        style={styles.keyboardView}
-      >
-        <View style={styles.content}>
-          <View style={styles.iconContainer}>
-            <User size={48} color={Colors.primary[500]} strokeWidth={1.5} />
+    <View style={styles.container}>
+      {/* Decorative background blobs */}
+      <View style={styles.blobTopRight} />
+      <View style={styles.blobBottomLeft} />
+
+      <SafeAreaView style={styles.safeArea}>
+        <KeyboardAvoidingView
+          behavior={Platform.OS === 'ios' ? 'padding' : 'height'}
+          style={styles.keyboardView}
+        >
+          {/* Progress Bar */}
+          <View style={styles.progressContainer}>
+            {[...Array(TOTAL_STEPS)].map((_, idx) => (
+              <View
+                key={idx}
+                style={[
+                  styles.progressSegment,
+                  idx < CURRENT_STEP ? styles.progressActive : styles.progressInactive,
+                ]}
+              />
+            ))}
           </View>
 
-          <Text style={styles.title}>ニックネーム</Text>
-          <Text style={styles.description}>
-            AI があなたに呼びかける名前を{'\n'}教えてください
-          </Text>
+          {/* Content */}
+          <View style={styles.content}>
+            <Text style={styles.emoji}>🤖</Text>
+            <Text style={styles.title}>Warm AI Guidance</Text>
+            <Text style={styles.description}>
+              No robotic charts. Just gentle, poetic advice to help you feel your best.
+            </Text>



================================================================================
ファイル: app/src/api/client.ts
行: 16 (開始行: 15)
コミット: a35fee7
作成日時: 2026-01-07T07:52:33Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟠 Major_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
find . -maxdepth 3 -name "app.config.*" -type f
```

Repository: Bluefinee/tempo-ai

Length of output: 44

---

🏁 Script executed:

```shell
ls -la | grep -E "app\.|\.config" | head -20
```

Repository: Bluefinee/tempo-ai

Length of output: 44

---

🏁 Script executed:

```shell
fd -t f "app\.config\.(js|ts|json)"
```

Repository: Bluefinee/tempo-ai

Length of output: 44

---

🏁 Script executed:

```shell
ls -la
```

Repository: Bluefinee/tempo-ai

Length of output: 577

---

🏁 Script executed:

```shell
find . -maxdepth 2 -type f -name "*.config.*"
```

Repository: Bluefinee/tempo-ai

Length of output: 181

---

🏁 Script executed:

```shell
cat -n app/src/api/client.ts 2>/dev/null || echo "File not found at that path"
```

Repository: Bluefinee/tempo-ai

Length of output: 3885

---

🏁 Script executed:

```shell
find . -path ./node_modules -prune -o -type f -name "*.ts" | grep -i config | head -20
```

Repository: Bluefinee/tempo-ai

Length of output: 119

---

🏁 Script executed:

```shell
ls -la app/ | head -20
```

Repository: Bluefinee/tempo-ai

Length of output: 993

---

🏁 Script executed:

```shell
find app -maxdepth 1 -type f
```

Repository: Bluefinee/tempo-ai

Length of output: 265

---

🏁 Script executed:

```shell
cat -n app/package.json 2>/dev/null | head -30
```

Repository: Bluefinee/tempo-ai

Length of output: 1194

---

🏁 Script executed:

```shell
cat -n app/app.json
```

Repository: Bluefinee/tempo-ai

Length of output: 1705

---



</details>

**Add `extra` configuration to app.json for production API URL.**

The `apiBaseUrl` is missing from `app.json`. Currently, the API client always falls back to `http://localhost:8787` since `Constants.expoConfig?.extra?.apiBaseUrl` is undefined. Add an `extra` field to the Expo configuration in app.json:

```json
{
  "expo": {
    ...
    "extra": {
      "apiBaseUrl": "https://api.your-domain.com"
    }
  }
}
```

This is required for production builds to use the correct API endpoint.

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/api/client.ts around lines 15 - 16, The API client constant
API_BASE_URL uses Constants.expoConfig?.extra?.apiBaseUrl and falls back to
'http://localhost:8787'; add an "extra" section to your Expo app.json with
"apiBaseUrl": "https://api.your-domain.com" so
Constants.expoConfig.extra.apiBaseUrl is defined in production builds, then
rebuild so API_BASE_URL resolves to the production endpoint instead of the
localhost fallback.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -1,110 +1,126 @@
 /**
  * API Client
+ * @see docs/specs/technical_spec.md
  */
 
-import { API_BASE_URL, REQUEST_TIMEOUT, DEFAULT_HEADERS } from './config';
-import {
+import Constants from 'expo-constants';
+import type {
   AdviceRequest,
   AdviceResponse,
-  WeatherRequest,
+  ApiError,
+  ApiResponse,
   WeatherResponse,
 } from './types';
 
-class ApiError extends Error {
-  constructor(
-    message: string,
-    public statusCode: number,
-    public code?: string
-  ) {
-    super(message);
-    this.name = 'ApiError';
-  }
+const API_BASE_URL = Constants.expoConfig?.extra?.apiBaseUrl ?? 'http://localhost:8787';
+const API_TIMEOUT = 30000;



================================================================================
ファイル: app/src/components/HealthAreaChart.tsx
行: 34 (開始行: 22)
コミット: a35fee7
作成日時: 2026-01-07T07:52:33Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**Export `HealthAreaChartProps` interface for type consumers.**

`ChartDataPoint` is exported but `HealthAreaChartProps` is not. Consumers may need this type for wrapper components or type-safe prop spreading.



```diff
-interface HealthAreaChartProps {
+export interface HealthAreaChartProps {
   data: ChartDataPoint[];
   colorHex: string;
   typicalRange: { min: number; max: number };
   unit: string;
   width?: number;
   height?: number;
 }
```

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
export interface ChartDataPoint {
  day: string;
  value: number;
}

export interface HealthAreaChartProps {
  data: ChartDataPoint[];
  colorHex: string;
  typicalRange: { min: number; max: number };
  unit: string;
  width?: number;
  height?: number;
}
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/components/HealthAreaChart.tsx around lines 22 - 34, The
HealthAreaChartProps interface is not exported while ChartDataPoint is; export
HealthAreaChartProps so external consumers can import the prop type. Update the
declaration of HealthAreaChartProps (the interface named HealthAreaChartProps)
to be exported and ensure any modules that consume it import the newly exported
symbol for type-safe prop spreading or wrapper components.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,263 @@
+/**
+ * HealthAreaChart - Health詳細用エリアチャート
+ * sozai/tempoai-health-summary/components/DetailSection.tsx のチャートを再現
+ */
+
+import React, { useMemo, useState } from 'react';
+import { View, Text, Dimensions, PanResponder, GestureResponderEvent } from 'react-native';
+import Svg, {
+  Path,
+  Defs,
+  LinearGradient,
+  Stop,
+  Rect,
+  Circle,
+  Line,
+  Text as SvgText,
+} from 'react-native-svg';
+import Animated, { FadeIn, FadeOut } from 'react-native-reanimated';
+
+import { colors } from '../theme';
+
+export interface ChartDataPoint {
+  day: string;
+  value: number;
+}
+
+interface HealthAreaChartProps {
+  data: ChartDataPoint[];
+  colorHex: string;
+  typicalRange: { min: number; max: number };
+  unit: string;
+  width?: number;
+  height?: number;
+}



================================================================================
ファイル: app/src/components/HealthAreaChart.tsx
行: 156 (開始行: 138)
コミット: a35fee7
作成日時: 2026-01-07T07:52:34Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟠 Major_

**Stale closure in PanResponder: `handleTouch` captures outdated `points`.**

The `handleTouch` function is defined outside `useMemo` but references `points` which can change. The `panResponder` memoizes with `[points]` dependency, but `handleTouch` itself isn't recreated, leading to potential stale closure issues.



<details>
<summary>🐛 Suggested fix</summary>

```diff
-  const handleTouch = (evt: GestureResponderEvent) => {
-    const touchX = evt.nativeEvent.locationX;
-    const closest = findClosestPoint(touchX);
-    setTouchedIndex(closest.index);
-    setTooltipPosition({ x: closest.x, y: closest.y });
-  };
-
   const panResponder = useMemo(
-    () =>
+    () => {
+      const handleTouch = (evt: GestureResponderEvent) => {
+        const touchX = evt.nativeEvent.locationX;
+        const closest = findClosestPoint(touchX);
+        setTouchedIndex(closest.index);
+        setTooltipPosition({ x: closest.x, y: closest.y });
+      };
+
+      return PanResponder.create({
         onStartShouldSetPanResponder: () => true,
         onMoveShouldSetPanResponder: () => true,
         onPanResponderGrant: handleTouch,
         onPanResponderMove: handleTouch,
         onPanResponderRelease: () => setTouchedIndex(null),
         onPanResponderTerminate: () => setTouchedIndex(null),
-      }),
+      });
+    },
     [points]
   );
```
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
  const panResponder = useMemo(
    () => {
      const handleTouch = (evt: GestureResponderEvent) => {
        const touchX = evt.nativeEvent.locationX;
        const closest = findClosestPoint(touchX);
        setTouchedIndex(closest.index);
        setTooltipPosition({ x: closest.x, y: closest.y });
      };

      return PanResponder.create({
        onStartShouldSetPanResponder: () => true,
        onMoveShouldSetPanResponder: () => true,
        onPanResponderGrant: handleTouch,
        onPanResponderMove: handleTouch,
        onPanResponderRelease: () => setTouchedIndex(null),
        onPanResponderTerminate: () => setTouchedIndex(null),
      });
    },
    [points]
  );
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/components/HealthAreaChart.tsx around lines 138 - 156, The
handleTouch function closes over changing points causing a stale-closure when
panResponder (created in useMemo) is reused; fix by ensuring the touch handler
and panResponder see the latest points — either move the handleTouch
implementation inside the useMemo so it is recreated when points change, or wrap
handleTouch with useCallback including points in its dependency array, then
create panResponder with that stable handler (ensure panResponder's dependency
list matches the handler's dependencies). Update references to findClosestPoint,
setTouchedIndex, and setTooltipPosition accordingly so they use the fresh
closure.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,263 @@
+/**
+ * HealthAreaChart - Health詳細用エリアチャート
+ * sozai/tempoai-health-summary/components/DetailSection.tsx のチャートを再現
+ */
+
+import React, { useMemo, useState } from 'react';
+import { View, Text, Dimensions, PanResponder, GestureResponderEvent } from 'react-native';
+import Svg, {
+  Path,
+  Defs,
+  LinearGradient,
+  Stop,
+  Rect,
+  Circle,
+  Line,
+  Text as SvgText,
+} from 'react-native-svg';
+import Animated, { FadeIn, FadeOut } from 'react-native-reanimated';
+
+import { colors } from '../theme';
+
+export interface ChartDataPoint {
+  day: string;
+  value: number;
+}
+
+interface HealthAreaChartProps {
+  data: ChartDataPoint[];
+  colorHex: string;
+  typicalRange: { min: number; max: number };
+  unit: string;
+  width?: number;
+  height?: number;
+}
+
+const PADDING_LEFT = 0;
+const PADDING_RIGHT = 0;
+const PADDING_TOP = 20;
+const PADDING_BOTTOM = 30;
+
+export const HealthAreaChart: React.FC<HealthAreaChartProps> = ({
+  data,
+  colorHex,
+  typicalRange,
+  unit,
+  width: propWidth,
+  height = 200,
+}) => {
+  const screenWidth = Dimensions.get('window').width;
+  const width = propWidth ?? screenWidth - 48;
+  const chartWidth = width - PADDING_LEFT - PADDING_RIGHT;
+  const chartHeight = height - PADDING_TOP - PADDING_BOTTOM;
+
+  const [touchedIndex, setTouchedIndex] = useState<number | null>(null);
+  const [tooltipPosition, setTooltipPosition] = useState({ x: 0, y: 0 });
+
+  // Y軸のドメインを計算（データとtypicalRangeを含む）
+  const { minY, maxY } = useMemo(() => {
+    const allValues = data.map((d) => d.value);
+    const minVal = Math.min(...allValues, typicalRange.min) * 0.95;
+    const maxVal = Math.max(...allValues, typicalRange.max) * 1.05;
+    return { minY: minVal, maxY: maxVal };
+  }, [data, typicalRange]);
+
+  const yRange = maxY - minY;
+
+  // データポイントの座標を計算
+  const points = useMemo(() => {
+    return data.map((d, index) => {
+      const x = PADDING_LEFT + (index / (data.length - 1)) * chartWidth;
+      const y = PADDING_TOP + chartHeight - ((d.value - minY) / yRange) * chartHeight;
+      return { x, y, data: d, index };
+    });
+  }, [data, chartWidth, chartHeight, minY, yRange]);
+
+  // SVGパスを生成（スムーズな曲線）
+  const areaPath = useMemo(() => {
+    if (points.length < 2) return '';
+
+    let path = `M ${points[0].x} ${points[0].y}`;
+
+    for (let i = 1; i < points.length; i++) {
+      const prev = points[i - 1];
+      const curr = points[i];
+      const cpX = (prev.x + curr.x) / 2;
+      path += ` Q ${prev.x + (cpX - prev.x) * 0.5} ${prev.y}, ${cpX} ${(prev.y + curr.y) / 2}`;
+      path += ` Q ${cpX + (curr.x - cpX) * 0.5} ${curr.y}, ${curr.x} ${curr.y}`;
+    }
+
+    // エリアを閉じる
+    path += ` L ${points[points.length - 1].x} ${PADDING_TOP + chartHeight}`;
+    path += ` L ${points[0].x} ${PADDING_TOP + chartHeight}`;
+    path += ' Z';
+
+    return path;
+  }, [points, chartHeight]);
+
+  // ラインパスを生成
+  const linePath = useMemo(() => {
+    if (points.length < 2) return '';
+
+    let path = `M ${points[0].x} ${points[0].y}`;
+
+    for (let i = 1; i < points.length; i++) {
+      const prev = points[i - 1];
+      const curr = points[i];
+      const cpX = (prev.x + curr.x) / 2;
+      path += ` Q ${prev.x + (cpX - prev.x) * 0.5} ${prev.y}, ${cpX} ${(prev.y + curr.y) / 2}`;
+      path += ` Q ${cpX + (curr.x - cpX) * 0.5} ${curr.y}, ${curr.x} ${curr.y}`;
+    }
+
+    return path;
+  }, [points]);
+
+  // Typical Rangeのバンド位置
+  const typicalRangeBand = useMemo(() => {
+    const y1 = PADDING_TOP + chartHeight - ((typicalRange.max - minY) / yRange) * chartHeight;
+    const y2 = PADDING_TOP + chartHeight - ((typicalRange.min - minY) / yRange) * chartHeight;
+    return { y1, y2, height: y2 - y1 };
+  }, [typicalRange, chartHeight, minY, yRange]);
+
+  // タッチハンドリング
+  const findClosestPoint = (touchX: number) => {
+    let closest = points[0];
+    let minDist = Math.abs(touchX - points[0].x);
+
+    for (const point of points) {
+      const dist = Math.abs(touchX - point.x);
+      if (dist < minDist) {
+        minDist = dist;
+        closest = point;
+      }
+    }
+
+    return closest;
+  };
+
+  const handleTouch = (evt: GestureResponderEvent) => {
+    const touchX = evt.nativeEvent.locationX;
+    const closest = findClosestPoint(touchX);
+    setTouchedIndex(closest.index);
+    setTooltipPosition({ x: closest.x, y: closest.y });
+  };
+
+  const panResponder = useMemo(
+    () =>
+      PanResponder.create({
+        onStartShouldSetPanResponder: () => true,
+        onMoveShouldSetPanResponder: () => true,
+        onPanResponderGrant: handleTouch,
+        onPanResponderMove: handleTouch,
+        onPanResponderRelease: () => setTouchedIndex(null),
+        onPanResponderTerminate: () => setTouchedIndex(null),
+      }),
+    [points]
+  );



================================================================================
ファイル: app/src/components/HealthMetricCard.tsx
行: 34 (開始行: 14)
コミット: a35fee7
作成日時: 2026-01-07T07:52:34Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
#!/bin/bash
# Check for IconType definition and usage
echo "=== Searching for IconType definition and usage ==="
rg -n "IconType" --type=ts --type=tsx

echo -e "\n=== Searching for HealthMetricCard usage ==="
rg -n "HealthMetricCard" --type=ts --type=tsx -B 2 -A 5
```

Repository: Bluefinee/tempo-ai

Length of output: 232

---

🏁 Script executed:

```shell
#!/bin/bash
# List TypeScript files in the app/src directory
echo "=== TypeScript/TSX files in app/src ==="
find app/src -name "*.ts" -o -name "*.tsx" | head -20

echo -e "\n=== Searching for IconType definition and usage ==="
rg -i "IconType" app/src

echo -e "\n=== Searching for HealthMetricCard usage ==="
rg "HealthMetricCard" app/src -A 5 -B 2
```

Repository: Bluefinee/tempo-ai

Length of output: 3276

---

🏁 Script executed:

```shell
#!/bin/bash
# Search for HealthMetricCard component instantiation/usage
echo "=== Where is HealthMetricCard instantiated/rendered? ==="
rg "<HealthMetricCard" app/src -B 2 -A 8

echo -e "\n=== Check if id or iconType are passed in parent components ==="
rg "HealthMetricCard" app/src -B 5 -A 15 | grep -E "(id=|iconType=|<HealthMetricCard)"
```

Repository: Bluefinee/tempo-ai

Length of output: 892

---

🏁 Script executed:

```shell
#!/bin/bash
# Find all files that import HealthMetricCard
echo "=== Files that import HealthMetricCard ==="
rg "import.*HealthMetricCard" app/src

echo -e "\n=== Check those files for HealthMetricCard usage ==="
for file in $(rg "import.*HealthMetricCard" app/src -l); do
  echo "=== File: $file ==="
  rg "HealthMetricCard" "$file" -B 2 -A 10
done
```

Repository: Bluefinee/tempo-ai

Length of output: 158

---

🏁 Script executed:

```shell
#!/bin/bash
# Check barrel export usage
echo "=== Check if components/index.ts is imported ==="
rg "from.*components(?:/index)?" app/src -A 3 | head -50

echo -e "\n=== Full HealthMetricCard.tsx file ==="
cat app/src/components/HealthMetricCard.tsx
```

Repository: Bluefinee/tempo-ai

Length of output: 3113

---



</details>

**Remove unused props `id` and `iconType` from the HealthMetricCard interface.**

The props `id` and `iconType` are defined in the interface (lines 15, 21) but never destructured (lines 26-34) or used in the component body. Per guidelines, unused code must be deleted.

Additionally, the `containerLastOdd` style object (line 73) is empty—remove it since it applies no styling.

<details>
<summary>Suggested cleanup</summary>

```diff
interface HealthMetricCardProps {
-  id: string;
   name: string;
   value: number | string;
   unit: string;
   status: MetricStatus;
   statusLabel: string;
-  iconType: IconType;
   isLastOdd?: boolean;
   onPress?: () => void;
}
```

```diff
const styles = StyleSheet.create({
  // ...
-  containerLastOdd: {
-    // Full width variant uses same styling
-  },
   pressed: {
```

If `IconType` is not exported elsewhere or used by parent components, remove the export as well.
</details>



> Committable suggestion skipped: line range outside the PR's diff.

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/components/HealthMetricCard.tsx around lines 14 - 34, Remove the
unused props and empty style: delete the unused id and iconType fields from the
HealthMetricCardProps interface and stop exporting IconType if it isn’t used
elsewhere (or ensure parents still import it before removing), then remove the
empty containerLastOdd style object from the styles definition; verify
HealthMetricCard (the React.FC) does not reference id or iconType anywhere and
adjust any imports that referenced IconType accordingly.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,130 @@
+/**
+ * HealthMetricCard - Health Summary グリッド用カード
+ * 白背景で浮遊感のある美しいカードデザイン（Apple Design Award品質）
+ */
+
+import React from 'react';
+import { View, Text, Pressable, StyleSheet } from 'react-native';
+import { Check, AlertCircle } from 'lucide-react-native';
+import { colors } from '../theme';
+
+export type IconType = 'activity' | 'heart' | 'wind' | 'droplet' | 'thermometer';
+export type MetricStatus = 'in-range' | 'out-of-range';
+
+interface HealthMetricCardProps {
+  id: string;
+  name: string;
+  value: number | string;
+  unit: string;
+  status: MetricStatus;
+  statusLabel: string;
+  iconType: IconType;
+  isLastOdd?: boolean;
+  onPress?: () => void;
+}
+
+export const HealthMetricCard: React.FC<HealthMetricCardProps> = ({
+  name,
+  value,
+  unit,
+  status,
+  statusLabel,
+  isLastOdd = false,
+  onPress,
+}) => {



================================================================================
ファイル: app/src/components/HealthMetricCard.tsx
行: 86 (開始行: 84)
コミット: a35fee7
作成日時: 2026-01-07T07:52:34Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**Empty style object `containerLastOdd` is dead code.**

The `containerLastOdd` style is applied conditionally (Line 42) but contains only a comment with no actual styling. Either implement the intended styling or remove the conditional and style definition.


<details>
<summary>🧹 Proposed fix - remove if not needed</summary>

```diff
-  containerLastOdd: {
-    // Full width variant uses same styling
-  },
```

And update the Pressable style:
```diff
-        isLastOdd && styles.containerLastOdd,
```
</details>


> Committable suggestion skipped: line range outside the PR's diff.

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/components/HealthMetricCard.tsx around lines 84 - 86, The style
object containerLastOdd is empty dead code; either implement the intended styles
or remove it and stop referencing it in the Pressable conditional. Remove the
containerLastOdd key from the styles object and update the Pressable style
array/conditional where containerLastOdd is referenced (e.g., the conditional on
line ~42 that applies containerLastOdd) to only include actual styles like
container or containerFullWidth, or alternatively add the missing style rules
inside containerLastOdd if a distinct last-odd layout is required; ensure no
references to containerLastOdd remain if you remove it.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,130 @@
+/**
+ * HealthMetricCard - Health Summary グリッド用カード
+ * 白背景で浮遊感のある美しいカードデザイン（Apple Design Award品質）
+ */
+
+import React from 'react';
+import { View, Text, Pressable, StyleSheet } from 'react-native';
+import { Check, AlertCircle } from 'lucide-react-native';
+import { colors } from '../theme';
+
+export type IconType = 'activity' | 'heart' | 'wind' | 'droplet' | 'thermometer';
+export type MetricStatus = 'in-range' | 'out-of-range';
+
+interface HealthMetricCardProps {
+  id: string;
+  name: string;
+  value: number | string;
+  unit: string;
+  status: MetricStatus;
+  statusLabel: string;
+  iconType: IconType;
+  isLastOdd?: boolean;
+  onPress?: () => void;
+}
+
+export const HealthMetricCard: React.FC<HealthMetricCardProps> = ({
+  name,
+  value,
+  unit,
+  status,
+  statusLabel,
+  isLastOdd = false,
+  onPress,
+}) => {
+  const isOutOfRange = status === 'out-of-range';
+
+  return (
+    <Pressable
+      onPress={onPress}
+      style={({ pressed }) => [
+        styles.container,
+        isLastOdd && styles.containerLastOdd,
+        pressed && styles.pressed,
+      ]}
+    >
+      {/* Label - テキストのみ、アイコンなし */}
+      <Text style={styles.label}>{name.toUpperCase()}</Text>
+
+      {/* Value */}
+      <View style={styles.valueRow}>
+        <Text style={styles.value}>{value}</Text>
+        <Text style={styles.unit}>{unit}</Text>
+      </View>
+
+      {/* Status Pill */}
+      <View style={styles.statusRow}>
+        {isOutOfRange ? (
+          <AlertCircle size={14} strokeWidth={2.5} color={colors.amber[500]} />
+        ) : (
+          <Check size={14} strokeWidth={3} color={colors.emerald[500]} />
+        )}
+        <Text style={[styles.statusText, isOutOfRange ? styles.statusOutOfRange : styles.statusInRange]}>
+          {statusLabel}
+        </Text>
+      </View>
+    </Pressable>
+  );
+};
+
+const styles = StyleSheet.create({
+  container: {
+    backgroundColor: '#FFFFFF',
+    borderRadius: 20,
+    padding: 16,
+    minHeight: 130,
+    justifyContent: 'space-between',
+    // 浮遊感のある強いシャドウ
+    shadowColor: '#000',
+    shadowOffset: { width: 0, height: 8 },
+    shadowOpacity: 0.12,
+    shadowRadius: 16,
+    elevation: 8,
+  },
+  containerLastOdd: {
+    // Full width variant uses same styling
+  },



================================================================================
ファイル: app/src/components/RhythmInteractiveChart.tsx
行: 586 (開始行: 1)
コミット: a35fee7
作成日時: 2026-01-07T07:52:34Z
================================================================================

【コメント】
_🛠️ Refactor suggestion_ | _🟠 Major_

**File exceeds 400-line limit - consider component extraction.**

At 586 lines, this file exceeds the 400-line guideline. Consider extracting reusable parts:

1. **Path generation utilities** (`generateSmoothPath`, Catmull-Rom logic) → `utils/chartPathUtils.ts`
2. **Tooltip component** → `ChartTooltip.tsx`
3. **Annotation badges** (Peak Focus, Afternoon Dip) → `ChartAnnotationBadge.tsx`

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/components/RhythmInteractiveChart.tsx around lines 1 - 586, The
RhythmInteractiveChart component file is too large (586 lines); extract reusable
logic and UI into smaller modules: move the Catmull-Rom to Bezier logic and
generateSmoothPath into a new utils module (e.g., export function
generateSmoothPath in utils/chartPathUtils.ts and update imports in
RhythmInteractiveChart), extract the tooltip block (the Animated.View with
tooltipInner, tooltipTime, tooltipEnergy* styles and related state props
touchedIndex, touchedPoint, tooltip animated values and style) into a
ChartTooltip.tsx component that accepts props (point, index, data, animated
styles) and replace the inline JSX with <ChartTooltip ... />, and extract the
annotation badge JSX (Peak/Afternoon Dip rendering and positioning logic) into a
ChartAnnotationBadge.tsx component that accepts props (annotation, type, width,
isDragging) and replace those inline blocks; update RhythmInteractiveChart to
import the new modules and remove the moved styles into either respective
components or a shared styles file to keep RhythmInteractiveChart under 400
lines.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,586 @@
+/**
+ * RhythmInteractiveChart - タッチ対応リズムチャート
+ * スムーズなドラッグ操作でツールチップ表示
+ */
+
+import React, { useState, useCallback, useMemo, useRef } from 'react';
+import { View, Text, Dimensions, StyleSheet, PanResponder } from 'react-native';
+import Svg, {
+  Path,
+  Defs,
+  LinearGradient,
+  Stop,
+  Line,
+  Circle,
+  Text as SvgText,
+} from 'react-native-svg';
+import Animated, {
+  useSharedValue,
+  useAnimatedStyle,
+  withSpring,
+  withTiming,
+  runOnJS,
+} from 'react-native-reanimated';
+
+import { Colors } from '../theme';
+
+export interface RhythmDataPoint {
+  time: string;
+  hour: number;
+  energy: number;
+  label?: string;
+}
+
+interface RhythmInteractiveChartProps {
+  data: RhythmDataPoint[];
+  currentHour: number;
+  width?: number;
+  height?: number;
+}
+
+const PADDING_LEFT = 16;
+const PADDING_RIGHT = 16;
+const PADDING_TOP = 50;
+const PADDING_BOTTOM = 35;
+
+export const RhythmInteractiveChart: React.FC<RhythmInteractiveChartProps> = ({
+  data,
+  currentHour,
+  width: propWidth,
+  height = 280,
+}) => {
+  const screenWidth = Dimensions.get('window').width;
+  const width = propWidth ?? screenWidth;
+  const chartWidth = width - PADDING_LEFT - PADDING_RIGHT;
+  const chartHeight = height - PADDING_TOP - PADDING_BOTTOM;
+
+  const [touchedIndex, setTouchedIndex] = useState<number | null>(null);
+  const [isDragging, setIsDragging] = useState(false);
+  const hideTimeoutRef = useRef<ReturnType<typeof setTimeout> | null>(null);
+
+  // アニメーション用
+  const tooltipOpacity = useSharedValue(0);
+  const tooltipScale = useSharedValue(0.8);
+  const cursorOpacity = useSharedValue(0);
+
+  // データポイントの座標を計算
+  const points = useMemo(() => {
+    if (data.length === 0) return [];
+    const minHour = Math.min(...data.map((d) => d.hour));
+    const maxHour = Math.max(...data.map((d) => d.hour));
+    const hourRange = maxHour - minHour || 1;
+
+    return data.map((d, index) => {
+      const x = PADDING_LEFT + ((d.hour - minHour) / hourRange) * chartWidth;
+      const y = PADDING_TOP + chartHeight - (d.energy / 100) * chartHeight;
+      return { x, y, data: d, index };
+    });
+  }, [data, chartWidth, chartHeight]);
+
+  // Catmull-Rom スプラインを三次ベジェ曲線に変換して滑らかな曲線を生成
+  const generateSmoothPath = useCallback(
+    (pts: typeof points, closePath: boolean) => {
+      if (pts.length < 2) return '';
+
+      // Catmull-Rom to Bezier conversion
+      const tension = 0.3; // 曲線の張り具合（0-1、小さいほど滑らか）
+
+      let path = `M ${pts[0].x} ${pts[0].y}`;
+
+      for (let i = 0; i < pts.length - 1; i++) {
+        const p0 = pts[Math.max(0, i - 1)];
+        const p1 = pts[i];
+        const p2 = pts[i + 1];
+        const p3 = pts[Math.min(pts.length - 1, i + 2)];
+
+        // コントロールポイントを計算
+        const cp1x = p1.x + ((p2.x - p0.x) * tension) / 2;
+        const cp1y = p1.y + ((p2.y - p0.y) * tension) / 2;
+        const cp2x = p2.x - ((p3.x - p1.x) * tension) / 2;
+        const cp2y = p2.y - ((p3.y - p1.y) * tension) / 2;
+
+        path += ` C ${cp1x} ${cp1y}, ${cp2x} ${cp2y}, ${p2.x} ${p2.y}`;
+      }
+
+      if (closePath) {
+        // エリアを閉じる
+        path += ` L ${pts[pts.length - 1].x} ${PADDING_TOP + chartHeight}`;
+        path += ` L ${pts[0].x} ${PADDING_TOP + chartHeight}`;
+        path += ' Z';
+      }
+
+      return path;
+    },
+    [chartHeight]
+  );
+
+  // SVGパスを生成（スムーズな曲線）
+  const areaPath = useMemo(() => {
+    return generateSmoothPath(points, true);
+  }, [points, generateSmoothPath]);
+
+  // ラインパスを生成
+  const linePath = useMemo(() => {
+    return generateSmoothPath(points, false);
+  }, [points, generateSmoothPath]);
+
+  // 現在時刻の位置を計算
+  const currentTimePosition = useMemo(() => {
+    if (data.length === 0) return { x: 0, y: 0, energy: 0 };
+    const minHour = Math.min(...data.map((d) => d.hour));
+    const maxHour = Math.max(...data.map((d) => d.hour));
+    const hourRange = maxHour - minHour || 1;
+
+    const x = PADDING_LEFT + ((currentHour - minHour) / hourRange) * chartWidth;
+
+    // 現在時刻に最も近いデータポイントのエネルギーを取得
+    const closestPoint = data.reduce((prev, curr) => {
+      return Math.abs(curr.hour - currentHour) < Math.abs(prev.hour - currentHour) ? curr : prev;
+    });
+    const y = PADDING_TOP + chartHeight - (closestPoint.energy / 100) * chartHeight;
+
+    return { x, y, energy: closestPoint.energy };
+  }, [data, currentHour, chartWidth, chartHeight]);
+
+  // ピークとディップの位置を取得
+  const annotations = useMemo(() => {
+    const peak = points.find((p) => p.data.label === 'Peak' || p.data.label === 'ピーク');
+    const dip = points.find((p) => p.data.label === 'Dip' || p.data.label === '低迷期');
+    return { peak, dip };
+  }, [points]);
+
+  // タッチ位置から最も近いポイントを見つける
+  const findClosestPoint = useCallback(
+    (touchX: number) => {
+      if (points.length === 0) return null;
+
+      let closest = points[0];
+      let minDist = Math.abs(touchX - points[0].x);
+
+      for (const point of points) {
+        const dist = Math.abs(touchX - point.x);
+        if (dist < minDist) {
+          minDist = dist;
+          closest = point;
+        }
+      }
+
+      return closest;
+    },
+    [points]
+  );
+
+  // ツールチップを表示
+  const showTooltip = useCallback(
+    (index: number) => {
+      if (hideTimeoutRef.current) {
+        clearTimeout(hideTimeoutRef.current);
+        hideTimeoutRef.current = null;
+      }
+      setTouchedIndex(index);
+      tooltipOpacity.value = withSpring(1, { damping: 20, stiffness: 300 });
+      tooltipScale.value = withSpring(1, { damping: 15, stiffness: 400 });
+      cursorOpacity.value = withTiming(1, { duration: 100 });
+    },
+    [tooltipOpacity, tooltipScale, cursorOpacity]
+  );
+
+  // ツールチップを非表示
+  const hideTooltip = useCallback(() => {
+    hideTimeoutRef.current = setTimeout(() => {
+      tooltipOpacity.value = withTiming(0, { duration: 200 });
+      tooltipScale.value = withTiming(0.8, { duration: 200 });
+      cursorOpacity.value = withTiming(0, { duration: 150 });
+      setTouchedIndex(null);
+      setIsDragging(false);
+    }, 1500);
+  }, [tooltipOpacity, tooltipScale, cursorOpacity]);
+
+  // PanResponder でドラッグ操作を処理
+  const panResponder = useMemo(
+    () =>
+      PanResponder.create({
+        onStartShouldSetPanResponder: () => true,
+        onMoveShouldSetPanResponder: () => true,
+        onPanResponderGrant: (evt) => {
+          const touchX = evt.nativeEvent.locationX;
+          const closest = findClosestPoint(touchX);
+          if (closest) {
+            runOnJS(setIsDragging)(true);
+            runOnJS(showTooltip)(closest.index);
+          }
+        },
+        onPanResponderMove: (evt) => {
+          const touchX = evt.nativeEvent.locationX;
+          const closest = findClosestPoint(touchX);
+          if (closest && closest.index !== touchedIndex) {
+            runOnJS(showTooltip)(closest.index);
+          }
+        },
+        onPanResponderRelease: () => {
+          runOnJS(hideTooltip)();
+        },
+        onPanResponderTerminate: () => {
+          runOnJS(hideTooltip)();
+        },
+      }),
+    [findClosestPoint, showTooltip, hideTooltip, touchedIndex]
+  );
+
+  // X軸ラベル
+  const xAxisLabels = useMemo(() => {
+    if (data.length === 0) return [];
+    const labels = ['6 AM', '12 PM', '6 PM', '12 AM'];
+    const minHour = Math.min(...data.map((d) => d.hour));
+    const maxHour = Math.max(...data.map((d) => d.hour));
+    const hourRange = maxHour - minHour || 1;
+
+    return labels.map((label, index) => {
+      const hour = 6 + index * 6;
+      const x = PADDING_LEFT + ((hour - minHour) / hourRange) * chartWidth;
+      return { label, x };
+    });
+  }, [data, chartWidth]);
+
+  // 現在時刻をフォーマット
+  const formatCurrentTime = () => {
+    const hours = Math.floor(currentHour);
+    const minutes = Math.round((currentHour - hours) * 60);
+    const period = hours >= 12 ? 'PM' : 'AM';
+    const displayHours = hours > 12 ? hours - 12 : hours === 0 ? 12 : hours;
+    return `${displayHours}:${minutes.toString().padStart(2, '0')} ${period}`;
+  };
+
+  // アニメーションスタイル
+  const tooltipAnimatedStyle = useAnimatedStyle(() => ({
+    opacity: tooltipOpacity.value,
+    transform: [{ scale: tooltipScale.value }],
+  }));
+
+  const cursorAnimatedStyle = useAnimatedStyle(() => ({
+    opacity: cursorOpacity.value,
+  }));
+
+  // タッチ中のポイントのY座標を計算
+  const touchedPoint = touchedIndex !== null ? points[touchedIndex] : null;
+
+  return (
+    <View style={styles.container}>
+      <View style={styles.chartContainer} {...panResponder.panHandlers}>
+        <Svg width={width} height={height}>
+          <Defs>
+            <LinearGradient id="areaGradient" x1="0" y1="0" x2="0" y2="1">
+              <Stop offset="0%" stopColor={Colors.indigo[400]} stopOpacity={0.3} />
+              <Stop offset="100%" stopColor={Colors.indigo[400]} stopOpacity={0.02} />
+            </LinearGradient>
+            <LinearGradient id="lineGradient" x1="0" y1="0" x2="1" y2="0">
+              <Stop offset="0%" stopColor={Colors.indigo[400]} />
+              <Stop offset="100%" stopColor={Colors.indigo[500]} />
+            </LinearGradient>
+            <LinearGradient id="cursorGradient" x1="0" y1="0" x2="0" y2="1">
+              <Stop offset="0%" stopColor={Colors.indigo[400]} stopOpacity={0.8} />
+              <Stop offset="100%" stopColor={Colors.indigo[400]} stopOpacity={0.1} />
+            </LinearGradient>
+          </Defs>
+
+          {/* エリア塗りつぶし */}
+          <Path d={areaPath} fill="url(#areaGradient)" />
+
+          {/* ライン */}
+          <Path d={linePath} stroke="url(#lineGradient)" strokeWidth={3.5} fill="none" />
+
+          {/* 現在時刻のインジケーター */}
+          {!isDragging && (
+            <>
+              <Line
+                x1={currentTimePosition.x}
+                y1={PADDING_TOP}
+                x2={currentTimePosition.x}
+                y2={PADDING_TOP + chartHeight}
+                stroke={Colors.amber[400]}
+                strokeWidth={2}
+                strokeDasharray="6 4"
+              />
+              <Circle
+                cx={currentTimePosition.x}
+                cy={currentTimePosition.y}
+                r={8}
+                fill={Colors.amber[400]}
+                stroke="white"
+                strokeWidth={3}
+              />
+            </>
+          )}
+
+          {/* ピークポイント */}
+          {annotations.peak && !isDragging && (
+            <Circle
+              cx={annotations.peak.x}
+              cy={annotations.peak.y}
+              r={5}
+              fill={Colors.indigo[500]}
+              stroke="white"
+              strokeWidth={2}
+            />
+          )}
+
+          {/* ディップポイント */}
+          {annotations.dip && !isDragging && (
+            <Circle
+              cx={annotations.dip.x}
+              cy={annotations.dip.y}
+              r={5}
+              fill={Colors.stone[400]}
+              stroke="white"
+              strokeWidth={2}
+            />
+          )}
+
+          {/* タッチ時のカーソル線 */}
+          {touchedPoint && (
+            <Line
+              x1={touchedPoint.x}
+              y1={PADDING_TOP}
+              x2={touchedPoint.x}
+              y2={PADDING_TOP + chartHeight}
+              stroke="url(#cursorGradient)"
+              strokeWidth={2}
+            />
+          )}
+
+          {/* タッチ時のドット（曲線上） */}
+          {touchedPoint && (
+            <>
+              {/* 外側のグロー */}
+              <Circle
+                cx={touchedPoint.x}
+                cy={touchedPoint.y}
+                r={16}
+                fill={Colors.indigo[400]}
+                opacity={0.2}
+              />
+              {/* 中間のリング */}
+              <Circle
+                cx={touchedPoint.x}
+                cy={touchedPoint.y}
+                r={10}
+                fill={Colors.indigo[400]}
+                opacity={0.4}
+              />
+              {/* 内側のドット */}
+              <Circle
+                cx={touchedPoint.x}
+                cy={touchedPoint.y}
+                r={6}
+                fill={Colors.indigo[500]}
+                stroke="white"
+                strokeWidth={2}
+              />
+            </>
+          )}
+
+          {/* X軸ラベル */}
+          {xAxisLabels.map((item, index) => (
+            <SvgText
+              key={index}
+              x={item.x}
+              y={height - 10}
+              fontSize={11}
+              fontWeight="500"
+              fill={Colors.stone[400]}
+              textAnchor="middle"
+            >
+              {item.label}
+            </SvgText>
+          ))}
+        </Svg>
+
+        {/* 「Now」ラベル - チャート上部に表示 */}
+        {!isDragging && (
+          <View
+            style={[
+              styles.nowLabel,
+              {
+                left: Math.max(8, Math.min(currentTimePosition.x - 55, width - 120)),
+                top: 8,
+              },
+            ]}
+          >
+            <View style={styles.nowLabelInner}>
+              <Text style={styles.nowLabelText}>Now {formatCurrentTime()}</Text>
+            </View>
+          </View>
+        )}
+
+        {/* ピークフォーカスバッジ - ポイントの下に表示 */}
+        {annotations.peak && !isDragging && (
+          <View
+            style={[
+              styles.badge,
+              {
+                left: Math.max(8, Math.min(annotations.peak.x - 40, width - 90)),
+                top: annotations.peak.y + 15,
+              },
+            ]}
+          >
+            <View style={[styles.badgeInner, styles.peakBadge]}>
+              <Text style={styles.peakBadgeText}>Peak Focus</Text>
+            </View>
+          </View>
+        )}
+
+        {/* アフタヌーンディップバッジ - ポイントの下に表示 */}
+        {annotations.dip && !isDragging && (
+          <View
+            style={[
+              styles.badge,
+              {
+                left: Math.max(8, Math.min(annotations.dip.x - 48, width - 110)),
+                top: annotations.dip.y + 15,
+              },
+            ]}
+          >
+            <View style={[styles.badgeInner, styles.dipBadge]}>
+              <Text style={styles.dipBadgeText}>Afternoon Dip</Text>
+            </View>
+          </View>
+        )}
+
+        {/* タッチ時のツールチップ */}
+        {touchedPoint && touchedIndex !== null && (
+          <Animated.View
+            style={[
+              styles.tooltip,
+              {
+                left: Math.max(8, Math.min(touchedPoint.x - 50, width - 110)),
+                top: Math.max(8, touchedPoint.y - 75),
+              },
+              tooltipAnimatedStyle,
+            ]}
+          >
+            <View style={styles.tooltipInner}>
+              <Text style={styles.tooltipTime}>{data[touchedIndex].time}</Text>
+              <View style={styles.tooltipEnergyContainer}>
+                <View style={styles.tooltipEnergyBar}>
+                  <View
+                    style={[
+                      styles.tooltipEnergyFill,
+                      { width: `${data[touchedIndex].energy}%` },
+                    ]}
+                  />
+                </View>
+                <Text style={styles.tooltipEnergy}>{data[touchedIndex].energy}%</Text>
+              </View>
+            </View>
+          </Animated.View>
+        )}
+      </View>
+    </View>
+  );
+};
+
+const styles = StyleSheet.create({
+  container: {
+    flex: 1,
+  },
+  chartContainer: {
+    position: 'relative',
+  },
+  nowLabel: {
+    position: 'absolute',
+  },
+  nowLabelInner: {
+    backgroundColor: Colors.amber[400],
+    paddingHorizontal: 12,
+    paddingVertical: 6,
+    borderRadius: 20,
+    shadowColor: Colors.amber[500],
+    shadowOffset: { width: 0, height: 3 },
+    shadowOpacity: 0.35,
+    shadowRadius: 6,
+    elevation: 4,
+  },
+  nowLabelText: {
+    color: Colors.white,
+    fontSize: 12,
+    fontWeight: '700',
+  },
+  badge: {
+    position: 'absolute',
+  },
+  badgeInner: {
+    paddingHorizontal: 10,
+    paddingVertical: 5,
+    borderRadius: 14,
+    borderWidth: 1,
+    shadowColor: '#000',
+    shadowOffset: { width: 0, height: 1 },
+    shadowOpacity: 0.08,
+    shadowRadius: 3,
+    elevation: 2,
+  },
+  peakBadge: {
+    backgroundColor: Colors.indigo[50],
+    borderColor: Colors.indigo[100],
+  },
+  peakBadgeText: {
+    color: Colors.indigo[600],
+    fontSize: 11,
+    fontWeight: '600',
+  },
+  dipBadge: {
+    backgroundColor: Colors.stone[50],
+    borderColor: Colors.stone[200],
+  },
+  dipBadgeText: {
+    color: Colors.stone[500],
+    fontSize: 11,
+    fontWeight: '500',
+  },
+  tooltip: {
+    position: 'absolute',
+  },
+  tooltipInner: {
+    backgroundColor: 'rgba(255, 255, 255, 0.98)',
+    paddingVertical: 12,
+    paddingHorizontal: 16,
+    borderRadius: 16,
+    borderWidth: 1,
+    borderColor: Colors.stone[100],
+    alignItems: 'center',
+    shadowColor: Colors.indigo[500],
+    shadowOffset: { width: 0, height: 8 },
+    shadowOpacity: 0.2,
+    shadowRadius: 20,
+    elevation: 8,
+    minWidth: 100,
+  },
+  tooltipTime: {
+    color: Colors.stone[800],
+    fontSize: 15,
+    fontWeight: '700',
+    marginBottom: 8,
+  },
+  tooltipEnergyContainer: {
+    flexDirection: 'row',
+    alignItems: 'center',
+    gap: 8,
+  },
+  tooltipEnergyBar: {
+    width: 50,
+    height: 6,
+    backgroundColor: Colors.stone[100],
+    borderRadius: 3,
+    overflow: 'hidden',
+  },
+  tooltipEnergyFill: {
+    height: '100%',
+    backgroundColor: Colors.indigo[500],
+    borderRadius: 3,
+  },
+  tooltipEnergy: {
+    color: Colors.indigo[500],
+    fontSize: 13,
+    fontWeight: '700',
+  },
+});



================================================================================
ファイル: app/src/components/RhythmInteractiveChart.tsx
行: 115 (開始行: 81)
コミット: a35fee7
作成日時: 2026-01-07T07:52:34Z
================================================================================

【コメント】
_🛠️ Refactor suggestion_ | _🟠 Major_

**Add explicit return type to `generateSmoothPath`.**

The callback lacks an explicit return type annotation.


<details>
<summary>Suggested fix</summary>

```diff
   const generateSmoothPath = useCallback(
-    (pts: typeof points, closePath: boolean) => {
+    (pts: typeof points, closePath: boolean): string => {
       if (pts.length < 2) return '';
       // ... rest of implementation
     },
     [chartHeight]
   );
```
</details>

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/components/RhythmInteractiveChart.tsx around lines 81 - 115, The
generateSmoothPath callback is missing an explicit return type; update its
signature so TypeScript knows it returns a string (e.g., annotate the arrow
function with : string), keeping the existing parameters (pts: typeof points,
closePath: boolean) and ensuring the early-return still returns '' and the final
return returns a string; leave the useCallback dependencies (chartHeight)
unchanged.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,586 @@
+/**
+ * RhythmInteractiveChart - タッチ対応リズムチャート
+ * スムーズなドラッグ操作でツールチップ表示
+ */
+
+import React, { useState, useCallback, useMemo, useRef } from 'react';
+import { View, Text, Dimensions, StyleSheet, PanResponder } from 'react-native';
+import Svg, {
+  Path,
+  Defs,
+  LinearGradient,
+  Stop,
+  Line,
+  Circle,
+  Text as SvgText,
+} from 'react-native-svg';
+import Animated, {
+  useSharedValue,
+  useAnimatedStyle,
+  withSpring,
+  withTiming,
+  runOnJS,
+} from 'react-native-reanimated';
+
+import { Colors } from '../theme';
+
+export interface RhythmDataPoint {
+  time: string;
+  hour: number;
+  energy: number;
+  label?: string;
+}
+
+interface RhythmInteractiveChartProps {
+  data: RhythmDataPoint[];
+  currentHour: number;
+  width?: number;
+  height?: number;
+}
+
+const PADDING_LEFT = 16;
+const PADDING_RIGHT = 16;
+const PADDING_TOP = 50;
+const PADDING_BOTTOM = 35;
+
+export const RhythmInteractiveChart: React.FC<RhythmInteractiveChartProps> = ({
+  data,
+  currentHour,
+  width: propWidth,
+  height = 280,
+}) => {
+  const screenWidth = Dimensions.get('window').width;
+  const width = propWidth ?? screenWidth;
+  const chartWidth = width - PADDING_LEFT - PADDING_RIGHT;
+  const chartHeight = height - PADDING_TOP - PADDING_BOTTOM;
+
+  const [touchedIndex, setTouchedIndex] = useState<number | null>(null);
+  const [isDragging, setIsDragging] = useState(false);
+  const hideTimeoutRef = useRef<ReturnType<typeof setTimeout> | null>(null);
+
+  // アニメーション用
+  const tooltipOpacity = useSharedValue(0);
+  const tooltipScale = useSharedValue(0.8);
+  const cursorOpacity = useSharedValue(0);
+
+  // データポイントの座標を計算
+  const points = useMemo(() => {
+    if (data.length === 0) return [];
+    const minHour = Math.min(...data.map((d) => d.hour));
+    const maxHour = Math.max(...data.map((d) => d.hour));
+    const hourRange = maxHour - minHour || 1;
+
+    return data.map((d, index) => {
+      const x = PADDING_LEFT + ((d.hour - minHour) / hourRange) * chartWidth;
+      const y = PADDING_TOP + chartHeight - (d.energy / 100) * chartHeight;
+      return { x, y, data: d, index };
+    });
+  }, [data, chartWidth, chartHeight]);
+
+  // Catmull-Rom スプラインを三次ベジェ曲線に変換して滑らかな曲線を生成
+  const generateSmoothPath = useCallback(
+    (pts: typeof points, closePath: boolean) => {
+      if (pts.length < 2) return '';
+
+      // Catmull-Rom to Bezier conversion
+      const tension = 0.3; // 曲線の張り具合（0-1、小さいほど滑らか）
+
+      let path = `M ${pts[0].x} ${pts[0].y}`;
+
+      for (let i = 0; i < pts.length - 1; i++) {
+        const p0 = pts[Math.max(0, i - 1)];
+        const p1 = pts[i];
+        const p2 = pts[i + 1];
+        const p3 = pts[Math.min(pts.length - 1, i + 2)];
+
+        // コントロールポイントを計算
+        const cp1x = p1.x + ((p2.x - p0.x) * tension) / 2;
+        const cp1y = p1.y + ((p2.y - p0.y) * tension) / 2;
+        const cp2x = p2.x - ((p3.x - p1.x) * tension) / 2;
+        const cp2y = p2.y - ((p3.y - p1.y) * tension) / 2;
+
+        path += ` C ${cp1x} ${cp1y}, ${cp2x} ${cp2y}, ${p2.x} ${p2.y}`;
+      }
+
+      if (closePath) {
+        // エリアを閉じる
+        path += ` L ${pts[pts.length - 1].x} ${PADDING_TOP + chartHeight}`;
+        path += ` L ${pts[0].x} ${PADDING_TOP + chartHeight}`;
+        path += ' Z';
+      }
+
+      return path;
+    },
+    [chartHeight]
+  );



================================================================================
ファイル: app/src/components/RhythmInteractiveChart.tsx
行: 171 (開始行: 153)
コミット: a35fee7
作成日時: 2026-01-07T07:52:34Z
================================================================================

【コメント】
_🛠️ Refactor suggestion_ | _🟠 Major_

**Add explicit return type.**


<details>
<summary>Suggested fix</summary>

```diff
   const findClosestPoint = useCallback(
-    (touchX: number) => {
+    (touchX: number): (typeof points)[number] | null => {
       if (points.length === 0) return null;
       // ...
     },
     [points]
   );
```
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
  const findClosestPoint = useCallback(
    (touchX: number): (typeof points)[number] | null => {
      if (points.length === 0) return null;

      let closest = points[0];
      let minDist = Math.abs(touchX - points[0].x);

      for (const point of points) {
        const dist = Math.abs(touchX - point.x);
        if (dist < minDist) {
          minDist = dist;
          closest = point;
        }
      }

      return closest;
    },
    [points]
  );
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/components/RhythmInteractiveChart.tsx around lines 153 - 171, The
findClosestPoint callback currently relies on type inference; add an explicit
return type (e.g., Point | null) to the function signature to make expected
output clear and prevent widening or inference bugs—update the declaration of
findClosestPoint to include the return type and ensure the Point type (or
appropriate interface) is imported/visible in the file so the function signature
reads with the explicit return type.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,586 @@
+/**
+ * RhythmInteractiveChart - タッチ対応リズムチャート
+ * スムーズなドラッグ操作でツールチップ表示
+ */
+
+import React, { useState, useCallback, useMemo, useRef } from 'react';
+import { View, Text, Dimensions, StyleSheet, PanResponder } from 'react-native';
+import Svg, {
+  Path,
+  Defs,
+  LinearGradient,
+  Stop,
+  Line,
+  Circle,
+  Text as SvgText,
+} from 'react-native-svg';
+import Animated, {
+  useSharedValue,
+  useAnimatedStyle,
+  withSpring,
+  withTiming,
+  runOnJS,
+} from 'react-native-reanimated';
+
+import { Colors } from '../theme';
+
+export interface RhythmDataPoint {
+  time: string;
+  hour: number;
+  energy: number;
+  label?: string;
+}
+
+interface RhythmInteractiveChartProps {
+  data: RhythmDataPoint[];
+  currentHour: number;
+  width?: number;
+  height?: number;
+}
+
+const PADDING_LEFT = 16;
+const PADDING_RIGHT = 16;
+const PADDING_TOP = 50;
+const PADDING_BOTTOM = 35;
+
+export const RhythmInteractiveChart: React.FC<RhythmInteractiveChartProps> = ({
+  data,
+  currentHour,
+  width: propWidth,
+  height = 280,
+}) => {
+  const screenWidth = Dimensions.get('window').width;
+  const width = propWidth ?? screenWidth;
+  const chartWidth = width - PADDING_LEFT - PADDING_RIGHT;
+  const chartHeight = height - PADDING_TOP - PADDING_BOTTOM;
+
+  const [touchedIndex, setTouchedIndex] = useState<number | null>(null);
+  const [isDragging, setIsDragging] = useState(false);
+  const hideTimeoutRef = useRef<ReturnType<typeof setTimeout> | null>(null);
+
+  // アニメーション用
+  const tooltipOpacity = useSharedValue(0);
+  const tooltipScale = useSharedValue(0.8);
+  const cursorOpacity = useSharedValue(0);
+
+  // データポイントの座標を計算
+  const points = useMemo(() => {
+    if (data.length === 0) return [];
+    const minHour = Math.min(...data.map((d) => d.hour));
+    const maxHour = Math.max(...data.map((d) => d.hour));
+    const hourRange = maxHour - minHour || 1;
+
+    return data.map((d, index) => {
+      const x = PADDING_LEFT + ((d.hour - minHour) / hourRange) * chartWidth;
+      const y = PADDING_TOP + chartHeight - (d.energy / 100) * chartHeight;
+      return { x, y, data: d, index };
+    });
+  }, [data, chartWidth, chartHeight]);
+
+  // Catmull-Rom スプラインを三次ベジェ曲線に変換して滑らかな曲線を生成
+  const generateSmoothPath = useCallback(
+    (pts: typeof points, closePath: boolean) => {
+      if (pts.length < 2) return '';
+
+      // Catmull-Rom to Bezier conversion
+      const tension = 0.3; // 曲線の張り具合（0-1、小さいほど滑らか）
+
+      let path = `M ${pts[0].x} ${pts[0].y}`;
+
+      for (let i = 0; i < pts.length - 1; i++) {
+        const p0 = pts[Math.max(0, i - 1)];
+        const p1 = pts[i];
+        const p2 = pts[i + 1];
+        const p3 = pts[Math.min(pts.length - 1, i + 2)];
+
+        // コントロールポイントを計算
+        const cp1x = p1.x + ((p2.x - p0.x) * tension) / 2;
+        const cp1y = p1.y + ((p2.y - p0.y) * tension) / 2;
+        const cp2x = p2.x - ((p3.x - p1.x) * tension) / 2;
+        const cp2y = p2.y - ((p3.y - p1.y) * tension) / 2;
+
+        path += ` C ${cp1x} ${cp1y}, ${cp2x} ${cp2y}, ${p2.x} ${p2.y}`;
+      }
+
+      if (closePath) {
+        // エリアを閉じる
+        path += ` L ${pts[pts.length - 1].x} ${PADDING_TOP + chartHeight}`;
+        path += ` L ${pts[0].x} ${PADDING_TOP + chartHeight}`;
+        path += ' Z';
+      }
+
+      return path;
+    },
+    [chartHeight]
+  );
+
+  // SVGパスを生成（スムーズな曲線）
+  const areaPath = useMemo(() => {
+    return generateSmoothPath(points, true);
+  }, [points, generateSmoothPath]);
+
+  // ラインパスを生成
+  const linePath = useMemo(() => {
+    return generateSmoothPath(points, false);
+  }, [points, generateSmoothPath]);
+
+  // 現在時刻の位置を計算
+  const currentTimePosition = useMemo(() => {
+    if (data.length === 0) return { x: 0, y: 0, energy: 0 };
+    const minHour = Math.min(...data.map((d) => d.hour));
+    const maxHour = Math.max(...data.map((d) => d.hour));
+    const hourRange = maxHour - minHour || 1;
+
+    const x = PADDING_LEFT + ((currentHour - minHour) / hourRange) * chartWidth;
+
+    // 現在時刻に最も近いデータポイントのエネルギーを取得
+    const closestPoint = data.reduce((prev, curr) => {
+      return Math.abs(curr.hour - currentHour) < Math.abs(prev.hour - currentHour) ? curr : prev;
+    });
+    const y = PADDING_TOP + chartHeight - (closestPoint.energy / 100) * chartHeight;
+
+    return { x, y, energy: closestPoint.energy };
+  }, [data, currentHour, chartWidth, chartHeight]);
+
+  // ピークとディップの位置を取得
+  const annotations = useMemo(() => {
+    const peak = points.find((p) => p.data.label === 'Peak' || p.data.label === 'ピーク');
+    const dip = points.find((p) => p.data.label === 'Dip' || p.data.label === '低迷期');
+    return { peak, dip };
+  }, [points]);
+
+  // タッチ位置から最も近いポイントを見つける
+  const findClosestPoint = useCallback(
+    (touchX: number) => {
+      if (points.length === 0) return null;
+
+      let closest = points[0];
+      let minDist = Math.abs(touchX - points[0].x);
+
+      for (const point of points) {
+        const dist = Math.abs(touchX - point.x);
+        if (dist < minDist) {
+          minDist = dist;
+          closest = point;
+        }
+      }
+
+      return closest;
+    },
+    [points]
+  );



================================================================================
ファイル: app/src/components/RhythmInteractiveChart.tsx
行: 197 (開始行: 174)
コミット: a35fee7
作成日時: 2026-01-07T07:52:34Z
================================================================================

【コメント】
_🛠️ Refactor suggestion_ | _🟠 Major_

**Add explicit return types to tooltip handlers.**


<details>
<summary>Suggested fix</summary>

```diff
   const showTooltip = useCallback(
-    (index: number) => {
+    (index: number): void => {
       // ...
     },
     [tooltipOpacity, tooltipScale, cursorOpacity]
   );

-  const hideTooltip = useCallback(() => {
+  const hideTooltip = useCallback((): void => {
     // ...
   }, [tooltipOpacity, tooltipScale, cursorOpacity]);
```
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
  const showTooltip = useCallback(
    (index: number): void => {
      if (hideTimeoutRef.current) {
        clearTimeout(hideTimeoutRef.current);
        hideTimeoutRef.current = null;
      }
      setTouchedIndex(index);
      tooltipOpacity.value = withSpring(1, { damping: 20, stiffness: 300 });
      tooltipScale.value = withSpring(1, { damping: 15, stiffness: 400 });
      cursorOpacity.value = withTiming(1, { duration: 100 });
    },
    [tooltipOpacity, tooltipScale, cursorOpacity]
  );

  // ツールチップを非表示
  const hideTooltip = useCallback((): void => {
    hideTimeoutRef.current = setTimeout(() => {
      tooltipOpacity.value = withTiming(0, { duration: 200 });
      tooltipScale.value = withTiming(0.8, { duration: 200 });
      cursorOpacity.value = withTiming(0, { duration: 150 });
      setTouchedIndex(null);
      setIsDragging(false);
    }, 1500);
  }, [tooltipOpacity, tooltipScale, cursorOpacity]);
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/components/RhythmInteractiveChart.tsx around lines 174 - 197, The
tooltip handler functions lack explicit return types; update the useCallback
declarations for showTooltip and hideTooltip to include explicit void return
types (e.g., annotate the callback signatures so showTooltip is (index: number):
void and hideTooltip is (): void) to satisfy the type checker and make
intentions clear, leaving the function bodies and dependency arrays unchanged.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,586 @@
+/**
+ * RhythmInteractiveChart - タッチ対応リズムチャート
+ * スムーズなドラッグ操作でツールチップ表示
+ */
+
+import React, { useState, useCallback, useMemo, useRef } from 'react';
+import { View, Text, Dimensions, StyleSheet, PanResponder } from 'react-native';
+import Svg, {
+  Path,
+  Defs,
+  LinearGradient,
+  Stop,
+  Line,
+  Circle,
+  Text as SvgText,
+} from 'react-native-svg';
+import Animated, {
+  useSharedValue,
+  useAnimatedStyle,
+  withSpring,
+  withTiming,
+  runOnJS,
+} from 'react-native-reanimated';
+
+import { Colors } from '../theme';
+
+export interface RhythmDataPoint {
+  time: string;
+  hour: number;
+  energy: number;
+  label?: string;
+}
+
+interface RhythmInteractiveChartProps {
+  data: RhythmDataPoint[];
+  currentHour: number;
+  width?: number;
+  height?: number;
+}
+
+const PADDING_LEFT = 16;
+const PADDING_RIGHT = 16;
+const PADDING_TOP = 50;
+const PADDING_BOTTOM = 35;
+
+export const RhythmInteractiveChart: React.FC<RhythmInteractiveChartProps> = ({
+  data,
+  currentHour,
+  width: propWidth,
+  height = 280,
+}) => {
+  const screenWidth = Dimensions.get('window').width;
+  const width = propWidth ?? screenWidth;
+  const chartWidth = width - PADDING_LEFT - PADDING_RIGHT;
+  const chartHeight = height - PADDING_TOP - PADDING_BOTTOM;
+
+  const [touchedIndex, setTouchedIndex] = useState<number | null>(null);
+  const [isDragging, setIsDragging] = useState(false);
+  const hideTimeoutRef = useRef<ReturnType<typeof setTimeout> | null>(null);
+
+  // アニメーション用
+  const tooltipOpacity = useSharedValue(0);
+  const tooltipScale = useSharedValue(0.8);
+  const cursorOpacity = useSharedValue(0);
+
+  // データポイントの座標を計算
+  const points = useMemo(() => {
+    if (data.length === 0) return [];
+    const minHour = Math.min(...data.map((d) => d.hour));
+    const maxHour = Math.max(...data.map((d) => d.hour));
+    const hourRange = maxHour - minHour || 1;
+
+    return data.map((d, index) => {
+      const x = PADDING_LEFT + ((d.hour - minHour) / hourRange) * chartWidth;
+      const y = PADDING_TOP + chartHeight - (d.energy / 100) * chartHeight;
+      return { x, y, data: d, index };
+    });
+  }, [data, chartWidth, chartHeight]);
+
+  // Catmull-Rom スプラインを三次ベジェ曲線に変換して滑らかな曲線を生成
+  const generateSmoothPath = useCallback(
+    (pts: typeof points, closePath: boolean) => {
+      if (pts.length < 2) return '';
+
+      // Catmull-Rom to Bezier conversion
+      const tension = 0.3; // 曲線の張り具合（0-1、小さいほど滑らか）
+
+      let path = `M ${pts[0].x} ${pts[0].y}`;
+
+      for (let i = 0; i < pts.length - 1; i++) {
+        const p0 = pts[Math.max(0, i - 1)];
+        const p1 = pts[i];
+        const p2 = pts[i + 1];
+        const p3 = pts[Math.min(pts.length - 1, i + 2)];
+
+        // コントロールポイントを計算
+        const cp1x = p1.x + ((p2.x - p0.x) * tension) / 2;
+        const cp1y = p1.y + ((p2.y - p0.y) * tension) / 2;
+        const cp2x = p2.x - ((p3.x - p1.x) * tension) / 2;
+        const cp2y = p2.y - ((p3.y - p1.y) * tension) / 2;
+
+        path += ` C ${cp1x} ${cp1y}, ${cp2x} ${cp2y}, ${p2.x} ${p2.y}`;
+      }
+
+      if (closePath) {
+        // エリアを閉じる
+        path += ` L ${pts[pts.length - 1].x} ${PADDING_TOP + chartHeight}`;
+        path += ` L ${pts[0].x} ${PADDING_TOP + chartHeight}`;
+        path += ' Z';
+      }
+
+      return path;
+    },
+    [chartHeight]
+  );
+
+  // SVGパスを生成（スムーズな曲線）
+  const areaPath = useMemo(() => {
+    return generateSmoothPath(points, true);
+  }, [points, generateSmoothPath]);
+
+  // ラインパスを生成
+  const linePath = useMemo(() => {
+    return generateSmoothPath(points, false);
+  }, [points, generateSmoothPath]);
+
+  // 現在時刻の位置を計算
+  const currentTimePosition = useMemo(() => {
+    if (data.length === 0) return { x: 0, y: 0, energy: 0 };
+    const minHour = Math.min(...data.map((d) => d.hour));
+    const maxHour = Math.max(...data.map((d) => d.hour));
+    const hourRange = maxHour - minHour || 1;
+
+    const x = PADDING_LEFT + ((currentHour - minHour) / hourRange) * chartWidth;
+
+    // 現在時刻に最も近いデータポイントのエネルギーを取得
+    const closestPoint = data.reduce((prev, curr) => {
+      return Math.abs(curr.hour - currentHour) < Math.abs(prev.hour - currentHour) ? curr : prev;
+    });
+    const y = PADDING_TOP + chartHeight - (closestPoint.energy / 100) * chartHeight;
+
+    return { x, y, energy: closestPoint.energy };
+  }, [data, currentHour, chartWidth, chartHeight]);
+
+  // ピークとディップの位置を取得
+  const annotations = useMemo(() => {
+    const peak = points.find((p) => p.data.label === 'Peak' || p.data.label === 'ピーク');
+    const dip = points.find((p) => p.data.label === 'Dip' || p.data.label === '低迷期');
+    return { peak, dip };
+  }, [points]);
+
+  // タッチ位置から最も近いポイントを見つける
+  const findClosestPoint = useCallback(
+    (touchX: number) => {
+      if (points.length === 0) return null;
+
+      let closest = points[0];
+      let minDist = Math.abs(touchX - points[0].x);
+
+      for (const point of points) {
+        const dist = Math.abs(touchX - point.x);
+        if (dist < minDist) {
+          minDist = dist;
+          closest = point;
+        }
+      }
+
+      return closest;
+    },
+    [points]
+  );
+
+  // ツールチップを表示
+  const showTooltip = useCallback(
+    (index: number) => {
+      if (hideTimeoutRef.current) {
+        clearTimeout(hideTimeoutRef.current);
+        hideTimeoutRef.current = null;
+      }
+      setTouchedIndex(index);
+      tooltipOpacity.value = withSpring(1, { damping: 20, stiffness: 300 });
+      tooltipScale.value = withSpring(1, { damping: 15, stiffness: 400 });
+      cursorOpacity.value = withTiming(1, { duration: 100 });
+    },
+    [tooltipOpacity, tooltipScale, cursorOpacity]
+  );
+
+  // ツールチップを非表示
+  const hideTooltip = useCallback(() => {
+    hideTimeoutRef.current = setTimeout(() => {
+      tooltipOpacity.value = withTiming(0, { duration: 200 });
+      tooltipScale.value = withTiming(0.8, { duration: 200 });
+      cursorOpacity.value = withTiming(0, { duration: 150 });
+      setTouchedIndex(null);
+      setIsDragging(false);
+    }, 1500);
+  }, [tooltipOpacity, tooltipScale, cursorOpacity]);



================================================================================
ファイル: app/src/components/RhythmInteractiveChart.tsx
行: 252 (開始行: 246)
コミット: a35fee7
作成日時: 2026-01-07T07:52:34Z
================================================================================

【コメント】
_🛠️ Refactor suggestion_ | _🟠 Major_

**Add explicit return type.**


<details>
<summary>Suggested fix</summary>

```diff
-  const formatCurrentTime = () => {
+  const formatCurrentTime = (): string => {
     const hours = Math.floor(currentHour);
     const minutes = Math.round((currentHour - hours) * 60);
     const period = hours >= 12 ? 'PM' : 'AM';
     const displayHours = hours > 12 ? hours - 12 : hours === 0 ? 12 : hours;
     return `${displayHours}:${minutes.toString().padStart(2, '0')} ${period}`;
   };
```
</details>

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/components/RhythmInteractiveChart.tsx around lines 246 - 252,
formatCurrentTime currently lacks an explicit return type; update its signature
to include a return type of string (e.g., const formatCurrentTime = (): string
=> { ... }) and ensure it still returns the formatted time string using the
existing logic that references currentHour.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,586 @@
+/**
+ * RhythmInteractiveChart - タッチ対応リズムチャート
+ * スムーズなドラッグ操作でツールチップ表示
+ */
+
+import React, { useState, useCallback, useMemo, useRef } from 'react';
+import { View, Text, Dimensions, StyleSheet, PanResponder } from 'react-native';
+import Svg, {
+  Path,
+  Defs,
+  LinearGradient,
+  Stop,
+  Line,
+  Circle,
+  Text as SvgText,
+} from 'react-native-svg';
+import Animated, {
+  useSharedValue,
+  useAnimatedStyle,
+  withSpring,
+  withTiming,
+  runOnJS,
+} from 'react-native-reanimated';
+
+import { Colors } from '../theme';
+
+export interface RhythmDataPoint {
+  time: string;
+  hour: number;
+  energy: number;
+  label?: string;
+}
+
+interface RhythmInteractiveChartProps {
+  data: RhythmDataPoint[];
+  currentHour: number;
+  width?: number;
+  height?: number;
+}
+
+const PADDING_LEFT = 16;
+const PADDING_RIGHT = 16;
+const PADDING_TOP = 50;
+const PADDING_BOTTOM = 35;
+
+export const RhythmInteractiveChart: React.FC<RhythmInteractiveChartProps> = ({
+  data,
+  currentHour,
+  width: propWidth,
+  height = 280,
+}) => {
+  const screenWidth = Dimensions.get('window').width;
+  const width = propWidth ?? screenWidth;
+  const chartWidth = width - PADDING_LEFT - PADDING_RIGHT;
+  const chartHeight = height - PADDING_TOP - PADDING_BOTTOM;
+
+  const [touchedIndex, setTouchedIndex] = useState<number | null>(null);
+  const [isDragging, setIsDragging] = useState(false);
+  const hideTimeoutRef = useRef<ReturnType<typeof setTimeout> | null>(null);
+
+  // アニメーション用
+  const tooltipOpacity = useSharedValue(0);
+  const tooltipScale = useSharedValue(0.8);
+  const cursorOpacity = useSharedValue(0);
+
+  // データポイントの座標を計算
+  const points = useMemo(() => {
+    if (data.length === 0) return [];
+    const minHour = Math.min(...data.map((d) => d.hour));
+    const maxHour = Math.max(...data.map((d) => d.hour));
+    const hourRange = maxHour - minHour || 1;
+
+    return data.map((d, index) => {
+      const x = PADDING_LEFT + ((d.hour - minHour) / hourRange) * chartWidth;
+      const y = PADDING_TOP + chartHeight - (d.energy / 100) * chartHeight;
+      return { x, y, data: d, index };
+    });
+  }, [data, chartWidth, chartHeight]);
+
+  // Catmull-Rom スプラインを三次ベジェ曲線に変換して滑らかな曲線を生成
+  const generateSmoothPath = useCallback(
+    (pts: typeof points, closePath: boolean) => {
+      if (pts.length < 2) return '';
+
+      // Catmull-Rom to Bezier conversion
+      const tension = 0.3; // 曲線の張り具合（0-1、小さいほど滑らか）
+
+      let path = `M ${pts[0].x} ${pts[0].y}`;
+
+      for (let i = 0; i < pts.length - 1; i++) {
+        const p0 = pts[Math.max(0, i - 1)];
+        const p1 = pts[i];
+        const p2 = pts[i + 1];
+        const p3 = pts[Math.min(pts.length - 1, i + 2)];
+
+        // コントロールポイントを計算
+        const cp1x = p1.x + ((p2.x - p0.x) * tension) / 2;
+        const cp1y = p1.y + ((p2.y - p0.y) * tension) / 2;
+        const cp2x = p2.x - ((p3.x - p1.x) * tension) / 2;
+        const cp2y = p2.y - ((p3.y - p1.y) * tension) / 2;
+
+        path += ` C ${cp1x} ${cp1y}, ${cp2x} ${cp2y}, ${p2.x} ${p2.y}`;
+      }
+
+      if (closePath) {
+        // エリアを閉じる
+        path += ` L ${pts[pts.length - 1].x} ${PADDING_TOP + chartHeight}`;
+        path += ` L ${pts[0].x} ${PADDING_TOP + chartHeight}`;
+        path += ' Z';
+      }
+
+      return path;
+    },
+    [chartHeight]
+  );
+
+  // SVGパスを生成（スムーズな曲線）
+  const areaPath = useMemo(() => {
+    return generateSmoothPath(points, true);
+  }, [points, generateSmoothPath]);
+
+  // ラインパスを生成
+  const linePath = useMemo(() => {
+    return generateSmoothPath(points, false);
+  }, [points, generateSmoothPath]);
+
+  // 現在時刻の位置を計算
+  const currentTimePosition = useMemo(() => {
+    if (data.length === 0) return { x: 0, y: 0, energy: 0 };
+    const minHour = Math.min(...data.map((d) => d.hour));
+    const maxHour = Math.max(...data.map((d) => d.hour));
+    const hourRange = maxHour - minHour || 1;
+
+    const x = PADDING_LEFT + ((currentHour - minHour) / hourRange) * chartWidth;
+
+    // 現在時刻に最も近いデータポイントのエネルギーを取得
+    const closestPoint = data.reduce((prev, curr) => {
+      return Math.abs(curr.hour - currentHour) < Math.abs(prev.hour - currentHour) ? curr : prev;
+    });
+    const y = PADDING_TOP + chartHeight - (closestPoint.energy / 100) * chartHeight;
+
+    return { x, y, energy: closestPoint.energy };
+  }, [data, currentHour, chartWidth, chartHeight]);
+
+  // ピークとディップの位置を取得
+  const annotations = useMemo(() => {
+    const peak = points.find((p) => p.data.label === 'Peak' || p.data.label === 'ピーク');
+    const dip = points.find((p) => p.data.label === 'Dip' || p.data.label === '低迷期');
+    return { peak, dip };
+  }, [points]);
+
+  // タッチ位置から最も近いポイントを見つける
+  const findClosestPoint = useCallback(
+    (touchX: number) => {
+      if (points.length === 0) return null;
+
+      let closest = points[0];
+      let minDist = Math.abs(touchX - points[0].x);
+
+      for (const point of points) {
+        const dist = Math.abs(touchX - point.x);
+        if (dist < minDist) {
+          minDist = dist;
+          closest = point;
+        }
+      }
+
+      return closest;
+    },
+    [points]
+  );
+
+  // ツールチップを表示
+  const showTooltip = useCallback(
+    (index: number) => {
+      if (hideTimeoutRef.current) {
+        clearTimeout(hideTimeoutRef.current);
+        hideTimeoutRef.current = null;
+      }
+      setTouchedIndex(index);
+      tooltipOpacity.value = withSpring(1, { damping: 20, stiffness: 300 });
+      tooltipScale.value = withSpring(1, { damping: 15, stiffness: 400 });
+      cursorOpacity.value = withTiming(1, { duration: 100 });
+    },
+    [tooltipOpacity, tooltipScale, cursorOpacity]
+  );
+
+  // ツールチップを非表示
+  const hideTooltip = useCallback(() => {
+    hideTimeoutRef.current = setTimeout(() => {
+      tooltipOpacity.value = withTiming(0, { duration: 200 });
+      tooltipScale.value = withTiming(0.8, { duration: 200 });
+      cursorOpacity.value = withTiming(0, { duration: 150 });
+      setTouchedIndex(null);
+      setIsDragging(false);
+    }, 1500);
+  }, [tooltipOpacity, tooltipScale, cursorOpacity]);
+
+  // PanResponder でドラッグ操作を処理
+  const panResponder = useMemo(
+    () =>
+      PanResponder.create({
+        onStartShouldSetPanResponder: () => true,
+        onMoveShouldSetPanResponder: () => true,
+        onPanResponderGrant: (evt) => {
+          const touchX = evt.nativeEvent.locationX;
+          const closest = findClosestPoint(touchX);
+          if (closest) {
+            runOnJS(setIsDragging)(true);
+            runOnJS(showTooltip)(closest.index);
+          }
+        },
+        onPanResponderMove: (evt) => {
+          const touchX = evt.nativeEvent.locationX;
+          const closest = findClosestPoint(touchX);
+          if (closest && closest.index !== touchedIndex) {
+            runOnJS(showTooltip)(closest.index);
+          }
+        },
+        onPanResponderRelease: () => {
+          runOnJS(hideTooltip)();
+        },
+        onPanResponderTerminate: () => {
+          runOnJS(hideTooltip)();
+        },
+      }),
+    [findClosestPoint, showTooltip, hideTooltip, touchedIndex]
+  );
+
+  // X軸ラベル
+  const xAxisLabels = useMemo(() => {
+    if (data.length === 0) return [];
+    const labels = ['6 AM', '12 PM', '6 PM', '12 AM'];
+    const minHour = Math.min(...data.map((d) => d.hour));
+    const maxHour = Math.max(...data.map((d) => d.hour));
+    const hourRange = maxHour - minHour || 1;
+
+    return labels.map((label, index) => {
+      const hour = 6 + index * 6;
+      const x = PADDING_LEFT + ((hour - minHour) / hourRange) * chartWidth;
+      return { label, x };
+    });
+  }, [data, chartWidth]);
+
+  // 現在時刻をフォーマット
+  const formatCurrentTime = () => {
+    const hours = Math.floor(currentHour);
+    const minutes = Math.round((currentHour - hours) * 60);
+    const period = hours >= 12 ? 'PM' : 'AM';
+    const displayHours = hours > 12 ? hours - 12 : hours === 0 ? 12 : hours;
+    return `${displayHours}:${minutes.toString().padStart(2, '0')} ${period}`;
+  };



================================================================================
ファイル: app/src/components/RhythmInteractiveChart.tsx
行: 262 (開始行: 260)
コミット: a35fee7
作成日時: 2026-01-07T07:52:34Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
rg -n "cursorAnimatedStyle" app/src/components/RhythmInteractiveChart.tsx
```

Repository: Bluefinee/tempo-ai

Length of output: 121

---

🏁 Script executed:

```shell
wc -l app/src/components/RhythmInteractiveChart.tsx
```

Repository: Bluefinee/tempo-ai

Length of output: 111

---

🏁 Script executed:

```shell
sed -n '250,350p' app/src/components/RhythmInteractiveChart.tsx
```

Repository: Bluefinee/tempo-ai

Length of output: 3403

---

🏁 Script executed:

```shell
rg -n "tooltipAnimatedStyle" app/src/components/RhythmInteractiveChart.tsx
```

Repository: Bluefinee/tempo-ai

Length of output: 162

---

🏁 Script executed:

```shell
sed -n '350,586p' app/src/components/RhythmInteractiveChart.tsx
```

Repository: Bluefinee/tempo-ai

Length of output: 6091

---



</details>

**Remove unused `cursorAnimatedStyle` variable.**

The `cursorAnimatedStyle` defined at lines 260-262 is not applied to any component in the JSX. The cursor is rendered as a static `<Line>` element without any animated styling. Delete this unused variable.

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/components/RhythmInteractiveChart.tsx around lines 260 - 262, Remove
the unused cursorAnimatedStyle declaration (the const cursorAnimatedStyle =
useAnimatedStyle(...) block) since it is not applied to the rendered <Line>;
delete those lines and any now-unused references/imports (e.g.,
useAnimatedStyle) left behind. If cursor animation was intended instead of
deletion, replace the static <Line> with an animated component and apply the
animated style/props (using cursorOpacity) to that animated Line (otherwise just
remove the cursorAnimatedStyle declaration).
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,586 @@
+/**
+ * RhythmInteractiveChart - タッチ対応リズムチャート
+ * スムーズなドラッグ操作でツールチップ表示
+ */
+
+import React, { useState, useCallback, useMemo, useRef } from 'react';
+import { View, Text, Dimensions, StyleSheet, PanResponder } from 'react-native';
+import Svg, {
+  Path,
+  Defs,
+  LinearGradient,
+  Stop,
+  Line,
+  Circle,
+  Text as SvgText,
+} from 'react-native-svg';
+import Animated, {
+  useSharedValue,
+  useAnimatedStyle,
+  withSpring,
+  withTiming,
+  runOnJS,
+} from 'react-native-reanimated';
+
+import { Colors } from '../theme';
+
+export interface RhythmDataPoint {
+  time: string;
+  hour: number;
+  energy: number;
+  label?: string;
+}
+
+interface RhythmInteractiveChartProps {
+  data: RhythmDataPoint[];
+  currentHour: number;
+  width?: number;
+  height?: number;
+}
+
+const PADDING_LEFT = 16;
+const PADDING_RIGHT = 16;
+const PADDING_TOP = 50;
+const PADDING_BOTTOM = 35;
+
+export const RhythmInteractiveChart: React.FC<RhythmInteractiveChartProps> = ({
+  data,
+  currentHour,
+  width: propWidth,
+  height = 280,
+}) => {
+  const screenWidth = Dimensions.get('window').width;
+  const width = propWidth ?? screenWidth;
+  const chartWidth = width - PADDING_LEFT - PADDING_RIGHT;
+  const chartHeight = height - PADDING_TOP - PADDING_BOTTOM;
+
+  const [touchedIndex, setTouchedIndex] = useState<number | null>(null);
+  const [isDragging, setIsDragging] = useState(false);
+  const hideTimeoutRef = useRef<ReturnType<typeof setTimeout> | null>(null);
+
+  // アニメーション用
+  const tooltipOpacity = useSharedValue(0);
+  const tooltipScale = useSharedValue(0.8);
+  const cursorOpacity = useSharedValue(0);
+
+  // データポイントの座標を計算
+  const points = useMemo(() => {
+    if (data.length === 0) return [];
+    const minHour = Math.min(...data.map((d) => d.hour));
+    const maxHour = Math.max(...data.map((d) => d.hour));
+    const hourRange = maxHour - minHour || 1;
+
+    return data.map((d, index) => {
+      const x = PADDING_LEFT + ((d.hour - minHour) / hourRange) * chartWidth;
+      const y = PADDING_TOP + chartHeight - (d.energy / 100) * chartHeight;
+      return { x, y, data: d, index };
+    });
+  }, [data, chartWidth, chartHeight]);
+
+  // Catmull-Rom スプラインを三次ベジェ曲線に変換して滑らかな曲線を生成
+  const generateSmoothPath = useCallback(
+    (pts: typeof points, closePath: boolean) => {
+      if (pts.length < 2) return '';
+
+      // Catmull-Rom to Bezier conversion
+      const tension = 0.3; // 曲線の張り具合（0-1、小さいほど滑らか）
+
+      let path = `M ${pts[0].x} ${pts[0].y}`;
+
+      for (let i = 0; i < pts.length - 1; i++) {
+        const p0 = pts[Math.max(0, i - 1)];
+        const p1 = pts[i];
+        const p2 = pts[i + 1];
+        const p3 = pts[Math.min(pts.length - 1, i + 2)];
+
+        // コントロールポイントを計算
+        const cp1x = p1.x + ((p2.x - p0.x) * tension) / 2;
+        const cp1y = p1.y + ((p2.y - p0.y) * tension) / 2;
+        const cp2x = p2.x - ((p3.x - p1.x) * tension) / 2;
+        const cp2y = p2.y - ((p3.y - p1.y) * tension) / 2;
+
+        path += ` C ${cp1x} ${cp1y}, ${cp2x} ${cp2y}, ${p2.x} ${p2.y}`;
+      }
+
+      if (closePath) {
+        // エリアを閉じる
+        path += ` L ${pts[pts.length - 1].x} ${PADDING_TOP + chartHeight}`;
+        path += ` L ${pts[0].x} ${PADDING_TOP + chartHeight}`;
+        path += ' Z';
+      }
+
+      return path;
+    },
+    [chartHeight]
+  );
+
+  // SVGパスを生成（スムーズな曲線）
+  const areaPath = useMemo(() => {
+    return generateSmoothPath(points, true);
+  }, [points, generateSmoothPath]);
+
+  // ラインパスを生成
+  const linePath = useMemo(() => {
+    return generateSmoothPath(points, false);
+  }, [points, generateSmoothPath]);
+
+  // 現在時刻の位置を計算
+  const currentTimePosition = useMemo(() => {
+    if (data.length === 0) return { x: 0, y: 0, energy: 0 };
+    const minHour = Math.min(...data.map((d) => d.hour));
+    const maxHour = Math.max(...data.map((d) => d.hour));
+    const hourRange = maxHour - minHour || 1;
+
+    const x = PADDING_LEFT + ((currentHour - minHour) / hourRange) * chartWidth;
+
+    // 現在時刻に最も近いデータポイントのエネルギーを取得
+    const closestPoint = data.reduce((prev, curr) => {
+      return Math.abs(curr.hour - currentHour) < Math.abs(prev.hour - currentHour) ? curr : prev;
+    });
+    const y = PADDING_TOP + chartHeight - (closestPoint.energy / 100) * chartHeight;
+
+    return { x, y, energy: closestPoint.energy };
+  }, [data, currentHour, chartWidth, chartHeight]);
+
+  // ピークとディップの位置を取得
+  const annotations = useMemo(() => {
+    const peak = points.find((p) => p.data.label === 'Peak' || p.data.label === 'ピーク');
+    const dip = points.find((p) => p.data.label === 'Dip' || p.data.label === '低迷期');
+    return { peak, dip };
+  }, [points]);
+
+  // タッチ位置から最も近いポイントを見つける
+  const findClosestPoint = useCallback(
+    (touchX: number) => {
+      if (points.length === 0) return null;
+
+      let closest = points[0];
+      let minDist = Math.abs(touchX - points[0].x);
+
+      for (const point of points) {
+        const dist = Math.abs(touchX - point.x);
+        if (dist < minDist) {
+          minDist = dist;
+          closest = point;
+        }
+      }
+
+      return closest;
+    },
+    [points]
+  );
+
+  // ツールチップを表示
+  const showTooltip = useCallback(
+    (index: number) => {
+      if (hideTimeoutRef.current) {
+        clearTimeout(hideTimeoutRef.current);
+        hideTimeoutRef.current = null;
+      }
+      setTouchedIndex(index);
+      tooltipOpacity.value = withSpring(1, { damping: 20, stiffness: 300 });
+      tooltipScale.value = withSpring(1, { damping: 15, stiffness: 400 });
+      cursorOpacity.value = withTiming(1, { duration: 100 });
+    },
+    [tooltipOpacity, tooltipScale, cursorOpacity]
+  );
+
+  // ツールチップを非表示
+  const hideTooltip = useCallback(() => {
+    hideTimeoutRef.current = setTimeout(() => {
+      tooltipOpacity.value = withTiming(0, { duration: 200 });
+      tooltipScale.value = withTiming(0.8, { duration: 200 });
+      cursorOpacity.value = withTiming(0, { duration: 150 });
+      setTouchedIndex(null);
+      setIsDragging(false);
+    }, 1500);
+  }, [tooltipOpacity, tooltipScale, cursorOpacity]);
+
+  // PanResponder でドラッグ操作を処理
+  const panResponder = useMemo(
+    () =>
+      PanResponder.create({
+        onStartShouldSetPanResponder: () => true,
+        onMoveShouldSetPanResponder: () => true,
+        onPanResponderGrant: (evt) => {
+          const touchX = evt.nativeEvent.locationX;
+          const closest = findClosestPoint(touchX);
+          if (closest) {
+            runOnJS(setIsDragging)(true);
+            runOnJS(showTooltip)(closest.index);
+          }
+        },
+        onPanResponderMove: (evt) => {
+          const touchX = evt.nativeEvent.locationX;
+          const closest = findClosestPoint(touchX);
+          if (closest && closest.index !== touchedIndex) {
+            runOnJS(showTooltip)(closest.index);
+          }
+        },
+        onPanResponderRelease: () => {
+          runOnJS(hideTooltip)();
+        },
+        onPanResponderTerminate: () => {
+          runOnJS(hideTooltip)();
+        },
+      }),
+    [findClosestPoint, showTooltip, hideTooltip, touchedIndex]
+  );
+
+  // X軸ラベル
+  const xAxisLabels = useMemo(() => {
+    if (data.length === 0) return [];
+    const labels = ['6 AM', '12 PM', '6 PM', '12 AM'];
+    const minHour = Math.min(...data.map((d) => d.hour));
+    const maxHour = Math.max(...data.map((d) => d.hour));
+    const hourRange = maxHour - minHour || 1;
+
+    return labels.map((label, index) => {
+      const hour = 6 + index * 6;
+      const x = PADDING_LEFT + ((hour - minHour) / hourRange) * chartWidth;
+      return { label, x };
+    });
+  }, [data, chartWidth]);
+
+  // 現在時刻をフォーマット
+  const formatCurrentTime = () => {
+    const hours = Math.floor(currentHour);
+    const minutes = Math.round((currentHour - hours) * 60);
+    const period = hours >= 12 ? 'PM' : 'AM';
+    const displayHours = hours > 12 ? hours - 12 : hours === 0 ? 12 : hours;
+    return `${displayHours}:${minutes.toString().padStart(2, '0')} ${period}`;
+  };
+
+  // アニメーションスタイル
+  const tooltipAnimatedStyle = useAnimatedStyle(() => ({
+    opacity: tooltipOpacity.value,
+    transform: [{ scale: tooltipScale.value }],
+  }));
+
+  const cursorAnimatedStyle = useAnimatedStyle(() => ({
+    opacity: cursorOpacity.value,
+  }));



================================================================================
ファイル: app/src/components/WindowCard.tsx
行: 44 (開始行: 29)
コミット: a35fee7
作成日時: 2026-01-07T07:52:34Z
================================================================================

【コメント】
_🛠️ Refactor suggestion_ | _🟠 Major_

**Add explicit return type for `getThemeConfig`.**

The helper function lacks an explicit return type annotation.


<details>
<summary>Suggested fix</summary>

```diff
+interface ThemeConfig {
+  iconBgColor: string;
+  iconColor: string;
+  timeColor: string;
+  accentColor: string;
+}
+
-const getThemeConfig = (theme: Theme) => {
+const getThemeConfig = (theme: Theme): ThemeConfig => {
   if (theme === 'day') {
     return {
       iconBgColor: 'rgba(251, 191, 36, 0.15)',
       iconColor: Colors.amber[500],
       timeColor: Colors.stone[500],
       accentColor: Colors.amber[400],
     };
   }
   return {
     iconBgColor: 'rgba(99, 102, 241, 0.12)',
     iconColor: Colors.indigo[500],
     timeColor: Colors.stone[500],
     accentColor: Colors.indigo[400],
   };
 };
```
</details>

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/components/WindowCard.tsx around lines 29 - 44, getThemeConfig
currently has no explicit return type; define a return type (e.g., type or
interface like ThemeConfig with keys iconBgColor, iconColor, timeColor,
accentColor) and annotate the function signature as getThemeConfig(theme:
Theme): ThemeConfig so both branches conform to that type; update or add the
ThemeConfig type near the function (and export it if used elsewhere) to ensure
proper typing.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,147 @@
+/**
+ * WindowCard - Upcoming Windows カード
+ * StyleSheet.createのみ使用（NativeWind排除）
+ */
+
+import React from 'react';
+import { View, Text, Pressable, StyleSheet } from 'react-native';
+import { Sun, Moon, LucideIcon } from 'lucide-react-native';
+import Animated, { FadeInDown } from 'react-native-reanimated';
+
+import { Colors } from '../theme';
+
+type Theme = 'day' | 'night';
+
+interface WindowCardProps {
+  title: string;
+  timeRange: string;
+  description: string;
+  icon?: LucideIcon;
+  iconType?: 'sun' | 'moon';
+  theme: Theme;
+  isActive?: boolean;
+  delay?: number;
+  onPress?: () => void;
+}
+
+const AnimatedPressable = Animated.createAnimatedComponent(Pressable);
+
+const getThemeConfig = (theme: Theme) => {
+  if (theme === 'day') {
+    return {
+      iconBgColor: 'rgba(251, 191, 36, 0.15)',
+      iconColor: Colors.amber[500],
+      timeColor: Colors.stone[500],
+      accentColor: Colors.amber[400],
+    };
+  }
+  return {
+    iconBgColor: 'rgba(99, 102, 241, 0.12)',
+    iconColor: Colors.indigo[500],
+    timeColor: Colors.stone[500],
+    accentColor: Colors.indigo[400],
+  };
+};



================================================================================
ファイル: app/src/constants/mockDataFactory.test.ts
行: 135 (開始行: 114)
コミット: a35fee7
作成日時: 2026-01-07T07:52:34Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**Consider adding `sampleCount` to test samples for complete coverage.**

The `calculateBaseline` tests use simplified sample objects without `sampleCount`, which works but differs from the actual `DailyHealthSample` type. The implementation from `mockDataFactory.ts` (line 98-117 in relevant snippets) generates samples with `sampleCount`.



This is fine since TypeScript structural typing allows the subset, but for documentation purposes you might want to include the full shape:

```diff
     const samples = [
-      { date: new Date(), value: 60 },
-      { date: new Date(), value: 70 },
-      { date: new Date(), value: 80 },
+      { date: new Date(), value: 60, sampleCount: 1 },
+      { date: new Date(), value: 70, sampleCount: 1 },
+      { date: new Date(), value: 80, sampleCount: 1 },
     ];
```


> Committable suggestion skipped: line range outside the PR's diff.

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/constants/mockDataFactory.test.ts around lines 114 - 135, The tests
for calculateBaseline use sample objects missing the sampleCount property
present on DailyHealthSample and produced by mockDataFactory; update each test
case to include a sampleCount number (e.g., sampleCount: 1 or another realistic
value) on every sample object used in the calculateBaseline specs so the test
data matches the full shape returned by mockDataFactory and the
DailyHealthSample type when calling calculateBaseline.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,237 @@
+/**
+ * モックデータファクトリのテスト
+ */
+
+import {
+  generateDateRange,
+  formatDateString,
+  seededRandom,
+  generateDailySamples,
+  calculateBaseline,
+  calculateTypicalRange,
+  calculateTrend,
+  getMockMetricHistory,
+} from './mockDataFactory';
+
+describe('mockDataFactory', () => {
+  describe('generateDateRange', () => {
+    it('指定した日数分の Date 配列を返す', () => {
+      const dates = generateDateRange(7);
+      expect(dates).toHaveLength(7);
+    });
+
+    it('古い日付から新しい日付の順に並ぶ', () => {
+      const dates = generateDateRange(7);
+      for (let i = 1; i < dates.length; i++) {
+        expect(dates[i].getTime()).toBeGreaterThan(dates[i - 1].getTime());
+      }
+    });
+
+    it('最後の日付は今日である', () => {
+      const dates = generateDateRange(7);
+      const today = new Date();
+      today.setHours(0, 0, 0, 0);
+      expect(dates[dates.length - 1].getTime()).toBe(today.getTime());
+    });
+  });
+
+  describe('formatDateString', () => {
+    it('YYYY-MM-DD 形式の文字列を返す', () => {
+      const date = new Date(2026, 0, 7); // 2026-01-07
+      expect(formatDateString(date)).toBe('2026-01-07');
+    });
+
+    it('月と日が1桁の場合はゼロパディングする', () => {
+      const date = new Date(2026, 0, 1); // 2026-01-01
+      expect(formatDateString(date)).toBe('2026-01-01');
+    });
+  });
+
+  describe('seededRandom', () => {
+    it('同じシードで同じ結果を返す', () => {
+      const result1 = seededRandom(42);
+      const result2 = seededRandom(42);
+      expect(result1).toBe(result2);
+    });
+
+    it('0-1 の間の数値を返す', () => {
+      for (let seed = 0; seed < 100; seed++) {
+        const result = seededRandom(seed);
+        expect(result).toBeGreaterThanOrEqual(0);
+        expect(result).toBeLessThan(1);
+      }
+    });
+
+    it('異なるシードで異なる結果を返す', () => {
+      const result1 = seededRandom(42);
+      const result2 = seededRandom(43);
+      expect(result1).not.toBe(result2);
+    });
+  });
+
+  describe('generateDailySamples', () => {
+    it('指定した日数分のサンプルを返す', () => {
+      const samples = generateDailySamples(70, 10, 7, 42);
+      expect(samples).toHaveLength(7);
+    });
+
+    it('各サンプルは Date と value を持つ', () => {
+      const samples = generateDailySamples(70, 10, 7, 42);
+      samples.forEach((sample) => {
+        expect(sample.date).toBeInstanceOf(Date);
+        expect(typeof sample.value).toBe('number');
+      });
+    });
+
+    it('同じパラメータで同じ結果を返す（再現性）', () => {
+      const samples1 = generateDailySamples(70, 10, 7, 42);
+      const samples2 = generateDailySamples(70, 10, 7, 42);
+
+      samples1.forEach((sample, index) => {
+        expect(sample.value).toBe(samples2[index].value);
+      });
+    });
+
+    it('値は基準値 ± 分散の範囲内', () => {
+      const baseValue = 70;
+      const variance = 10;
+      const samples = generateDailySamples(baseValue, variance, 100, 42);
+
+      samples.forEach((sample) => {
+        expect(sample.value).toBeGreaterThanOrEqual(baseValue - variance);
+        expect(sample.value).toBeLessThanOrEqual(baseValue + variance);
+      });
+    });
+
+    it('負の値にならない', () => {
+      const samples = generateDailySamples(5, 10, 100, 42);
+      samples.forEach((sample) => {
+        expect(sample.value).toBeGreaterThanOrEqual(0);
+      });
+    });
+  });
+
+  describe('calculateBaseline', () => {
+    it('直近 N 日の平均を返す', () => {
+      const samples = [
+        { date: new Date(), value: 60 },
+        { date: new Date(), value: 70 },
+        { date: new Date(), value: 80 },
+      ];
+      expect(calculateBaseline(samples, 3)).toBe(70);
+    });
+
+    it('サンプル数が N より少ない場合は全データの平均を返す', () => {
+      const samples = [
+        { date: new Date(), value: 60 },
+        { date: new Date(), value: 80 },
+      ];
+      expect(calculateBaseline(samples, 30)).toBe(70);
+    });
+
+    it('空配列の場合は 0 を返す', () => {
+      expect(calculateBaseline([], 30)).toBe(0);
+    });
+  });



================================================================================
ファイル: app/src/domain/services/energyCurveGenerator.ts
行: 29 (開始行: 27)
コミット: a35fee7
作成日時: 2026-01-07T07:52:34Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟠 Major_

**Non-deterministic output will cause UI inconsistency.**

Using `Math.random()` in the energy calculation means the curve will be different on every call, potentially causing chart flickering on re-renders.



<details>
<summary>🐛 Suggested fix</summary>

```diff
     if (hoursSinceWake < 0) {
       // 就寝中（深夜）
-      energy = 20 + Math.random() * 5;
+      energy = 22; // Fixed baseline for sleeping state
     }
```

If variation is needed, consider using a seeded random or derive variation from the input time.
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
    if (hoursSinceWake < 0) {
      // 就寝中（深夜）
      energy = 22; // Fixed baseline for sleeping state
    }
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/domain/services/energyCurveGenerator.ts around lines 27 - 29, The
energy assignment inside the hoursSinceWake < 0 branch uses Math.random()
causing non-deterministic outputs; replace the Math.random() usage in that
branch (where energy is set to "20 + Math.random() * 5") with a deterministic
variation derived from inputs or a seeded PRNG so repeated calls produce the
same curve for the same inputs; for example compute a small deterministic offset
from hoursSinceWake or the provided time (or use a seeded RNG initialized from a
stable seed such as user/session id or time bucket) and add that offset instead
of Math.random() so charts no longer flicker on re-renders.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,75 @@
+export interface EnergyCurvePoint {
+  time: string; // "HH:MM"
+  energy: number; // 0-100
+}
+
+/**
+ * サーカディアンリズムに基づくエネルギーカーブ生成
+ */
+export const generateEnergyCurve = (
+  wakeUpTime: Date,
+  bedtime: Date,
+  recoveryScore: number // その日のRecoveryスコアで全体調整
+): EnergyCurvePoint[] => {
+  const baseEnergy = recoveryScore * 0.8;
+  const points: EnergyCurvePoint[] = [];
+
+  // 24時間を30分刻みで計算
+  for (let hour = 0; hour < 24; hour += 0.5) {
+    const time = new Date(wakeUpTime);
+    time.setHours(Math.floor(hour), (hour % 1) * 60, 0, 0);
+
+    const hoursSinceWake =
+      (time.getTime() - wakeUpTime.getTime()) / (1000 * 60 * 60);
+
+    let energy: number;
+
+    if (hoursSinceWake < 0) {
+      // 就寝中（深夜）
+      energy = 20 + Math.random() * 5;



================================================================================
ファイル: app/src/domain/services/energyCurveGenerator.ts
行: 52 (開始行: 47)
コミット: a35fee7
作成日時: 2026-01-07T07:52:34Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

**Unused variable: `hoursToBedtime` is calculated but never used.**

The variable is computed but the energy calculation on line 51 doesn't reference it.



<details>
<summary>🐛 Fix: Remove or use the variable</summary>

```diff
     } else {
       // Wind Down: 就寝に向けて低下
-      const hoursToBedtime =
-        (bedtime.getTime() - time.getTime()) / (1000 * 60 * 60);
-      energy = Math.max(30, 70 - Math.max(0, 13 - hoursSinceWake) * 3);
+      const hoursToBedtime = (bedtime.getTime() - time.getTime()) / (1000 * 60 * 60);
+      // Use hoursToBedtime for gradual wind-down
+      energy = Math.max(30, 30 + hoursToBedtime * 10);
     }
```

Or simply remove if not needed.
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
    } else {
      // Wind Down: 就寝に向けて低下
      const hoursToBedtime = (bedtime.getTime() - time.getTime()) / (1000 * 60 * 60);
      // Use hoursToBedtime for gradual wind-down
      energy = Math.max(30, 30 + hoursToBedtime * 10);
    }
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/domain/services/energyCurveGenerator.ts around lines 47 - 52, Remove
the unused local variable hoursToBedtime: either delete the line that computes
hoursToBedtime (the const hoursToBedtime = (bedtime.getTime() - time.getTime())
/ (1000 * 60 * 60);) since the subsequent energy calculation in the Wind Down
branch uses hoursSinceWake and not hoursToBedtime, or modify the energy
computation in the Wind Down branch (the assignment to energy) to incorporate
hoursToBedtime if that value was intended to influence the energy curve.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,75 @@
+export interface EnergyCurvePoint {
+  time: string; // "HH:MM"
+  energy: number; // 0-100
+}
+
+/**
+ * サーカディアンリズムに基づくエネルギーカーブ生成
+ */
+export const generateEnergyCurve = (
+  wakeUpTime: Date,
+  bedtime: Date,
+  recoveryScore: number // その日のRecoveryスコアで全体調整
+): EnergyCurvePoint[] => {
+  const baseEnergy = recoveryScore * 0.8;
+  const points: EnergyCurvePoint[] = [];
+
+  // 24時間を30分刻みで計算
+  for (let hour = 0; hour < 24; hour += 0.5) {
+    const time = new Date(wakeUpTime);
+    time.setHours(Math.floor(hour), (hour % 1) * 60, 0, 0);
+
+    const hoursSinceWake =
+      (time.getTime() - wakeUpTime.getTime()) / (1000 * 60 * 60);
+
+    let energy: number;
+
+    if (hoursSinceWake < 0) {
+      // 就寝中（深夜）
+      energy = 20 + Math.random() * 5;
+    } else if (hoursSinceWake < 2) {
+      // Wake Window: 緩やかに上昇
+      energy = 40 + hoursSinceWake * 10;
+    } else if (hoursSinceWake < 5) {
+      // Peak Focus: 最高値（サイン波）
+      const t = (hoursSinceWake - 2) / 3;
+      energy = 80 + Math.sin(t * Math.PI) * 15;
+    } else if (hoursSinceWake < 7) {
+      // Midday: 緩やかに低下
+      energy = 75 - (hoursSinceWake - 5) * 5;
+    } else if (hoursSinceWake < 9) {
+      // Afternoon Dip: 最低値
+      const t = (hoursSinceWake - 7) / 2;
+      energy = 50 - Math.sin(t * Math.PI) * 5;
+    } else if (hoursSinceWake < 13) {
+      // Second Wind: 回復
+      energy = 50 + (hoursSinceWake - 9) * 5;
+    } else {
+      // Wind Down: 就寝に向けて低下
+      const hoursToBedtime =
+        (bedtime.getTime() - time.getTime()) / (1000 * 60 * 60);
+      energy = Math.max(30, 70 - Math.max(0, 13 - hoursSinceWake) * 3);
+    }



================================================================================
ファイル: app/src/domain/services/energyCurveGenerator.ts
行: 70 (開始行: 66)
コミット: a35fee7
作成日時: 2026-01-07T07:52:34Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

<details>
<summary>🧩 Analysis chain</summary>

🏁 Script executed:

```shell
# Find all formatTime implementations
rg -n "const formatTime|function formatTime" --type=ts
```

Repository: Bluefinee/tempo-ai

Length of output: 624

---

🏁 Script executed:

```shell
# Check the specific lines in energyCurveGenerator.ts
sed -n '60,75p' app/src/domain/services/energyCurveGenerator.ts
```

Repository: Bluefinee/tempo-ai

Length of output: 421

---

🏁 Script executed:

```shell
# Check the specific lines in mockData.ts around line 525
sed -n '520,535p' app/src/constants/mockData.ts
```

Repository: Bluefinee/tempo-ai

Length of output: 435

---

🏁 Script executed:

```shell
# Search for all usages of formatTime
rg "formatTime" --type=ts -B 2 -A 2
```

Repository: Bluefinee/tempo-ai

Length of output: 5600

---



</details>

**Remove duplicate `formatTime` or consolidate to shared utility.**

The `formatTime` function in `energyCurveGenerator.ts` and `rhythmPhaseCalculator.ts` (line 54) are identical implementations. Additionally, `app/src/constants/mockData.ts` exports a `formatTime` function, though it differs: it does not pad hours (produces "H:MM" instead of "HH:MM").

If hour padding is required, consolidate the two service implementations to use the mockData version and update it to pad hours. Otherwise, consider centralizing the padded version to `app/src/constants/mockData.ts` as a shared utility.

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/domain/services/energyCurveGenerator.ts around lines 66 - 70, Both
service files define identical formatTime functions; consolidate by removing the
local formatTime from energyCurveGenerator.ts and rhythmPhaseCalculator.ts and
instead import the shared formatTime from mockData
(app/src/constants/mockData.ts). Update the exported formatTime implementation
in mockData to pad hours to two digits (use hours.toString().padStart(2,'0') and
minutes similarly) while preserving the function signature and export name, then
update the two service files to import that shared function and remove their
duplicates.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,75 @@
+export interface EnergyCurvePoint {
+  time: string; // "HH:MM"
+  energy: number; // 0-100
+}
+
+/**
+ * サーカディアンリズムに基づくエネルギーカーブ生成
+ */
+export const generateEnergyCurve = (
+  wakeUpTime: Date,
+  bedtime: Date,
+  recoveryScore: number // その日のRecoveryスコアで全体調整
+): EnergyCurvePoint[] => {
+  const baseEnergy = recoveryScore * 0.8;
+  const points: EnergyCurvePoint[] = [];
+
+  // 24時間を30分刻みで計算
+  for (let hour = 0; hour < 24; hour += 0.5) {
+    const time = new Date(wakeUpTime);
+    time.setHours(Math.floor(hour), (hour % 1) * 60, 0, 0);
+
+    const hoursSinceWake =
+      (time.getTime() - wakeUpTime.getTime()) / (1000 * 60 * 60);
+
+    let energy: number;
+
+    if (hoursSinceWake < 0) {
+      // 就寝中（深夜）
+      energy = 20 + Math.random() * 5;
+    } else if (hoursSinceWake < 2) {
+      // Wake Window: 緩やかに上昇
+      energy = 40 + hoursSinceWake * 10;
+    } else if (hoursSinceWake < 5) {
+      // Peak Focus: 最高値（サイン波）
+      const t = (hoursSinceWake - 2) / 3;
+      energy = 80 + Math.sin(t * Math.PI) * 15;
+    } else if (hoursSinceWake < 7) {
+      // Midday: 緩やかに低下
+      energy = 75 - (hoursSinceWake - 5) * 5;
+    } else if (hoursSinceWake < 9) {
+      // Afternoon Dip: 最低値
+      const t = (hoursSinceWake - 7) / 2;
+      energy = 50 - Math.sin(t * Math.PI) * 5;
+    } else if (hoursSinceWake < 13) {
+      // Second Wind: 回復
+      energy = 50 + (hoursSinceWake - 9) * 5;
+    } else {
+      // Wind Down: 就寝に向けて低下
+      const hoursToBedtime =
+        (bedtime.getTime() - time.getTime()) / (1000 * 60 * 60);
+      energy = Math.max(30, 70 - Math.max(0, 13 - hoursSinceWake) * 3);
+    }
+
+    // Recoveryスコアで全体調整
+    energy = energy * (baseEnergy / 70);
+
+    points.push({
+      time: formatTime(time),
+      energy: clamp(energy, 0, 100),
+    });
+  }
+
+  return points;
+};
+
+const formatTime = (date: Date): string => {
+  const hours = date.getHours().toString().padStart(2, '0');
+  const minutes = date.getMinutes().toString().padStart(2, '0');
+  return `${hours}:${minutes}`;
+};



================================================================================
ファイル: app/src/i18n/index.ts
行: 27 (開始行: N/A)
コミット: a35fee7
作成日時: 2026-01-07T07:52:34Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

**Replace default export with named export.**

Per coding guidelines, named exports should be used instead of default exports for better refactoring and tree-shaking. Based on learnings, this applies to all TypeScript/JavaScript files.



<details>
<summary>📝 Proposed fix</summary>

```diff
-export default i18n;
+export { i18n };
```

Consumers will need to update their imports:
```typescript
// Before:
import i18n from '../i18n';

// After:
import { i18n } from '../i18n';
```
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
export { i18n };
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/i18n/index.ts at line 27, Replace the default export of the i18n
instance with a named export: change the export statement that currently exports
i18n as default to export a named symbol (i18n) and update any consumers to
import { i18n } instead of the default import; ensure the symbol name stays
"i18n" to avoid API changes, and run a quick search/replace for "import i18n
from" to update all imports accordingly.
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,32 @@
+import { getLocales } from 'expo-localization';
+import { I18n } from 'i18n-js';
+import ja from './locales/ja.json';
+
+const i18n = new I18n({ ja });
+
+// デバイスロケール取得
+const deviceLocale = getLocales()[0]?.languageCode ?? 'ja';
+
+// 日本語のみサポート（将来英語追加）
+i18n.locale = deviceLocale === 'ja' ? 'ja' : 'ja';
+i18n.defaultLocale = 'ja';
+i18n.enableFallback = true;
+
+export const t = (key: string, options?: Record<string, unknown>): string => {
+  return i18n.t(key, options);
+};
+
+export const setLocale = (locale: string): void => {
+  i18n.locale = locale;
+};
+
+export const getLocale = (): string => {
+  return i18n.locale;
+};
+
+export default i18n;



================================================================================
ファイル: app/src/stores/breatheStore.ts
行: 45 (開始行: 31)
コミット: a35fee7
作成日時: 2026-01-07T07:52:34Z
================================================================================

【コメント】
_🛠️ Refactor suggestion_ | _🟠 Major_

**Duplicate action methods detected.**

The interface declares both short-form (`start`, `pause`, `resume`, `stop`) and long-form (`startSession`, `pauseSession`, `resumeSession`, `stopSession`) actions that perform identical operations. This creates unnecessary API surface and potential confusion.

Consider keeping only one set of action names for clarity.



<details>
<summary>♻️ Suggested cleanup</summary>

```diff
 interface BreatheState {
   // ...
   // Actions
-  start: () => void;
-  pause: () => void;
-  resume: () => void;
-  stop: () => void;
   startSession: () => void;
   pauseSession: () => void;
   resumeSession: () => void;
   stopSession: () => void;
   // ... rest of actions
 }
```

Then remove the duplicate implementations (lines 70-95) and update any consumers accordingly.
</details>


> Committable suggestion skipped: line range outside the PR's diff.

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/stores/breatheStore.ts around lines 31 - 45, The interface and store
expose duplicate actions — short names (start, pause, resume, stop) and long
names (startSession, pauseSession, resumeSession, stopSession) that do the same
work; remove one set (pick either short or long) and delete the duplicate
implementations (the other set) in the breathe store, update the interface to
only declare the chosen methods (and keep related helpers like setPhase,
updateElapsedTime, setPhaseTimeRemaining, setHapticEnabled,
incrementCompletedSessions, reset), and update all consumers to call the
retained method names so only a single clear API surface remains.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,173 @@
+/**
+ * Breathe Store - 呼吸エクササイズ状態管理
+ * @see docs/specs/product_spec.md
+ */
+
+import { create } from 'zustand';
+
+// ========================================
+// Types
+// ========================================
+
+type BreathePhase = 'idle' | 'inhale' | 'hold' | 'exhale';
+
+interface BreatheState {
+  // Session State
+  isActive: boolean;
+  isPaused: boolean;
+  phase: BreathePhase;
+
+  // Timing
+  sessionDuration: number;      // 総セッション時間（秒）
+  elapsedTime: number;          // 経過時間（秒）
+  phaseTimeRemaining: number;   // 現在フェーズ残り時間（ミリ秒）
+
+  // Settings
+  hapticEnabled: boolean;
+
+  // Stats
+  completedSessions: number;
+
+  // Actions
+  start: () => void;
+  pause: () => void;
+  resume: () => void;
+  stop: () => void;
+  startSession: () => void;
+  pauseSession: () => void;
+  resumeSession: () => void;
+  stopSession: () => void;
+  setPhase: (phase: BreathePhase) => void;
+  updateElapsedTime: (seconds: number) => void;
+  setPhaseTimeRemaining: (ms: number) => void;
+  setHapticEnabled: (enabled: boolean) => void;
+  incrementCompletedSessions: () => void;
+  reset: () => void;



================================================================================
ファイル: app/src/stores/breatheStore.ts
行: 68 (開始行: 58)
コミット: a35fee7
作成日時: 2026-01-07T07:52:34Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**Consider persisting `completedSessions` across app restarts.**

The `completedSessions` counter tracks user progress but will reset when the app restarts. If this is intended for long-term tracking, consider adding Zustand's `persist` middleware.



<details>
<summary>♻️ Example with persistence</summary>

```diff
-import { create } from 'zustand';
+import { create } from 'zustand';
+import { persist, createJSONStorage } from 'zustand/middleware';
+import AsyncStorage from '@react-native-async-storage/async-storage';

-export const useBreatheStore = create<BreatheState>((set, get) => ({
+export const useBreatheStore = create<BreatheState>()(
+  persist(
+    (set, get) => ({
       // ... existing implementation
+    }),
+    {
+      name: 'breathe-storage',
+      storage: createJSONStorage(() => AsyncStorage),
+      partialize: (state) => ({ completedSessions: state.completedSessions }),
+    }
+  )
+);
```
</details>


> Committable suggestion skipped: line range outside the PR's diff.

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/stores/breatheStore.ts around lines 58 - 68, The completedSessions
counter in useBreatheStore is ephemeral; wrap the create<BreatheState> call with
Zustand's persist middleware so completedSessions survives app restarts; modify
useBreatheStore to use persist(create<BreatheState>(...)) (or compose persist
with create) supplying a unique key and appropriate storage and include
completedSessions in the persisted state (use partialize/whitelist if you only
want to persist that field), keeping existing state shape and methods intact.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,173 @@
+/**
+ * Breathe Store - 呼吸エクササイズ状態管理
+ * @see docs/specs/product_spec.md
+ */
+
+import { create } from 'zustand';
+
+// ========================================
+// Types
+// ========================================
+
+type BreathePhase = 'idle' | 'inhale' | 'hold' | 'exhale';
+
+interface BreatheState {
+  // Session State
+  isActive: boolean;
+  isPaused: boolean;
+  phase: BreathePhase;
+
+  // Timing
+  sessionDuration: number;      // 総セッション時間（秒）
+  elapsedTime: number;          // 経過時間（秒）
+  phaseTimeRemaining: number;   // 現在フェーズ残り時間（ミリ秒）
+
+  // Settings
+  hapticEnabled: boolean;
+
+  // Stats
+  completedSessions: number;
+
+  // Actions
+  start: () => void;
+  pause: () => void;
+  resume: () => void;
+  stop: () => void;
+  startSession: () => void;
+  pauseSession: () => void;
+  resumeSession: () => void;
+  stopSession: () => void;
+  setPhase: (phase: BreathePhase) => void;
+  updateElapsedTime: (seconds: number) => void;
+  setPhaseTimeRemaining: (ms: number) => void;
+  setHapticEnabled: (enabled: boolean) => void;
+  incrementCompletedSessions: () => void;
+  reset: () => void;
+}
+
+// ========================================
+// Constants
+// ========================================
+
+const DEFAULT_SESSION_DURATION = 60; // 1分
+
+// ========================================
+// Store
+// ========================================
+
+export const useBreatheStore = create<BreatheState>((set, get) => ({
+  // Initial State
+  isActive: false,
+  isPaused: false,
+  phase: 'idle',
+  sessionDuration: DEFAULT_SESSION_DURATION,
+  elapsedTime: 0,
+  phaseTimeRemaining: 0,
+  hapticEnabled: true,
+  completedSessions: 0,
+



================================================================================
ファイル: app/app/(main)/breathe.tsx
行: 12 (開始行: N/A)
コミット: a35fee7
作成日時: 2026-01-07T07:53:47Z
================================================================================

【コメント】
_🛠️ Refactor suggestion_ | _🟠 Major_

**Remove unused SVG imports `Defs`, `RadialGradient`, and `Stop`.**


<details>
<summary>♻️ Proposed fix</summary>

```diff
-import Svg, { Circle, Defs, RadialGradient, Stop } from 'react-native-svg';
+import Svg, { Circle } from 'react-native-svg';
```
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
import Svg, { Circle } from 'react-native-svg';
```

</details>

<!-- suggestion_end -->

<details>
<summary>🧰 Tools</summary>

<details>
<summary>🪛 GitHub Check: 🧹 Lint</summary>

[warning] 12-12: 
'Stop' is defined but never used                       

---

[warning] 12-12: 
'RadialGradient' is defined but never used             

---

[warning] 12-12: 
'Defs' is defined but never used                       

</details>

</details>

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(main)/breathe.tsx at line 12, The import line in breathe.tsx pulls
in unused SVG symbols; remove Defs, RadialGradient, and Stop from the import
declaration so only Svg and Circle are imported (i.e., update the import
statement that currently includes Svg, Circle, Defs, RadialGradient, Stop to
import only Svg and Circle). Ensure no other references to Defs, RadialGradient,
or Stop exist in the file before committing.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,431 @@
+/**
+ * BreatheScreen - 呼吸エクササイズ画面
+ * sozai/new/tempoai/screens/BreatheScreen.tsx を React Native で完全再現
+ * フルスクリーンモーダル風デザイン
+ */
+
+import React, { useCallback, useEffect, useRef, useState } from 'react';
+import { View, Text, Pressable, Dimensions } from 'react-native';
+import { useSafeAreaInsets } from 'react-native-safe-area-context';
+import { useRouter } from 'expo-router';
+import { X, Play, Pause, Wind } from 'lucide-react-native';
+import Svg, { Circle, Defs, RadialGradient, Stop } from 'react-native-svg';



================================================================================
ファイル: app/app/(main)/breathe.tsx
行: 20 (開始行: 19)
コミット: a35fee7
作成日時: 2026-01-07T07:53:48Z
================================================================================

【コメント】
_🛠️ Refactor suggestion_ | _🟠 Major_

**Remove unused animation imports `withRepeat` and `withSequence`.**


<details>
<summary>♻️ Proposed fix</summary>

```diff
 import Animated, {
   useSharedValue,
   useAnimatedStyle,
   withTiming,
-  withRepeat,
-  withSequence,
   Easing,
 } from 'react-native-reanimated';
```
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
import Animated, {
  useSharedValue,
  useAnimatedStyle,
  withTiming,
  Easing,
} from 'react-native-reanimated';
```

</details>

<!-- suggestion_end -->

<details>
<summary>🧰 Tools</summary>

<details>
<summary>🪛 GitHub Check: 🧹 Lint</summary>

[warning] 20-20: 
'withSequence' is defined but never used               

---

[warning] 19-19: 
'withRepeat' is defined but never used                 

</details>

</details>

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(main)/breathe.tsx around lines 19 - 20, The imports withRepeat and
withSequence are unused in breathe.tsx; remove them from the import list to
avoid dead imports and lint errors by deleting the identifiers `withRepeat` and
`withSequence` from the import statement that currently includes them in the
file's top-level imports (retain any other imported symbols).
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,431 @@
+/**
+ * BreatheScreen - 呼吸エクササイズ画面
+ * sozai/new/tempoai/screens/BreatheScreen.tsx を React Native で完全再現
+ * フルスクリーンモーダル風デザイン
+ */
+
+import React, { useCallback, useEffect, useRef, useState } from 'react';
+import { View, Text, Pressable, Dimensions } from 'react-native';
+import { useSafeAreaInsets } from 'react-native-safe-area-context';
+import { useRouter } from 'expo-router';
+import { X, Play, Pause, Wind } from 'lucide-react-native';
+import Svg, { Circle, Defs, RadialGradient, Stop } from 'react-native-svg';
+import { LinearGradient } from 'expo-linear-gradient';
+import * as Haptics from 'expo-haptics';
+import Animated, {
+  useSharedValue,
+  useAnimatedStyle,
+  withTiming,
+  withRepeat,
+  withSequence,



================================================================================
ファイル: app/app/(main)/energy-detail.tsx
行: 12 (開始行: N/A)
コミット: a35fee7
作成日時: 2026-01-07T07:53:48Z
================================================================================

【コメント】
_🛠️ Refactor suggestion_ | _🟠 Major_

**Remove unused imports `Circle` and `Line`.**

Static analysis indicates these SVG components are imported but never used.


<details>
<summary>♻️ Proposed fix</summary>

```diff
-import Svg, { Path, Line, Circle, Defs, LinearGradient, Stop } from 'react-native-svg';
+import Svg, { Path, Defs, LinearGradient, Stop } from 'react-native-svg';
```
</details>

<details>
<summary>🧰 Tools</summary>

<details>
<summary>🪛 GitHub Check: 🧹 Lint</summary>

[warning] 12-12: 
'Circle' is defined but never used          

---

[warning] 12-12: 
'Line' is defined but never used            

</details>

</details>

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(main)/energy-detail.tsx at line 12, The import list in
energy-detail.tsx includes unused SVG components Circle and Line; remove Circle
and Line from the import statement (keep Svg, Path, Defs, LinearGradient, Stop)
so the import reads only the symbols actually used and eliminate the
static-analysis warning referring to unused imports Circle and Line.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,383 @@
+/**
+ * EnergyDetailScreen - Energy詳細画面
+ * Contributing Factors（4要素グリッド）とDaily Curve（エネルギー曲線）を含む
+ */
+
+import React, { useState } from 'react';
+import { View, Text, ScrollView, Pressable } from 'react-native';
+import { SafeAreaView, useSafeAreaInsets } from 'react-native-safe-area-context';
+import { useRouter } from 'expo-router';
+import { ChevronLeft, Activity, Moon, Zap, ThermometerSun } from 'lucide-react-native';
+import Animated, { FadeInDown } from 'react-native-reanimated';
+import Svg, { Path, Line, Circle, Defs, LinearGradient, Stop } from 'react-native-svg';



================================================================================
ファイル: app/app/(main)/energy-detail.tsx
行: 124 (開始行: N/A)
コミット: a35fee7
作成日時: 2026-01-07T07:53:48Z
================================================================================

【コメント】
_🛠️ Refactor suggestion_ | _🟠 Major_

**Remove unused variable `markerX`.**

This variable is calculated but never used in the component.


<details>
<summary>♻️ Proposed fix</summary>

```diff
     const currentPosition = Math.max(0, Math.min(100, ((currentHour - 6) / 16) * 100));
-    const markerX = (currentPosition / 100) * chartWidth;
```
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
     const currentPosition = Math.max(0, Math.min(100, ((currentHour - 6) / 16) * 100));
```

</details>

<!-- suggestion_end -->

<details>
<summary>🧰 Tools</summary>

<details>
<summary>🪛 GitHub Check: 🧹 Lint</summary>

[warning] 124-124: 
'markerX' is assigned a value but never used

</details>

</details>

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(main)/energy-detail.tsx at line 124, The variable markerX is
computed as const markerX = (currentPosition / 100) * chartWidth but never used
in the component; remove this unused declaration (markerX) from
energy-detail.tsx and any related dead references to avoid lint warnings and
unnecessary computation, keeping currentPosition and chartWidth intact if they
are used elsewhere.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,383 @@
+/**
+ * EnergyDetailScreen - Energy詳細画面
+ * Contributing Factors（4要素グリッド）とDaily Curve（エネルギー曲線）を含む
+ */
+
+import React, { useState } from 'react';
+import { View, Text, ScrollView, Pressable } from 'react-native';
+import { SafeAreaView, useSafeAreaInsets } from 'react-native-safe-area-context';
+import { useRouter } from 'expo-router';
+import { ChevronLeft, Activity, Moon, Zap, ThermometerSun } from 'lucide-react-native';
+import Animated, { FadeInDown } from 'react-native-reanimated';
+import Svg, { Path, Line, Circle, Defs, LinearGradient, Stop } from 'react-native-svg';
+
+import {
+  CircularProgress,
+  TimeframeSelector,
+  MiniBarChart,
+  type Timeframe,
+} from '../../src/components';
+import { colors, FontFamily } from '../../src/theme';
+import { t } from '../../src/i18n';
+import { MOCK_DETAIL } from '../../src/constants/mockData';
+import { useHealthStore } from '../../src/stores/healthStore';
+
+// スコアに応じたステータスを取得
+const getEnergyStatus = (score: number): string => {
+  if (score >= 85) return t('score.energy.status.excellent');
+  if (score >= 65) return t('score.energy.status.good');
+  if (score >= 45) return t('score.energy.status.fair');
+  return t('score.energy.status.low');
+};
+
+// Contributing Factor カードコンポーネント（詳細版）
+interface FactorCardProps {
+  icon: React.ElementType;
+  iconColor: string;
+  iconBg: string;
+  label: string;
+  value: number;
+  trend: string;
+  trendDirection: 'up' | 'down' | 'stable';
+  detail: string;
+}
+
+const FactorCard = ({
+  icon: Icon,
+  iconColor,
+  iconBg,
+  label,
+  value,
+  trend,
+  trendDirection,
+  detail,
+}: FactorCardProps): React.ReactElement => {
+  const getTrendColor = () => {
+    if (trendDirection === 'up') return colors.emerald[500];
+    if (trendDirection === 'down') return colors.rose[500];
+    return colors.stone[400];
+  };
+
+  return (
+    <View
+      className="bg-white p-4 rounded-2xl border border-stone-100"
+      style={{
+        shadowColor: '#000',
+        shadowOffset: { width: 0, height: 2 },
+        shadowOpacity: 0.04,
+        shadowRadius: 6,
+        elevation: 2,
+      }}
+    >
+      <View className="flex-row items-center justify-between mb-2">
+        <View className="flex-row items-center" style={{ gap: 10 }}>
+          <View className="p-2 rounded-xl" style={{ backgroundColor: iconBg }}>
+            <Icon size={16} color={iconColor} />
+          </View>
+          <Text className="text-xs font-bold text-stone-500 uppercase">{label}</Text>
+        </View>
+        <Text className="text-xs font-bold" style={{ color: getTrendColor() }}>
+          {trend}
+        </Text>
+      </View>
+      <Text className="text-2xl font-bold text-stone-900 mb-1">{value}%</Text>
+      <Text className="text-[10px] text-stone-400">{detail}</Text>
+    </View>
+  );
+};
+
+export default function EnergyDetailScreen(): React.ReactElement {
+  const router = useRouter();
+  const insets = useSafeAreaInsets();
+  const [timeframe, setTimeframe] = useState<Timeframe>('7D');
+  const { dailySnapshot } = useHealthStore();
+  
+  // healthStoreから計算済みのスコアを取得
+  const energyScore = dailySnapshot?.scores?.energy ?? 0;
+  
+  const data = MOCK_DETAIL.energy;
+
+  const handleBack = () => {
+    router.back();
+  };
+
+  // Daily Curve SVGをレンダリング
+  const renderDailyCurve = () => {
+    const chartWidth = 300;
+    const chartHeight = 100;
+
+    // エネルギー曲線のパス（朝に上昇、昼にピーク、午後に下降、夕方に少し回復）
+    // 正弦波に基づく自然なエネルギーパターン
+    const curvePath = `
+      M0,80
+      C20,80 20,20 40,20
+      C55,20 55,60 65,60
+      C75,60 75,30 85,30
+      C95,30 100,80 100,80
+    `;
+
+    // 現在時刻の位置（例: 10:42 = 約40%の位置）
+    const now = new Date();
+    const currentHour = now.getHours() + now.getMinutes() / 60;
+    // 6時〜22時を0〜100%にマッピング
+    const currentPosition = Math.max(0, Math.min(100, ((currentHour - 6) / 16) * 100));
+    const markerX = (currentPosition / 100) * chartWidth;



================================================================================
ファイル: app/app/(main)/energy-detail.tsx
行: 274 (開始行: 270)
コミット: a35fee7
作成日時: 2026-01-07T07:53:48Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

**Inconsistent score source: uses `data.score` instead of `energyScore`.**

The circular display at line 263 correctly uses `energyScore` from the store, but the status badge here uses `data.score` from mock data. This inconsistency could display mismatched values.


<details>
<summary>🐛 Proposed fix</summary>

```diff
               <Text className="text-xs font-bold text-amber-600 uppercase tracking-widest">
-                  {getEnergyStatus(data.score)}
+                  {getEnergyStatus(energyScore)}
                 </Text>
```
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
              >
                <Text className="text-xs font-bold text-amber-600 uppercase tracking-widest">
                  {getEnergyStatus(energyScore)}
                </Text>
              </View>
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(main)/energy-detail.tsx around lines 270 - 274, The status badge is
calling getEnergyStatus(data.score) which uses mock data instead of the real
store value; update the call to use energyScore (i.e., change
getEnergyStatus(data.score) to getEnergyStatus(energyScore)) so the badge
matches the circular display, and verify that the energyScore variable from the
store is in scope where getEnergyStatus is invoked (adjust imports/props if
needed).
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,383 @@
+/**
+ * EnergyDetailScreen - Energy詳細画面
+ * Contributing Factors（4要素グリッド）とDaily Curve（エネルギー曲線）を含む
+ */
+
+import React, { useState } from 'react';
+import { View, Text, ScrollView, Pressable } from 'react-native';
+import { SafeAreaView, useSafeAreaInsets } from 'react-native-safe-area-context';
+import { useRouter } from 'expo-router';
+import { ChevronLeft, Activity, Moon, Zap, ThermometerSun } from 'lucide-react-native';
+import Animated, { FadeInDown } from 'react-native-reanimated';
+import Svg, { Path, Line, Circle, Defs, LinearGradient, Stop } from 'react-native-svg';
+
+import {
+  CircularProgress,
+  TimeframeSelector,
+  MiniBarChart,
+  type Timeframe,
+} from '../../src/components';
+import { colors, FontFamily } from '../../src/theme';
+import { t } from '../../src/i18n';
+import { MOCK_DETAIL } from '../../src/constants/mockData';
+import { useHealthStore } from '../../src/stores/healthStore';
+
+// スコアに応じたステータスを取得
+const getEnergyStatus = (score: number): string => {
+  if (score >= 85) return t('score.energy.status.excellent');
+  if (score >= 65) return t('score.energy.status.good');
+  if (score >= 45) return t('score.energy.status.fair');
+  return t('score.energy.status.low');
+};
+
+// Contributing Factor カードコンポーネント（詳細版）
+interface FactorCardProps {
+  icon: React.ElementType;
+  iconColor: string;
+  iconBg: string;
+  label: string;
+  value: number;
+  trend: string;
+  trendDirection: 'up' | 'down' | 'stable';
+  detail: string;
+}
+
+const FactorCard = ({
+  icon: Icon,
+  iconColor,
+  iconBg,
+  label,
+  value,
+  trend,
+  trendDirection,
+  detail,
+}: FactorCardProps): React.ReactElement => {
+  const getTrendColor = () => {
+    if (trendDirection === 'up') return colors.emerald[500];
+    if (trendDirection === 'down') return colors.rose[500];
+    return colors.stone[400];
+  };
+
+  return (
+    <View
+      className="bg-white p-4 rounded-2xl border border-stone-100"
+      style={{
+        shadowColor: '#000',
+        shadowOffset: { width: 0, height: 2 },
+        shadowOpacity: 0.04,
+        shadowRadius: 6,
+        elevation: 2,
+      }}
+    >
+      <View className="flex-row items-center justify-between mb-2">
+        <View className="flex-row items-center" style={{ gap: 10 }}>
+          <View className="p-2 rounded-xl" style={{ backgroundColor: iconBg }}>
+            <Icon size={16} color={iconColor} />
+          </View>
+          <Text className="text-xs font-bold text-stone-500 uppercase">{label}</Text>
+        </View>
+        <Text className="text-xs font-bold" style={{ color: getTrendColor() }}>
+          {trend}
+        </Text>
+      </View>
+      <Text className="text-2xl font-bold text-stone-900 mb-1">{value}%</Text>
+      <Text className="text-[10px] text-stone-400">{detail}</Text>
+    </View>
+  );
+};
+
+export default function EnergyDetailScreen(): React.ReactElement {
+  const router = useRouter();
+  const insets = useSafeAreaInsets();
+  const [timeframe, setTimeframe] = useState<Timeframe>('7D');
+  const { dailySnapshot } = useHealthStore();
+  
+  // healthStoreから計算済みのスコアを取得
+  const energyScore = dailySnapshot?.scores?.energy ?? 0;
+  
+  const data = MOCK_DETAIL.energy;
+
+  const handleBack = () => {
+    router.back();
+  };
+
+  // Daily Curve SVGをレンダリング
+  const renderDailyCurve = () => {
+    const chartWidth = 300;
+    const chartHeight = 100;
+
+    // エネルギー曲線のパス（朝に上昇、昼にピーク、午後に下降、夕方に少し回復）
+    // 正弦波に基づく自然なエネルギーパターン
+    const curvePath = `
+      M0,80
+      C20,80 20,20 40,20
+      C55,20 55,60 65,60
+      C75,60 75,30 85,30
+      C95,30 100,80 100,80
+    `;
+
+    // 現在時刻の位置（例: 10:42 = 約40%の位置）
+    const now = new Date();
+    const currentHour = now.getHours() + now.getMinutes() / 60;
+    // 6時〜22時を0〜100%にマッピング
+    const currentPosition = Math.max(0, Math.min(100, ((currentHour - 6) / 16) * 100));
+    const markerX = (currentPosition / 100) * chartWidth;
+
+    return (
+      <View
+        className="bg-white p-5 rounded-3xl border border-stone-100"
+        style={{
+          shadowColor: '#000',
+          shadowOffset: { width: 0, height: 4 },
+          shadowOpacity: 0.06,
+          shadowRadius: 10,
+          elevation: 4,
+        }}
+      >
+        <Text className="text-xs font-bold text-stone-400 uppercase mb-2">
+          {t('detail.energy.dailyCurve')}
+        </Text>
+
+        {/* Peak Focus と Afternoon Dip のラベル */}
+        <View className="flex-row justify-between mb-2">
+          <View className="flex-row items-center" style={{ gap: 4 }}>
+            <View className="w-2 h-2 rounded-full bg-amber-500" />
+            <Text className="text-[10px] text-stone-500">
+              {t('detail.energy.peakFocus')} {data.peakFocus.start}-{data.peakFocus.end}
+            </Text>
+          </View>
+          <View className="flex-row items-center" style={{ gap: 4 }}>
+            <View className="w-2 h-2 rounded-full bg-stone-300" />
+            <Text className="text-[10px] text-stone-500">
+              {t('detail.energy.afternoonDip')} {data.afternoonDip.start}-{data.afternoonDip.end}
+            </Text>
+          </View>
+        </View>
+
+        <View style={{ height: chartHeight + 20, position: 'relative' }}>
+          <Svg width="100%" height={chartHeight} viewBox={`0 0 100 ${chartHeight}`} preserveAspectRatio="none">
+            <Defs>
+              <LinearGradient id="curveGradient" x1="0" y1="0" x2="0" y2="1">
+                <Stop offset="0" stopColor={colors.amber[500]} stopOpacity="0.3" />
+                <Stop offset="1" stopColor={colors.amber[500]} stopOpacity="0" />
+              </LinearGradient>
+            </Defs>
+
+            {/* 塗りつぶしエリア */}
+            <Path
+              d={`${curvePath} V${chartHeight} H0 Z`}
+              fill="url(#curveGradient)"
+            />
+
+            {/* 曲線 */}
+            <Path
+              d={curvePath}
+              fill="none"
+              stroke={colors.amber[500]}
+              strokeWidth={2}
+            />
+          </Svg>
+
+          {/* 現在時刻マーカー */}
+          <View
+            style={{
+              position: 'absolute',
+              left: `${currentPosition}%`,
+              top: 0,
+              bottom: 20,
+              width: 2,
+              backgroundColor: colors.amber[400],
+              borderStyle: 'dashed',
+            }}
+          />
+          <View
+            style={{
+              position: 'absolute',
+              left: `${currentPosition}%`,
+              top: -4,
+              width: 8,
+              height: 8,
+              borderRadius: 4,
+              backgroundColor: colors.amber[500],
+              marginLeft: -3,
+            }}
+          />
+
+          {/* 時間軸ラベル */}
+          <View
+            className="flex-row justify-between absolute bottom-0 left-0 right-0"
+            style={{ paddingHorizontal: 4 }}
+          >
+            <Text className="text-[10px] text-stone-400">6:00</Text>
+            <Text className="text-[10px] text-stone-400">12:00</Text>
+            <Text className="text-[10px] text-stone-400">18:00</Text>
+            <Text className="text-[10px] text-stone-400">22:00</Text>
+          </View>
+        </View>
+      </View>
+    );
+  };
+
+  return (
+    <View className="flex-1 bg-stone-100">
+      <SafeAreaView className="flex-1" edges={['top']}>
+        {/* Header */}
+        <View className="flex-row items-center px-6 py-4 border-b border-stone-100 bg-stone-100">
+          <Pressable
+            onPress={handleBack}
+            className="w-10 h-10 items-center justify-center rounded-full"
+            style={({ pressed }) => [
+              { backgroundColor: pressed ? colors.stone[100] : 'transparent' },
+            ]}
+          >
+            <ChevronLeft size={24} color={colors.stone[600]} />
+          </Pressable>
+          <Text className="text-lg font-bold text-stone-900 ml-4">{t('score.energy.label')}</Text>
+        </View>
+
+        <ScrollView
+          contentContainerStyle={{
+            paddingBottom: insets.bottom + 32,
+          }}
+          showsVerticalScrollIndicator={false}
+        >
+          <View className="px-6 py-6" style={{ gap: 24 }}>
+            {/* Main Circular Display */}
+            <Animated.View
+              entering={FadeInDown.duration(400)}
+              className="items-center"
+            >
+              <View className="relative w-48 h-48 items-center justify-center mb-4">
+                <CircularProgress
+                  size={192}
+                  strokeWidth={16}
+                  progress={energyScore}
+                  color={colors.amber[500]}
+                  backgroundColor={colors.stone[200]}
+                />
+                <View className="absolute items-center">
+                  <Text
+                    className="text-5xl font-bold text-stone-900"
+                    style={{ fontFamily: FontFamily.serif }}
+                  >
+                    {Math.round(energyScore)}%
+                  </Text>
+                </View>
+              </View>
+              <View
+                className="px-4 py-1.5 rounded-full"
+                style={{ backgroundColor: colors.amber[50] }}
+              >
+                <Text className="text-xs font-bold text-amber-600 uppercase tracking-widest">
+                  {getEnergyStatus(data.score)}
+                </Text>
+              </View>



================================================================================
ファイル: app/app/(main)/health-detail.tsx
行: 76 (開始行: 63)
コミット: a35fee7
作成日時: 2026-01-07T07:53:48Z
================================================================================

【コメント】
_🛠️ Refactor suggestion_ | _🟠 Major_

**`getStatusLabel` function is defined but never used.**

This helper function exists but the component uses `metric.statusLabel` directly from mock data instead.


<details>
<summary>♻️ Remove or use the helper</summary>

Either remove the unused function, or use it when constructing metrics:
```diff
-// ステータスラベルを動的に生成するヘルパー
-const getStatusLabel = (
-  status: MetricStatus,
-  typicalRange: { min: number; max: number },
-  value: number | string
-): string => {
-  if (status === 'in-range') {
-    return `${t('metric.health.status.within')} ${typicalRange.min}-${typicalRange.max}`;
-  }
-  const numValue = typeof value === 'number' ? value : parseFloat(value);
-  if (numValue < typicalRange.min) {
-    return `Low < ${typicalRange.min}`;
-  }
-  return `High > ${typicalRange.max}`;
-};
```
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion

```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(main)/health-detail.tsx around lines 63 - 76, The getStatusLabel
helper is defined but unused; either delete it or wire it up where
metric.statusLabel is currently read from mock data: locate the component that
consumes metric.statusLabel (references to metric.statusLabel) and replace that
with a computed label by calling getStatusLabel(metric.status,
metric.typicalRange, metric.value), or alternatively remove the getStatusLabel
declaration if you prefer the hardcoded mock label; ensure you update any
import/exports or tests that expect the previous behavior.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,738 @@
+/**
+ * HealthDetailScreen - Health詳細画面
+ * sozai/tempoai-health-summary/ を React Native で完全再現
+ */
+
+import React, { useState, useRef, useCallback, useMemo } from 'react';
+import { View, Text, ScrollView, Pressable, Dimensions } from 'react-native';
+import { SafeAreaView, useSafeAreaInsets } from 'react-native-safe-area-context';
+import { useRouter } from 'expo-router';
+import {
+  ChevronLeft,
+  Check,
+  AlertCircle,
+  ArrowUp,
+  ArrowDown,
+  CheckCircle2,
+  Activity,
+  Heart,
+  Wind,
+  Droplets,
+  Thermometer,
+} from 'lucide-react-native';
+import { BlurView } from 'expo-blur';
+import Animated, { FadeInDown } from 'react-native-reanimated';
+
+import { HealthAreaChart, type ChartDataPoint } from '../../src/components';
+import { colors } from '../../src/theme';
+import { t } from '../../src/i18n';
+
+export type IconType = 'activity' | 'heart' | 'wind' | 'droplet' | 'thermometer';
+export type MetricStatus = 'in-range' | 'out-of-range';
+export type Timeframe = '7D' | '30D' | '60D';
+export type BaselineTrend = 'up' | 'down' | 'neutral';
+
+// メトリクスデータ型
+interface MetricData {
+  id: string;
+  name: string;
+  shortName: string;
+  cardLabel: string; // カード用の改行付きラベル
+  value: number | string;
+  unit: string;
+  status: MetricStatus;
+  statusLabel: string;
+  colorHex: string;
+  typicalRange: { min: number; max: number };
+  baseline: string;
+  baselineTrend: BaselineTrend;
+  iconType: IconType;
+  chartData: ChartDataPoint[];
+}
+
+// アイコンマッピング
+const ICON_MAP = {
+  activity: Activity,
+  heart: Heart,
+  wind: Wind,
+  droplet: Droplets,
+  thermometer: Thermometer,
+};
+
+// ステータスラベルを動的に生成するヘルパー
+const getStatusLabel = (
+  status: MetricStatus,
+  typicalRange: { min: number; max: number },
+  value: number | string
+): string => {
+  if (status === 'in-range') {
+    return `${t('metric.health.status.within')} ${typicalRange.min}-${typicalRange.max}`;
+  }
+  const numValue = typeof value === 'number' ? value : parseFloat(value);
+  if (numValue < typicalRange.min) {
+    return `Low < ${typicalRange.min}`;
+  }
+  return `High > ${typicalRange.max}`;
+};



================================================================================
ファイル: app/app/(main)/health-detail.tsx
行: 249 (開始行: 246)
コミット: a35fee7
作成日時: 2026-01-07T07:53:48Z
================================================================================

【コメント】
_🛠️ Refactor suggestion_ | _🟠 Major_

**Duplicate `seededRandom` implementation.**

This function duplicates `seededRandom` from `app/src/constants/mockDataFactory.ts`. Import from the existing source instead.


<details>
<summary>♻️ Proposed fix</summary>

```diff
+import { seededRandom } from '../../src/constants/mockDataFactory';
+
 // In generateTimeframeData:
-      const seededRandom = (seed: number): number => {
-        const x = Math.sin(seed * 12.9898 + seed * 78.233) * 43758.5453;
-        return x - Math.floor(x);
-      };
+      // Use imported seededRandom
```
</details>

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(main)/health-detail.tsx around lines 246 - 249, The local
seededRandom function in health-detail.tsx is a duplicate; delete the local
implementation and instead import the existing seededRandom from the
mockDataFactory module that already exports it, add the import at the top of the
file (e.g., import { seededRandom } from 'mockDataFactory' or the module name
used in the repo), keep all usages unchanged, and run type-checking/compile to
ensure the imported symbol matches the expected signature.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,738 @@
+/**
+ * HealthDetailScreen - Health詳細画面
+ * sozai/tempoai-health-summary/ を React Native で完全再現
+ */
+
+import React, { useState, useRef, useCallback, useMemo } from 'react';
+import { View, Text, ScrollView, Pressable, Dimensions } from 'react-native';
+import { SafeAreaView, useSafeAreaInsets } from 'react-native-safe-area-context';
+import { useRouter } from 'expo-router';
+import {
+  ChevronLeft,
+  Check,
+  AlertCircle,
+  ArrowUp,
+  ArrowDown,
+  CheckCircle2,
+  Activity,
+  Heart,
+  Wind,
+  Droplets,
+  Thermometer,
+} from 'lucide-react-native';
+import { BlurView } from 'expo-blur';
+import Animated, { FadeInDown } from 'react-native-reanimated';
+
+import { HealthAreaChart, type ChartDataPoint } from '../../src/components';
+import { colors } from '../../src/theme';
+import { t } from '../../src/i18n';
+
+export type IconType = 'activity' | 'heart' | 'wind' | 'droplet' | 'thermometer';
+export type MetricStatus = 'in-range' | 'out-of-range';
+export type Timeframe = '7D' | '30D' | '60D';
+export type BaselineTrend = 'up' | 'down' | 'neutral';
+
+// メトリクスデータ型
+interface MetricData {
+  id: string;
+  name: string;
+  shortName: string;
+  cardLabel: string; // カード用の改行付きラベル
+  value: number | string;
+  unit: string;
+  status: MetricStatus;
+  statusLabel: string;
+  colorHex: string;
+  typicalRange: { min: number; max: number };
+  baseline: string;
+  baselineTrend: BaselineTrend;
+  iconType: IconType;
+  chartData: ChartDataPoint[];
+}
+
+// アイコンマッピング
+const ICON_MAP = {
+  activity: Activity,
+  heart: Heart,
+  wind: Wind,
+  droplet: Droplets,
+  thermometer: Thermometer,
+};
+
+// ステータスラベルを動的に生成するヘルパー
+const getStatusLabel = (
+  status: MetricStatus,
+  typicalRange: { min: number; max: number },
+  value: number | string
+): string => {
+  if (status === 'in-range') {
+    return `${t('metric.health.status.within')} ${typicalRange.min}-${typicalRange.max}`;
+  }
+  const numValue = typeof value === 'number' ? value : parseFloat(value);
+  if (numValue < typicalRange.min) {
+    return `Low < ${typicalRange.min}`;
+  }
+  return `High > ${typicalRange.max}`;
+};
+
+// モックデータ - sozai/tempoai-health-summary と同じ（i18n対応）
+const getMetrics = (): MetricData[] => [
+  {
+    id: 'hrv',
+    name: t('metric.health.hrv.name'),
+    shortName: t('metric.health.hrv.shortName'),
+    cardLabel: t('metric.health.hrv.cardLabel'),
+    value: 82,
+    unit: 'ms',
+    status: 'in-range',
+    statusLabel: `${t('metric.health.status.within')} 58-97`,
+    colorHex: '#22C55E',
+    typicalRange: { min: 58, max: 97 },
+    baseline: '77 ms',
+    baselineTrend: 'up',
+    iconType: 'activity',
+    chartData: [
+      { day: 'T', value: 65 },
+      { day: 'F', value: 78 },
+      { day: 'S', value: 72 },
+      { day: 'S', value: 85 },
+      { day: 'M', value: 71 },
+      { day: 'T', value: 88 },
+      { day: 'W', value: 82 },
+    ],
+  },
+  {
+    id: 'rhr',
+    name: t('metric.health.rhr.name'),
+    shortName: t('metric.health.rhr.shortName'),
+    cardLabel: t('metric.health.rhr.cardLabel'),
+    value: 59,
+    unit: 'bpm',
+    status: 'out-of-range',
+    statusLabel: 'Low < 53',
+    colorHex: '#F87171',
+    typicalRange: { min: 54, max: 63 },
+    baseline: '59 bpm',
+    baselineTrend: 'down',
+    iconType: 'heart',
+    chartData: [
+      { day: 'T', value: 56 },
+      { day: 'F', value: 52 },
+      { day: 'S', value: 52 },
+      { day: 'S', value: 59 },
+      { day: 'M', value: 62 },
+      { day: 'T', value: 59 },
+      { day: 'W', value: 51 },
+    ],
+  },
+  {
+    id: 'resp',
+    name: t('metric.health.resp.name'),
+    shortName: t('metric.health.resp.shortName'),
+    cardLabel: t('metric.health.resp.cardLabel'),
+    value: 11.0,
+    unit: 'BrPM',
+    status: 'in-range',
+    statusLabel: `${t('metric.health.status.within')} 10.5-16`,
+    colorHex: '#3B82F6',
+    typicalRange: { min: 10.5, max: 16 },
+    baseline: '11.2 BrPM',
+    baselineTrend: 'neutral',
+    iconType: 'wind',
+    chartData: [
+      { day: 'T', value: 11.2 },
+      { day: 'F', value: 10.8 },
+      { day: 'S', value: 11.5 },
+      { day: 'S', value: 11.0 },
+      { day: 'M', value: 11.1 },
+      { day: 'T', value: 10.9 },
+      { day: 'W', value: 11.0 },
+    ],
+  },
+  {
+    id: 'spo2',
+    name: t('metric.health.spo2.name'),
+    shortName: t('metric.health.spo2.shortName'),
+    cardLabel: t('metric.health.spo2.cardLabel'),
+    value: 98,
+    unit: '%',
+    status: 'in-range',
+    statusLabel: `${t('metric.health.status.within')} 96-99`,
+    colorHex: '#14B8A6',
+    typicalRange: { min: 96, max: 99 },
+    baseline: '98%',
+    baselineTrend: 'neutral',
+    iconType: 'droplet',
+    chartData: [
+      { day: 'T', value: 97 },
+      { day: 'F', value: 98 },
+      { day: 'S', value: 99 },
+      { day: 'S', value: 98 },
+      { day: 'M', value: 97 },
+      { day: 'T', value: 98 },
+      { day: 'W', value: 98 },
+    ],
+  },
+  {
+    id: 'temp',
+    name: t('metric.health.temp.name'),
+    shortName: t('metric.health.temp.shortName'),
+    cardLabel: t('metric.health.temp.cardLabel'),
+    value: 36.4,
+    unit: '°C',
+    status: 'in-range',
+    statusLabel: `${t('metric.health.status.within')} 36.1-36.9`,
+    colorHex: '#F59E0B',
+    typicalRange: { min: 36.1, max: 36.9 },
+    baseline: '36.4°C',
+    baselineTrend: 'neutral',
+    iconType: 'thermometer',
+    chartData: [
+      { day: 'T', value: 36.3 },
+      { day: 'F', value: 36.5 },
+      { day: 'S', value: 36.4 },
+      { day: 'S', value: 36.2 },
+      { day: 'M', value: 36.6 },
+      { day: 'T', value: 36.3 },
+      { day: 'W', value: 36.4 },
+    ],
+  },
+];
+
+export default function HealthDetailScreen(): React.ReactElement {
+  const router = useRouter();
+  const insets = useSafeAreaInsets();
+  const scrollViewRef = useRef<ScrollView>(null);
+
+  // i18n対応のメトリクスデータを取得
+  const metrics = useMemo(() => getMetrics(), []);
+  const [activeSection, setActiveSection] = useState<string>('hrv');
+
+  // セクションの位置を保持
+  const sectionPositions = useRef<Record<string, number>>({});
+
+  const handleBack = () => {
+    router.back();
+  };
+
+  // セクションへスクロール
+  const scrollToSection = useCallback((id: string) => {
+    setActiveSection(id);
+    const sectionY = sectionPositions.current[id];
+    const containerY = sectionPositions.current['_container'] || 0;
+    if (sectionY !== undefined && scrollViewRef.current) {
+      // コンテナの開始位置 + セクションの相対位置 - ヘッダー分のオフセット
+      const scrollY = containerY + sectionY - 70;
+      scrollViewRef.current.scrollTo({ y: scrollY, animated: true });
+    }
+  }, []);
+
+  // セクションの位置を記録
+  const handleSectionLayout = useCallback((id: string, y: number) => {
+    sectionPositions.current[id] = y;
+  }, []);
+
+  // タイムフレームに基づいてデータを生成
+  const generateTimeframeData = useCallback(
+    (
+      baseData: ChartDataPoint[],
+      tf: Timeframe,
+      typicalRange: { min: number; max: number }
+    ): ChartDataPoint[] => {
+      const baseValue =
+        typeof baseData[0]?.value === 'number' ? baseData[0].value : typicalRange.min;
+      const range = typicalRange.max - typicalRange.min;
+
+      const seededRandom = (seed: number): number => {
+        const x = Math.sin(seed * 12.9898 + seed * 78.233) * 43758.5453;
+        return x - Math.floor(x);
+      };



================================================================================
ファイル: app/app/(main)/insights.tsx
行: 116 (開始行: 114)
コミット: a35fee7
作成日時: 2026-01-07T07:53:48Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**Hardcoded "Insights" header should use i18n.**

The subtitle uses `t('screen.insights.subtitle')` but the main title is hardcoded. Maintain consistency for localization.


<details>
<summary>♻️ Proposed fix</summary>

```diff
             <Text
               className="text-3xl text-stone-900 tracking-tight mb-1"
               style={{ fontFamily: FontFamily.serif }}
             >
-              Insights
+              {t('screen.insights.title')}
             </Text>
```
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
            >
              {t('screen.insights.title')}
            </Text>
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(main)/insights.tsx around lines 114 - 116, The main title text is
hardcoded as "Insights"; replace that literal with the i18n lookup (e.g.,
t('screen.insights.title')) to match the subtitle's i18n usage. Locate the Text
element that currently renders "Insights" in the insights.tsx component and swap
the string for t('screen.insights.title'); ensure the component has access to
the same translation function (t) used for t('screen.insights.subtitle') and add
the new key to your locale resource files.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,260 @@
+/**
+ * InsightsScreen - インサイト画面
+ * sozai/tempoai/screens/InsightsScreen.tsx を React Native で完全再現
+ */
+
+import React from 'react';
+import { View, Text, ScrollView, Pressable } from 'react-native';
+import { SafeAreaView, useSafeAreaInsets } from 'react-native-safe-area-context';
+import { LinearGradient } from 'expo-linear-gradient';
+import {
+  TrendingUp,
+  Calendar,
+  AlertCircle,
+  CheckCircle2,
+  ArrowRight,
+  LucideIcon,
+} from 'lucide-react-native';
+import Animated, { FadeInDown } from 'react-native-reanimated';
+
+import { TAB_BAR_HEIGHT } from './_layout';
+import { colors, FontFamily } from '../../src/theme';
+import { t } from '../../src/i18n';
+
+// モックデータ
+const MOCK_DATA = {
+  avgScore: 74,
+  todayIndex: 1, // Tuesday
+  weeklyScores: [40, 60, 75, 45, 80, 90, 70],
+};
+
+const WEEKDAYS = ['M', 'T', 'W', 'T', 'F', 'S', 'S'];
+
+// Alert configurations
+interface AlertConfig {
+  Icon: LucideIcon;
+  color: string;
+  bg: string;
+  title: string;
+  desc: string;
+  time: string;
+}
+
+// Note: Alert translations are in ja.json under alert.*
+const getAlerts = (): AlertConfig[] => [
+  {
+    Icon: CheckCircle2,
+    color: colors.emerald[500],
+    bg: colors.emerald[50],
+    title: t('alert.recoveryComplete.title'),
+    desc: t('alert.recoveryComplete.description'),
+    time: t('alert.recoveryComplete.time'),
+  },
+  {
+    Icon: AlertCircle,
+    color: colors.amber[500],
+    bg: colors.amber[50],
+    title: t('alert.lateCaffeine.title'),
+    desc: t('alert.lateCaffeine.description'),
+    time: t('alert.lateCaffeine.time'),
+  },
+  {
+    Icon: Calendar,
+    color: colors.indigo[500],
+    bg: colors.indigo[50],
+    title: t('alert.weekendJetlag.title'),
+    desc: t('alert.weekendJetlag.description'),
+    time: t('alert.weekendJetlag.time'),
+  },
+];
+
+// Alert Item Component
+const AlertItem: React.FC<{ alert: AlertConfig; delay: number }> = ({ alert, delay }) => {
+  const { Icon, color, bg, title, desc, time } = alert;
+
+  return (
+    <Animated.View
+      entering={FadeInDown.delay(delay).duration(400)}
+      className="bg-white p-4 rounded-2xl border border-stone-100 flex-row items-start"
+      style={{ gap: 16 }}
+    >
+      <View className="p-2 rounded-xl mt-0.5" style={{ backgroundColor: bg }}>
+        <Icon size={20} color={color} />
+      </View>
+      <View className="flex-1">
+        <View className="flex-row justify-between items-center mb-1">
+          <Text className="font-bold text-stone-900 text-sm">{title}</Text>
+          <Text className="text-[10px] text-stone-400 font-medium">{time}</Text>
+        </View>
+        <Text className="text-xs text-stone-500 leading-relaxed">{desc}</Text>
+      </View>
+    </Animated.View>
+  );
+};
+
+const AnimatedPressable = Animated.createAnimatedComponent(Pressable);
+
+export default function InsightsScreen(): React.ReactElement {
+  const insets = useSafeAreaInsets();
+
+  return (
+    <View className="flex-1 bg-stone-100">
+      <SafeAreaView className="flex-1" edges={['top']}>
+        <ScrollView
+          contentContainerStyle={{
+            paddingBottom: TAB_BAR_HEIGHT + insets.bottom + 24,
+          }}
+          showsVerticalScrollIndicator={false}
+        >
+          {/* Header */}
+          <Animated.View entering={FadeInDown.duration(400)} className="pt-14 px-6 mb-6">
+            <Text
+              className="text-3xl text-stone-900 tracking-tight mb-1"
+              style={{ fontFamily: FontFamily.serif }}
+            >
+              Insights
+            </Text>



================================================================================
ファイル: app/app/(main)/insights.tsx
行: 240 (開始行: 177)
コミット: a35fee7
作成日時: 2026-01-07T07:53:48Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

**Discovery card missing `onPress` handler.**

`AnimatedPressable` is used but no `onPress` is provided. Either add navigation/action or use a regular `Animated.View` if no interaction is intended.


<details>
<summary>🐛 Add onPress or change to View</summary>

```diff
-            <AnimatedPressable
+            <Animated.View
               entering={FadeInDown.delay(200).duration(400)}
-              style={({ pressed }) => [
-                {
-                  borderRadius: 24,
-                  overflow: 'hidden',
-                },
-                pressed && { transform: [{ scale: 0.98 }] },
-              ]}
+              style={{
+                borderRadius: 24,
+                overflow: 'hidden',
+              }}
             >
             ...
-            </AnimatedPressable>
+            </Animated.View>
```

Or add an `onPress` handler for navigation to discovery details.
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
            <Animated.View
              entering={FadeInDown.delay(200).duration(400)}
              style={{
                borderRadius: 24,
                overflow: 'hidden',
              }}
            >
              <LinearGradient
                colors={[colors.indigo[400], colors.indigo[600]]}
                start={{ x: 0, y: 0 }}
                end={{ x: 1, y: 1 }}
                style={{
                  borderRadius: 24,
                  padding: 24,
                  shadowColor: colors.indigo[500],
                  shadowOffset: { width: 0, height: 12 },
                  shadowOpacity: 0.4,
                  shadowRadius: 24,
                  elevation: 12,
                }}
              >
                <View className="flex-row items-center mb-4" style={{ gap: 8 }}>
                  <View
                    style={{
                      backgroundColor: 'rgba(255, 255, 255, 0.2)',
                      padding: 8,
                      borderRadius: 10,
                    }}
                  >
                    <TrendingUp size={16} color="#FFFFFF" />
                  </View>
                  <Text className="text-xs font-bold text-white/90 uppercase tracking-wider">
                    {t('screen.insights.topDiscovery')}
                  </Text>
                </View>
                <Text
                  className="text-[22px] font-bold text-white mb-3"
                  style={{ lineHeight: 30 }}
                >
                  {t('screen.insights.topDiscoveryTitle')}
                </Text>
                <Text
                  className="text-sm text-white/80 mb-5"
                  style={{ lineHeight: 22 }}
                >
                  {t('screen.insights.topDiscoveryDescription')}
                </Text>
                <View
                  className="flex-row items-center self-start"
                  style={{
                    backgroundColor: 'rgba(255, 255, 255, 0.15)',
                    paddingHorizontal: 16,
                    paddingVertical: 10,
                    borderRadius: 12,
                  }}
                >
                  <Text className="text-sm font-semibold text-white mr-2">{t('screen.insights.viewDetails')}</Text>
                  <ArrowRight size={14} color="#FFFFFF" />
                </View>
              </LinearGradient>
            </Animated.View>
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(main)/insights.tsx around lines 177 - 240, The AnimatedPressable
here lacks an onPress handler so the card appears interactive but does nothing;
either add a proper onPress prop to AnimatedPressable (e.g., call the navigation
to discovery details or trigger the intended action) by wiring it to the
screen's navigation function or callback, or if the card is purely decorative
change AnimatedPressable to a non-interactive Animated.View (or View) to avoid
misleading affordance; locate the AnimatedPressable element in insights.tsx and
update the component accordingly.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,260 @@
+/**
+ * InsightsScreen - インサイト画面
+ * sozai/tempoai/screens/InsightsScreen.tsx を React Native で完全再現
+ */
+
+import React from 'react';
+import { View, Text, ScrollView, Pressable } from 'react-native';
+import { SafeAreaView, useSafeAreaInsets } from 'react-native-safe-area-context';
+import { LinearGradient } from 'expo-linear-gradient';
+import {
+  TrendingUp,
+  Calendar,
+  AlertCircle,
+  CheckCircle2,
+  ArrowRight,
+  LucideIcon,
+} from 'lucide-react-native';
+import Animated, { FadeInDown } from 'react-native-reanimated';
+
+import { TAB_BAR_HEIGHT } from './_layout';
+import { colors, FontFamily } from '../../src/theme';
+import { t } from '../../src/i18n';
+
+// モックデータ
+const MOCK_DATA = {
+  avgScore: 74,
+  todayIndex: 1, // Tuesday
+  weeklyScores: [40, 60, 75, 45, 80, 90, 70],
+};
+
+const WEEKDAYS = ['M', 'T', 'W', 'T', 'F', 'S', 'S'];
+
+// Alert configurations
+interface AlertConfig {
+  Icon: LucideIcon;
+  color: string;
+  bg: string;
+  title: string;
+  desc: string;
+  time: string;
+}
+
+// Note: Alert translations are in ja.json under alert.*
+const getAlerts = (): AlertConfig[] => [
+  {
+    Icon: CheckCircle2,
+    color: colors.emerald[500],
+    bg: colors.emerald[50],
+    title: t('alert.recoveryComplete.title'),
+    desc: t('alert.recoveryComplete.description'),
+    time: t('alert.recoveryComplete.time'),
+  },
+  {
+    Icon: AlertCircle,
+    color: colors.amber[500],
+    bg: colors.amber[50],
+    title: t('alert.lateCaffeine.title'),
+    desc: t('alert.lateCaffeine.description'),
+    time: t('alert.lateCaffeine.time'),
+  },
+  {
+    Icon: Calendar,
+    color: colors.indigo[500],
+    bg: colors.indigo[50],
+    title: t('alert.weekendJetlag.title'),
+    desc: t('alert.weekendJetlag.description'),
+    time: t('alert.weekendJetlag.time'),
+  },
+];
+
+// Alert Item Component
+const AlertItem: React.FC<{ alert: AlertConfig; delay: number }> = ({ alert, delay }) => {
+  const { Icon, color, bg, title, desc, time } = alert;
+
+  return (
+    <Animated.View
+      entering={FadeInDown.delay(delay).duration(400)}
+      className="bg-white p-4 rounded-2xl border border-stone-100 flex-row items-start"
+      style={{ gap: 16 }}
+    >
+      <View className="p-2 rounded-xl mt-0.5" style={{ backgroundColor: bg }}>
+        <Icon size={20} color={color} />
+      </View>
+      <View className="flex-1">
+        <View className="flex-row justify-between items-center mb-1">
+          <Text className="font-bold text-stone-900 text-sm">{title}</Text>
+          <Text className="text-[10px] text-stone-400 font-medium">{time}</Text>
+        </View>
+        <Text className="text-xs text-stone-500 leading-relaxed">{desc}</Text>
+      </View>
+    </Animated.View>
+  );
+};
+
+const AnimatedPressable = Animated.createAnimatedComponent(Pressable);
+
+export default function InsightsScreen(): React.ReactElement {
+  const insets = useSafeAreaInsets();
+
+  return (
+    <View className="flex-1 bg-stone-100">
+      <SafeAreaView className="flex-1" edges={['top']}>
+        <ScrollView
+          contentContainerStyle={{
+            paddingBottom: TAB_BAR_HEIGHT + insets.bottom + 24,
+          }}
+          showsVerticalScrollIndicator={false}
+        >
+          {/* Header */}
+          <Animated.View entering={FadeInDown.duration(400)} className="pt-14 px-6 mb-6">
+            <Text
+              className="text-3xl text-stone-900 tracking-tight mb-1"
+              style={{ fontFamily: FontFamily.serif }}
+            >
+              Insights
+            </Text>
+            <Text className="text-sm text-stone-500">{t('screen.insights.subtitle')}</Text>
+          </Animated.View>
+
+          <View className="px-6" style={{ gap: 32 }}>
+            {/* Weekly Strip */}
+            <Animated.View
+              entering={FadeInDown.delay(100).duration(400)}
+              className="bg-white p-4 rounded-2xl border border-stone-100"
+              style={{
+                shadowColor: colors.stone[900],
+                shadowOffset: { width: 0, height: 4 },
+                shadowOpacity: 0.06,
+                shadowRadius: 20,
+                elevation: 4,
+              }}
+            >
+              <View className="flex-row justify-between items-center mb-4">
+                <Text className="text-sm font-bold text-stone-800">{t('screen.insights.thisWeek')}</Text>
+                <View className="bg-indigo-50 px-2 py-1 rounded-md">
+                  <Text className="text-xs font-medium text-indigo-500">
+                    {t('screen.insights.avgScore')}: {MOCK_DATA.avgScore}
+                  </Text>
+                </View>
+              </View>
+
+              <View className="flex-row justify-between items-end pb-2" style={{ height: 96 }}>
+                {MOCK_DATA.weeklyScores.map((score, i) => {
+                  const isToday = i === MOCK_DATA.todayIndex;
+                  return (
+                    <View key={i} className="flex-1 items-center h-full" style={{ gap: 8 }}>
+                      {/* Bar Track */}
+                      <View className="flex-1 w-2 bg-stone-100 rounded-full overflow-hidden justify-end">
+                        <View
+                          className="w-full rounded-full"
+                          style={{
+                            height: `${score}%`,
+                            backgroundColor: isToday ? colors.indigo[500] : colors.indigo[300],
+                            opacity: isToday ? 1 : 0.6,
+                          }}
+                        />
+                      </View>
+                      {/* Day Label */}
+                      <View className="items-center h-4 justify-start">
+                        <Text
+                          className="text-[10px] font-bold leading-none"
+                          style={{ color: isToday ? colors.indigo[600] : colors.stone[400] }}
+                        >
+                          {WEEKDAYS[i]}
+                        </Text>
+                        {isToday && (
+                          <View className="w-1 h-1 bg-indigo-500 rounded-full mt-1" />
+                        )}
+                      </View>
+                    </View>
+                  );
+                })}
+              </View>
+            </Animated.View>
+
+            {/* Discovery Card */}
+            <AnimatedPressable
+              entering={FadeInDown.delay(200).duration(400)}
+              style={({ pressed }) => [
+                {
+                  borderRadius: 24,
+                  overflow: 'hidden',
+                },
+                pressed && { transform: [{ scale: 0.98 }] },
+              ]}
+            >
+              <LinearGradient
+                colors={[colors.indigo[400], colors.indigo[600]]}
+                start={{ x: 0, y: 0 }}
+                end={{ x: 1, y: 1 }}
+                style={{
+                  borderRadius: 24,
+                  padding: 24,
+                  shadowColor: colors.indigo[500],
+                  shadowOffset: { width: 0, height: 12 },
+                  shadowOpacity: 0.4,
+                  shadowRadius: 24,
+                  elevation: 12,
+                }}
+              >
+                <View className="flex-row items-center mb-4" style={{ gap: 8 }}>
+                  <View
+                    style={{
+                      backgroundColor: 'rgba(255, 255, 255, 0.2)',
+                      padding: 8,
+                      borderRadius: 10,
+                    }}
+                  >
+                    <TrendingUp size={16} color="#FFFFFF" />
+                  </View>
+                  <Text className="text-xs font-bold text-white/90 uppercase tracking-wider">
+                    {t('screen.insights.topDiscovery')}
+                  </Text>
+                </View>
+                <Text
+                  className="text-[22px] font-bold text-white mb-3"
+                  style={{ lineHeight: 30 }}
+                >
+                  {t('screen.insights.topDiscoveryTitle')}
+                </Text>
+                <Text
+                  className="text-sm text-white/80 mb-5"
+                  style={{ lineHeight: 22 }}
+                >
+                  {t('screen.insights.topDiscoveryDescription')}
+                </Text>
+                <View
+                  className="flex-row items-center self-start"
+                  style={{
+                    backgroundColor: 'rgba(255, 255, 255, 0.15)',
+                    paddingHorizontal: 16,
+                    paddingVertical: 10,
+                    borderRadius: 12,
+                  }}
+                >
+                  <Text className="text-sm font-semibold text-white mr-2">{t('screen.insights.viewDetails')}</Text>
+                  <ArrowRight size={14} color="#FFFFFF" />
+                </View>
+              </LinearGradient>
+            </AnimatedPressable>



================================================================================
ファイル: app/app/(main)/insights.tsx
行: 252 (開始行: 250)
コミット: a35fee7
作成日時: 2026-01-07T07:53:48Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**`getAlerts()` called on every render creates new array.**

While the list is static, calling `getAlerts()` in render creates a new array and triggers `t()` calls each time. Consider memoizing.


<details>
<summary>♻️ Proposed optimization</summary>

```diff
 export default function InsightsScreen(): React.ReactElement {
   const insets = useSafeAreaInsets();
+  const alerts = React.useMemo(() => getAlerts(), []);

   // In render:
-                {getAlerts().map((alert, index) => (
+                {alerts.map((alert, index) => (
```
</details>

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(main)/insights.tsx around lines 250 - 252, getAlerts() is being
invoked on every render creating a new array and retriggering t() calls; memoize
the alerts so the array and translations are stable across renders. Modify the
component that renders {getAlerts().map(...)} to compute a memoizedAlerts using
React.useMemo(() => getAlerts(), [/* dependencies: include only values that
actually change translations, e.g., locale or props used by getAlerts */]) and
then render memoizedAlerts.map((alert, index) => <AlertItem key={index}
alert={alert} delay={350 + index * 50} />); ensure you reference getAlerts and
AlertItem to locate the code and include locale or any inputs to getAlerts in
the dependency array so translations update when needed.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,260 @@
+/**
+ * InsightsScreen - インサイト画面
+ * sozai/tempoai/screens/InsightsScreen.tsx を React Native で完全再現
+ */
+
+import React from 'react';
+import { View, Text, ScrollView, Pressable } from 'react-native';
+import { SafeAreaView, useSafeAreaInsets } from 'react-native-safe-area-context';
+import { LinearGradient } from 'expo-linear-gradient';
+import {
+  TrendingUp,
+  Calendar,
+  AlertCircle,
+  CheckCircle2,
+  ArrowRight,
+  LucideIcon,
+} from 'lucide-react-native';
+import Animated, { FadeInDown } from 'react-native-reanimated';
+
+import { TAB_BAR_HEIGHT } from './_layout';
+import { colors, FontFamily } from '../../src/theme';
+import { t } from '../../src/i18n';
+
+// モックデータ
+const MOCK_DATA = {
+  avgScore: 74,
+  todayIndex: 1, // Tuesday
+  weeklyScores: [40, 60, 75, 45, 80, 90, 70],
+};
+
+const WEEKDAYS = ['M', 'T', 'W', 'T', 'F', 'S', 'S'];
+
+// Alert configurations
+interface AlertConfig {
+  Icon: LucideIcon;
+  color: string;
+  bg: string;
+  title: string;
+  desc: string;
+  time: string;
+}
+
+// Note: Alert translations are in ja.json under alert.*
+const getAlerts = (): AlertConfig[] => [
+  {
+    Icon: CheckCircle2,
+    color: colors.emerald[500],
+    bg: colors.emerald[50],
+    title: t('alert.recoveryComplete.title'),
+    desc: t('alert.recoveryComplete.description'),
+    time: t('alert.recoveryComplete.time'),
+  },
+  {
+    Icon: AlertCircle,
+    color: colors.amber[500],
+    bg: colors.amber[50],
+    title: t('alert.lateCaffeine.title'),
+    desc: t('alert.lateCaffeine.description'),
+    time: t('alert.lateCaffeine.time'),
+  },
+  {
+    Icon: Calendar,
+    color: colors.indigo[500],
+    bg: colors.indigo[50],
+    title: t('alert.weekendJetlag.title'),
+    desc: t('alert.weekendJetlag.description'),
+    time: t('alert.weekendJetlag.time'),
+  },
+];
+
+// Alert Item Component
+const AlertItem: React.FC<{ alert: AlertConfig; delay: number }> = ({ alert, delay }) => {
+  const { Icon, color, bg, title, desc, time } = alert;
+
+  return (
+    <Animated.View
+      entering={FadeInDown.delay(delay).duration(400)}
+      className="bg-white p-4 rounded-2xl border border-stone-100 flex-row items-start"
+      style={{ gap: 16 }}
+    >
+      <View className="p-2 rounded-xl mt-0.5" style={{ backgroundColor: bg }}>
+        <Icon size={20} color={color} />
+      </View>
+      <View className="flex-1">
+        <View className="flex-row justify-between items-center mb-1">
+          <Text className="font-bold text-stone-900 text-sm">{title}</Text>
+          <Text className="text-[10px] text-stone-400 font-medium">{time}</Text>
+        </View>
+        <Text className="text-xs text-stone-500 leading-relaxed">{desc}</Text>
+      </View>
+    </Animated.View>
+  );
+};
+
+const AnimatedPressable = Animated.createAnimatedComponent(Pressable);
+
+export default function InsightsScreen(): React.ReactElement {
+  const insets = useSafeAreaInsets();
+
+  return (
+    <View className="flex-1 bg-stone-100">
+      <SafeAreaView className="flex-1" edges={['top']}>
+        <ScrollView
+          contentContainerStyle={{
+            paddingBottom: TAB_BAR_HEIGHT + insets.bottom + 24,
+          }}
+          showsVerticalScrollIndicator={false}
+        >
+          {/* Header */}
+          <Animated.View entering={FadeInDown.duration(400)} className="pt-14 px-6 mb-6">
+            <Text
+              className="text-3xl text-stone-900 tracking-tight mb-1"
+              style={{ fontFamily: FontFamily.serif }}
+            >
+              Insights
+            </Text>
+            <Text className="text-sm text-stone-500">{t('screen.insights.subtitle')}</Text>
+          </Animated.View>
+
+          <View className="px-6" style={{ gap: 32 }}>
+            {/* Weekly Strip */}
+            <Animated.View
+              entering={FadeInDown.delay(100).duration(400)}
+              className="bg-white p-4 rounded-2xl border border-stone-100"
+              style={{
+                shadowColor: colors.stone[900],
+                shadowOffset: { width: 0, height: 4 },
+                shadowOpacity: 0.06,
+                shadowRadius: 20,
+                elevation: 4,
+              }}
+            >
+              <View className="flex-row justify-between items-center mb-4">
+                <Text className="text-sm font-bold text-stone-800">{t('screen.insights.thisWeek')}</Text>
+                <View className="bg-indigo-50 px-2 py-1 rounded-md">
+                  <Text className="text-xs font-medium text-indigo-500">
+                    {t('screen.insights.avgScore')}: {MOCK_DATA.avgScore}
+                  </Text>
+                </View>
+              </View>
+
+              <View className="flex-row justify-between items-end pb-2" style={{ height: 96 }}>
+                {MOCK_DATA.weeklyScores.map((score, i) => {
+                  const isToday = i === MOCK_DATA.todayIndex;
+                  return (
+                    <View key={i} className="flex-1 items-center h-full" style={{ gap: 8 }}>
+                      {/* Bar Track */}
+                      <View className="flex-1 w-2 bg-stone-100 rounded-full overflow-hidden justify-end">
+                        <View
+                          className="w-full rounded-full"
+                          style={{
+                            height: `${score}%`,
+                            backgroundColor: isToday ? colors.indigo[500] : colors.indigo[300],
+                            opacity: isToday ? 1 : 0.6,
+                          }}
+                        />
+                      </View>
+                      {/* Day Label */}
+                      <View className="items-center h-4 justify-start">
+                        <Text
+                          className="text-[10px] font-bold leading-none"
+                          style={{ color: isToday ? colors.indigo[600] : colors.stone[400] }}
+                        >
+                          {WEEKDAYS[i]}
+                        </Text>
+                        {isToday && (
+                          <View className="w-1 h-1 bg-indigo-500 rounded-full mt-1" />
+                        )}
+                      </View>
+                    </View>
+                  );
+                })}
+              </View>
+            </Animated.View>
+
+            {/* Discovery Card */}
+            <AnimatedPressable
+              entering={FadeInDown.delay(200).duration(400)}
+              style={({ pressed }) => [
+                {
+                  borderRadius: 24,
+                  overflow: 'hidden',
+                },
+                pressed && { transform: [{ scale: 0.98 }] },
+              ]}
+            >
+              <LinearGradient
+                colors={[colors.indigo[400], colors.indigo[600]]}
+                start={{ x: 0, y: 0 }}
+                end={{ x: 1, y: 1 }}
+                style={{
+                  borderRadius: 24,
+                  padding: 24,
+                  shadowColor: colors.indigo[500],
+                  shadowOffset: { width: 0, height: 12 },
+                  shadowOpacity: 0.4,
+                  shadowRadius: 24,
+                  elevation: 12,
+                }}
+              >
+                <View className="flex-row items-center mb-4" style={{ gap: 8 }}>
+                  <View
+                    style={{
+                      backgroundColor: 'rgba(255, 255, 255, 0.2)',
+                      padding: 8,
+                      borderRadius: 10,
+                    }}
+                  >
+                    <TrendingUp size={16} color="#FFFFFF" />
+                  </View>
+                  <Text className="text-xs font-bold text-white/90 uppercase tracking-wider">
+                    {t('screen.insights.topDiscovery')}
+                  </Text>
+                </View>
+                <Text
+                  className="text-[22px] font-bold text-white mb-3"
+                  style={{ lineHeight: 30 }}
+                >
+                  {t('screen.insights.topDiscoveryTitle')}
+                </Text>
+                <Text
+                  className="text-sm text-white/80 mb-5"
+                  style={{ lineHeight: 22 }}
+                >
+                  {t('screen.insights.topDiscoveryDescription')}
+                </Text>
+                <View
+                  className="flex-row items-center self-start"
+                  style={{
+                    backgroundColor: 'rgba(255, 255, 255, 0.15)',
+                    paddingHorizontal: 16,
+                    paddingVertical: 10,
+                    borderRadius: 12,
+                  }}
+                >
+                  <Text className="text-sm font-semibold text-white mr-2">{t('screen.insights.viewDetails')}</Text>
+                  <ArrowRight size={14} color="#FFFFFF" />
+                </View>
+              </LinearGradient>
+            </AnimatedPressable>
+
+            {/* Recent Alerts */}
+            <View className="pt-2">
+              <Animated.View entering={FadeInDown.delay(300).duration(400)}>
+                <Text className="text-sm font-bold text-stone-900 uppercase tracking-wide mb-4">
+                  {t('screen.insights.recentAlerts')}
+                </Text>
+              </Animated.View>
+              <View style={{ gap: 16 }}>
+                {getAlerts().map((alert, index) => (
+                  <AlertItem key={index} alert={alert} delay={350 + index * 50} />
+                ))}



================================================================================
ファイル: app/app/(main)/rhythm.tsx
行: 442 (開始行: 1)
コミット: a35fee7
作成日時: 2026-01-07T07:53:48Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟠 Major_

**File exceeds 400-line limit - component splitting required.**

This file contains 442 lines, exceeding the 400-line limit specified in the coding guidelines. Please split this component into smaller, focused components.

Suggested splits:
1. Extract `UpcomingWindowsSection` (lines 142-238) into a separate component
2. Extract `EnvironmentalDataSection` (lines 240-437) into a separate component
3. Consider extracting helper functions (formatDate, getPressureTrendIcon) to a utilities file



Based on coding guidelines: "ファイル 400 行以下（超過時はコンポーネント分割を要求）"

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,442 @@
+/**
+ * RhythmScreen - サーカディアンリズム可視化
+ * sozai/tempoai-circadian-sync/ を React Native で完全再現
+ */
+
+import React, { useMemo } from 'react';
+import { View, Text, ScrollView, Pressable } from 'react-native';
+import { SafeAreaView, useSafeAreaInsets } from 'react-native-safe-area-context';
+import { useRouter } from 'expo-router';
+import {
+  Calendar,
+  Sun,
+  Moon,
+  Sunrise,
+  Sunset,
+  Cloud,
+  Gauge,
+  Droplets,
+  CircleDot,
+} from 'lucide-react-native';
+import Animated, { FadeInDown } from 'react-native-reanimated';
+
+import { TAB_BAR_HEIGHT } from './_layout';
+import { RhythmInteractiveChart, type RhythmDataPoint } from '../../src/components';
+import { colors, FontFamily } from '../../src/theme';
+import { t } from '../../src/i18n';
+import { getEnvironmentData } from '../../src/constants/mockData';
+
+// モックデータ: 24時間分のエネルギーレベル (sozai版と同じデータ形式)
+const RHYTHM_DATA: RhythmDataPoint[] = [
+  { time: '6 AM', hour: 6, energy: 30 },
+  { time: '7 AM', hour: 7, energy: 45 },
+  { time: '8 AM', hour: 8, energy: 65 },
+  { time: '9 AM', hour: 9, energy: 80 },
+  { time: '10 AM', hour: 10, energy: 90, label: 'Peak' },
+  { time: '11 AM', hour: 11, energy: 92 },
+  { time: '12 PM', hour: 12, energy: 85 },
+  { time: '1 PM', hour: 13, energy: 70 },
+  { time: '2 PM', hour: 14, energy: 55 },
+  { time: '3 PM', hour: 15, energy: 50, label: 'Dip' },
+  { time: '4 PM', hour: 16, energy: 60 },
+  { time: '5 PM', hour: 17, energy: 75 },
+  { time: '6 PM', hour: 18, energy: 85 },
+  { time: '7 PM', hour: 19, energy: 80 },
+  { time: '8 PM', hour: 20, energy: 60 },
+  { time: '9 PM', hour: 21, energy: 40 },
+  { time: '10 PM', hour: 22, energy: 25 },
+  { time: '11 PM', hour: 23, energy: 15 },
+  { time: '12 AM', hour: 24, energy: 10 },
+];
+
+// 気圧トレンドのアイコンを取得
+const getPressureTrendIcon = (trend: 'up' | 'stable' | 'down'): string => {
+  switch (trend) {
+    case 'up':
+      return '↑';
+    case 'stable':
+      return '→';
+    case 'down':
+      return '↓';
+  }
+};
+
+// 日付フォーマット（英語）
+const formatDate = (date: Date): string => {
+  const days = ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'];
+  const months = [
+    'January',
+    'February',
+    'March',
+    'April',
+    'May',
+    'June',
+    'July',
+    'August',
+    'September',
+    'October',
+    'November',
+    'December',
+  ];
+  const day = days[date.getDay()];
+  const month = months[date.getMonth()];
+  const dateNum = date.getDate();
+  return `${day}, ${month} ${dateNum}`;
+};
+
+export default function RhythmScreen(): React.ReactElement {
+  const router = useRouter();
+  const insets = useSafeAreaInsets();
+
+  // 現在時刻を取得
+  const now = useMemo(() => new Date(), []);
+  const currentHour = now.getHours() + now.getMinutes() / 60;
+  const dateString = formatDate(now);
+
+  // 環境データを取得
+  const envData = useMemo(() => getEnvironmentData(), []);
+
+  const handleSeeDetails = () => {
+    router.push('/rhythm-detail');
+  };
+
+  return (
+    <View className="flex-1 bg-stone-50">
+      <SafeAreaView className="flex-1" edges={['top']}>
+        <ScrollView
+          contentContainerStyle={{
+            paddingBottom: TAB_BAR_HEIGHT + insets.bottom + 24,
+          }}
+          showsVerticalScrollIndicator={false}
+        >
+          {/* ヘッダー */}
+          <Animated.View entering={FadeInDown.duration(400)} className="px-6 pt-12 pb-2">
+            {/* 日付 */}
+            <View className="flex-row items-center mb-1" style={{ gap: 8 }}>
+              <Calendar size={16} color={colors.stone[400]} />
+              <Text
+                className="text-xs font-medium text-stone-400 uppercase"
+                style={{ letterSpacing: 1.5 }}
+              >
+                {dateString}
+              </Text>
+            </View>
+            {/* タイトル */}
+            <Text
+              className="text-3xl font-bold text-stone-900 tracking-tight"
+              style={{ fontFamily: FontFamily.serif }}
+            >
+              {t('screen.rhythm.title')}
+            </Text>
+            {/* サブタイトル */}
+            <Text className="text-base text-stone-500 mt-1 leading-relaxed">
+              {t('screen.rhythm.subtitle')}
+            </Text>
+          </Animated.View>
+
+          {/* リズムチャート */}
+          <Animated.View entering={FadeInDown.delay(100).duration(400)} className="mt-4">
+            <RhythmInteractiveChart data={RHYTHM_DATA} currentHour={currentHour} height={280} />
+          </Animated.View>
+
+          {/* Upcoming Windows セクション */}
+          <View className="px-6 mt-8">
+            {/* セクションヘッダー */}
+            <Animated.View
+              entering={FadeInDown.delay(200).duration(400)}
+              className="flex-row items-center justify-between mb-4"
+            >
+              <Text
+                className="text-xs font-bold text-stone-400 uppercase"
+                style={{ letterSpacing: 1.5 }}
+              >
+                {t('screen.rhythm.upcomingWindows')}
+              </Text>
+              <Pressable onPress={handleSeeDetails}>
+                <Text className="text-xs font-semibold text-indigo-600">{t('screen.rhythm.seeDetails')}</Text>
+              </Pressable>
+            </Animated.View>
+
+            {/* WindowCards - larger cards with colored borders */}
+            <View style={{ gap: 16 }}>
+              {/* Peak Energy Card - Active with amber border */}
+              <View
+                className="bg-white rounded-2xl"
+                style={{
+                  padding: 20,
+                  borderWidth: 2,
+                  borderColor: colors.amber[400],
+                  shadowColor: colors.amber[500],
+                  shadowOffset: { width: 0, height: 6 },
+                  shadowOpacity: 0.15,
+                  shadowRadius: 12,
+                  elevation: 6,
+                }}
+              >
+                <View className="flex-row items-center" style={{ gap: 16 }}>
+                  <View
+                    className="p-4 rounded-xl"
+                    style={{ backgroundColor: colors.amber[100] }}
+                  >
+                    <Sun size={28} color={colors.amber[600]} />
+                  </View>
+                  <View style={{ flex: 1 }}>
+                    <View className="flex-row justify-between items-center mb-2">
+                      <Text className="text-lg font-bold text-stone-900">{t('screen.rhythm.phases.peakEnergy.title')}</Text>
+                      <Text
+                        className="text-xs text-stone-500 font-medium"
+                        style={{ fontFamily: 'monospace' }}
+                      >
+                        {t('screen.rhythm.phases.peakEnergy.time')}
+                      </Text>
+                    </View>
+                    <Text className="text-sm text-stone-600 leading-relaxed">
+                      {t('screen.rhythm.phases.peakEnergy.description')}
+                    </Text>
+                  </View>
+                </View>
+              </View>
+
+              {/* Melatonin Window Card - with indigo border */}
+              <View
+                className="bg-white rounded-2xl"
+                style={{
+                  padding: 20,
+                  borderWidth: 2,
+                  borderColor: colors.indigo[400],
+                  shadowColor: colors.indigo[500],
+                  shadowOffset: { width: 0, height: 6 },
+                  shadowOpacity: 0.15,
+                  shadowRadius: 12,
+                  elevation: 6,
+                }}
+              >
+                <View className="flex-row items-center" style={{ gap: 16 }}>
+                  <View
+                    className="p-4 rounded-xl"
+                    style={{ backgroundColor: colors.indigo[100] }}
+                  >
+                    <Moon size={28} color={colors.indigo[600]} />
+                  </View>
+                  <View style={{ flex: 1 }}>
+                    <View className="flex-row justify-between items-center mb-2">
+                      <Text className="text-lg font-bold text-stone-900">{t('screen.rhythm.phases.melatoninWindow.title')}</Text>
+                      <Text
+                        className="text-xs text-stone-500 font-medium"
+                        style={{ fontFamily: 'monospace' }}
+                      >
+                        {t('screen.rhythm.phases.melatoninWindow.time')}
+                      </Text>
+                    </View>
+                    <Text className="text-sm text-stone-600 leading-relaxed">
+                      {t('screen.rhythm.phases.melatoninWindow.description')}
+                    </Text>
+                  </View>
+                </View>
+              </View>
+            </View>
+          </View>
+
+          {/* Environmental Data セクション */}
+          <View className="px-6 mt-8">
+            {/* セクションヘッダー */}
+            <Animated.View entering={FadeInDown.delay(350).duration(400)} className="mb-4">
+              <Text
+                className="text-xs font-bold text-stone-400 uppercase"
+                style={{ letterSpacing: 1.5 }}
+              >
+                {t('screen.rhythm.environmentalData')}
+              </Text>
+            </Animated.View>
+
+            {/* Row 1: Sunrise & Sunset */}
+            <View className="flex-row" style={{ gap: 12 }}>
+              {/* Sunrise Card */}
+              <View
+                className="flex-1 bg-white rounded-2xl"
+                style={{
+                  padding: 16,
+                  borderWidth: 1,
+                  borderColor: colors.stone[100],
+                  shadowColor: '#000',
+                  shadowOffset: { width: 0, height: 4 },
+                  shadowOpacity: 0.06,
+                  shadowRadius: 10,
+                  elevation: 4,
+                }}
+              >
+                <View className="flex-row items-center justify-between">
+                  <View>
+                    <Text className="text-xs font-semibold text-stone-500 mb-1">{t('screen.rhythm.sunrise')}</Text>
+                    <Text className="text-xl font-bold text-stone-900">{envData.sunrise}</Text>
+                  </View>
+                  <View
+                    className="p-3 rounded-xl"
+                    style={{ backgroundColor: colors.amber[100] }}
+                  >
+                    <Sunrise size={24} color={colors.amber[500]} />
+                  </View>
+                </View>
+              </View>
+
+              {/* Sunset Card */}
+              <View
+                className="flex-1 bg-white rounded-2xl"
+                style={{
+                  padding: 16,
+                  borderWidth: 1,
+                  borderColor: colors.stone[100],
+                  shadowColor: '#000',
+                  shadowOffset: { width: 0, height: 4 },
+                  shadowOpacity: 0.06,
+                  shadowRadius: 10,
+                  elevation: 4,
+                }}
+              >
+                <View className="flex-row items-center justify-between">
+                  <View>
+                    <Text className="text-xs font-semibold text-stone-500 mb-1">{t('screen.rhythm.sunset')}</Text>
+                    <Text className="text-xl font-bold text-stone-900">{envData.sunset}</Text>
+                  </View>
+                  <View
+                    className="p-3 rounded-xl"
+                    style={{ backgroundColor: colors.rose[100] }}
+                  >
+                    <Sunset size={24} color={colors.rose[500]} />
+                  </View>
+                </View>
+              </View>
+            </View>
+
+            {/* Row 2: Weather & Pressure */}
+            <View className="flex-row mt-3" style={{ gap: 12 }}>
+              {/* Weather Card */}
+              <View
+                className="flex-1 bg-white rounded-2xl"
+                style={{
+                  padding: 16,
+                  borderWidth: 1,
+                  borderColor: colors.stone[100],
+                  shadowColor: '#000',
+                  shadowOffset: { width: 0, height: 4 },
+                  shadowOpacity: 0.06,
+                  shadowRadius: 10,
+                  elevation: 4,
+                }}
+              >
+                <View className="flex-row items-center justify-between">
+                  <View>
+                    <Text className="text-xs font-semibold text-stone-500 mb-1">{t('screen.rhythm.environment.weather')}</Text>
+                    <Text className="text-xl font-bold text-stone-900">{envData.weather.condition}</Text>
+                    <Text className="text-xs text-stone-400">{envData.weather.temperature}°C</Text>
+                  </View>
+                  <View
+                    className="p-3 rounded-xl"
+                    style={{ backgroundColor: colors.blue[50] }}
+                  >
+                    <Cloud size={24} color={colors.blue[500]} />
+                  </View>
+                </View>
+              </View>
+
+              {/* Pressure Card */}
+              <View
+                className="flex-1 bg-white rounded-2xl"
+                style={{
+                  padding: 16,
+                  borderWidth: 1,
+                  borderColor: colors.stone[100],
+                  shadowColor: '#000',
+                  shadowOffset: { width: 0, height: 4 },
+                  shadowOpacity: 0.06,
+                  shadowRadius: 10,
+                  elevation: 4,
+                }}
+              >
+                <View className="flex-row items-center justify-between">
+                  <View>
+                    <Text className="text-xs font-semibold text-stone-500 mb-1">{t('screen.rhythm.environment.pressure')}</Text>
+                    <Text className="text-xl font-bold text-stone-900">
+                      {envData.pressure.value}
+                      <Text className="text-sm font-normal text-stone-400"> hPa</Text>
+                    </Text>
+                    <Text className="text-xs text-stone-400">
+                      {getPressureTrendIcon(envData.pressure.trend)} {t(`screen.rhythm.environment.pressureTrend.${envData.pressure.trend}`)}
+                    </Text>
+                  </View>
+                  <View
+                    className="p-3 rounded-xl"
+                    style={{ backgroundColor: colors.emerald[100] }}
+                  >
+                    <Gauge size={24} color={colors.emerald[500]} />
+                  </View>
+                </View>
+              </View>
+            </View>
+
+            {/* Row 3: UV & Moon Phase */}
+            <View className="flex-row mt-3" style={{ gap: 12 }}>
+              {/* UV Card */}
+              <View
+                className="flex-1 bg-white rounded-2xl"
+                style={{
+                  padding: 16,
+                  borderWidth: 1,
+                  borderColor: colors.stone[100],
+                  shadowColor: '#000',
+                  shadowOffset: { width: 0, height: 4 },
+                  shadowOpacity: 0.06,
+                  shadowRadius: 10,
+                  elevation: 4,
+                }}
+              >
+                <View className="flex-row items-center justify-between">
+                  <View>
+                    <Text className="text-xs font-semibold text-stone-500 mb-1">{t('screen.rhythm.environment.uv')}</Text>
+                    <Text className="text-xl font-bold text-stone-900">{envData.uv.index}</Text>
+                    <Text className="text-xs text-stone-400">{envData.uv.level}</Text>
+                  </View>
+                  <View
+                    className="p-3 rounded-xl"
+                    style={{ backgroundColor: colors.amber[50] }}
+                  >
+                    <Droplets size={24} color={colors.amber[500]} />
+                  </View>
+                </View>
+              </View>
+
+              {/* Moon Phase Card */}
+              <View
+                className="flex-1 bg-white rounded-2xl"
+                style={{
+                  padding: 16,
+                  borderWidth: 1,
+                  borderColor: colors.stone[100],
+                  shadowColor: '#000',
+                  shadowOffset: { width: 0, height: 4 },
+                  shadowOpacity: 0.06,
+                  shadowRadius: 10,
+                  elevation: 4,
+                }}
+              >
+                <View className="flex-row items-center justify-between">
+                  <View>
+                    <Text className="text-xs font-semibold text-stone-500 mb-1">{t('screen.rhythm.environment.moonPhase')}</Text>
+                    <Text className="text-xl font-bold text-stone-900">{envData.moonPhase.phase}</Text>
+                    <Text className="text-xs text-stone-400">{envData.moonPhase.illumination}%</Text>
+                  </View>
+                  <View
+                    className="p-3 rounded-xl"
+                    style={{ backgroundColor: colors.purple[50] }}
+                  >
+                    <CircleDot size={24} color={colors.purple[500]} />
+                  </View>
+                </View>
+              </View>
+            </View>
+          </View>
+        </ScrollView>
+      </SafeAreaView>
+    </View>
+  );
+}



================================================================================
ファイル: app/app/(main)/rhythm.tsx
行: 87 (開始行: N/A)
コミット: a35fee7
作成日時: 2026-01-07T07:53:48Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟠 Major_

**Use arrow function instead of function declaration.**

Convert to const arrow function per coding guidelines.



<details>
<summary>♻️ Proposed fix</summary>

```diff
-export default function RhythmScreen(): React.ReactElement {
+const RhythmScreen = (): React.ReactElement => {
```

Add export at the end:

```diff
+
+export default RhythmScreen;
```
</details>

Based on coding guidelines: "Never use function declarations; use arrow functions"

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
const RhythmScreen = (): React.ReactElement => {
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(main)/rhythm.tsx at line 87, Replace the function declaration for
RhythmScreen with a const arrow function: change the declaration "export default
function RhythmScreen(): React.ReactElement { ... }" to a local constant "const
RhythmScreen = (): React.ReactElement => { ... }" and then add a separate
"export default RhythmScreen" statement at the end of the module so the
component follows the arrow-function guideline.
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,442 @@
+/**
+ * RhythmScreen - サーカディアンリズム可視化
+ * sozai/tempoai-circadian-sync/ を React Native で完全再現
+ */
+
+import React, { useMemo } from 'react';
+import { View, Text, ScrollView, Pressable } from 'react-native';
+import { SafeAreaView, useSafeAreaInsets } from 'react-native-safe-area-context';
+import { useRouter } from 'expo-router';
+import {
+  Calendar,
+  Sun,
+  Moon,
+  Sunrise,
+  Sunset,
+  Cloud,
+  Gauge,
+  Droplets,
+  CircleDot,
+} from 'lucide-react-native';
+import Animated, { FadeInDown } from 'react-native-reanimated';
+
+import { TAB_BAR_HEIGHT } from './_layout';
+import { RhythmInteractiveChart, type RhythmDataPoint } from '../../src/components';
+import { colors, FontFamily } from '../../src/theme';
+import { t } from '../../src/i18n';
+import { getEnvironmentData } from '../../src/constants/mockData';
+
+// モックデータ: 24時間分のエネルギーレベル (sozai版と同じデータ形式)
+const RHYTHM_DATA: RhythmDataPoint[] = [
+  { time: '6 AM', hour: 6, energy: 30 },
+  { time: '7 AM', hour: 7, energy: 45 },
+  { time: '8 AM', hour: 8, energy: 65 },
+  { time: '9 AM', hour: 9, energy: 80 },
+  { time: '10 AM', hour: 10, energy: 90, label: 'Peak' },
+  { time: '11 AM', hour: 11, energy: 92 },
+  { time: '12 PM', hour: 12, energy: 85 },
+  { time: '1 PM', hour: 13, energy: 70 },
+  { time: '2 PM', hour: 14, energy: 55 },
+  { time: '3 PM', hour: 15, energy: 50, label: 'Dip' },
+  { time: '4 PM', hour: 16, energy: 60 },
+  { time: '5 PM', hour: 17, energy: 75 },
+  { time: '6 PM', hour: 18, energy: 85 },
+  { time: '7 PM', hour: 19, energy: 80 },
+  { time: '8 PM', hour: 20, energy: 60 },
+  { time: '9 PM', hour: 21, energy: 40 },
+  { time: '10 PM', hour: 22, energy: 25 },
+  { time: '11 PM', hour: 23, energy: 15 },
+  { time: '12 AM', hour: 24, energy: 10 },
+];
+
+// 気圧トレンドのアイコンを取得
+const getPressureTrendIcon = (trend: 'up' | 'stable' | 'down'): string => {
+  switch (trend) {
+    case 'up':
+      return '↑';
+    case 'stable':
+      return '→';
+    case 'down':
+      return '↓';
+  }
+};
+
+// 日付フォーマット（英語）
+const formatDate = (date: Date): string => {
+  const days = ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'];
+  const months = [
+    'January',
+    'February',
+    'March',
+    'April',
+    'May',
+    'June',
+    'July',
+    'August',
+    'September',
+    'October',
+    'November',
+    'December',
+  ];
+  const day = days[date.getDay()];
+  const month = months[date.getMonth()];
+  const dateNum = date.getDate();
+  return `${day}, ${month} ${dateNum}`;
+};
+
+export default function RhythmScreen(): React.ReactElement {



================================================================================
ファイル: app/app/(main)/rhythm.tsx
行: 101 (開始行: 99)
コミット: a35fee7
作成日時: 2026-01-07T07:53:48Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

**Add explicit return type to handleSeeDetails.**



<details>
<summary>♻️ Proposed fix</summary>

```diff
-  const handleSeeDetails = () => {
+  const handleSeeDetails = (): void => {
     router.push('/rhythm-detail');
   };
```
</details>

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(main)/rhythm.tsx around lines 99 - 101, The handler function
handleSeeDetails lacks an explicit return type; update its signature to include
an explicit return type of void (e.g., const handleSeeDetails = (): void => {
... }) so callers and the TypeScript compiler clearly know it returns nothing;
keep the body using router.push('/rhythm-detail') unchanged and ensure any
surrounding references still accept a void-returning callback.
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,442 @@
+/**
+ * RhythmScreen - サーカディアンリズム可視化
+ * sozai/tempoai-circadian-sync/ を React Native で完全再現
+ */
+
+import React, { useMemo } from 'react';
+import { View, Text, ScrollView, Pressable } from 'react-native';
+import { SafeAreaView, useSafeAreaInsets } from 'react-native-safe-area-context';
+import { useRouter } from 'expo-router';
+import {
+  Calendar,
+  Sun,
+  Moon,
+  Sunrise,
+  Sunset,
+  Cloud,
+  Gauge,
+  Droplets,
+  CircleDot,
+} from 'lucide-react-native';
+import Animated, { FadeInDown } from 'react-native-reanimated';
+
+import { TAB_BAR_HEIGHT } from './_layout';
+import { RhythmInteractiveChart, type RhythmDataPoint } from '../../src/components';
+import { colors, FontFamily } from '../../src/theme';
+import { t } from '../../src/i18n';
+import { getEnvironmentData } from '../../src/constants/mockData';
+
+// モックデータ: 24時間分のエネルギーレベル (sozai版と同じデータ形式)
+const RHYTHM_DATA: RhythmDataPoint[] = [
+  { time: '6 AM', hour: 6, energy: 30 },
+  { time: '7 AM', hour: 7, energy: 45 },
+  { time: '8 AM', hour: 8, energy: 65 },
+  { time: '9 AM', hour: 9, energy: 80 },
+  { time: '10 AM', hour: 10, energy: 90, label: 'Peak' },
+  { time: '11 AM', hour: 11, energy: 92 },
+  { time: '12 PM', hour: 12, energy: 85 },
+  { time: '1 PM', hour: 13, energy: 70 },
+  { time: '2 PM', hour: 14, energy: 55 },
+  { time: '3 PM', hour: 15, energy: 50, label: 'Dip' },
+  { time: '4 PM', hour: 16, energy: 60 },
+  { time: '5 PM', hour: 17, energy: 75 },
+  { time: '6 PM', hour: 18, energy: 85 },
+  { time: '7 PM', hour: 19, energy: 80 },
+  { time: '8 PM', hour: 20, energy: 60 },
+  { time: '9 PM', hour: 21, energy: 40 },
+  { time: '10 PM', hour: 22, energy: 25 },
+  { time: '11 PM', hour: 23, energy: 15 },
+  { time: '12 AM', hour: 24, energy: 10 },
+];
+
+// 気圧トレンドのアイコンを取得
+const getPressureTrendIcon = (trend: 'up' | 'stable' | 'down'): string => {
+  switch (trend) {
+    case 'up':
+      return '↑';
+    case 'stable':
+      return '→';
+    case 'down':
+      return '↓';
+  }
+};
+
+// 日付フォーマット（英語）
+const formatDate = (date: Date): string => {
+  const days = ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'];
+  const months = [
+    'January',
+    'February',
+    'March',
+    'April',
+    'May',
+    'June',
+    'July',
+    'August',
+    'September',
+    'October',
+    'November',
+    'December',
+  ];
+  const day = days[date.getDay()];
+  const month = months[date.getMonth()];
+  const dateNum = date.getDate();
+  return `${day}, ${month} ${dateNum}`;
+};
+
+export default function RhythmScreen(): React.ReactElement {
+  const router = useRouter();
+  const insets = useSafeAreaInsets();
+
+  // 現在時刻を取得
+  const now = useMemo(() => new Date(), []);
+  const currentHour = now.getHours() + now.getMinutes() / 60;
+  const dateString = formatDate(now);
+
+  // 環境データを取得
+  const envData = useMemo(() => getEnvironmentData(), []);
+
+  const handleSeeDetails = () => {
+    router.push('/rhythm-detail');
+  };



================================================================================
ファイル: app/app/(main)/rhythm.tsx
行: 108 (開始行: N/A)
コミット: a35fee7
作成日時: 2026-01-07T07:53:48Z
================================================================================

【コメント】
_🛠️ Refactor suggestion_ | _🟠 Major_

**Extract inline styles to StyleSheet.create().**

This file extensively uses inline styles, violating the coding guidelines. Extract all inline styles to a `styles` object using `StyleSheet.create()`.



Based on coding guidelines: "**StyleSheet.create()** でスタイル定義（インラインスタイル禁止）"


Also applies to: 165-174, 203-212, 257-266, 285-294, 316-325, 344-354, 382-391, 411-420

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,442 @@
+/**
+ * RhythmScreen - サーカディアンリズム可視化
+ * sozai/tempoai-circadian-sync/ を React Native で完全再現
+ */
+
+import React, { useMemo } from 'react';
+import { View, Text, ScrollView, Pressable } from 'react-native';
+import { SafeAreaView, useSafeAreaInsets } from 'react-native-safe-area-context';
+import { useRouter } from 'expo-router';
+import {
+  Calendar,
+  Sun,
+  Moon,
+  Sunrise,
+  Sunset,
+  Cloud,
+  Gauge,
+  Droplets,
+  CircleDot,
+} from 'lucide-react-native';
+import Animated, { FadeInDown } from 'react-native-reanimated';
+
+import { TAB_BAR_HEIGHT } from './_layout';
+import { RhythmInteractiveChart, type RhythmDataPoint } from '../../src/components';
+import { colors, FontFamily } from '../../src/theme';
+import { t } from '../../src/i18n';
+import { getEnvironmentData } from '../../src/constants/mockData';
+
+// モックデータ: 24時間分のエネルギーレベル (sozai版と同じデータ形式)
+const RHYTHM_DATA: RhythmDataPoint[] = [
+  { time: '6 AM', hour: 6, energy: 30 },
+  { time: '7 AM', hour: 7, energy: 45 },
+  { time: '8 AM', hour: 8, energy: 65 },
+  { time: '9 AM', hour: 9, energy: 80 },
+  { time: '10 AM', hour: 10, energy: 90, label: 'Peak' },
+  { time: '11 AM', hour: 11, energy: 92 },
+  { time: '12 PM', hour: 12, energy: 85 },
+  { time: '1 PM', hour: 13, energy: 70 },
+  { time: '2 PM', hour: 14, energy: 55 },
+  { time: '3 PM', hour: 15, energy: 50, label: 'Dip' },
+  { time: '4 PM', hour: 16, energy: 60 },
+  { time: '5 PM', hour: 17, energy: 75 },
+  { time: '6 PM', hour: 18, energy: 85 },
+  { time: '7 PM', hour: 19, energy: 80 },
+  { time: '8 PM', hour: 20, energy: 60 },
+  { time: '9 PM', hour: 21, energy: 40 },
+  { time: '10 PM', hour: 22, energy: 25 },
+  { time: '11 PM', hour: 23, energy: 15 },
+  { time: '12 AM', hour: 24, energy: 10 },
+];
+
+// 気圧トレンドのアイコンを取得
+const getPressureTrendIcon = (trend: 'up' | 'stable' | 'down'): string => {
+  switch (trend) {
+    case 'up':
+      return '↑';
+    case 'stable':
+      return '→';
+    case 'down':
+      return '↓';
+  }
+};
+
+// 日付フォーマット（英語）
+const formatDate = (date: Date): string => {
+  const days = ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'];
+  const months = [
+    'January',
+    'February',
+    'March',
+    'April',
+    'May',
+    'June',
+    'July',
+    'August',
+    'September',
+    'October',
+    'November',
+    'December',
+  ];
+  const day = days[date.getDay()];
+  const month = months[date.getMonth()];
+  const dateNum = date.getDate();
+  return `${day}, ${month} ${dateNum}`;
+};
+
+export default function RhythmScreen(): React.ReactElement {
+  const router = useRouter();
+  const insets = useSafeAreaInsets();
+
+  // 現在時刻を取得
+  const now = useMemo(() => new Date(), []);
+  const currentHour = now.getHours() + now.getMinutes() / 60;
+  const dateString = formatDate(now);
+
+  // 環境データを取得
+  const envData = useMemo(() => getEnvironmentData(), []);
+
+  const handleSeeDetails = () => {
+    router.push('/rhythm-detail');
+  };
+
+  return (
+    <View className="flex-1 bg-stone-50">
+      <SafeAreaView className="flex-1" edges={['top']}>
+        <ScrollView
+          contentContainerStyle={{
+            paddingBottom: TAB_BAR_HEIGHT + insets.bottom + 24,



================================================================================
ファイル: app/app/(main)/sleep-detail.tsx
行: 32 (開始行: N/A)
コミット: a35fee7
作成日時: 2026-01-07T07:53:48Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟠 Major_

**Use arrow function instead of function declaration.**

The coding guidelines require arrow functions consistently instead of function declarations. Convert this to a const arrow function.



<details>
<summary>♻️ Proposed fix</summary>

```diff
-export default function SleepDetailScreen(): React.ReactElement {
+const SleepDetailScreen = (): React.ReactElement => {
```

And add at the end of the file:

```diff
+
+export default SleepDetailScreen;
```
</details>

Based on coding guidelines: "Use arrow functions consistently instead of function declarations" and "Never use function declarations; use arrow functions"


> Committable suggestion skipped: line range outside the PR's diff.

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(main)/sleep-detail.tsx at line 32, Replace the function declaration
for SleepDetailScreen with a const arrow function: change "export default
function SleepDetailScreen(): React.ReactElement {" to "const SleepDetailScreen
= (): React.ReactElement => {", and then add "export default SleepDetailScreen"
at the end of the file to preserve the default export; keep the same body and
return type.
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,241 @@
+/**
+ * SleepDetailScreen - Sleep詳細画面
+ */
+
+import React, { useState } from 'react';
+import { View, Text, ScrollView, Pressable } from 'react-native';
+import { SafeAreaView, useSafeAreaInsets } from 'react-native-safe-area-context';
+import { useRouter } from 'expo-router';
+import { ChevronLeft, Moon, Sun, Sparkles } from 'lucide-react-native';
+import Animated, { FadeInDown } from 'react-native-reanimated';
+
+import {
+  DualRingProgress,
+  TimeframeSelector,
+  MiniBarChart,
+  SleepStagesBar,
+  type Timeframe,
+} from '../../src/components';
+import { colors, FontFamily } from '../../src/theme';
+import { t } from '../../src/i18n';
+import { MOCK_DETAIL } from '../../src/constants/mockData';
+import { useHealthStore } from '../../src/stores/healthStore';
+
+// スコアに応じたステータスを取得
+const getSleepStatus = (score: number): string => {
+  if (score >= 85) return t('score.sleep.status.excellent');
+  if (score >= 70) return t('score.sleep.status.good');
+  if (score >= 50) return t('score.sleep.status.fair');
+  return t('score.sleep.status.poor');
+};
+
+export default function SleepDetailScreen(): React.ReactElement {



================================================================================
ファイル: app/app/(main)/sleep-detail.tsx
行: 45 (開始行: 43)
コミット: a35fee7
作成日時: 2026-01-07T07:53:48Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

**Add explicit return type to handleBack.**

All functions should declare explicit return types per coding guidelines.



<details>
<summary>♻️ Proposed fix</summary>

```diff
-  const handleBack = () => {
+  const handleBack = (): void => {
     router.back();
   };
```
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
  const handleBack = (): void => {
    router.back();
  };
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(main)/sleep-detail.tsx around lines 43 - 45, handleBack currently
lacks an explicit return type; update its declaration to include a return type
(void) to satisfy coding guidelines by changing the function signature for
handleBack to explicitly return void while keeping the body calling
router.back(), e.g., declare function/method handleBack(): void { ... } so the
symbol handleBack has an explicit return type.
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,241 @@
+/**
+ * SleepDetailScreen - Sleep詳細画面
+ */
+
+import React, { useState } from 'react';
+import { View, Text, ScrollView, Pressable } from 'react-native';
+import { SafeAreaView, useSafeAreaInsets } from 'react-native-safe-area-context';
+import { useRouter } from 'expo-router';
+import { ChevronLeft, Moon, Sun, Sparkles } from 'lucide-react-native';
+import Animated, { FadeInDown } from 'react-native-reanimated';
+
+import {
+  DualRingProgress,
+  TimeframeSelector,
+  MiniBarChart,
+  SleepStagesBar,
+  type Timeframe,
+} from '../../src/components';
+import { colors, FontFamily } from '../../src/theme';
+import { t } from '../../src/i18n';
+import { MOCK_DETAIL } from '../../src/constants/mockData';
+import { useHealthStore } from '../../src/stores/healthStore';
+
+// スコアに応じたステータスを取得
+const getSleepStatus = (score: number): string => {
+  if (score >= 85) return t('score.sleep.status.excellent');
+  if (score >= 70) return t('score.sleep.status.good');
+  if (score >= 50) return t('score.sleep.status.fair');
+  return t('score.sleep.status.poor');
+};
+
+export default function SleepDetailScreen(): React.ReactElement {
+  const router = useRouter();
+  const insets = useSafeAreaInsets();
+  const [timeframe, setTimeframe] = useState<Timeframe>('7D');
+  const { dailySnapshot } = useHealthStore();
+  
+  // healthStoreから計算済みのスコアを取得
+  const sleepScore = dailySnapshot?.scores?.sleep ?? 0;
+  
+  const data = MOCK_DETAIL.sleep;
+
+  const handleBack = () => {
+    router.back();
+  };



================================================================================
ファイル: app/app/(main)/sleep-detail.tsx
行: 57 (開始行: 55)
コミット: a35fee7
作成日時: 2026-01-07T07:53:49Z
================================================================================

【コメント】
_🛠️ Refactor suggestion_ | _🟠 Major_

**Extract inline styles to StyleSheet.create().**

The coding guidelines require using `StyleSheet.create()` for style definitions and prohibit inline styles. This file has extensive inline style usage throughout the component.

Consider extracting all inline styles to a `styles` object created with `StyleSheet.create()` at the bottom of the file for better performance and maintainability.



Based on coding guidelines: "**StyleSheet.create()** でスタイル定義（インラインスタイル禁止）"


Also applies to: 65-67, 75-75, 101-101, 112-112, 143-149, 163-169, 185-191, 203-221

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(main)/sleep-detail.tsx around lines 55 - 57, The component in
sleep-detail.tsx uses inline styles (e.g., the Pressable style={({ pressed }) =>
[...]}) and many other inline style props; extract all inline styles into a
styles object created with StyleSheet.create() at the bottom of the file and
replace inline occurrences with references to that object, using style arrays
for conditional states (for example replace style={({ pressed }) => [{
backgroundColor: pressed ? colors.stone[100] : 'transparent' }]) with style={({
pressed }) => [styles.pressable, pressed && styles.pressablePressed] or with a
wrapper that maps pressed to styles.pressablePressed), and move color constants
to the style definitions where possible; update all similar inline usages in
this component (the Pressable, Text, View and any Touchable/pressable elements)
to use styles.<newline/>
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,241 @@
+/**
+ * SleepDetailScreen - Sleep詳細画面
+ */
+
+import React, { useState } from 'react';
+import { View, Text, ScrollView, Pressable } from 'react-native';
+import { SafeAreaView, useSafeAreaInsets } from 'react-native-safe-area-context';
+import { useRouter } from 'expo-router';
+import { ChevronLeft, Moon, Sun, Sparkles } from 'lucide-react-native';
+import Animated, { FadeInDown } from 'react-native-reanimated';
+
+import {
+  DualRingProgress,
+  TimeframeSelector,
+  MiniBarChart,
+  SleepStagesBar,
+  type Timeframe,
+} from '../../src/components';
+import { colors, FontFamily } from '../../src/theme';
+import { t } from '../../src/i18n';
+import { MOCK_DETAIL } from '../../src/constants/mockData';
+import { useHealthStore } from '../../src/stores/healthStore';
+
+// スコアに応じたステータスを取得
+const getSleepStatus = (score: number): string => {
+  if (score >= 85) return t('score.sleep.status.excellent');
+  if (score >= 70) return t('score.sleep.status.good');
+  if (score >= 50) return t('score.sleep.status.fair');
+  return t('score.sleep.status.poor');
+};
+
+export default function SleepDetailScreen(): React.ReactElement {
+  const router = useRouter();
+  const insets = useSafeAreaInsets();
+  const [timeframe, setTimeframe] = useState<Timeframe>('7D');
+  const { dailySnapshot } = useHealthStore();
+  
+  // healthStoreから計算済みのスコアを取得
+  const sleepScore = dailySnapshot?.scores?.sleep ?? 0;
+  
+  const data = MOCK_DETAIL.sleep;
+
+  const handleBack = () => {
+    router.back();
+  };
+
+  return (
+    <View className="flex-1 bg-stone-100">
+      <SafeAreaView className="flex-1" edges={['top']}>
+        {/* Header */}
+        <View className="flex-row items-center px-6 py-4 border-b border-stone-100 bg-stone-100">
+          <Pressable
+            onPress={handleBack}
+            className="w-10 h-10 items-center justify-center rounded-full"
+            style={({ pressed }) => [
+              { backgroundColor: pressed ? colors.stone[100] : 'transparent' },
+            ]}



================================================================================
ファイル: app/app/(onboarding)/basic-info.tsx
行: 18 (開始行: N/A)
コミット: a35fee7
作成日時: 2026-01-07T07:53:49Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

**Unused import: `BorderRadius`**

`BorderRadius` is imported but not used anywhere in this file. Remove the unused import to keep the code clean.

```diff
-import { Colors, Spacing, BorderRadius, FontFamily } from '../../src/theme';
+import { Colors, Spacing, FontFamily } from '../../src/theme';
```

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
import { Colors, Spacing, FontFamily } from '../../src/theme';
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(onboarding)/basic-info.tsx at line 18, Remove the unused import
symbol BorderRadius from the import list in basic-info.tsx; modify the import
statement that currently imports { Colors, Spacing, BorderRadius, FontFamily }
so it only imports the actually used symbols (e.g., Colors, Spacing, FontFamily)
to eliminate the unused BorderRadius import.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -1,20 +1,29 @@
+/**
+ * BasicInfoScreen - 基本情報入力画面
+ * sozai/new のスタイルを React Native で再現
+ * Step 4 of 9
+ */
+
 import React, { useState } from 'react';
 import {
   View,
   Text,
   StyleSheet,
-  SafeAreaView,
   ScrollView,
   TouchableOpacity,
+  Dimensions,
 } from 'react-native';
+import { SafeAreaView } from 'react-native-safe-area-context';
 import { useRouter } from 'expo-router';
-import { User2 } from 'lucide-react-native';
-import { Colors, Spacing, Typography, BorderRadius } from '../../src/theme';
+import { Colors, Spacing, BorderRadius, FontFamily } from '../../src/theme';



================================================================================
ファイル: app/app/(onboarding)/chronotype.tsx
行: 120 (開始行: 118)
コミット: a35fee7
作成日時: 2026-01-07T07:53:49Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**Hardcoded "Continue" should use i18n translation.**

Other screens use `t()` for button labels. This should be consistent for localization support.


<details>
<summary>♻️ Proposed fix</summary>

```diff
           <PrimaryButton onPress={handleNext}>
-            Continue
+            {t('common.continue')}
           </PrimaryButton>
```
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
          <PrimaryButton onPress={handleNext}>
            {t('common.continue')}
          </PrimaryButton>
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(onboarding)/chronotype.tsx around lines 118 - 120, Replace the
hardcoded "Continue" label on the PrimaryButton with the i18n translation call
(e.g. t('common.continue') or the appropriate key your project uses) so the
button uses localized text; locate the PrimaryButton element in chronotype.tsx
(the one with onPress={handleNext}) and change its children to t('...'), and
ensure the file imports or uses the existing useTranslation hook (or the t
function already in scope) so t is available.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -50,110 +61,192 @@ export default function ChronotypeScreen(): JSX.Element {
   };
 
   return (
-    <SafeAreaView style={styles.container}>
-      <View style={styles.content}>
-        <Text style={styles.title}>あなたのタイプは？</Text>
-        <Text style={styles.description}>
-          朝型・夜型の傾向を教えてください{'\n'}
-          アドバイスの最適化に使用します
-        </Text>
+    <View style={styles.container}>
+      {/* Decorative background blobs */}
+      <View style={styles.blobTopRight} />
+      <View style={styles.blobBottomLeft} />
 
-        <View style={styles.options}>
-          {CHRONOTYPE_OPTIONS.map((option) => (
-            <TouchableOpacity
-              key={option.value}
+      <SafeAreaView style={styles.safeArea}>
+        {/* Progress Bar */}
+        <View style={styles.progressContainer}>
+          {[...Array(TOTAL_STEPS)].map((_, idx) => (
+            <View
+              key={idx}
               style={[
-                styles.optionCard,
-                chronotype === option.value && styles.optionCardSelected,
+                styles.progressSegment,
+                idx < CURRENT_STEP ? styles.progressActive : styles.progressInactive,
               ]}
-              onPress={() => setChronotype(option.value)}
-            >
-              <View style={styles.optionIcon}>{option.icon}</View>
-              <Text
+            />
+          ))}
+        </View>
+
+        {/* Content */}
+        <View style={styles.content}>
+          <Text style={styles.emoji}>🌙</Text>
+          <Text style={styles.title}>Circadian Sync</Text>
+          <Text style={styles.description}>
+            Light, food, and movement timed right can fix your sleep and boost your focus.
+          </Text>
+
+          <View style={styles.options}>
+            {CHRONOTYPE_OPTIONS.map((option) => (
+              <TouchableOpacity
+                key={option.value}
                 style={[
-                  styles.optionLabel,
-                  chronotype === option.value && styles.optionLabelSelected,
+                  styles.optionCard,
+                  chronotype === option.value && styles.optionCardSelected,
                 ]}
+                onPress={() => setChronotype(option.value)}
               >
-                {getChronotypeLabel(option.value)}
-              </Text>
-              <Text style={styles.optionDescription}>{option.description}</Text>
-            </TouchableOpacity>
-          ))}
+                <View style={styles.optionIcon}>{option.icon}</View>
+                <Text
+                  style={[
+                    styles.optionLabel,
+                    chronotype === option.value && styles.optionLabelSelected,
+                  ]}
+                >
+                  {getChronotypeLabel(option.value)}
+                </Text>
+                <Text style={styles.optionDescription}>{option.description}</Text>
+              </TouchableOpacity>
+            ))}
+          </View>
         </View>
-      </View>
 
-      <View style={styles.footer}>
-        <PrimaryButton onPress={handleNext}>次へ</PrimaryButton>
-      </View>
-    </SafeAreaView>
+        {/* Footer */}
+        <View style={styles.footer}>
+          <PrimaryButton onPress={handleNext}>
+            Continue
+          </PrimaryButton>



================================================================================
ファイル: app/app/(onboarding)/healthkit.tsx
行: 11 (開始行: N/A)
コミット: a35fee7
作成日時: 2026-01-07T07:53:49Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

**Unused import: `Heart`**

`Heart` is imported from `lucide-react-native` but is never used in the component (an emoji is used instead on line 54). Remove the unused import.

```diff
-import { Heart } from 'lucide-react-native';
```

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion

```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/app/(onboarding)/healthkit.tsx at line 11, Remove the unused import
"Heart" from 'lucide-react-native' in the component (the emoji on line 54 is
used instead); delete the `import { Heart } from 'lucide-react-native';`
statement and verify there are no other references to the Heart symbol (or
replace the emoji with <Heart /> if you intended to use the icon).
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -1,11 +1,22 @@
+/**
+ * HealthKitScreen - ヘルスケア連携画面
+ * sozai/new のスタイルを React Native で再現
+ * Step 2 of 9
+ */
+
 import React from 'react';
-import { View, Text, StyleSheet, SafeAreaView } from 'react-native';
+import { View, Text, StyleSheet, Dimensions } from 'react-native';
+import { SafeAreaView } from 'react-native-safe-area-context';
 import { useRouter } from 'expo-router';
 import { Heart } from 'lucide-react-native';



================================================================================
ファイル: app/src/api/helpers/adviceRequestBuilder.ts
行: 91 (開始行: N/A)
コミット: a35fee7
作成日時: 2026-01-07T07:53:49Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**TODO comment indicates incomplete implementation.**

The `vsTargetBedtime` calculation is hardcoded as `'+15min'`. This should be computed from actual bedtime data vs. target.

Would you like me to help implement the actual calculation or open an issue to track this?

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/api/helpers/adviceRequestBuilder.ts at line 91, The vsTargetBedtime
value is hardcoded; replace it with a computed difference between the actual
bedtime and the user's target bedtime (e.g., implement a helper
computeVsTargetBedtime(actualBedtime, targetBedtime) and assign its result to
vsTargetBedtime). Parse both times into a common unit (minutes), compute
minutesDiff = actualMinutes - targetMinutes, format the result as a string with
'+' or '-' and 'min' (e.g., '+15min' or '-10min'), and handle missing/invalid
inputs by returning null or a sensible default. Update references in
adviceRequestBuilder.ts to call computeVsTargetBedtime where vsTargetBedtime is
currently set so the value is derived from real data.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -1,141 +1,123 @@
 /**
  * AdviceRequest構築ヘルパー
- * ストアの状態からAPIリクエストを構築
+ * ストアの状態からAPIリクエストを構築（新形式）
  */
 
-import type { AdviceRequest } from '../types';
-import { useUserStore } from '../../stores/userStore';
-import { useHealthStore } from '../../stores/healthStore';
-import { useInsightStore } from '../../stores/insightStore';
+import type { AdviceRequest, HealthMetrics, UserProfile, WeatherData, ScoresData, RhythmPhases } from '../types';
 
-/** 曜日名の配列（英語） */
-const DAY_NAMES = [
-  'Sunday',
-  'Monday',
-  'Tuesday',
-  'Wednesday',
-  'Thursday',
-  'Friday',
-  'Saturday',
-] as const;
+interface BuildAdviceRequestParams {
+  healthStore: {
+    metrics: {
+      sleep: {
+        durationMinutes: number;
+        deepSleepMinutes: number;
+        deepSleepPercent?: number;
+        remSleepMinutes: number;
+        remSleepPercent?: number;
+        bedtime?: string;
+        wakeTime?: string;
+      } | null;
+      hrv: { current?: number; baseline?: number; baseline30d?: number } | null;
+      rhr?: { current: number; baseline: number } | null;
+    };
+    // 4スコア（将来的に実装）
+    recoveryScore?: number;
+    sleepScore?: number;
+    rhythmScore?: number;
+    energyScore?: number;
+  };
+  userStore: {
+    profile: { goals?: unknown; wakeUpTime?: unknown; windDownTime?: unknown } | null;
+  };
+  weather: WeatherData;
+}
 
 /**
- * 現在のストア状態からAdviceRequestを構築
- * @returns AdviceRequest または null（プロファイルがない場合）
+ * アドバイスAPIリクエストを構築
  */
-export const buildAdviceRequest = (): AdviceRequest | null => {
-  const userState = useUserStore.getState();
-  const healthState = useHealthStore.getState();
-  const insightState = useInsightStore.getState();
+export const buildAdviceRequest = ({
+  healthStore,
+  userStore,
+  weather,
+}: BuildAdviceRequestParams): AdviceRequest => {
+  const profile = userStore.profile;
+  const goals = (profile?.goals as string[]) ?? ['better_sleep'];
+  const wakeUpTime = (profile?.wakeUpTime as string) ?? '07:00';
+  const windDownTime = (profile?.windDownTime as string) ?? '23:00';
 
-  const { profile } = userState;
-  if (!profile) {
-    console.warn('buildAdviceRequest: profile is null');
-    return null;
-  }
-
-  const {
-    sleepMetrics,
-    hrvMetrics,
-    activityMetrics,
-    dailyScores,
-    rhythmAnalysis,
-    weather,
-  } = healthState;
+  const user: UserProfile = {
+    goals: goals as ('better_sleep' | 'more_energy' | 'less_stress' | 'peak_performance')[],
+    wakeUpTime,
+    windDownTime,
+  };
 
-  const { todayMood, todayMode } = insightState;
+  // 4スコア（現在はモックデータ、将来的にhealthStoreから取得）
+  const scores: ScoresData = {
+    recovery: healthStore.recoveryScore ?? 70,
+    sleep: healthStore.sleepScore ?? 85,
+    rhythm: healthStore.rhythmScore ?? 92,
+    energy: healthStore.energyScore ?? 78,
+  };
 
-  // 現在時刻情報
-  const now = new Date();
-  const hours = now.getHours().toString().padStart(2, '0');
-  const minutes = now.getMinutes().toString().padStart(2, '0');
-  const currentTime = `${hours}:${minutes}`;
-  const dayOfWeek = DAY_NAMES[now.getDay()];
+  const sleep = healthStore.metrics.sleep;
+  const hrv = healthStore.metrics.hrv;
+  const rhr = healthStore.metrics.rhr;
 
-  // スコアのデフォルト値
-  const scores = dailyScores || {
-    autonomic: 0,
-    sleep: 0,
-    rhythm: 0,
-    activity: 0,
-  };
+  // 睡眠データの計算
+  const durationMinutes = sleep?.durationMinutes ?? 428;
+  const deepSleepMinutes = sleep?.deepSleepMinutes ?? 105;
+  const remSleepMinutes = sleep?.remSleepMinutes ?? 95;
+  const deepSleepPercent = sleep?.deepSleepPercent ?? (durationMinutes > 0 ? Math.round((deepSleepMinutes / durationMinutes) * 100) : 23);
+  const remSleepPercent = sleep?.remSleepPercent ?? (durationMinutes > 0 ? Math.round((remSleepMinutes / durationMinutes) * 100) : 22);
 
-  // リズム分析のデフォルト値
-  const rhythm = rhythmAnalysis || {
-    bedtimeStddevMinutes: 0,
-    wakeTimeStddevMinutes: 0,
-    consecutiveStableDays: 0,
-    status: 'unstable' as const,
+  const healthMetrics: HealthMetrics = {
+    hrv: {
+      current: hrv?.current ?? hrv?.baseline30d ?? 82,
+      baseline: hrv?.baseline ?? hrv?.baseline30d ?? 77,
+      deviation: hrv?.current && hrv?.baseline ? ((hrv.current - hrv.baseline) / hrv.baseline * 100) : 6,
+    },
+    rhr: {
+      current: rhr?.current ?? 59,
+      baseline: rhr?.baseline ?? 59,
+    },
+    sleep: {
+      durationMinutes,
+      deepSleepMinutes,
+      deepSleepPercent,
+      remSleepMinutes,
+      remSleepPercent,
+      bedtime: sleep?.bedtime ?? '23:15',
+      wakeTime: sleep?.wakeTime ?? '06:45',
+      vsTargetBedtime: '+15min', // TODO: 実際の計算を実装



================================================================================
ファイル: app/src/api/helpers/adviceRequestBuilder.ts
行: 113 (開始行: 95)
コミット: a35fee7
作成日時: 2026-01-07T07:53:49Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**Hardcoded mock `rhythmPhases` should be parameterized or computed.**

The `rhythmPhases` object contains static time values that won't adapt to user preferences or actual circadian rhythm calculations. This could lead to inconsistent advice if the user's wake/wind-down times differ.

Consider accepting `rhythmPhases` as a parameter or deriving it from the user's schedule:

<details>
<summary>♻️ Suggested approach</summary>

```diff
 interface BuildAdviceRequestParams {
   // ... existing fields
+  rhythmPhases?: RhythmPhases;
 }

 export const buildAdviceRequest = ({
   healthStore,
   userStore,
   weather,
+  rhythmPhases: providedRhythmPhases,
 }: BuildAdviceRequestParams): AdviceRequest => {
   // ...
-  const rhythmPhases: RhythmPhases = {
-    peakFocus: { start: '09:00', end: '12:00' },
-    // ...hardcoded values
-  };
+  const rhythmPhases = providedRhythmPhases ?? calculateDefaultRhythmPhases(wakeUpTime, windDownTime);
```
</details>


> Committable suggestion skipped: line range outside the PR's diff.

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/api/helpers/adviceRequestBuilder.ts around lines 95 - 113, The
hardcoded rhythmPhases object should be parameterized or computed; update the
function that defines/uses rhythmPhases (look for the rhythmPhases constant in
adviceRequestBuilder.ts) to accept an optional RhythmPhases parameter (or derive
it from user schedule fields like wakeTime/sleepTime or preferences) and use
that input when building the advice request, falling back to a sensible computed
default if none provided; ensure any callers of the function are updated to pass
through user schedule/preferences or rely on the computed default.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -1,141 +1,123 @@
 /**
  * AdviceRequest構築ヘルパー
- * ストアの状態からAPIリクエストを構築
+ * ストアの状態からAPIリクエストを構築（新形式）
  */
 
-import type { AdviceRequest } from '../types';
-import { useUserStore } from '../../stores/userStore';
-import { useHealthStore } from '../../stores/healthStore';
-import { useInsightStore } from '../../stores/insightStore';
+import type { AdviceRequest, HealthMetrics, UserProfile, WeatherData, ScoresData, RhythmPhases } from '../types';
 
-/** 曜日名の配列（英語） */
-const DAY_NAMES = [
-  'Sunday',
-  'Monday',
-  'Tuesday',
-  'Wednesday',
-  'Thursday',
-  'Friday',
-  'Saturday',
-] as const;
+interface BuildAdviceRequestParams {
+  healthStore: {
+    metrics: {
+      sleep: {
+        durationMinutes: number;
+        deepSleepMinutes: number;
+        deepSleepPercent?: number;
+        remSleepMinutes: number;
+        remSleepPercent?: number;
+        bedtime?: string;
+        wakeTime?: string;
+      } | null;
+      hrv: { current?: number; baseline?: number; baseline30d?: number } | null;
+      rhr?: { current: number; baseline: number } | null;
+    };
+    // 4スコア（将来的に実装）
+    recoveryScore?: number;
+    sleepScore?: number;
+    rhythmScore?: number;
+    energyScore?: number;
+  };
+  userStore: {
+    profile: { goals?: unknown; wakeUpTime?: unknown; windDownTime?: unknown } | null;
+  };
+  weather: WeatherData;
+}
 
 /**
- * 現在のストア状態からAdviceRequestを構築
- * @returns AdviceRequest または null（プロファイルがない場合）
+ * アドバイスAPIリクエストを構築
  */
-export const buildAdviceRequest = (): AdviceRequest | null => {
-  const userState = useUserStore.getState();
-  const healthState = useHealthStore.getState();
-  const insightState = useInsightStore.getState();
+export const buildAdviceRequest = ({
+  healthStore,
+  userStore,
+  weather,
+}: BuildAdviceRequestParams): AdviceRequest => {
+  const profile = userStore.profile;
+  const goals = (profile?.goals as string[]) ?? ['better_sleep'];
+  const wakeUpTime = (profile?.wakeUpTime as string) ?? '07:00';
+  const windDownTime = (profile?.windDownTime as string) ?? '23:00';
 
-  const { profile } = userState;
-  if (!profile) {
-    console.warn('buildAdviceRequest: profile is null');
-    return null;
-  }
-
-  const {
-    sleepMetrics,
-    hrvMetrics,
-    activityMetrics,
-    dailyScores,
-    rhythmAnalysis,
-    weather,
-  } = healthState;
+  const user: UserProfile = {
+    goals: goals as ('better_sleep' | 'more_energy' | 'less_stress' | 'peak_performance')[],
+    wakeUpTime,
+    windDownTime,
+  };
 
-  const { todayMood, todayMode } = insightState;
+  // 4スコア（現在はモックデータ、将来的にhealthStoreから取得）
+  const scores: ScoresData = {
+    recovery: healthStore.recoveryScore ?? 70,
+    sleep: healthStore.sleepScore ?? 85,
+    rhythm: healthStore.rhythmScore ?? 92,
+    energy: healthStore.energyScore ?? 78,
+  };
 
-  // 現在時刻情報
-  const now = new Date();
-  const hours = now.getHours().toString().padStart(2, '0');
-  const minutes = now.getMinutes().toString().padStart(2, '0');
-  const currentTime = `${hours}:${minutes}`;
-  const dayOfWeek = DAY_NAMES[now.getDay()];
+  const sleep = healthStore.metrics.sleep;
+  const hrv = healthStore.metrics.hrv;
+  const rhr = healthStore.metrics.rhr;
 
-  // スコアのデフォルト値
-  const scores = dailyScores || {
-    autonomic: 0,
-    sleep: 0,
-    rhythm: 0,
-    activity: 0,
-  };
+  // 睡眠データの計算
+  const durationMinutes = sleep?.durationMinutes ?? 428;
+  const deepSleepMinutes = sleep?.deepSleepMinutes ?? 105;
+  const remSleepMinutes = sleep?.remSleepMinutes ?? 95;
+  const deepSleepPercent = sleep?.deepSleepPercent ?? (durationMinutes > 0 ? Math.round((deepSleepMinutes / durationMinutes) * 100) : 23);
+  const remSleepPercent = sleep?.remSleepPercent ?? (durationMinutes > 0 ? Math.round((remSleepMinutes / durationMinutes) * 100) : 22);
 
-  // リズム分析のデフォルト値
-  const rhythm = rhythmAnalysis || {
-    bedtimeStddevMinutes: 0,
-    wakeTimeStddevMinutes: 0,
-    consecutiveStableDays: 0,
-    status: 'unstable' as const,
+  const healthMetrics: HealthMetrics = {
+    hrv: {
+      current: hrv?.current ?? hrv?.baseline30d ?? 82,
+      baseline: hrv?.baseline ?? hrv?.baseline30d ?? 77,
+      deviation: hrv?.current && hrv?.baseline ? ((hrv.current - hrv.baseline) / hrv.baseline * 100) : 6,
+    },
+    rhr: {
+      current: rhr?.current ?? 59,
+      baseline: rhr?.baseline ?? 59,
+    },
+    sleep: {
+      durationMinutes,
+      deepSleepMinutes,
+      deepSleepPercent,
+      remSleepMinutes,
+      remSleepPercent,
+      bedtime: sleep?.bedtime ?? '23:15',
+      wakeTime: sleep?.wakeTime ?? '06:45',
+      vsTargetBedtime: '+15min', // TODO: 実際の計算を実装
+    },
   };
 
-  const request: AdviceRequest = {
-    profile: {
-      nickname: profile.nickname,
-      age: profile.age,
-      gender: profile.gender,
-      chronotype: profile.chronotype,
-      occupation: profile.occupation,
-      exerciseFrequency: profile.exerciseFrequency,
-      targetBedtime: profile.targetBedtime,
+  // RhythmPhases（現在はモックデータ、将来的に計算）
+  const rhythmPhases: RhythmPhases = {
+    peakFocus: {
+      start: '09:00',
+      end: '12:00',
     },
-    healthData: {
-      sleep: sleepMetrics
-        ? {
-            bedtime: sleepMetrics.bedtime.toISOString(),
-            wakeTime: sleepMetrics.wakeTime.toISOString(),
-            durationHours: sleepMetrics.durationMinutes / 60,
-            deepSleepMinutes: sleepMetrics.deepSleepMinutes,
-            remSleepMinutes: sleepMetrics.remSleepMinutes,
-            deepSleepRatio:
-              sleepMetrics.durationMinutes > 0
-                ? sleepMetrics.deepSleepMinutes / sleepMetrics.durationMinutes
-                : 0,
-          }
-        : undefined,
-      hrv: hrvMetrics
-        ? {
-            value: hrvMetrics.value,
-            baseline30d: hrvMetrics.baseline30d,
-            deviationPercent:
-              hrvMetrics.baseline30d > 0
-                ? ((hrvMetrics.value - hrvMetrics.baseline30d) /
-                    hrvMetrics.baseline30d) *
-                  100
-                : 0,
-          }
-        : undefined,
-      activity: activityMetrics
-        ? {
-            stepsYesterday: activityMetrics.stepsYesterday,
-            activeMinutesYesterday: activityMetrics.activeMinutesYesterday,
-          }
-        : undefined,
-      scores,
-      rhythmAnalysis: rhythm,
+    afternoonDip: {
+      start: '14:00',
+      end: '16:00',
     },
-    location: {
-      // TODO: 実際の位置情報を使用
-      latitude: 35.6762,
-      longitude: 139.6503,
-      city: '東京',
+    secondWind: {
+      start: '17:00',
+      end: '19:00',
     },
-    context: {
-      currentTime,
-      dayOfWeek,
-      mood: todayMood ?? undefined,
-      todayMode: todayMode || 'normal',
+    windDown: {
+      start: '21:00',
+      end: '23:00',
     },
-    weather: weather
-      ? {
-          temperature: weather.temp,
-          humidity: healthState.weatherHumidity ?? 50,
-          pressure: weather.pressure,
-          weatherCode: healthState.weatherCode ?? 0,
-          uvIndexMax: weather.uv,
-        }
-      : undefined,
   };



================================================================================
ファイル: app/src/components/HealthMetricDetail.tsx
行: 28 (開始行: 17)
コミット: a35fee7
作成日時: 2026-01-07T07:53:49Z
================================================================================

【コメント】
_🛠️ Refactor suggestion_ | _🟠 Major_

**Export the `HealthMetricDetailProps` interface**

The interface is used as props for a public component but is not exported. Per coding guidelines, public APIs should be documented and exported for better type safety when consuming this component.

```diff
-interface HealthMetricDetailProps {
+export interface HealthMetricDetailProps {
```

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
export interface HealthMetricDetailProps {
  id: string;
  name: string;
  value: number | string;
  unit: string;
  colorHex: string;
  chartData: ChartDataPoint[];
  typicalRange: { min: number; max: number };
  baseline: string;
  baselineTrend: BaselineTrend;
  delay?: number;
}
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/components/HealthMetricDetail.tsx around lines 17 - 28, The
HealthMetricDetailProps interface is currently declared but not exported; update
its declaration to export it so consumers can import the prop type (export
interface HealthMetricDetailProps { ... }). Ensure the exported name matches
usages (e.g., HealthMetricDetail component props/type annotations) and update
any local imports/exports if necessary to keep typings consistent across
modules.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,294 @@
+/**
+ * HealthMetricDetail - Health詳細セクション
+ * sozai/tempoai-health-summary/components/DetailSection.tsx を再現
+ */
+
+import React, { useState, useMemo } from 'react';
+import { View, Text, Pressable, StyleSheet } from 'react-native';
+import { ArrowUp, ArrowDown, CheckCircle2 } from 'lucide-react-native';
+import Animated, { FadeInDown } from 'react-native-reanimated';
+
+import { colors } from '../theme';
+import { HealthAreaChart, type ChartDataPoint } from './HealthAreaChart';
+
+export type Timeframe = '7D' | '30D' | '60D';
+export type BaselineTrend = 'up' | 'down' | 'neutral';
+
+interface HealthMetricDetailProps {
+  id: string;
+  name: string;
+  value: number | string;
+  unit: string;
+  colorHex: string;
+  chartData: ChartDataPoint[];
+  typicalRange: { min: number; max: number };
+  baseline: string;
+  baselineTrend: BaselineTrend;
+  delay?: number;
+}



================================================================================
ファイル: app/src/components/HealthMetricDetail.tsx
行: 43 (開始行: 40)
コミット: a35fee7
作成日時: 2026-01-07T07:53:49Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**Duplicated `seededRandom` function**

This function is identical to the one in `app/src/constants/mockDataFactory.ts` (lines 62-65). Consider importing the existing utility to avoid code duplication.

<details>
<summary>♻️ Suggested refactor</summary>

```diff
+import { seededRandom } from '../constants/mockDataFactory';

-  // シード値を基にした擬似ランダム生成
-  const seededRandom = (seed: number): number => {
-    const x = Math.sin(seed * 12.9898 + seed * 78.233) * 43758.5453;
-    return x - Math.floor(x);
-  };
```

</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
import { seededRandom } from '../constants/mockDataFactory';
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/components/HealthMetricDetail.tsx around lines 40 - 43, The local
seededRandom function in HealthMetricDetail.tsx is a duplicate of the utility in
mockDataFactory; remove the local implementation and import the existing
seededRandom utility from the mockDataFactory module (use the correct
named/default import that matches how seededRandom is exported there) then
update any local references to use the imported function.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,294 @@
+/**
+ * HealthMetricDetail - Health詳細セクション
+ * sozai/tempoai-health-summary/components/DetailSection.tsx を再現
+ */
+
+import React, { useState, useMemo } from 'react';
+import { View, Text, Pressable, StyleSheet } from 'react-native';
+import { ArrowUp, ArrowDown, CheckCircle2 } from 'lucide-react-native';
+import Animated, { FadeInDown } from 'react-native-reanimated';
+
+import { colors } from '../theme';
+import { HealthAreaChart, type ChartDataPoint } from './HealthAreaChart';
+
+export type Timeframe = '7D' | '30D' | '60D';
+export type BaselineTrend = 'up' | 'down' | 'neutral';
+
+interface HealthMetricDetailProps {
+  id: string;
+  name: string;
+  value: number | string;
+  unit: string;
+  colorHex: string;
+  chartData: ChartDataPoint[];
+  typicalRange: { min: number; max: number };
+  baseline: string;
+  baselineTrend: BaselineTrend;
+  delay?: number;
+}
+
+// タイムフレームに基づいてデータを生成
+const generateTimeframeData = (
+  baseData: ChartDataPoint[],
+  timeframe: Timeframe,
+  typicalRange: { min: number; max: number }
+): ChartDataPoint[] => {
+  const baseValue = typeof baseData[0]?.value === 'number' ? baseData[0].value : typicalRange.min;
+  const range = typicalRange.max - typicalRange.min;
+
+  // シード値を基にした擬似ランダム生成
+  const seededRandom = (seed: number): number => {
+    const x = Math.sin(seed * 12.9898 + seed * 78.233) * 43758.5453;
+    return x - Math.floor(x);
+  };



================================================================================
ファイル: app/src/components/HealthMetricDetail.tsx
行: 95 (開始行: 84)
コミット: a35fee7
作成日時: 2026-01-07T07:53:49Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

**Unused prop: `id`**

The `id` prop is destructured from props but is never used in the component. Either remove it from the interface and destructuring, or use it (e.g., as a key for accessibility or analytics).

```diff
 export const HealthMetricDetail: React.FC<HealthMetricDetailProps> = ({
-  id,
   name,
   value,
   // ...rest
```




> Committable suggestion skipped: line range outside the PR's diff.

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/components/HealthMetricDetail.tsx around lines 84 - 95, The
component HealthMetricDetail currently destructures an unused prop id; remove id
from the HealthMetricDetailProps type and from the destructuring list in the
HealthMetricDetail function, or alternatively use id (for example, set it as the
root element's id attribute or a data-id/data-testid attribute, or include it in
any analytics/event payload) so it is referenced; update any callers if you
remove the prop to stop passing id, or add the attribute usage inside
HealthMetricDetail to consume it.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,294 @@
+/**
+ * HealthMetricDetail - Health詳細セクション
+ * sozai/tempoai-health-summary/components/DetailSection.tsx を再現
+ */
+
+import React, { useState, useMemo } from 'react';
+import { View, Text, Pressable, StyleSheet } from 'react-native';
+import { ArrowUp, ArrowDown, CheckCircle2 } from 'lucide-react-native';
+import Animated, { FadeInDown } from 'react-native-reanimated';
+
+import { colors } from '../theme';
+import { HealthAreaChart, type ChartDataPoint } from './HealthAreaChart';
+
+export type Timeframe = '7D' | '30D' | '60D';
+export type BaselineTrend = 'up' | 'down' | 'neutral';
+
+interface HealthMetricDetailProps {
+  id: string;
+  name: string;
+  value: number | string;
+  unit: string;
+  colorHex: string;
+  chartData: ChartDataPoint[];
+  typicalRange: { min: number; max: number };
+  baseline: string;
+  baselineTrend: BaselineTrend;
+  delay?: number;
+}
+
+// タイムフレームに基づいてデータを生成
+const generateTimeframeData = (
+  baseData: ChartDataPoint[],
+  timeframe: Timeframe,
+  typicalRange: { min: number; max: number }
+): ChartDataPoint[] => {
+  const baseValue = typeof baseData[0]?.value === 'number' ? baseData[0].value : typicalRange.min;
+  const range = typicalRange.max - typicalRange.min;
+
+  // シード値を基にした擬似ランダム生成
+  const seededRandom = (seed: number): number => {
+    const x = Math.sin(seed * 12.9898 + seed * 78.233) * 43758.5453;
+    return x - Math.floor(x);
+  };
+
+  const generateValue = (index: number, seed: number): number => {
+    const variance = range * 0.4;
+    const random = seededRandom(index + seed);
+    const value = baseValue + (random - 0.5) * variance * 2;
+    return Math.round(Math.max(typicalRange.min * 0.85, Math.min(typicalRange.max * 1.15, value)) * 10) / 10;
+  };
+
+  switch (timeframe) {
+    case '7D':
+      // 7日間: 元のデータをそのまま使用
+      return baseData;
+
+    case '30D': {
+      // 30日間: 週単位のラベルで5データポイント
+      const labels = ['W1', 'W2', 'W3', 'W4', 'Now'];
+      return labels.map((label, i) => ({
+        day: label,
+        value: i === labels.length - 1
+          ? (typeof baseData[baseData.length - 1]?.value === 'number' ? baseData[baseData.length - 1].value : baseValue)
+          : generateValue(i, 30),
+      }));
+    }
+
+    case '60D': {
+      // 60日間: 2週間単位のラベルで5データポイント
+      const labels = ['6w', '4w', '2w', '1w', 'Now'];
+      return labels.map((label, i) => ({
+        day: label,
+        value: i === labels.length - 1
+          ? (typeof baseData[baseData.length - 1]?.value === 'number' ? baseData[baseData.length - 1].value : baseValue)
+          : generateValue(i, 60),
+      }));
+    }
+
+    default:
+      return baseData;
+  }
+};
+
+export const HealthMetricDetail: React.FC<HealthMetricDetailProps> = ({
+  id,
+  name,
+  value,
+  unit,
+  colorHex,
+  chartData,
+  typicalRange,
+  baseline,
+  baselineTrend,
+  delay = 0,
+}) => {



================================================================================
ファイル: app/src/constants/mockData.ts
行: 519 (開始行: 506)
コミット: a35fee7
作成日時: 2026-01-07T07:53:49Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**Helper functions should be in a utility module.**

`getGreeting`, `formatTime`, `formatCurrentTime`, `formatDate`, and `formatDateEnglish` are general-purpose utilities that don't belong in a mock data file. They're likely used across the app and should be in `app/src/utils/`.

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/constants/mockData.ts around lines 506 - 519, Extract the
general-purpose helper functions getGreeting, formatTime, formatCurrentTime,
formatDate, and formatDateEnglish from app/src/constants/mockData.ts into a new
utility module under app/src/utils/ (e.g., app/src/utils/dateTime.ts), export
them as named exports, update any imports that referenced these functions from
mockData to import from the new utils module, and run a quick search/replace for
any other usages to ensure no broken imports remain; preserve existing function
signatures and behavior and add an index export if your project prefers
centralized utils exports.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -136,76 +423,496 @@ export const MOCK_HEALTH_METRICS: HealthMetrics = {
   },
 };
 
-// モックリズム分析
 export const MOCK_RHYTHM_ANALYSIS: RhythmAnalysis = {
   bedtimeStddevMinutes: 22,
   wakeTimeStddevMinutes: 18,
   consecutiveStableDays: 5,
-  status: 'stable',
+  status: "stable",
   isStable: true,
   bedtimeConsistencyScore: 85,
   wakeTimeConsistencyScore: 88,
 };
 
-// モック日次スコア履歴（週間）
 export const MOCK_WEEKLY_SCORES: DailyScoreSnapshot[] = [
   {
-    id: '1',
-    date: new Date('2025-01-01'),
-    autonomicScore: 78,
+    id: "1",
+    date: new Date("2025-01-01"),
+    recoveryScore: 78,
     sleepScore: 65,
     rhythmScore: 82,
-    activityScore: 70,
+    energyScore: 70,
   },
   {
-    id: '2',
-    date: new Date('2025-01-02'),
-    autonomicScore: 80,
+    id: "2",
+    date: new Date("2025-01-02"),
+    recoveryScore: 80,
     sleepScore: 70,
     rhythmScore: 85,
-    activityScore: 75,
+    energyScore: 75,
   },
   {
-    id: '3',
-    date: new Date('2025-01-03'),
-    autonomicScore: 75,
+    id: "3",
+    date: new Date("2025-01-03"),
+    recoveryScore: 75,
     sleepScore: 68,
     rhythmScore: 88,
-    activityScore: 65,
+    energyScore: 65,
   },
   {
-    id: '4',
-    date: new Date('2025-01-04'),
-    autonomicScore: 82,
+    id: "4",
+    date: new Date("2025-01-04"),
+    recoveryScore: 82,
     sleepScore: 74,
     rhythmScore: 90,
-    activityScore: 78,
+    energyScore: 78,
   },
   {
-    id: '5',
-    date: new Date('2025-01-05'),
-    autonomicScore: 85,
+    id: "5",
+    date: new Date("2025-01-05"),
+    recoveryScore: 85,
     sleepScore: 72,
     rhythmScore: 94,
-    activityScore: 80,
+    energyScore: 80,
   },
   {
-    id: '6',
-    date: new Date('2025-01-06'),
-    autonomicScore: 85,
+    id: "6",
+    date: new Date("2025-01-06"),
+    recoveryScore: 85,
     sleepScore: 72,
     rhythmScore: 94,
-    activityScore: 82,
+    energyScore: 82,
   },
   {
-    id: '7',
-    date: new Date('2025-01-07'),
-    autonomicScore: 88,
+    id: "7",
+    date: new Date("2025-01-07"),
+    recoveryScore: 88,
     sleepScore: 76,
     rhythmScore: 95,
-    activityScore: 85,
+    energyScore: 85,
   },
 ];
 
-// 分析期間
-export type TimePeriod = 'weekly' | 'monthly';
+export type TimePeriod = "weekly" | "monthly";
+
+// =============================================================================
+// HELPER FUNCTIONS
+// =============================================================================
+
+/**
+ * 時間帯に応じた挨拶を取得（「さん」付き）
+ * @param name ユーザー名（省略時は挨拶のみ）
+ * @returns "こんにちは太郎さん" 形式
+ */
+export const getGreeting = (name?: string): string => {
+  const hour = new Date().getHours();
+  let greeting: string;
+  if (hour >= 5 && hour < 12) {
+    greeting = "おはようございます";
+  } else if (hour >= 12 && hour < 17) {
+    greeting = "こんにちは";
+  } else if (hour >= 17 && hour < 21) {
+    greeting = "こんばんは";
+  } else {
+    greeting = "お疲れさまです";
+  }
+  return name ? `${greeting}${name}さん` : greeting;
+};



================================================================================
ファイル: app/src/constants/mockData.ts
行: 603 (開始行: 590)
コミット: a35fee7
作成日時: 2026-01-07T07:53:49Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

**Mid-file imports violate import organization conventions.**

Imports should be grouped at the top of the file. Having imports at line 590 makes the file harder to understand and maintain.

<details>
<summary>♻️ Move imports to top of file</summary>

```diff
+import {
+  HealthMetricHistory,
+  DailySnapshot,
+  RealtimeMetrics,
+  RealtimeHealthMetric,
+  BarChartDataPoint,
+} from '../domain/models/healthHistory';
+import {
+  getMockMetricHistory,
+  getAllScoreHistories,
+  getAllHealthMetricHistories,
+  formatDateString,
+} from './mockDataFactory';
+import { toBarChartData, calculateDeviationPercent } from '../utils/healthDataTransformer';

 import { Sun, Moon } from "lucide-react-native";
 // ... rest of existing imports
```
</details>


> Committable suggestion skipped: line range outside the PR's diff.

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/constants/mockData.ts around lines 590 - 603, The imports for
HealthMetricHistory, DailySnapshot, RealtimeMetrics, RealtimeHealthMetric,
BarChartDataPoint, getMockMetricHistory, getAllScoreHistories,
getAllHealthMetricHistories, formatDateString, toBarChartData, and
calculateDeviationPercent are mid-file and must be moved to the top of the
module; relocate these import statements from their current position to the top
of the file, group them logically (type/model imports together, factory helpers
together, and util transformers together), and ensure import ordering/formatting
matches the project's import organization convention.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -136,76 +423,496 @@ export const MOCK_HEALTH_METRICS: HealthMetrics = {
   },
 };
 
-// モックリズム分析
 export const MOCK_RHYTHM_ANALYSIS: RhythmAnalysis = {
   bedtimeStddevMinutes: 22,
   wakeTimeStddevMinutes: 18,
   consecutiveStableDays: 5,
-  status: 'stable',
+  status: "stable",
   isStable: true,
   bedtimeConsistencyScore: 85,
   wakeTimeConsistencyScore: 88,
 };
 
-// モック日次スコア履歴（週間）
 export const MOCK_WEEKLY_SCORES: DailyScoreSnapshot[] = [
   {
-    id: '1',
-    date: new Date('2025-01-01'),
-    autonomicScore: 78,
+    id: "1",
+    date: new Date("2025-01-01"),
+    recoveryScore: 78,
     sleepScore: 65,
     rhythmScore: 82,
-    activityScore: 70,
+    energyScore: 70,
   },
   {
-    id: '2',
-    date: new Date('2025-01-02'),
-    autonomicScore: 80,
+    id: "2",
+    date: new Date("2025-01-02"),
+    recoveryScore: 80,
     sleepScore: 70,
     rhythmScore: 85,
-    activityScore: 75,
+    energyScore: 75,
   },
   {
-    id: '3',
-    date: new Date('2025-01-03'),
-    autonomicScore: 75,
+    id: "3",
+    date: new Date("2025-01-03"),
+    recoveryScore: 75,
     sleepScore: 68,
     rhythmScore: 88,
-    activityScore: 65,
+    energyScore: 65,
   },
   {
-    id: '4',
-    date: new Date('2025-01-04'),
-    autonomicScore: 82,
+    id: "4",
+    date: new Date("2025-01-04"),
+    recoveryScore: 82,
     sleepScore: 74,
     rhythmScore: 90,
-    activityScore: 78,
+    energyScore: 78,
   },
   {
-    id: '5',
-    date: new Date('2025-01-05'),
-    autonomicScore: 85,
+    id: "5",
+    date: new Date("2025-01-05"),
+    recoveryScore: 85,
     sleepScore: 72,
     rhythmScore: 94,
-    activityScore: 80,
+    energyScore: 80,
   },
   {
-    id: '6',
-    date: new Date('2025-01-06'),
-    autonomicScore: 85,
+    id: "6",
+    date: new Date("2025-01-06"),
+    recoveryScore: 85,
     sleepScore: 72,
     rhythmScore: 94,
-    activityScore: 82,
+    energyScore: 82,
   },
   {
-    id: '7',
-    date: new Date('2025-01-07'),
-    autonomicScore: 88,
+    id: "7",
+    date: new Date("2025-01-07"),
+    recoveryScore: 88,
     sleepScore: 76,
     rhythmScore: 95,
-    activityScore: 85,
+    energyScore: 85,
   },
 ];
 
-// 分析期間
-export type TimePeriod = 'weekly' | 'monthly';
+export type TimePeriod = "weekly" | "monthly";
+
+// =============================================================================
+// HELPER FUNCTIONS
+// =============================================================================
+
+/**
+ * 時間帯に応じた挨拶を取得（「さん」付き）
+ * @param name ユーザー名（省略時は挨拶のみ）
+ * @returns "こんにちは太郎さん" 形式
+ */
+export const getGreeting = (name?: string): string => {
+  const hour = new Date().getHours();
+  let greeting: string;
+  if (hour >= 5 && hour < 12) {
+    greeting = "おはようございます";
+  } else if (hour >= 12 && hour < 17) {
+    greeting = "こんにちは";
+  } else if (hour >= 17 && hour < 21) {
+    greeting = "こんばんは";
+  } else {
+    greeting = "お疲れさまです";
+  }
+  return name ? `${greeting}${name}さん` : greeting;
+};
+
+/**
+ * 現在時刻を "HH:MM" 形式でフォーマット
+ * @param date 日時（省略時は現在日時）
+ * @returns "10:42" 形式
+ */
+export const formatTime = (date: Date = new Date()): string => {
+  const hours = date.getHours();
+  const minutes = date.getMinutes().toString().padStart(2, "0");
+  return `${hours}:${minutes}`;
+};
+
+/**
+ * 現在時刻を "現在 HH:MM" 形式でフォーマット
+ * @param date 日時（省略時は現在日時）
+ * @returns "現在 10:42" 形式
+ */
+export const formatCurrentTime = (date: Date = new Date()): string => {
+  return `現在 ${formatTime(date)}`;
+};
+
+/**
+ * 日付を日本語形式でフォーマット
+ * @param date 日付（省略時は現在日時）
+ * @returns "1月7日（火）" 形式
+ */
+export const formatDate = (date: Date = new Date()): string => {
+  const weekdays = ["日", "月", "火", "水", "木", "金", "土"];
+  const month = date.getMonth() + 1;
+  const day = date.getDate();
+  const weekday = weekdays[date.getDay()];
+  return `${month}月${day}日（${weekday}）`;
+};
+
+/**
+ * 日付を英語形式でフォーマット（詩的タイトル用）
+ * @param date 日付（省略時は現在日時）
+ * @returns "Tuesday, January 7" 形式
+ */
+export const formatDateEnglish = (date: Date = new Date()): string => {
+  const days = [
+    "Sunday",
+    "Monday",
+    "Tuesday",
+    "Wednesday",
+    "Thursday",
+    "Friday",
+    "Saturday",
+  ];
+  const months = [
+    "January",
+    "February",
+    "March",
+    "April",
+    "May",
+    "June",
+    "July",
+    "August",
+    "September",
+    "October",
+    "November",
+    "December",
+  ];
+  return `${days[date.getDay()]}, ${months[date.getMonth()]} ${date.getDate()}`;
+};
+
+// =============================================================================
+// MOCK_DETAIL - HealthKit 対応版
+// =============================================================================
+
+import {
+  HealthMetricHistory,
+  DailySnapshot,
+  RealtimeMetrics,
+  RealtimeHealthMetric,
+  BarChartDataPoint,
+} from '../domain/models/healthHistory';
+import {
+  getMockMetricHistory,
+  getAllScoreHistories,
+  getAllHealthMetricHistories,
+  formatDateString,
+} from './mockDataFactory';
+import { toBarChartData, calculateDeviationPercent } from '../utils/healthDataTransformer';



================================================================================
ファイル: app/src/constants/mockData.ts
行: 721 (開始行: 714)
コミット: a35fee7
作成日時: 2026-01-07T07:53:49Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**Getter pattern in mock objects may cause unexpected behavior.**

The `get history()` getters recompute `toBarChartData` on every access. While this ensures fresh data, it also means:
1. Multiple accesses return different object references (potential React re-render issues)
2. Performance overhead on repeated access

Consider memoizing or precomputing:

<details>
<summary>♻️ Precompute history data</summary>

```typescript
const createHistoryData = (samples: DailyHealthSample[]) => ({
  '7D': toBarChartData(samples.slice(-7), '7D', 'ja'),
  '30D': toBarChartData(samples.slice(-30), '30D', 'ja'),
  '60D': toBarChartData(samples, '60D', 'ja'),
});

// In recovery object:
rawHistory: scoreHistories.recoveryScore,
history: createHistoryData(scoreHistories.recoveryScore.samples),
```
</details>

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/constants/mockData.ts around lines 714 - 721, The getter get
history() recomputes toBarChartData on every access causing new object
references and potential re-renders; fix by precomputing/memoizing the history
object instead of using a getter: create a helper like
createHistoryData(samples) that calls toBarChartData for '7D', '30D', '60D' and
replace the get history() usage with a concrete history property (e.g., history:
createHistoryData(this.rawHistory.samples)) so history returns the same
reference and avoids repeated computation.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -136,76 +423,496 @@ export const MOCK_HEALTH_METRICS: HealthMetrics = {
   },
 };
 
-// モックリズム分析
 export const MOCK_RHYTHM_ANALYSIS: RhythmAnalysis = {
   bedtimeStddevMinutes: 22,
   wakeTimeStddevMinutes: 18,
   consecutiveStableDays: 5,
-  status: 'stable',
+  status: "stable",
   isStable: true,
   bedtimeConsistencyScore: 85,
   wakeTimeConsistencyScore: 88,
 };
 
-// モック日次スコア履歴（週間）
 export const MOCK_WEEKLY_SCORES: DailyScoreSnapshot[] = [
   {
-    id: '1',
-    date: new Date('2025-01-01'),
-    autonomicScore: 78,
+    id: "1",
+    date: new Date("2025-01-01"),
+    recoveryScore: 78,
     sleepScore: 65,
     rhythmScore: 82,
-    activityScore: 70,
+    energyScore: 70,
   },
   {
-    id: '2',
-    date: new Date('2025-01-02'),
-    autonomicScore: 80,
+    id: "2",
+    date: new Date("2025-01-02"),
+    recoveryScore: 80,
     sleepScore: 70,
     rhythmScore: 85,
-    activityScore: 75,
+    energyScore: 75,
   },
   {
-    id: '3',
-    date: new Date('2025-01-03'),
-    autonomicScore: 75,
+    id: "3",
+    date: new Date("2025-01-03"),
+    recoveryScore: 75,
     sleepScore: 68,
     rhythmScore: 88,
-    activityScore: 65,
+    energyScore: 65,
   },
   {
-    id: '4',
-    date: new Date('2025-01-04'),
-    autonomicScore: 82,
+    id: "4",
+    date: new Date("2025-01-04"),
+    recoveryScore: 82,
     sleepScore: 74,
     rhythmScore: 90,
-    activityScore: 78,
+    energyScore: 78,
   },
   {
-    id: '5',
-    date: new Date('2025-01-05'),
-    autonomicScore: 85,
+    id: "5",
+    date: new Date("2025-01-05"),
+    recoveryScore: 85,
     sleepScore: 72,
     rhythmScore: 94,
-    activityScore: 80,
+    energyScore: 80,
   },
   {
-    id: '6',
-    date: new Date('2025-01-06'),
-    autonomicScore: 85,
+    id: "6",
+    date: new Date("2025-01-06"),
+    recoveryScore: 85,
     sleepScore: 72,
     rhythmScore: 94,
-    activityScore: 82,
+    energyScore: 82,
   },
   {
-    id: '7',
-    date: new Date('2025-01-07'),
-    autonomicScore: 88,
+    id: "7",
+    date: new Date("2025-01-07"),
+    recoveryScore: 88,
     sleepScore: 76,
     rhythmScore: 95,
-    activityScore: 85,
+    energyScore: 85,
   },
 ];
 
-// 分析期間
-export type TimePeriod = 'weekly' | 'monthly';
+export type TimePeriod = "weekly" | "monthly";
+
+// =============================================================================
+// HELPER FUNCTIONS
+// =============================================================================
+
+/**
+ * 時間帯に応じた挨拶を取得（「さん」付き）
+ * @param name ユーザー名（省略時は挨拶のみ）
+ * @returns "こんにちは太郎さん" 形式
+ */
+export const getGreeting = (name?: string): string => {
+  const hour = new Date().getHours();
+  let greeting: string;
+  if (hour >= 5 && hour < 12) {
+    greeting = "おはようございます";
+  } else if (hour >= 12 && hour < 17) {
+    greeting = "こんにちは";
+  } else if (hour >= 17 && hour < 21) {
+    greeting = "こんばんは";
+  } else {
+    greeting = "お疲れさまです";
+  }
+  return name ? `${greeting}${name}さん` : greeting;
+};
+
+/**
+ * 現在時刻を "HH:MM" 形式でフォーマット
+ * @param date 日時（省略時は現在日時）
+ * @returns "10:42" 形式
+ */
+export const formatTime = (date: Date = new Date()): string => {
+  const hours = date.getHours();
+  const minutes = date.getMinutes().toString().padStart(2, "0");
+  return `${hours}:${minutes}`;
+};
+
+/**
+ * 現在時刻を "現在 HH:MM" 形式でフォーマット
+ * @param date 日時（省略時は現在日時）
+ * @returns "現在 10:42" 形式
+ */
+export const formatCurrentTime = (date: Date = new Date()): string => {
+  return `現在 ${formatTime(date)}`;
+};
+
+/**
+ * 日付を日本語形式でフォーマット
+ * @param date 日付（省略時は現在日時）
+ * @returns "1月7日（火）" 形式
+ */
+export const formatDate = (date: Date = new Date()): string => {
+  const weekdays = ["日", "月", "火", "水", "木", "金", "土"];
+  const month = date.getMonth() + 1;
+  const day = date.getDate();
+  const weekday = weekdays[date.getDay()];
+  return `${month}月${day}日（${weekday}）`;
+};
+
+/**
+ * 日付を英語形式でフォーマット（詩的タイトル用）
+ * @param date 日付（省略時は現在日時）
+ * @returns "Tuesday, January 7" 形式
+ */
+export const formatDateEnglish = (date: Date = new Date()): string => {
+  const days = [
+    "Sunday",
+    "Monday",
+    "Tuesday",
+    "Wednesday",
+    "Thursday",
+    "Friday",
+    "Saturday",
+  ];
+  const months = [
+    "January",
+    "February",
+    "March",
+    "April",
+    "May",
+    "June",
+    "July",
+    "August",
+    "September",
+    "October",
+    "November",
+    "December",
+  ];
+  return `${days[date.getDay()]}, ${months[date.getMonth()]} ${date.getDate()}`;
+};
+
+// =============================================================================
+// MOCK_DETAIL - HealthKit 対応版
+// =============================================================================
+
+import {
+  HealthMetricHistory,
+  DailySnapshot,
+  RealtimeMetrics,
+  RealtimeHealthMetric,
+  BarChartDataPoint,
+} from '../domain/models/healthHistory';
+import {
+  getMockMetricHistory,
+  getAllScoreHistories,
+  getAllHealthMetricHistories,
+  formatDateString,
+} from './mockDataFactory';
+import { toBarChartData, calculateDeviationPercent } from '../utils/healthDataTransformer';
+
+/**
+ * HealthKit 対応版の詳細データ構造
+ *
+ * 特徴:
+ * - rawHistory: Date 型を含む HealthKit 形式のデータ
+ * - history: BarChart 互換形式への変換ゲッター
+ * - ベースライン・典型範囲を含む
+ */
+export interface MockDetailRecovery {
+  score: number;
+  status: string;
+  hrv: { value: number; unit: string; change: number; baseline: number };
+  rhr: { value: number; unit: string; change: number; baseline: number };
+  analysis: string;
+  calculatedAt: string;
+  rawHistory: HealthMetricHistory;
+  history: {
+    '7D': BarChartDataPoint[];
+    '30D': BarChartDataPoint[];
+    '60D': BarChartDataPoint[];
+  };
+  weeklyAverage: number;
+}
+
+export interface MockDetailSleep {
+  score: number;
+  duration: { hours: number; minutes: number; percentage: number };
+  quality: { percentage: number };
+  analysis: string;
+  stages: Array<{ stage: 'deep' | 'rem' | 'light' | 'awake'; percentage: number }>;
+  timing: {
+    bedtime: { actual: string; target: string; diff: string };
+    wakeTime: { actual: string; target: string; diff: string };
+  };
+  rawHistory: HealthMetricHistory;
+  history: {
+    '7D': BarChartDataPoint[];
+    '30D': BarChartDataPoint[];
+    '60D': BarChartDataPoint[];
+  };
+}
+
+export interface MockDetailRhythm {
+  score: number;
+  status: string;
+  analysis: string;
+  consistency: {
+    bedtime: { target: string; deviation: string };
+    wakeTime: { target: string; deviation: string };
+  };
+  contributingFactors: {
+    bedtimeVariance: { value: number; label: string; trend: string; trendDirection: 'up' | 'down' | 'stable'; detail: string };
+    wakeVariance: { value: number; label: string; trend: string; trendDirection: 'up' | 'down' | 'stable'; detail: string };
+    weekendShift: { value: number; label: string; trend: string; trendDirection: 'up' | 'down' | 'stable'; detail: string };
+    socialJetlag: { value: number; label: string; trend: string; trendDirection: 'up' | 'down' | 'stable'; detail: string };
+  };
+  weeklyPattern: Array<{ day: string; offset: number }>;
+  rawHistory: HealthMetricHistory;
+  history: {
+    '7D': BarChartDataPoint[];
+    '30D': BarChartDataPoint[];
+    '60D': BarChartDataPoint[];
+  };
+}
+
+export interface MockDetailEnergy {
+  score: number;
+  status: string;
+  analysis: string;
+  contributingFactors: {
+    recovery: { value: number; label: string; trend: string; trendDirection: 'up' | 'down' | 'stable'; detail: string };
+    sleep: { value: number; label: string; trend: string; trendDirection: 'up' | 'down' | 'stable'; detail: string };
+    activity: { value: number; label: string; trend: string; trendDirection: 'up' | 'down' | 'stable'; detail: string };
+    weather: { value: number; label: string; trend: string; trendDirection: 'up' | 'down' | 'stable'; detail: string };
+  };
+  peakFocus: { start: string; end: string };
+  afternoonDip: { start: string; end: string };
+  rawHistory: HealthMetricHistory;
+  history: {
+    '7D': BarChartDataPoint[];
+    '30D': BarChartDataPoint[];
+    '60D': BarChartDataPoint[];
+  };
+}
+
+export interface MockDetail {
+  recovery: MockDetailRecovery;
+  sleep: MockDetailSleep;
+  rhythm: MockDetailRhythm;
+  energy: MockDetailEnergy;
+}
+
+/**
+ * HealthKit 対応版のモックデータを生成
+ * rawHistory は Date を含む HealthKit 形式、history は BarChart 互換形式
+ */
+const createMockDetail = (): MockDetail => {
+  const scoreHistories = getAllScoreHistories('60D');
+
+  return {
+    recovery: {
+      score: 70,
+      status: 'トレーニング準備OK',
+      hrv: { value: 82, unit: 'ms', change: 5, baseline: 77 },
+      rhr: { value: 59, unit: 'bpm', change: 0, baseline: 59 },
+      analysis:
+        '回復スコアは、HRVの日中平均82ms（5:39に取得、60日平均の77msより6%高い）と、安静時心拍数59bpm（22:06に取得、60日平均の59bpmと同等）に基づいています。',
+      calculatedAt: '5:39',
+      rawHistory: scoreHistories.recoveryScore,
+      get history() {
+        const samples = this.rawHistory.samples;
+        return {
+          '7D': toBarChartData(samples.slice(-7), '7D', 'ja'),
+          '30D': toBarChartData(samples.slice(-30), '30D', 'ja'),
+          '60D': toBarChartData(samples, '60D', 'ja'),
+        };
+      },



================================================================================
ファイル: app/src/domain/models/insight.ts
行: 44 (開始行: 41)
コミット: a35fee7
作成日時: 2026-01-07T07:53:49Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**Remove excessive trailing blank lines.**

The file has 4 trailing blank lines. Clean code should have at most one trailing newline.

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/domain/models/insight.ts around lines 41 - 44, The file
app/src/domain/models/insight.ts currently ends with multiple trailing blank
lines; remove the extra blank lines so the file ends with a single trailing
newline (ensure exactly one newline at EOF) to satisfy clean-code formatting.
```

</details>

<!-- fingerprinting:phantom:poseidon:puma -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,44 @@
+/**
+ * Insight（インサイト・アラート）ドメインモデル
+ * @see docs/specs/metrics_spec.md Section 4
+ */
+
+export type AlertType =
+  | 'recovery_needed'
+  | 'recovery_complete'
+  | 'sleep_deficit'
+  | 'late_bedtime'
+  | 'weekend_jetlag'
+  | 'low_activity';
+
+export type AlertPriority = 'high' | 'medium' | 'low';
+
+export type AlertIcon = '⚠️' | '✓' | '🌙' | '⏰' | '📅' | '🚶';
+
+export interface Alert {
+  readonly id: string;
+  readonly type: AlertType;
+  readonly icon: AlertIcon;
+  readonly title: string;
+  readonly message: string;
+  readonly timestamp: Date;
+  readonly priority: AlertPriority;
+}
+
+export interface TopDiscovery {
+  readonly title: string;
+  readonly description: string;
+  readonly impact?: string; // e.g., "+18% deep sleep"
+}
+
+export interface WeeklyInsight {
+  readonly weeklyScores: readonly number[]; // 7日分
+  readonly avgScore: number;
+  readonly topDiscovery: TopDiscovery | null;
+  readonly recentAlerts: readonly Alert[];
+}
+
+
+
+
+



================================================================================
ファイル: app/src/domain/models/rhythm.ts
行: 73 (開始行: 70)
コミット: a35fee7
作成日時: 2026-01-07T07:53:49Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**Remove trailing blank lines**

There are extra blank lines at the end of the file. Clean these up for consistency.

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/domain/models/rhythm.ts around lines 70 - 73, The file
app/src/domain/models/rhythm.ts contains extra trailing blank lines at the end;
open rhythm.ts and remove the redundant empty lines so the file ends cleanly
(ensure it ends with a single newline character), keeping the exported symbol(s)
such as the Rhythm type/class untouched.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,73 @@
+/**
+ * Rhythm（サーカディアンリズム）ドメインモデル
+ * @see docs/specs/metrics_spec.md Section 3
+ */
+
+export type RhythmPhaseName =
+  | 'Wake Window'
+  | 'Peak Focus'
+  | 'Afternoon Dip'
+  | 'Second Wind'
+  | 'Wind Down'
+  | 'Melatonin Window';
+
+export type RhythmPhaseType = 'high' | 'low' | 'transition' | 'sleep';
+
+export interface RhythmPhase {
+  readonly name: RhythmPhaseName;
+  readonly start: Date;
+  readonly end: Date;
+  readonly type: RhythmPhaseType;
+  readonly isCurrent: boolean;
+}
+
+export interface CircadianRhythm {
+  readonly phases: readonly RhythmPhase[];
+  readonly currentPhase: RhythmPhase | null;
+  readonly nextPhase: RhythmPhase | null;
+  readonly minutesToNextPhase: number | null;
+  readonly sunrise: string;
+  readonly sunset: string;
+}
+
+export interface EnergyCurvePoint {
+  readonly hour: number;
+  readonly level: number; // 0-100
+}
+
+export type EnergyCurve = readonly EnergyCurvePoint[];
+
+/**
+ * RhythmAreaChart用のデータポイント
+ */
+export interface RhythmPoint {
+  readonly time: string; // "6時", "12時" など
+  readonly hour: number; // 6, 12, 18 など（計算用）
+  readonly energy: number; // 0-100
+  readonly label?: string; // "ピーク", "低迷期" など
+}
+
+/**
+ * Upcoming Windowsカード用のデータ
+ */
+export interface WindowCardData {
+  readonly title: string;
+  readonly timeRange: string;
+  readonly description: string;
+  readonly icon: 'sun' | 'moon';
+  readonly theme: 'day' | 'night';
+  readonly isActive?: boolean;
+}
+
+/**
+ * 日の出/日の入りデータ
+ */
+export interface SunData {
+  readonly sunrise: string; // "7:12"
+  readonly sunset: string; // "17:45"
+}
+
+
+
+
+



================================================================================
ファイル: app/src/domain/services/alertGenerator.ts
行: 32 (開始行: 30)
コミット: a35fee7
作成日時: 2026-01-07T07:53:49Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**Use `substring` instead of deprecated `substr`.**

`String.prototype.substr` is deprecated. Replace with `substring` for modern JavaScript standards.


<details>
<summary>♻️ Proposed fix</summary>

```diff
 const generateId = (): string => {
-  return `alert_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`;
+  return `alert_${Date.now()}_${Math.random().toString(36).substring(2, 11)}`;
 };
```
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
const generateId = (): string => {
  return `alert_${Date.now()}_${Math.random().toString(36).substring(2, 11)}`;
};
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/domain/services/alertGenerator.ts around lines 30 - 32, The
generateId function uses the deprecated String.prototype.substr; replace the
substr call on Math.random().toString(36).substr(2, 9) with an equivalent
substring call (e.g., Math.random().toString(36).substring(2, 11)) so the
generated ID length remains the same, keeping the function name generateId and
the surrounding template string unchanged.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,136 @@
+/**
+ * Alert 生成サービス
+ * @see docs/specs/metrics_spec.md Section 4
+ */
+
+import { Alert, AlertPriority } from '../models/insight';
+import { HrvMetrics, ActivityMetrics } from './tempoScoreCalculator';
+
+// ========================================
+// Types
+// ========================================
+
+interface AlertInput {
+  hrv: HrvMetrics | null;
+  sleep: {
+    durationMinutes: number;
+    bedtime: Date;
+    targetBedtime: Date;
+  } | null;
+  activity: ActivityMetrics | null;
+  rhythmData: {
+    weekendWakeShiftMinutes: number;
+  } | null;
+}
+
+// ========================================
+// Alert Generation
+// ========================================
+
+const generateId = (): string => {
+  return `alert_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`;
+};



================================================================================
ファイル: app/src/domain/services/scoreCalculator.ts
行: 102 (開始行: 85)
コミット: a35fee7
作成日時: 2026-01-07T07:53:49Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟠 Major_

**Potential division by zero in `calculateSleepScore`**

If all sleep stage minutes are zero, `totalSleep` will be 0, causing `deepRatio` and `remRatio` to be `NaN`. Add a guard to handle this edge case.

<details>
<summary>🐛 Proposed fix</summary>

```diff
   const totalSleep =
     input.stages.deepMinutes +
     input.stages.remMinutes +
     input.stages.lightMinutes;

+  // Guard against division by zero
+  if (totalSleep === 0) {
+    return 0;
+  }
+
   // Duration Score (40%)
   const durationRatio = input.duration.minutes / input.duration.targetMinutes;
```

</details>

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/domain/services/scoreCalculator.ts around lines 85 - 102, In
calculateSleepScore, totalSleep can be zero causing deepRatio/remRatio to become
NaN; add a guard after computing totalSleep (check totalSleep === 0) and in that
case set deepRatio and remRatio to 0 (or set qualityScore to 0 directly) before
using scoreRange, ensuring downstream math uses valid numbers; update the
deepRatio/remRatio assignments and qualityScore computation to use this guarded
values so no division by zero occurs.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -1,84 +1,195 @@
 /**
- * スコア計算 Facade
- * 各スコア計算を統合して ConditionAssessment を返す
+ * 4つの独立スコア計算関数
+ * Recovery, Sleep, Rhythm, Energy
  */
 
-import { HealthMetrics, RhythmAnalysis } from '../models';
-import { ConditionAssessment, createScore, DEFAULT_SCORE } from '../models/score';
-import { calculateSleepScore } from './sleepScoreCalculator';
-import { calculateAutonomicScore } from './autonomicScoreCalculator';
-import { calculateRhythmScore } from './rhythmScoreCalculator';
-import { calculateActivityScore } from './activityScoreCalculator';
+// ========================================
+// Helper Functions
+// ========================================
+
+const clamp = (value: number, min: number, max: number): number => {
+  return Math.min(Math.max(value, min), max);
+};
+
+const scoreRange = (value: number, min: number, max: number): number => {
+  if (value >= min && value <= max) return 100;
+  if (value < min) return (value / min) * 100;
+  return Math.max(0, 100 - (value - max) * 200);
+};
+
+// ========================================
+// 1. Recovery Score
+// ========================================
 
 /**
- * 全スコアを計算してコンディション評価を返す
- * @param metrics ヘルスメトリクス
- * @param rhythmAnalysis リズム分析データ
- * @returns コンディション評価
+ * Recovery Score 計算
+ * Recovery = HRV (60%) + RHR (20%) + Sleep Quality (20%)
  */
-export const calculateConditionAssessment = (
-  metrics: HealthMetrics,
-  rhythmAnalysis: RhythmAnalysis
-): ConditionAssessment => {
-  // 各スコアを計算（データがない場合はデフォルトスコア）
-  const sleepScore = metrics.sleep ? calculateSleepScore(metrics.sleep) : DEFAULT_SCORE;
-
-  const autonomicScore = metrics.hrv
-    ? calculateAutonomicScore(metrics.hrv, metrics.sleep)
-    : DEFAULT_SCORE;
-
-  const rhythmScore = calculateRhythmScore(
-    rhythmAnalysis,
-    metrics.auxiliary?.wristTemperatureDeviation
-  );
+export interface RecoveryScoreInput {
+  hrv: {
+    current: number; // 現在のHRV (ms)
+    baseline: number; // ベースライン (60日平均)
+  };
+  rhr: {
+    current: number; // 現在のRHR (bpm)
+    baseline: number; // ベースライン (60日平均)
+  };
+  sleepQuality: number; // Sleep Scoreから取得 (0-100)
+}
 
-  const activityScore = metrics.activity
-    ? calculateActivityScore(metrics.activity)
-    : DEFAULT_SCORE;
+export const calculateRecoveryScore = (input: RecoveryScoreInput): number => {
+  // HRVスコア（60%）- 高いほど良い
+  // ベースラインの70-130%の範囲で0-100点に変換
+  const hrvRatio = input.hrv.current / input.hrv.baseline;
+  const hrvScore = clamp(((hrvRatio - 0.7) / 0.6) * 100, 0, 100);
 
-  // 平均スコアを計算
-  const averageScore = Math.round(
-    (sleepScore.value + autonomicScore.value + rhythmScore.value + activityScore.value) / 4
-  );
+  // RHRスコア（20%）- 低いほど良い
+  // ベースラインの85-115%の範囲で0-100点に変換（逆比）
+  const rhrRatio = input.rhr.baseline / input.rhr.current;
+  const rhrScore = clamp(((rhrRatio - 0.85) / 0.3) * 100, 0, 100);
+
+  // 睡眠の質（20%）
+  const sleepScore = input.sleepQuality;
+
+  return Math.round(hrvScore * 0.6 + rhrScore * 0.2 + sleepScore * 0.2);
+};
+
+// ========================================
+// 2. Sleep Score
+// ========================================
+
+/**
+ * Sleep Score 計算
+ * Sleep = Duration (40%) + Quality (40%) + Timing (20%)
+ */
+export interface SleepScoreInput {
+  duration: {
+    minutes: number; // 実際の睡眠時間 (分)
+    targetMinutes: number; // 目標睡眠時間 (分、デフォルト450 = 7.5h)
+  };
+  stages: {
+    deepMinutes: number;
+    remMinutes: number;
+    lightMinutes: number;
+    awakeMinutes: number;
+  };
+  timing?: {
+    // オプション（データがない場合はDurationとQualityのみで計算）
+    actualBedtime: Date;
+    targetBedtime: Date;
+    actualWakeTime: Date;
+    targetWakeTime: Date;
+  };
+}
+
+export const calculateSleepScore = (input: SleepScoreInput): number => {
+  const totalSleep =
+    input.stages.deepMinutes +
+    input.stages.remMinutes +
+    input.stages.lightMinutes;
+
+  // Duration Score (40%)
+  const durationRatio = input.duration.minutes / input.duration.targetMinutes;
+  const durationScore = clamp(durationRatio * 100, 0, 100);
 
-  // 全体ステータスを決定
-  const overallStatus = createScore(averageScore).status;
-
-  // 最も弱い領域を特定
-  const scores = [
-    { area: 'sleep' as const, value: sleepScore.value },
-    { area: 'autonomic' as const, value: autonomicScore.value },
-    { area: 'rhythm' as const, value: rhythmScore.value },
-    { area: 'activity' as const, value: activityScore.value },
-  ];
-  const weakest = scores.reduce((min, current) =>
-    current.value < min.value ? current : min
+  // Quality Score (40%)
+  const deepRatio = input.stages.deepMinutes / totalSleep;
+  const remRatio = input.stages.remMinutes / totalSleep;
+
+  const deepScore = scoreRange(deepRatio, 0.15, 0.25); // 15-25%が理想
+  const remScore = scoreRange(remRatio, 0.2, 0.25); // 20-25%が理想
+
+  const qualityScore = deepScore * 0.5 + remScore * 0.5;



================================================================================
ファイル: app/src/domain/services/tempoScoreCalculator.ts
行: 95 (開始行: 74)
コミット: a35fee7
作成日時: 2026-01-07T07:53:49Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**Consider extracting sleep scoring thresholds as constants.**

The sleep scoring functions contain several magic numbers (7, 8, 6, 9 hours for duration; 0.15, 0.25 for deep sleep ratio; 0.2, 0.25 for REM ratio). While the inline comments help, extracting these to named constants would improve maintainability and make the spec easier to update.

<details>
<summary>📝 Example constant extraction</summary>

```typescript
const SLEEP_DURATION_THRESHOLDS = {
  optimalMin: 7,
  optimalMax: 8,
  acceptableMin: 6,
  acceptableMax: 9,
} as const;

const DEEP_SLEEP_RATIO = { min: 0.15, max: 0.25 } as const;
const REM_SLEEP_RATIO = { min: 0.20, max: 0.25 } as const;
```
</details>

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/domain/services/tempoScoreCalculator.ts around lines 74 - 95, The
scoreDuration, scoreDeepSleep, and scoreRemSleep functions use magic numbers for
thresholds; extract these into named constants (e.g., SLEEP_DURATION_THRESHOLDS
with optimalMin/optimalMax/acceptableMin/acceptableMax, DEEP_SLEEP_RATIO with
min/max, REM_SLEEP_RATIO with min/max) and replace the literals
(7,8,6,9,0.15,0.25,0.2,0.25 and the fallback mins like 60) with those constants
so the scoring logic in scoreDuration, scoreDeepSleep, and scoreRemSleep reads
against the named values for clarity and easier future changes.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,181 @@
+/**
+ * Tempo Score 計算サービス
+ * @see docs/specs/metrics_spec.md Section 2
+ *
+ * Tempo Score = HRV Score × 0.40
+ *             + Sleep Score × 0.35
+ *             + Rhythm Score × 0.15
+ *             + Activity Score × 0.10
+ */
+
+// ========================================
+// Types
+// ========================================
+
+export interface SleepMetrics {
+  readonly durationMinutes: number;
+  readonly deepSleepRatio: number; // 0.0-1.0
+  readonly remSleepRatio: number;  // 0.0-1.0
+}
+
+export interface HrvMetrics {
+  readonly current: number;       // ms
+  readonly baseline30d: number;   // ms
+}
+
+export interface RhythmMetrics {
+  readonly bedtimeStddevMinutes: number;
+  readonly wakeTimeStddevMinutes: number;
+}
+
+export interface ActivityMetrics {
+  readonly steps: number;
+  readonly goal?: number; // default: 8000
+}
+
+export interface TempoScoreComponents {
+  readonly hrvScore: number;
+  readonly sleepScore: number;
+  readonly rhythmScore: number;
+  readonly activityScore: number;
+}
+
+export interface TempoScoreResult {
+  readonly score: number;
+  readonly components: TempoScoreComponents;
+  readonly isCalibrating: boolean;
+}
+
+// ========================================
+// Utility Functions
+// ========================================
+
+const clamp = (value: number, min: number, max: number): number =>
+  Math.max(min, Math.min(max, value));
+
+// ========================================
+// HRV Score (40%)
+// ========================================
+
+export const calculateHrvScore = (hrv: HrvMetrics): number => {
+  if (hrv.baseline30d === 0) return 70; // キャリブレーション中
+
+  const ratio = hrv.current / hrv.baseline30d;
+  const baseScore = 70;
+  const deviation = (ratio - 1.0) * 100;
+
+  return clamp(baseScore + deviation, 0, 100);
+};
+
+// ========================================
+// Sleep Score (35%)
+// ========================================
+
+const scoreDuration = (minutes: number): number => {
+  const hours = minutes / 60;
+  if (hours >= 7 && hours <= 8) return 100;
+  if (hours >= 6 && hours < 7) return 70 + (hours - 6) * 30;
+  if (hours > 8 && hours <= 9) return 100 - (hours - 8) * 20;
+  if (hours < 6) return Math.max(0, hours * 11.67);
+  return 60; // 9時間超
+};
+
+const scoreDeepSleep = (ratio: number): number => {
+  // 15-25%が理想
+  if (ratio >= 0.15 && ratio <= 0.25) return 100;
+  if (ratio < 0.15) return (ratio / 0.15) * 100;
+  return Math.max(60, 100 - (ratio - 0.25) * 200);
+};
+
+const scoreRemSleep = (ratio: number): number => {
+  // 20-25%が理想
+  if (ratio >= 0.2 && ratio <= 0.25) return 100;
+  if (ratio < 0.2) return (ratio / 0.2) * 100;
+  return Math.max(60, 100 - (ratio - 0.25) * 200);
+};



================================================================================
ファイル: app/src/domain/services/tempoScoreCalculator.ts
行: 181 (開始行: 177)
コミット: a35fee7
作成日時: 2026-01-07T07:53:49Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**Remove trailing blank lines.**

There are 4 trailing empty lines at the end of the file.

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/domain/services/tempoScoreCalculator.ts around lines 177 - 181,
Remove the 4 trailing blank lines at the end of tempoScoreCalculator.ts so the
file ends with a single newline (no extra empty lines); update the file to
ensure EOF contains exactly one newline character and re-run formatting/linting
to confirm no trailing blank lines remain.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,181 @@
+/**
+ * Tempo Score 計算サービス
+ * @see docs/specs/metrics_spec.md Section 2
+ *
+ * Tempo Score = HRV Score × 0.40
+ *             + Sleep Score × 0.35
+ *             + Rhythm Score × 0.15
+ *             + Activity Score × 0.10
+ */
+
+// ========================================
+// Types
+// ========================================
+
+export interface SleepMetrics {
+  readonly durationMinutes: number;
+  readonly deepSleepRatio: number; // 0.0-1.0
+  readonly remSleepRatio: number;  // 0.0-1.0
+}
+
+export interface HrvMetrics {
+  readonly current: number;       // ms
+  readonly baseline30d: number;   // ms
+}
+
+export interface RhythmMetrics {
+  readonly bedtimeStddevMinutes: number;
+  readonly wakeTimeStddevMinutes: number;
+}
+
+export interface ActivityMetrics {
+  readonly steps: number;
+  readonly goal?: number; // default: 8000
+}
+
+export interface TempoScoreComponents {
+  readonly hrvScore: number;
+  readonly sleepScore: number;
+  readonly rhythmScore: number;
+  readonly activityScore: number;
+}
+
+export interface TempoScoreResult {
+  readonly score: number;
+  readonly components: TempoScoreComponents;
+  readonly isCalibrating: boolean;
+}
+
+// ========================================
+// Utility Functions
+// ========================================
+
+const clamp = (value: number, min: number, max: number): number =>
+  Math.max(min, Math.min(max, value));
+
+// ========================================
+// HRV Score (40%)
+// ========================================
+
+export const calculateHrvScore = (hrv: HrvMetrics): number => {
+  if (hrv.baseline30d === 0) return 70; // キャリブレーション中
+
+  const ratio = hrv.current / hrv.baseline30d;
+  const baseScore = 70;
+  const deviation = (ratio - 1.0) * 100;
+
+  return clamp(baseScore + deviation, 0, 100);
+};
+
+// ========================================
+// Sleep Score (35%)
+// ========================================
+
+const scoreDuration = (minutes: number): number => {
+  const hours = minutes / 60;
+  if (hours >= 7 && hours <= 8) return 100;
+  if (hours >= 6 && hours < 7) return 70 + (hours - 6) * 30;
+  if (hours > 8 && hours <= 9) return 100 - (hours - 8) * 20;
+  if (hours < 6) return Math.max(0, hours * 11.67);
+  return 60; // 9時間超
+};
+
+const scoreDeepSleep = (ratio: number): number => {
+  // 15-25%が理想
+  if (ratio >= 0.15 && ratio <= 0.25) return 100;
+  if (ratio < 0.15) return (ratio / 0.15) * 100;
+  return Math.max(60, 100 - (ratio - 0.25) * 200);
+};
+
+const scoreRemSleep = (ratio: number): number => {
+  // 20-25%が理想
+  if (ratio >= 0.2 && ratio <= 0.25) return 100;
+  if (ratio < 0.2) return (ratio / 0.2) * 100;
+  return Math.max(60, 100 - (ratio - 0.25) * 200);
+};
+
+export const calculateSleepScore = (sleep: SleepMetrics): number => {
+  const durationScore = scoreDuration(sleep.durationMinutes);
+  const deepScore = scoreDeepSleep(sleep.deepSleepRatio);
+  const remScore = scoreRemSleep(sleep.remSleepRatio);
+
+  return durationScore * 0.5 + deepScore * 0.3 + remScore * 0.2;
+};
+
+// ========================================
+// Rhythm Score (15%)
+// ========================================
+
+const consistencyScore = (stddevMinutes: number): number => {
+  if (stddevMinutes <= 15) return 100; // 非常に安定
+  if (stddevMinutes <= 30) return 85;  // 安定
+  if (stddevMinutes <= 45) return 70;  // やや安定
+  if (stddevMinutes <= 60) return 55;  // やや不安定
+  if (stddevMinutes <= 90) return 40;  // 不安定
+  return 25; // 非常に不安定
+};
+
+export const calculateRhythmScore = (rhythm: RhythmMetrics): number => {
+  const bedtimeScore = consistencyScore(rhythm.bedtimeStddevMinutes);
+  const wakeScore = consistencyScore(rhythm.wakeTimeStddevMinutes);
+
+  return (bedtimeScore + wakeScore) / 2;
+};
+
+// ========================================
+// Activity Score (10%)
+// ========================================
+
+export const calculateActivityScore = (activity: ActivityMetrics): number => {
+  const goal = activity.goal ?? 8000;
+  const ratio = activity.steps / goal;
+
+  if (ratio >= 1.0) return 100;
+  if (ratio >= 0.75) return 80 + (ratio - 0.75) * 80;
+  if (ratio >= 0.5) return 60 + (ratio - 0.5) * 80;
+  return ratio * 120;
+};
+
+// ========================================
+// Tempo Score (Total)
+// ========================================
+
+export const calculateTempoScore = (
+  hrvMetrics: HrvMetrics | null,
+  sleepMetrics: SleepMetrics | null,
+  rhythmMetrics: RhythmMetrics | null,
+  activityMetrics: ActivityMetrics | null,
+  isCalibrating: boolean = false
+): TempoScoreResult => {
+  // デフォルト値（キャリブレーション中）
+  const hrvScore = hrvMetrics ? calculateHrvScore(hrvMetrics) : 70;
+  const sleepScore = sleepMetrics ? calculateSleepScore(sleepMetrics) : 70;
+  const rhythmScore = rhythmMetrics ? calculateRhythmScore(rhythmMetrics) : 70;
+  const activityScore = activityMetrics ? calculateActivityScore(activityMetrics) : 70;
+
+  // 重み付け計算
+  const score = clamp(
+    hrvScore * 0.40 +
+    sleepScore * 0.35 +
+    rhythmScore * 0.15 +
+    activityScore * 0.10,
+    0,
+    100
+  );
+
+  return {
+    score: Math.round(score),
+    components: {
+      hrvScore: Math.round(hrvScore),
+      sleepScore: Math.round(sleepScore),
+      rhythmScore: Math.round(rhythmScore),
+      activityScore: Math.round(activityScore),
+    },
+    isCalibrating,
+  };
+};
+
+
+
+
+



================================================================================
ファイル: app/src/services/dataSourceAdapter.ts
行: 80 (開始行: 77)
コミット: a35fee7
作成日時: 2026-01-07T07:53:49Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

**Fix indentation inconsistency**

The `const result` line has inconsistent indentation compared to the rest of the method.

```diff
-      const result = await apiClient.getWeather(lat, lon);
+    const result = await apiClient.getWeather(lat, lon);
```

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
    const result = await apiClient.getWeather(lat, lon);
    if (!result.success) {
      throw new Error('Failed to fetch weather data');
    }
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/services/dataSourceAdapter.ts around lines 77 - 80, Align the
indentation of the `const result = await apiClient.getWeather(lat, lon);` line
with the surrounding method block so it matches the same indentation level as
the `if (!result.success) {` line; locate the call to `apiClient.getWeather` and
adjust leading spaces/tabs so `const result` and the subsequent `if
(!result.success)` are at the same indentation depth within the function,
preserving the existing style (tabs or spaces) used elsewhere in
`dataSourceAdapter.ts`.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,111 @@
+import { DATA_SOURCE_CONFIG } from '../config/dataSource';
+import { apiClient } from '../api/client';
+import {
+  MOCK_SLEEP_METRICS,
+  MOCK_HRV_METRICS,
+  MOCK_ACTIVITY_METRICS,
+  MOCK_RHYTHM_ANALYSIS,
+  MOCK_WEATHER,
+  MOCK_AI_RESPONSE,
+} from '../constants/mockData';
+import type {
+  SleepMetrics,
+  HRVMetrics,
+  ActivityMetrics,
+  RhythmAnalysis,
+  SimpleWeatherData,
+} from '../domain/models';
+import type { AdviceRequest, AdviceResponse } from '../api/types';
+
+/**
+ * データソースアダプター
+ * Mock と実データを統一インターフェースで提供
+ */
+class DataSourceAdapter {
+  /**
+   * 睡眠メトリクスを取得
+   */
+  async getSleepMetrics(): Promise<SleepMetrics> {
+    if (DATA_SOURCE_CONFIG.USE_MOCK_HEALTHKIT) {
+      return MOCK_SLEEP_METRICS;
+    }
+    // TODO: 実装 - HealthKitService.fetchSleepMetrics()
+    throw new Error('Real HealthKit integration not yet implemented');
+  }
+
+  /**
+   * HRVメトリクスを取得
+   */
+  async getHRVMetrics(): Promise<HRVMetrics> {
+    if (DATA_SOURCE_CONFIG.USE_MOCK_HEALTHKIT) {
+      return MOCK_HRV_METRICS;
+    }
+    // TODO: 実装 - HealthKitService.fetchHRVMetrics()
+    throw new Error('Real HealthKit integration not yet implemented');
+  }
+
+  /**
+   * アクティビティメトリクスを取得
+   */
+  async getActivityMetrics(): Promise<ActivityMetrics> {
+    if (DATA_SOURCE_CONFIG.USE_MOCK_HEALTHKIT) {
+      return MOCK_ACTIVITY_METRICS;
+    }
+    // TODO: 実装 - HealthKitService.fetchActivityMetrics()
+    throw new Error('Real HealthKit integration not yet implemented');
+  }
+
+  /**
+   * リズム分析を取得
+   */
+  async getRhythmAnalysis(): Promise<RhythmAnalysis> {
+    if (DATA_SOURCE_CONFIG.USE_MOCK_HEALTHKIT) {
+      return MOCK_RHYTHM_ANALYSIS;
+    }
+    // TODO: 実装 - HealthKitService.fetchRhythmAnalysis()
+    throw new Error('Real HealthKit integration not yet implemented');
+  }
+
+  /**
+   * 天気データを取得
+   */
+  async getWeather(lat: number, lon: number): Promise<SimpleWeatherData> {
+    if (DATA_SOURCE_CONFIG.USE_MOCK_WEATHER) {
+      return MOCK_WEATHER;
+    }
+
+      const result = await apiClient.getWeather(lat, lon);
+    if (!result.success) {
+      throw new Error('Failed to fetch weather data');
+    }



================================================================================
ファイル: app/src/services/dataSourceAdapter.ts
行: 80 (開始行: 78)
コミット: a35fee7
作成日時: 2026-01-07T07:53:50Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**Consider custom error types for domain-specific errors**

Per coding guidelines, use custom error types for domain-specific errors rather than generic `Error`. This would improve error handling upstream and provide better debugging context.

<details>
<summary>♻️ Example improvement</summary>

```typescript
// In a separate errors file
export class WeatherFetchError extends Error {
  constructor(message: string) {
    super(message);
    this.name = 'WeatherFetchError';
  }
}

export class AIAdviceError extends Error {
  constructor(message: string) {
    super(message);
    this.name = 'AIAdviceError';
  }
}
```

</details>




Also applies to: 101-103

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/services/dataSourceAdapter.ts around lines 78 - 80, Replace the
generic Error throws in dataSourceAdapter.ts (e.g., the throw in the block that
checks result.success and the similar throws around lines 101-103) with
domain-specific custom error classes: define WeatherFetchError and AIAdviceError
in a new errors module, export them, import them into dataSourceAdapter.ts, and
throw new WeatherFetchError(message, { cause: originalError }) or new
AIAdviceError(...) as appropriate so upstream code can distinguish and handle
these cases; ensure each custom error sets its name property and preserves the
original error (via cause or by attaching it) for debugging.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,111 @@
+import { DATA_SOURCE_CONFIG } from '../config/dataSource';
+import { apiClient } from '../api/client';
+import {
+  MOCK_SLEEP_METRICS,
+  MOCK_HRV_METRICS,
+  MOCK_ACTIVITY_METRICS,
+  MOCK_RHYTHM_ANALYSIS,
+  MOCK_WEATHER,
+  MOCK_AI_RESPONSE,
+} from '../constants/mockData';
+import type {
+  SleepMetrics,
+  HRVMetrics,
+  ActivityMetrics,
+  RhythmAnalysis,
+  SimpleWeatherData,
+} from '../domain/models';
+import type { AdviceRequest, AdviceResponse } from '../api/types';
+
+/**
+ * データソースアダプター
+ * Mock と実データを統一インターフェースで提供
+ */
+class DataSourceAdapter {
+  /**
+   * 睡眠メトリクスを取得
+   */
+  async getSleepMetrics(): Promise<SleepMetrics> {
+    if (DATA_SOURCE_CONFIG.USE_MOCK_HEALTHKIT) {
+      return MOCK_SLEEP_METRICS;
+    }
+    // TODO: 実装 - HealthKitService.fetchSleepMetrics()
+    throw new Error('Real HealthKit integration not yet implemented');
+  }
+
+  /**
+   * HRVメトリクスを取得
+   */
+  async getHRVMetrics(): Promise<HRVMetrics> {
+    if (DATA_SOURCE_CONFIG.USE_MOCK_HEALTHKIT) {
+      return MOCK_HRV_METRICS;
+    }
+    // TODO: 実装 - HealthKitService.fetchHRVMetrics()
+    throw new Error('Real HealthKit integration not yet implemented');
+  }
+
+  /**
+   * アクティビティメトリクスを取得
+   */
+  async getActivityMetrics(): Promise<ActivityMetrics> {
+    if (DATA_SOURCE_CONFIG.USE_MOCK_HEALTHKIT) {
+      return MOCK_ACTIVITY_METRICS;
+    }
+    // TODO: 実装 - HealthKitService.fetchActivityMetrics()
+    throw new Error('Real HealthKit integration not yet implemented');
+  }
+
+  /**
+   * リズム分析を取得
+   */
+  async getRhythmAnalysis(): Promise<RhythmAnalysis> {
+    if (DATA_SOURCE_CONFIG.USE_MOCK_HEALTHKIT) {
+      return MOCK_RHYTHM_ANALYSIS;
+    }
+    // TODO: 実装 - HealthKitService.fetchRhythmAnalysis()
+    throw new Error('Real HealthKit integration not yet implemented');
+  }
+
+  /**
+   * 天気データを取得
+   */
+  async getWeather(lat: number, lon: number): Promise<SimpleWeatherData> {
+    if (DATA_SOURCE_CONFIG.USE_MOCK_WEATHER) {
+      return MOCK_WEATHER;
+    }
+
+      const result = await apiClient.getWeather(lat, lon);
+    if (!result.success) {
+      throw new Error('Failed to fetch weather data');
+    }



================================================================================
ファイル: app/src/services/dataSourceAdapter.ts
行: 103 (開始行: 100)
コミット: a35fee7
作成日時: 2026-01-07T07:53:50Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

**Fix indentation inconsistency**

Same indentation issue as `getWeather` - the `const result` line has extra leading whitespace.

```diff
-      const result = await apiClient.generateAdvice(request);
+    const result = await apiClient.generateAdvice(request);
```

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
    const result = await apiClient.generateAdvice(request);
    if (!result.success) {
      throw new Error('Failed to generate AI advice');
    }
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/services/dataSourceAdapter.ts around lines 100 - 103, The
indentation for the line declaring "const result = await
apiClient.generateAdvice(request);" is inconsistent with the surrounding code
(same issue as getWeather); fix by aligning its leading whitespace with adjacent
statements in the same block so it matches the block's indentation style (adjust
the indentation of the const result line in the generateAdvice call to match
surrounding lines).
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -0,0 +1,111 @@
+import { DATA_SOURCE_CONFIG } from '../config/dataSource';
+import { apiClient } from '../api/client';
+import {
+  MOCK_SLEEP_METRICS,
+  MOCK_HRV_METRICS,
+  MOCK_ACTIVITY_METRICS,
+  MOCK_RHYTHM_ANALYSIS,
+  MOCK_WEATHER,
+  MOCK_AI_RESPONSE,
+} from '../constants/mockData';
+import type {
+  SleepMetrics,
+  HRVMetrics,
+  ActivityMetrics,
+  RhythmAnalysis,
+  SimpleWeatherData,
+} from '../domain/models';
+import type { AdviceRequest, AdviceResponse } from '../api/types';
+
+/**
+ * データソースアダプター
+ * Mock と実データを統一インターフェースで提供
+ */
+class DataSourceAdapter {
+  /**
+   * 睡眠メトリクスを取得
+   */
+  async getSleepMetrics(): Promise<SleepMetrics> {
+    if (DATA_SOURCE_CONFIG.USE_MOCK_HEALTHKIT) {
+      return MOCK_SLEEP_METRICS;
+    }
+    // TODO: 実装 - HealthKitService.fetchSleepMetrics()
+    throw new Error('Real HealthKit integration not yet implemented');
+  }
+
+  /**
+   * HRVメトリクスを取得
+   */
+  async getHRVMetrics(): Promise<HRVMetrics> {
+    if (DATA_SOURCE_CONFIG.USE_MOCK_HEALTHKIT) {
+      return MOCK_HRV_METRICS;
+    }
+    // TODO: 実装 - HealthKitService.fetchHRVMetrics()
+    throw new Error('Real HealthKit integration not yet implemented');
+  }
+
+  /**
+   * アクティビティメトリクスを取得
+   */
+  async getActivityMetrics(): Promise<ActivityMetrics> {
+    if (DATA_SOURCE_CONFIG.USE_MOCK_HEALTHKIT) {
+      return MOCK_ACTIVITY_METRICS;
+    }
+    // TODO: 実装 - HealthKitService.fetchActivityMetrics()
+    throw new Error('Real HealthKit integration not yet implemented');
+  }
+
+  /**
+   * リズム分析を取得
+   */
+  async getRhythmAnalysis(): Promise<RhythmAnalysis> {
+    if (DATA_SOURCE_CONFIG.USE_MOCK_HEALTHKIT) {
+      return MOCK_RHYTHM_ANALYSIS;
+    }
+    // TODO: 実装 - HealthKitService.fetchRhythmAnalysis()
+    throw new Error('Real HealthKit integration not yet implemented');
+  }
+
+  /**
+   * 天気データを取得
+   */
+  async getWeather(lat: number, lon: number): Promise<SimpleWeatherData> {
+    if (DATA_SOURCE_CONFIG.USE_MOCK_WEATHER) {
+      return MOCK_WEATHER;
+    }
+
+      const result = await apiClient.getWeather(lat, lon);
+    if (!result.success) {
+      throw new Error('Failed to fetch weather data');
+    }
+
+    return {
+      temp: result.data.temperature,
+      condition: result.data.description || 'sunny',
+      pressure: result.data.pressure,
+      pressureTrend: result.data.pressureTrend,
+      uv: 0, // TODO: UV index is not available yet
+      location: result.data.location || '現在地',
+    };
+  }
+
+  /**
+   * AIアドバイスを取得
+   */
+  async getAIAdvice(request: AdviceRequest): Promise<AdviceResponse> {
+    if (DATA_SOURCE_CONFIG.USE_MOCK_AI) {
+      return MOCK_AI_RESPONSE;
+    }
+
+      const result = await apiClient.generateAdvice(request);
+    if (!result.success) {
+      throw new Error('Failed to generate AI advice');
+    }



================================================================================
ファイル: app/src/stores/healthStore.ts
行: 47 (開始行: 25)
コミット: a35fee7
作成日時: 2026-01-07T07:53:50Z
================================================================================

【コメント】
_🧹 Nitpick_ | _🔵 Trivial_

**Type aliasing imports suggest naming conflicts.**

The imports use aliases like `HrvMetrics as NewHrvMetrics`, `SleepMetrics as NewSleepMetrics`, etc. This indicates naming conflicts between domain models. Consider:
1. Renaming types at source to be more specific (e.g., `TempoHrvMetrics`)
2. Using namespaced imports

<details>
<summary>♻️ Namespaced import approach</summary>

```diff
-import {
-  TempoScoreResult,
-  calculateTempoScore,
-  HrvMetrics as NewHrvMetrics,
-  SleepMetrics as NewSleepMetrics,
-  RhythmMetrics,
-  ActivityMetrics as NewActivityMetrics,
-} from '../domain/services/tempoScoreCalculator';
+import * as TempoCalc from '../domain/services/tempoScoreCalculator';

// Usage:
// TempoCalc.calculateTempoScore(...)
// TempoCalc.HrvMetrics
```
</details>


> Committable suggestion skipped: line range outside the PR's diff.

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -1,31 +1,71 @@
 import { create } from 'zustand';
+import { persist, createJSONStorage } from 'zustand/middleware';
+import AsyncStorage from '@react-native-async-storage/async-storage';
 import {
-  DailyScores,
   RhythmAnalysis,
   SleepMetrics,
   HRVMetrics,
   ActivityMetrics,
   SimpleWeatherData,
+  DailySnapshot,
+  RealtimeMetrics,
 } from '../domain/models';
-import { calculateConditionAssessment, calculatePressureTrend } from '../domain/services';
-import { getWeatherCondition } from '../domain/models/weather';
 import { apiClient } from '../api/client';
 import {
   MOCK_SLEEP_METRICS,
   MOCK_HRV_METRICS,
   MOCK_ACTIVITY_METRICS,
   MOCK_RHYTHM_ANALYSIS,
   MOCK_WEATHER,
+  createMockDailySnapshot,
+  createMockRealtimeMetrics,
 } from '../constants/mockData';
+import { formatDateString } from '../constants/mockDataFactory';
+import { dataSourceAdapter } from '../services/dataSourceAdapter';
+import {
+  TempoScoreResult,
+  calculateTempoScore,
+  HrvMetrics as NewHrvMetrics,
+  SleepMetrics as NewSleepMetrics,
+  RhythmMetrics,
+  ActivityMetrics as NewActivityMetrics,
+} from '../domain/services/tempoScoreCalculator';
+import {
+  CircadianRhythm,
+  EnergyCurve,
+  RhythmPhase,
+} from '../domain/models/rhythm';
+import {
+  calculateCircadianRhythm,
+  calculateEnergyCurve,
+} from '../domain/services/rhythmCalculator';
+import {
+  calculateRecoveryScore,
+  calculateSleepScore,
+  calculateRhythmScore,
+  calculateEnergyScore,
+} from '../domain/services/scoreCalculator';



================================================================================
ファイル: app/src/stores/healthStore.ts
行: 360 (開始行: 354)
コミット: a35fee7
作成日時: 2026-01-07T07:53:50Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

**Silent failure with `console.warn` violates error handling guidelines.**

When metrics are missing, the function logs a warning and returns without updating state. Per coding guidelines, silent failures with only console.log/warn should be avoided. Consider setting an error state or throwing:

<details>
<summary>🐛 Suggested fix</summary>

```diff
       calculateDailyScores: () => {
         const { sleepMetrics, hrvMetrics, activityMetrics, rhythmAnalysis, weather } = get();

         if (!sleepMetrics || !hrvMetrics || !activityMetrics || !rhythmAnalysis) {
-          console.warn('Missing metrics for score calculation');
-          return;
+          set({ 
+            error: 'Missing required metrics for score calculation. Please sync your health data.' 
+          });
+          return;
         }
```
</details>

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/stores/healthStore.ts around lines 354 - 360, The
calculateDailyScores function currently silences missing-metric cases with
console.warn and an early return; replace this silent failure by reporting the
error via the store's error handling API instead of console.warn — when get()
shows any of sleepMetrics, hrvMetrics, activityMetrics, or rhythmAnalysis is
missing, call the store updater (e.g., set({ ... })) to set a descriptive error
state (or throw a typed Error if the calling flow expects exceptions) including
which metric is missing, and ensure no further state updates occur; remove the
console.warn and update any tests/consumers to expect the error state/exception
from calculateDailyScores.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -179,34 +232,272 @@ export const useHealthStore = create<HealthState>()((set, get) => ({
       lastMetricsUpdate: new Date(),
       lastWeatherUpdate: new Date(),
     });
-    get().calculateScores();
   },
 
-  resetHealth: () =>
-    set({
-      sleepMetrics: null,
-      hrvMetrics: null,
-      activityMetrics: null,
-      dailyScores: null,
-      rhythmAnalysis: null,
-      weather: null,
-      weatherCode: null,
-      weatherHumidity: null,
-      isLoadingMetrics: false,
-      isLoadingWeather: false,
-      metricsError: null,
-      weatherError: null,
-      lastMetricsUpdate: null,
-      lastWeatherUpdate: null,
+      resetHealth: () =>
+        set({
+          sleepMetrics: null,
+          hrvMetrics: null,
+          activityMetrics: null,
+          rhythmAnalysis: null,
+          weather: null,
+          weatherCode: null,
+          weatherHumidity: null,
+          isLoadingMetrics: false,
+          isLoadingWeather: false,
+          metricsError: null,
+          weatherError: null,
+          lastMetricsUpdate: null,
+          lastWeatherUpdate: null,
+        }),
+
+      // 新規: Actions
+      setMetrics: (newMetrics) => {
+        set((state) => ({
+          metrics: {
+            ...state.metrics,
+            ...newMetrics,
+          },
+        }));
+      },
+
+      calculateAndSetTempoScore: () => {
+        const { metrics, calibrationDaysCompleted } = get();
+        const isCalibrating = calibrationDaysCompleted < 7;
+
+        const tempoScore = calculateTempoScore(
+          metrics.hrv,
+          metrics.sleep,
+          metrics.rhythm,
+          metrics.activity,
+          isCalibrating
+        );
+
+        set({ tempoScore });
+      },
+
+      calculateAndSetCircadianRhythm: (wakeUpTime, windDownTime, sunrise, sunset) => {
+        const circadianRhythm = calculateCircadianRhythm(
+          wakeUpTime,
+          windDownTime,
+          sunrise,
+          sunset
+        );
+        const energyCurve = calculateEnergyCurve(wakeUpTime, windDownTime);
+
+        set({ circadianRhythm, energyCurve });
+      },
+
+      startCalibration: () => {
+        const now = new Date().toISOString();
+        set({
+          calibrationStartDate: now,
+          calibrationDaysCompleted: 0,
+        });
+      },
+
+      incrementCalibrationDay: () => {
+        set((state) => ({
+          calibrationDaysCompleted: Math.min(state.calibrationDaysCompleted + 1, 7),
+        }));
+      },
+
+      setLoading: (isLoading) => set({ isLoading }),
+
+      setError: (error) => set({ error }),
+
+      reset: () => set(initialState),
+
+      // HealthKit 対応: 新規アクション実装
+      shouldCalculateSnapshot: (): boolean => {
+        const { lastSnapshotDate } = get();
+        const today = formatDateString(new Date());
+        return lastSnapshotDate !== today;
+      },
+
+      calculateDailySnapshot: async (): Promise<void> => {
+        const state = get();
+
+        // 今日すでに算出済みの場合はスキップ
+        if (!state.shouldCalculateSnapshot()) {
+          return;
+        }
+
+        set({ isLoading: true, error: null });
+
+        try {
+          // TODO: Replace with actual HealthKit data fetch and score calculation
+          // For now, use mock data
+          await new Promise((resolve) => setTimeout(resolve, 300));
+
+          const snapshot = createMockDailySnapshot();
+
+          set({
+            dailySnapshot: snapshot,
+            lastSnapshotDate: snapshot.date,
+            isLoading: false,
+          });
+        } catch (error) {
+          set({
+            isLoading: false,
+            error:
+              error instanceof Error
+                ? error.message
+                : 'Failed to calculate daily snapshot',
+          });
+        }
+      },
+
+      /**
+       * 4つの独立スコアを計算
+       */
+      calculateDailyScores: () => {
+        const { sleepMetrics, hrvMetrics, activityMetrics, rhythmAnalysis, weather } = get();
+
+        if (!sleepMetrics || !hrvMetrics || !activityMetrics || !rhythmAnalysis) {
+          console.warn('Missing metrics for score calculation');
+          return;
+        }



================================================================================
ファイル: app/src/stores/healthStore.ts
行: 421 (開始行: 414)
コミット: a35fee7
作成日時: 2026-01-07T07:53:50Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

**Type assertion `as DailySnapshot` may hide type mismatches.**

The spread of `currentSnapshot` (which could be `null`) with new properties and then asserting `as DailySnapshot` could mask issues if `currentSnapshot` is null and required fields are missing.

<details>
<summary>🛡️ Type-safe approach</summary>

```diff
         const currentSnapshot = get().dailySnapshot;
+        const now = new Date();
         set({
           dailySnapshot: {
-            ...currentSnapshot,
-            date: formatDateString(new Date()),
+            date: formatDateString(now),
+            calculatedAt: currentSnapshot?.calculatedAt ?? now,
             scores: dailyScores,
-          } as DailySnapshot,
+          },
         });
```
</details>


> Committable suggestion skipped: line range outside the PR's diff.

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/stores/healthStore.ts around lines 414 - 421, The code is currently
using a type assertion on a spread of currentSnapshot which may be null
(currentSnapshot, dailySnapshot) and that can hide missing required fields;
instead, check for null/undefined and explicitly construct a valid DailySnapshot
object: if currentSnapshot is present merge its known fields with the new date
and scores, otherwise create a new DailySnapshot with all required properties
populated (use formatDateString(new Date()) and dailyScores), then call set({
dailySnapshot: constructedSnapshot }); avoid using "as DailySnapshot" to mask
type errors and reference get(), set(), dailySnapshot, currentSnapshot,
dailyScores, formatDateString, and the DailySnapshot type when implementing the
explicit construction and null check.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -179,34 +232,272 @@ export const useHealthStore = create<HealthState>()((set, get) => ({
       lastMetricsUpdate: new Date(),
       lastWeatherUpdate: new Date(),
     });
-    get().calculateScores();
   },
 
-  resetHealth: () =>
-    set({
-      sleepMetrics: null,
-      hrvMetrics: null,
-      activityMetrics: null,
-      dailyScores: null,
-      rhythmAnalysis: null,
-      weather: null,
-      weatherCode: null,
-      weatherHumidity: null,
-      isLoadingMetrics: false,
-      isLoadingWeather: false,
-      metricsError: null,
-      weatherError: null,
-      lastMetricsUpdate: null,
-      lastWeatherUpdate: null,
+      resetHealth: () =>
+        set({
+          sleepMetrics: null,
+          hrvMetrics: null,
+          activityMetrics: null,
+          rhythmAnalysis: null,
+          weather: null,
+          weatherCode: null,
+          weatherHumidity: null,
+          isLoadingMetrics: false,
+          isLoadingWeather: false,
+          metricsError: null,
+          weatherError: null,
+          lastMetricsUpdate: null,
+          lastWeatherUpdate: null,
+        }),
+
+      // 新規: Actions
+      setMetrics: (newMetrics) => {
+        set((state) => ({
+          metrics: {
+            ...state.metrics,
+            ...newMetrics,
+          },
+        }));
+      },
+
+      calculateAndSetTempoScore: () => {
+        const { metrics, calibrationDaysCompleted } = get();
+        const isCalibrating = calibrationDaysCompleted < 7;
+
+        const tempoScore = calculateTempoScore(
+          metrics.hrv,
+          metrics.sleep,
+          metrics.rhythm,
+          metrics.activity,
+          isCalibrating
+        );
+
+        set({ tempoScore });
+      },
+
+      calculateAndSetCircadianRhythm: (wakeUpTime, windDownTime, sunrise, sunset) => {
+        const circadianRhythm = calculateCircadianRhythm(
+          wakeUpTime,
+          windDownTime,
+          sunrise,
+          sunset
+        );
+        const energyCurve = calculateEnergyCurve(wakeUpTime, windDownTime);
+
+        set({ circadianRhythm, energyCurve });
+      },
+
+      startCalibration: () => {
+        const now = new Date().toISOString();
+        set({
+          calibrationStartDate: now,
+          calibrationDaysCompleted: 0,
+        });
+      },
+
+      incrementCalibrationDay: () => {
+        set((state) => ({
+          calibrationDaysCompleted: Math.min(state.calibrationDaysCompleted + 1, 7),
+        }));
+      },
+
+      setLoading: (isLoading) => set({ isLoading }),
+
+      setError: (error) => set({ error }),
+
+      reset: () => set(initialState),
+
+      // HealthKit 対応: 新規アクション実装
+      shouldCalculateSnapshot: (): boolean => {
+        const { lastSnapshotDate } = get();
+        const today = formatDateString(new Date());
+        return lastSnapshotDate !== today;
+      },
+
+      calculateDailySnapshot: async (): Promise<void> => {
+        const state = get();
+
+        // 今日すでに算出済みの場合はスキップ
+        if (!state.shouldCalculateSnapshot()) {
+          return;
+        }
+
+        set({ isLoading: true, error: null });
+
+        try {
+          // TODO: Replace with actual HealthKit data fetch and score calculation
+          // For now, use mock data
+          await new Promise((resolve) => setTimeout(resolve, 300));
+
+          const snapshot = createMockDailySnapshot();
+
+          set({
+            dailySnapshot: snapshot,
+            lastSnapshotDate: snapshot.date,
+            isLoading: false,
+          });
+        } catch (error) {
+          set({
+            isLoading: false,
+            error:
+              error instanceof Error
+                ? error.message
+                : 'Failed to calculate daily snapshot',
+          });
+        }
+      },
+
+      /**
+       * 4つの独立スコアを計算
+       */
+      calculateDailyScores: () => {
+        const { sleepMetrics, hrvMetrics, activityMetrics, rhythmAnalysis, weather } = get();
+
+        if (!sleepMetrics || !hrvMetrics || !activityMetrics || !rhythmAnalysis) {
+          console.warn('Missing metrics for score calculation');
+          return;
+        }
+
+        // 1. Sleep Score計算（先に計算する必要がある）
+        const sleepScore = calculateSleepScore({
+          duration: {
+            minutes: sleepMetrics.durationMinutes,
+            targetMinutes: 450, // TODO: userProfileから取得
+          },
+          stages: {
+            deepMinutes: sleepMetrics.deepSleepMinutes,
+            remMinutes: sleepMetrics.remSleepMinutes,
+            lightMinutes: sleepMetrics.durationMinutes - sleepMetrics.deepSleepMinutes - sleepMetrics.remSleepMinutes,
+            awakeMinutes: 0, // TODO: HealthKitから取得
+          },
+          // timingは後で実装（HealthKitから就寝・起床時刻を取得）
+        });
+
+        // 2. Recovery Score計算
+        const recoveryScore = calculateRecoveryScore({
+          hrv: {
+            current: hrvMetrics.value,
+            baseline: hrvMetrics.baseline30d,
+          },
+          rhr: {
+            current: 60, // TODO: 実際のRHRを取得
+            baseline: 60, // TODO: RHRベースラインを計算
+          },
+          sleepQuality: sleepScore,
+        });
+
+        // 3. Rhythm Score計算
+        const rhythmScore = calculateRhythmScore({
+          bedtimeStddevMinutes: rhythmAnalysis.bedtimeStddevMinutes,
+          wakeTimeStddevMinutes: rhythmAnalysis.wakeTimeStddevMinutes,
+        });
+
+        // 4. Energy Score計算
+        const energyScore = calculateEnergyScore({
+          recovery: recoveryScore,
+          sleep: sleepScore,
+          weather: {
+            pressure: weather?.pressure ?? 1013,
+            pressureTrend: weather?.pressureTrend ?? 'stable',
+          },
+        });
+
+        // 5. DailyScoresとして保存
+        const dailyScores: DailyScores = {
+          recovery: recoveryScore,
+          sleep: sleepScore,
+          rhythm: rhythmScore,
+          energy: energyScore,
+        };
+
+        const currentSnapshot = get().dailySnapshot;
+        set({
+          dailySnapshot: {
+            ...currentSnapshot,
+            date: formatDateString(new Date()),
+            scores: dailyScores,
+          } as DailySnapshot,
+        });



================================================================================
ファイル: app/src/api/helpers/adviceRequestBuilder.ts
行: 32 (開始行: 29)
コミット: a35fee7
作成日時: 2026-01-07T07:55:03Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

**Type safety concern with `unknown` fields and runtime assertions.**

The `profile` fields use `unknown` type with direct type assertions (`as string[]`, `as string`) at lines 44-46. This bypasses type checking and may cause runtime issues if the actual data doesn't match.

Consider using type guards or a validation schema:

<details>
<summary>🛡️ Suggested type-safe approach</summary>

```diff
  userStore: {
-   profile: { goals?: unknown; wakeUpTime?: unknown; windDownTime?: unknown } | null;
+   profile: { goals?: string[]; wakeUpTime?: string; windDownTime?: string } | null;
  };
```

Or validate at runtime:
```typescript
const isStringArray = (val: unknown): val is string[] => 
  Array.isArray(val) && val.every(v => typeof v === 'string');

const goals = isStringArray(profile?.goals) ? profile.goals : ['better_sleep'];
```
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
  userStore: {
    profile: { goals?: string[]; wakeUpTime?: string; windDownTime?: string } | null;
  };
  weather: WeatherData;
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/api/helpers/adviceRequestBuilder.ts around lines 29 - 32, The code
casts userStore.profile fields (profile?.goals, wakeUpTime, windDownTime) from
unknown to specific types which bypasses type safety; replace the direct
assertions with runtime type guards or a validation step (e.g., an isStringArray
guard for goals and isString guard for times) and use those guards to set typed
variables with safe fallbacks (e.g., default goals array or null) before passing
into build/advice request construction; update references to profile?.goals,
profile?.wakeUpTime, and profile?.windDownTime so they use the validated values
instead of raw casts.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -1,141 +1,123 @@
 /**
  * AdviceRequest構築ヘルパー
- * ストアの状態からAPIリクエストを構築
+ * ストアの状態からAPIリクエストを構築（新形式）
  */
 
-import type { AdviceRequest } from '../types';
-import { useUserStore } from '../../stores/userStore';
-import { useHealthStore } from '../../stores/healthStore';
-import { useInsightStore } from '../../stores/insightStore';
+import type { AdviceRequest, HealthMetrics, UserProfile, WeatherData, ScoresData, RhythmPhases } from '../types';
 
-/** 曜日名の配列（英語） */
-const DAY_NAMES = [
-  'Sunday',
-  'Monday',
-  'Tuesday',
-  'Wednesday',
-  'Thursday',
-  'Friday',
-  'Saturday',
-] as const;
+interface BuildAdviceRequestParams {
+  healthStore: {
+    metrics: {
+      sleep: {
+        durationMinutes: number;
+        deepSleepMinutes: number;
+        deepSleepPercent?: number;
+        remSleepMinutes: number;
+        remSleepPercent?: number;
+        bedtime?: string;
+        wakeTime?: string;
+      } | null;
+      hrv: { current?: number; baseline?: number; baseline30d?: number } | null;
+      rhr?: { current: number; baseline: number } | null;
+    };
+    // 4スコア（将来的に実装）
+    recoveryScore?: number;
+    sleepScore?: number;
+    rhythmScore?: number;
+    energyScore?: number;
+  };
+  userStore: {
+    profile: { goals?: unknown; wakeUpTime?: unknown; windDownTime?: unknown } | null;
+  };
+  weather: WeatherData;



================================================================================
ファイル: app/src/stores/healthStore.ts
行: 431 (開始行: 427)
コミット: a35fee7
作成日時: 2026-01-07T07:55:03Z
================================================================================

【コメント】
_⚠️ Potential issue_ | _🟡 Minor_

**Hardcoded Tokyo coordinates in `initialize()`.**

The coordinates `35.6762, 139.6503` are hardcoded for Tokyo. This should use the device's actual location or a user preference.

<details>
<summary>🌍 Suggested approach</summary>

```diff
       initialize: async () => {
         await get().fetchTodayMetrics();
-        await get().fetchWeather(35.6762, 139.6503); // TODO: 実際の位置情報を取得
+        // TODO: Get actual location from expo-location
+        // For now, skip weather fetch or use a default
+        try {
+          // const location = await Location.getCurrentPositionAsync({});
+          // await get().fetchWeather(location.coords.latitude, location.coords.longitude);
+          await get().fetchWeather(35.6762, 139.6503); // Default: Tokyo
+        } catch {
+          // Weather fetch is optional - continue without it
+        }
         get().calculateDailyScores();
       },
```
</details>

<!-- suggestion_start -->

<details>
<summary>📝 Committable suggestion</summary>

> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.

```suggestion
      initialize: async () => {
        await get().fetchTodayMetrics();
        // TODO: Get actual location from expo-location
        // For now, skip weather fetch or use a default
        try {
          // const location = await Location.getCurrentPositionAsync({});
          // await get().fetchWeather(location.coords.latitude, location.coords.longitude);
          await get().fetchWeather(35.6762, 139.6503); // Default: Tokyo
        } catch {
          // Weather fetch is optional - continue without it
        }
        get().calculateDailyScores();
      },
```

</details>

<!-- suggestion_end -->

<details>
<summary>🤖 Prompt for AI Agents</summary>

```
In @app/src/stores/healthStore.ts around lines 427 - 431, The initialize()
method currently passes hardcoded Tokyo coordinates to fetchWeather (35.6762,
139.6503); change it to obtain the actual coordinates (prefer user
preference/store value, else use device geolocation API, and fall back to a
default) and then call get().fetchWeather(lat, lon). Specifically, update
initialize() to: 1) check for a saved location in the store/settings, 2) if not
present request device location (e.g., navigator.geolocation or platform
equivalent) with proper permission/error handling, 3) call
get().fetchWeather(...) with the resolved lat/lon, and 4) handle failures by
falling back to a safe default and logging errors; reference the initialize
function and fetchWeather method when making the change.
```

</details>

<!-- fingerprinting:phantom:medusa:ocelot -->

<!-- This is an auto-generated comment by CodeRabbit -->

【対象コード】
@@ -179,34 +232,272 @@ export const useHealthStore = create<HealthState>()((set, get) => ({
       lastMetricsUpdate: new Date(),
       lastWeatherUpdate: new Date(),
     });
-    get().calculateScores();
   },
 
-  resetHealth: () =>
-    set({
-      sleepMetrics: null,
-      hrvMetrics: null,
-      activityMetrics: null,
-      dailyScores: null,
-      rhythmAnalysis: null,
-      weather: null,
-      weatherCode: null,
-      weatherHumidity: null,
-      isLoadingMetrics: false,
-      isLoadingWeather: false,
-      metricsError: null,
-      weatherError: null,
-      lastMetricsUpdate: null,
-      lastWeatherUpdate: null,
+      resetHealth: () =>
+        set({
+          sleepMetrics: null,
+          hrvMetrics: null,
+          activityMetrics: null,
+          rhythmAnalysis: null,
+          weather: null,
+          weatherCode: null,
+          weatherHumidity: null,
+          isLoadingMetrics: false,
+          isLoadingWeather: false,
+          metricsError: null,
+          weatherError: null,
+          lastMetricsUpdate: null,
+          lastWeatherUpdate: null,
+        }),
+
+      // 新規: Actions
+      setMetrics: (newMetrics) => {
+        set((state) => ({
+          metrics: {
+            ...state.metrics,
+            ...newMetrics,
+          },
+        }));
+      },
+
+      calculateAndSetTempoScore: () => {
+        const { metrics, calibrationDaysCompleted } = get();
+        const isCalibrating = calibrationDaysCompleted < 7;
+
+        const tempoScore = calculateTempoScore(
+          metrics.hrv,
+          metrics.sleep,
+          metrics.rhythm,
+          metrics.activity,
+          isCalibrating
+        );
+
+        set({ tempoScore });
+      },
+
+      calculateAndSetCircadianRhythm: (wakeUpTime, windDownTime, sunrise, sunset) => {
+        const circadianRhythm = calculateCircadianRhythm(
+          wakeUpTime,
+          windDownTime,
+          sunrise,
+          sunset
+        );
+        const energyCurve = calculateEnergyCurve(wakeUpTime, windDownTime);
+
+        set({ circadianRhythm, energyCurve });
+      },
+
+      startCalibration: () => {
+        const now = new Date().toISOString();
+        set({
+          calibrationStartDate: now,
+          calibrationDaysCompleted: 0,
+        });
+      },
+
+      incrementCalibrationDay: () => {
+        set((state) => ({
+          calibrationDaysCompleted: Math.min(state.calibrationDaysCompleted + 1, 7),
+        }));
+      },
+
+      setLoading: (isLoading) => set({ isLoading }),
+
+      setError: (error) => set({ error }),
+
+      reset: () => set(initialState),
+
+      // HealthKit 対応: 新規アクション実装
+      shouldCalculateSnapshot: (): boolean => {
+        const { lastSnapshotDate } = get();
+        const today = formatDateString(new Date());
+        return lastSnapshotDate !== today;
+      },
+
+      calculateDailySnapshot: async (): Promise<void> => {
+        const state = get();
+
+        // 今日すでに算出済みの場合はスキップ
+        if (!state.shouldCalculateSnapshot()) {
+          return;
+        }
+
+        set({ isLoading: true, error: null });
+
+        try {
+          // TODO: Replace with actual HealthKit data fetch and score calculation
+          // For now, use mock data
+          await new Promise((resolve) => setTimeout(resolve, 300));
+
+          const snapshot = createMockDailySnapshot();
+
+          set({
+            dailySnapshot: snapshot,
+            lastSnapshotDate: snapshot.date,
+            isLoading: false,
+          });
+        } catch (error) {
+          set({
+            isLoading: false,
+            error:
+              error instanceof Error
+                ? error.message
+                : 'Failed to calculate daily snapshot',
+          });
+        }
+      },
+
+      /**
+       * 4つの独立スコアを計算
+       */
+      calculateDailyScores: () => {
+        const { sleepMetrics, hrvMetrics, activityMetrics, rhythmAnalysis, weather } = get();
+
+        if (!sleepMetrics || !hrvMetrics || !activityMetrics || !rhythmAnalysis) {
+          console.warn('Missing metrics for score calculation');
+          return;
+        }
+
+        // 1. Sleep Score計算（先に計算する必要がある）
+        const sleepScore = calculateSleepScore({
+          duration: {
+            minutes: sleepMetrics.durationMinutes,
+            targetMinutes: 450, // TODO: userProfileから取得
+          },
+          stages: {
+            deepMinutes: sleepMetrics.deepSleepMinutes,
+            remMinutes: sleepMetrics.remSleepMinutes,
+            lightMinutes: sleepMetrics.durationMinutes - sleepMetrics.deepSleepMinutes - sleepMetrics.remSleepMinutes,
+            awakeMinutes: 0, // TODO: HealthKitから取得
+          },
+          // timingは後で実装（HealthKitから就寝・起床時刻を取得）
+        });
+
+        // 2. Recovery Score計算
+        const recoveryScore = calculateRecoveryScore({
+          hrv: {
+            current: hrvMetrics.value,
+            baseline: hrvMetrics.baseline30d,
+          },
+          rhr: {
+            current: 60, // TODO: 実際のRHRを取得
+            baseline: 60, // TODO: RHRベースラインを計算
+          },
+          sleepQuality: sleepScore,
+        });
+
+        // 3. Rhythm Score計算
+        const rhythmScore = calculateRhythmScore({
+          bedtimeStddevMinutes: rhythmAnalysis.bedtimeStddevMinutes,
+          wakeTimeStddevMinutes: rhythmAnalysis.wakeTimeStddevMinutes,
+        });
+
+        // 4. Energy Score計算
+        const energyScore = calculateEnergyScore({
+          recovery: recoveryScore,
+          sleep: sleepScore,
+          weather: {
+            pressure: weather?.pressure ?? 1013,
+            pressureTrend: weather?.pressureTrend ?? 'stable',
+          },
+        });
+
+        // 5. DailyScoresとして保存
+        const dailyScores: DailyScores = {
+          recovery: recoveryScore,
+          sleep: sleepScore,
+          rhythm: rhythmScore,
+          energy: energyScore,
+        };
+
+        const currentSnapshot = get().dailySnapshot;
+        set({
+          dailySnapshot: {
+            ...currentSnapshot,
+            date: formatDateString(new Date()),
+            scores: dailyScores,
+          } as DailySnapshot,
+        });
+      },
+
+      /**
+       * アプリ起動時に呼び出す初期化関数
+       */
+      initialize: async () => {
+        await get().fetchTodayMetrics();
+        await get().fetchWeather(35.6762, 139.6503); // TODO: 実際の位置情報を取得
+        get().calculateDailyScores();
+      },


