# SwiftLint Configuration for Tempo AI iOS

# 除外するルール
disabled_rules:
  - trailing_whitespace
  - force_cast
  - function_body_length
  - type_body_length

# 有効化するオプションルール  
opt_in_rules:
  - explicit_init
  - first_where
  - sorted_imports
  - vertical_parameter_alignment
  - closure_spacing
  - empty_count
  - explicit_type_interface
  - file_name

# 対象ディレクトリ
included:
  - TempoAI/TempoAI

# 除外ディレクトリ
excluded:
  - TempoAI/TempoAI/Assets.xcassets
  - TempoAI/TempoAI/Preview Content

# ルール設定
line_length: 120
identifier_name:
  min_length: 2
  max_length: 60
  excluded:
    - id
    - url
    - api
type_name:
  min_length: 3
  max_length: 40
function_parameter_count:
  warning: 5
  error: 8
cyclomatic_complexity:
  warning: 10
  error: 20